# History.md - Scan-log Archive

NOT read by default - consult only when digging into old changes.
Moved here verbatim by project-os/rotate.ps1. Movement only: rows are never rewritten, compressed, or deleted.

| Date | Area | What changed |
|---|---|---|
| 2026-09-10 | project-os | **This project now has its own written rules, and they are repeated to the assistant on every message.** ProjectOS was installed into an empty repository: the rule files, the living files, and two guards that refuse a write outside the project or a one-way command. Values only Rotem can supply are marked as waiting, never guessed. |
| 2026-09-10 | project-os | **The install is now in git, and one process correction is on record.** Rotem pointed out that the commit is mine to make and the push is his, so the waiting-on-you framing was wrong and is logged in Mistakes.md. |
| 2026-09-10 | plan | **The product plan now has a written review beside it, and a build plan for the first stage.** Sixteen findings, three of them blocking, and six product decisions left open for Rotem. The build plan carries the proposed component split, the Stage 0 steps with the evidence that closes each one, and a named fallback for every way the stack can fail. |
| 2026-09-10 | plan | **The plan is one file now, not two.** The review, the six open decisions and the technical stages all sit in `project-os/Plan.md`, which is what was asked for; the separate review document is gone and its content moved in whole. |
| 2026-09-10 | plan | **The plan is corrected where it was guessing.** The elevated-window hotkey claim is withdrawn to something to verify, the latency harness gained the mark it was missing, the coordinate contract became four named spaces, the output test became three separate checks, and Stage 0 shrank to one capture path and two real destinations. Callout numbers keep their gaps in every output. |
| 2026-09-10 | plan | **The plan is approved for Stage 0, and the product baseline now lives in the repository.** The four corrections landed: the two capture-path fallback lists agree, the typing check compares the live editor against the copied image, task pickup names the product plan and says which document wins where they differ, and every finding carries a status instead of an open checkbox. |
| 2026-09-10 | plan | **Stage 0 has nothing left to wait for.** The two paste destinations are Claude and ChatGPT, which also settles the clipboard question: both read it as a web application does, so PNG is the format that decides acceptance. |
| 2026-09-10 | plan | **Recon is now an image viewer as well as a capture tool, and both plans say so.** Opening a file is a normal way in, one window carries a viewing mode behind an explicit Annotate action, format support is stated per format instead of as common images, and an external file is never modified or imported by being looked at. |
| 2026-09-10 | plan | **There is one plan file now, and only one.** `project-os/Plan.md` holds the product, the behavior, the invariant, the architecture, the decisions and the stages in thirteen parts; `Product_plan.md` is gone and its content is inside. Splitting a plan across two documents is now a rule against it, in CLAUDE.md rule 13. |
| 2026-09-10 | plan | **The viewing and persistence contracts are now defined, and one of them was a real defect.** Reopening an annotated file resumes its one document, Save As can no longer overwrite anything, the displayed animation frame is a proved requirement rather than a figure of speech, an SVG's raster size is fixed when annotation begins, orientation and color are tested through the whole cycle, and the navigation context has four activation rules. |
| 2026-09-10 | plan | **The corrections are consistent now, and the plan comes to you unasked from here on.** Six leftovers were found and fixed: the navigation keys still walked "captures" in three places, the JPG row still applied orientation at display time, S0.7 never recorded the display gamut its own revisit trigger depends on, and part 6c justified deferring TIFF with a reason the rest of the plan contradicts. |
| 2026-09-10 | plan | **The technology is now chosen on its merits, and choosing it exposed a defect in the plan.** A Rust host owns the pixels and a web view lays out the text; the two-provider decode split is gone, because a preserved image created inside the renderer cannot survive a premultiplied canvas byte for byte. The familiarity argument is struck, the three open format decisions closed, and Stage 0 gained a boundary measurement before anything is built on it. |
| 2026-09-10 | plan | **The stack is a candidate again, and three real defects are fixed.** A fit-to-window proxy could never have served 100% zoom, the stated text-direction override did not exist in the mechanism named for it, and the annotation export was sized to cut off anything in the added margin. The stack argument also overstated what the alternatives cannot do, so part 5 now names what is not established and carries a comparison, and part 8 can reject the design. |
| 2026-09-10 | host | **Recon exists as a process now: it sits in the tray and answers a global hotkey with no window open.** S0.1 is built and two of its three checks are proved in a log: the hotkey fired twice with no window ever created, and a second instance reported the clash instead of dying quietly. The third, quit from the tray menu, needs one click that no automation route here could drive. |
| 2026-09-10 | host | **S0.1 is closed: the tray quit works and leaves nothing behind.** Rotem started the host himself, fired the hotkey with a real keypress, and clicked Quit; the process logged the quit and exited, and no recon-host and nothing else from the project tree remained. |
| 2026-09-11 | host | **Recon can take a screenshot now: hotkey, frozen screen, dimmed overlay, drag a region, and a PNG comes out.** Escape cancels and captures nothing, a second hotkey during a selection is ignored, and an open menu lands in the freeze. The self test caught the important defect: the process was DPI-unaware, so a 3840x2160 display at 225% was reporting itself as 1707x960 and every coordinate was quietly wrong. |
| 2026-09-11 | plan | **S0.2 is closed, with the two-display check deferred instead of pretended.** Rotem has one display, so a negative desktop origin and a mixed-scale layout cannot be produced here. The arithmetic for both is unit-tested; the platform half becomes a watch item for the first time a second display is ever attached. |
| 2026-09-11 | host | **The boundary is measured, and it says the pinned design was the right one for a different reason than the plan gave.** A whole frame crosses into the web view in 290 ms, the proxy the design pins in 40 to 74 ms. The figure the design was argued against was two to four times pessimistic. A local socket is about twice as fast as the message channel for large payloads, and the annotation layer should cross encoded: 162 KB and 3.5 ms against 31.6 MB and 359 ms. |
| 2026-09-11 | editor | **Recon has an editor: an image on a canvas, and a callout you can place, type into, move, re-anchor and delete.** Twenty-six checks pass, including the two that matter: a wrapped line lays out at exactly the same height at every zoom, and a one-pixel checkerboard is still a checkerboard at actual size after a fit view and a pan. The pre-created hidden window's first frame is current, proved by looking at the screen rather than asking the page. Two open findings came out of it: the fit view costs a second, and the window is not DPI-scaled. |
| 2026-09-11 | editor | **Looking at the editor found two things twenty-six assertions did not.** An image smaller than the window sat in the corner rather than the middle, now fixed and centred with the callout layer following it; and a note is unreadable at fit zoom on a wide capture, which is a product question rather than a defect. |
| 2026-09-11 | editor | **A note's text size is now yours: 20 pixels by default, and `Ctrl +` or `Ctrl -` changes it.** The size belongs to each note, and the size you used last is what the next note gets; the bubble's padding and number badge scale with it. That closes the question from looking at the editor: there is no minimum on-screen size, because a floor would make the editor and the exported file disagree. |
| 2026-09-13 | review | **Everything built so far was reviewed as a whole, and the direction holds.** Twelve findings, none blocking: the four Stage 0 pieces have never run as one, every figure in the plan is a debug-build number, S0.5 cannot close as written because its cycle tests need the export and the store, and a two-line note probably loses its line break on commit. The document is `notes/2026-09-13-review.md`, with a recommended step order and the verdicts left to Rotem. |
| 2026-09-13 | project-os | **The rule files describe the real project now, the plan has a new step before S0.5, and every step names Fable or Opus.** CLAUDE.md says what Recon is and its three setup blocks are filled from the project's own practice; Map.md names the stack and the checks; the two calibration files carry the worst bug class and rows seeded from the four steps' real defects. Plan: S0.4b wires the islands, fixes the fit view and re-measures in release; S0.5's cycle legs are carried to S0.6 and S1.8; the export spike runs first. Conversations.md rule 19: the Next step names its model. |
| 2026-09-13 | host, editor | **Recon works end to end now: hotkey, drag, and the capture is in the editor 13 ms later.** The editor is created hidden at startup on the product path and shown with the capture; a fit view is 47 ms instead of 1.1 s, because the resample was being compiled unoptimised and now runs from a pyramid in an optimised crate; one worker serves only the newest region request; a two-line note keeps its line break; the check-only commands are out of the product build; every figure in the plan has a release column, which closed the PNG encoder finding. |
| 2026-09-13 | editor, host | **The export route works on the shipping web view: the notes leave the editor as a layer, the host lays it over the untouched capture, and every pixel the notes did not touch is exact.** The one unspecified behaviour the design leaned on, reading a canvas back after drawing the serialized text layer, is clean on WebView2 152.0.4191.66. The whole export of a 5120x1440 capture takes about 65 ms in release. |
| 2026-09-13 | host, editor | **Recon opens files now: nine formats through one door, and a file is only ever read.** PNG, JPEG, BMP, GIF, WebP, TIFF and SVG decode with their orientation applied once, their colour profile converted once, their transparency kept and their frames or pages reachable by index; the editor shows an opened file and steps through it. Every test file hashed the same after being opened. HEIC and AVIF wait on a real file each, and AVIF's decoder is Windows' own rather than the one the plan named, which is a question for Rotem. |
| 2026-09-13 | host | **S0.5 is closed on twenty-eight real files from public test sets, and two of them found what a generated file could not.** HEIC and AVIF decode here, a rotated AVIF comes out upright, a real animated WebP steps through twelve frames, real Figma and Illustrator exports render like the web view. Windows' decoder was ignoring embedded colour profiles, fixed by reading the profile and converting it like every other route; and it hands back one frame of an animated AVIF, which sharpens the AVIF question for Rotem. |
| 2026-09-13 | plan | **AVIF is decided: it decodes through the Windows imaging stack, like HEIC, with the named-codec message as its degraded state and a first frame for an animation.** Rotem delegated the call; the alternative was a C toolchain on every machine that builds Recon, for an animated format daily use has not met. Recorded in Decisions, part 6c, §3.7, F51 and F53. |
| 2026-09-13 | host, editor | **S0.6 is built and closed: the composer, the clipboard, and the three fidelity checks, with the live editor and the copied image pixel-identical mid-typing.** A source pixel a note does not cover is exact in every output, alpha included; six reference documents are committed with their environment; a note inside a margin exports at its offset; the clipboard carries PNG, CF_DIBV5 and CF_DIB, and both Claude and ChatGPT took the paste. Ctrl+Shift+C copies. |
| 2026-09-13 | host, editor | **S0.7 is measured and closed: from the hotkey the overlay is usable in at most 87 ms, and from the selection the editor is usable in at most 134 ms, over thirty clean runs.** Memory has a floor of about 170 MB, nearly all the hidden web view, and does not grow across thirty captures or a twenty-eight-file walk. Six marks per run on one clock, a CSV per run set, startup measured apart. |
| 2026-09-13 | host, plan | **S0.8 is closed and Stage 0 is done: no part 8 row fired, and the recommendation is to go to Stage 1 on this stack.** HDR is now detected and logged at startup and at every freeze; the main display turns out to be wide gamut, which fires the sRGB decision's own trigger (F60); focus returns after Escape and a vanished window does no harm; the elevated-hotkey case waits on one key press by Rotem (F62). |
| 2026-09-13 | plan | **F60 is decided: a capture stays sRGB on a wide-gamut display, because the captured numbers are what the application drew.** The revisit trigger moves to a colour-managed desktop, which the product now detects. Rotem delegated the call. |
| 2026-09-13 | plan | **F62 is measured: the capture hotkey fires with an elevated window in front.** Rotem pressed it with Task Manager in front; the release product logged the fire, the overlay came up, and a selection reached the editor. Stage 0 has no open question left but the tray floor. |
| 2026-09-13 | host, editor, docs | **The whole repository reviewed after Stage 0, twelve findings, the seven that could bite fixed.** The one that mattered: clicking from a note being typed straight into another note lost the first note's text; a switch now commits it, and a check guards it. A superseded region answer no longer logs an error, the copy key works under a Hebrew layout, an opened still is no longer held twice, and the product build is warning-free. Five nits stay open with their homes. |
| 2026-09-14 | host, editor | **S1.1 is built: a new capture keeps the previous one and its notes, the editor hides on close or Escape and hands the focus back to the application the capture began in, the tray opens the editor, and the empty state names the hotkey.** The web view got its security policy and the region scheme answers the page's origin only. Run on a 225% display for the first time: the canvas offset now lands on a physical pixel (F74), and the antialiasing figure is recorded (F75). |
| 2026-09-14 | host, editor | **S1.2 is built up to the hand: every way in reaches the one window.** A file on the command line opens at startup, a second instance hands its file to the running window and exits, a dropped file opens, and Ctrl+O opens Windows' own picker. Registration lists Recon in "Open with" and in Default apps under the user and takes no default; running it, and the Explorer checks, wait for Rotem. |
| 2026-09-14 | host, editor | **S1.3 is built: the wheel pans, Ctrl with the wheel zooms around the pointer, F11 enters and leaves fullscreen, and the file's name is in the window title.** The rest of the viewing surface was already there from Stage 0. |
| 2026-09-14 | host, editor | **S1.4 is built: previous and next walk the opened file's folder in Explorer's own order, with the position and the context on screen.** The listing is read once, the ends stop, and a file that vanished since is skipped. |
| 2026-09-14 | plan | **S1.2 is closed by Rotem's hand: he ran the registration, a double-click in Explorer opened the image in Recon, and a dropped image replaced it.** |
| 2026-09-14 | host, editor | **S1.5 is built: a file opens in viewing, where a click creates nothing and a drag pans; A switches to annotation, which creates one managed document for the file or resumes it, never two; leaving annotation keeps the work.** A reopened file shows the file as it is, with the route to its edit; resuming shows the document's own preserved pixels. |
| 2026-09-14 | editor | **The Annotate control is both: the A key and a button at the top right, at Rotem's call.** |
| 2026-09-14 | editor | **S1.6 is built: a new note lands on the first free place around its anchor, in a fixed order; a note that cannot fit takes a neutral margin that goes when it is no longer needed; numbers keep their gaps in the output; the pan reaches the margin at any zoom.** |
| 2026-09-14 | editor | **S1.7 is built: every stage-1 key of the contract does what §3.6 says, the viewing keys are inert while a note is typed, and undo and redo walk the notes' history one operation at a time.** Copy and Return copies, then hides unless the document changed meanwhile. |
| 2026-09-14 | host, editor | **S1.8 is built: every document is on disk from its first moment, one folder each with the image written once and the notes saved as they are typed, the latest reopens at startup, and a failed save says so.** The document number is its creation time, so it survives a restart. |
| 2026-09-14 | host, editor | **S1.9 is built: the same keys walk Recon history, oldest to newest, captures and annotated files alike, with the position named; a capture or a document shown from history activates it, opening a file activates the folder, and Annotate changes nothing.** |
| 2026-09-14 | host, editor | **S1.10 is built: Copy and Return commits, copies, saves and only then hides, and every failure is a line on screen with the work kept: a held clipboard, a refused save, a change mid-copy, a target closed or absent, a focus the system refused.** |
| 2026-09-14 | host, editor | **S1.11 is built: Ctrl+S opens Windows' Save As on the last export folder with a name derived from the source and marked as annotated, writes a new file only, and offers a free name when the chosen one exists.** The notice under the HUD is state now, so a redraw cannot wipe it. |
| 2026-09-14 | plan, backlog | **Rotem's hand closed the Stage 1 path: a capture, Annotate, a note, Copy and Return, Save As, a restart and the document back from the tray. F79, F81, F82 and F83 go to the Backlog at his word.** |
| 2026-09-14 | host, editor | **S2.1 and S2.2, pulled forward at Rotem's word: a timeline of thumbnails along the bottom, one per document, a click showing it; a click on the tray icon opens the editor.** Stage 2's first steps are in the plan; the trial waits on S2.3. |
| 2026-09-14 | host, editor | **S2.3 and S2.4: Copy and Save As are buttons beside the mode as well as keys, and the HUD says what the store holds.** Rotem's first-day window was the midnight release, before those keys existed; the release is rebuilt at its path. |
| 2026-09-14 | plan | **Stage 2's remaining steps are in the plan: deletion, which waits on Rotem's call, and export beyond PNG.** The run stops at the first decision that is his. |
| 2026-09-14 | host, editor | **S2.5 is built at Rotem's calls: a document is deleted by the × on its thumbnail or by Ctrl+Delete, into Recon's own trash, swept for good after thirty days; the neighbour shows, the last one leaves the editor empty.** |
| 2026-09-14 | host | **S2.6 is built: Save As offers JPEG beside PNG, encoded by the chosen name's extension at a fixed quality, through the same never-overwrite path.** |
| 2026-09-14 | host, editor | **S2.7 is built: the timeline ends with a Trash chip when something was deleted, it opens the trash in the strip, and Restore brings a document back with its notes.** |
| 2026-09-14 | plan | **Rotem's hand on the new release: the timeline takes every capture, Save As works, Copy works.** |
| 2026-09-14 | editor | **S3.1 is built: a tool in hand, callout by default; L or the Arrow button, then a drag draws an arrow that is selected, moved, deleted, undone, saved and exported like a note.** Stage 3's four steps are in the plan. |
| 2026-09-14 | editor | **S3.2 is built: R draws a rectangle and H a highlight by the same drag, selected, moved, undone, saved and exported like the arrow.** |
| 2026-09-14 | editor | **S3.3 is built: T, then a click, types plain words where it was clicked, no number, no bubble, no arrow, and the notes' numbering is untouched.** |
| 2026-09-14 | host, editor | **S3.4 is built: B, then a drag, blurs a region, on screen through a box over the picture and in the output by the composer on a copy of the source, never the document's own pixels.** Stage 3's four tools are in. |
| 2026-09-14 | plan, backlog | **Everything to do with Rogers, Stage 4 whole, goes to the Backlog at Rotem's word.** |
| 2026-09-14 | host | **Launching Recon opens its window like any application; the tray, the hide on close and the tray's Quit stay as they were.** |
| 2026-09-14 | host | **Closing the window hides it at once; the host no longer waits a second on the page, which could not answer while the host waited.** Quit exits when the page has saved, or after a second and a half. |
| 2026-09-14 | host | **During a capture the window under the pointer is lit and framed, and a click with no drag captures it; a drag still draws its own rectangle.** The windows are listed once when the overlay comes up, a window's visible frame is what gets lit, cut to the pointer's display, and the click is told from a drag by the system's own drag threshold. Found in passing, and pre-existing: a copy of the screen with the overlay up shows the frame but not the dim, with the committed overlay too. |
| 2026-09-14 | host | **The capture veil darkens the screen now.** It was drawn all along, but at 45% opacity in the window's own alpha channel, and the compositor showed the identical live desktop through it, cancelling it exactly; Rotem confirmed the screen looked normal. The one black pixel the veil is stretched from now carries a solid alpha. |
| 2026-09-14 | plan | **Rotem's hand on the 19:05 release: the screen darkens, the window under the mouse is lit, and it works nicely.** |
| 2026-09-14 | host | **The selection follows the part of the window under the pointer, the way Snagit does: on a browser's page it is the page area without the tabs and the address bar, on a plain window it is the window.** A window's parts are its child windows at every depth, listed the first time the pointer rests on it; the smallest one under the pointer wins, cut to the window's frame and the display. Firefox draws its page without a child window, so there the whole browser is what gets lit. |
| 2026-09-14 | plan | **Rotem's hand on the 20:13 release: the frame sits on Chrome's page alone, and he called it perfect.** |
| 2026-09-14 | plan | **Firefox needs no support, at Rotem's word: the whole browser lit there is final, and nothing further is planned for it.** |
| 2026-09-14 | host | **The frame around the lit area is dashed now and walks slowly: light dashes and dark gaps, six pixels each, one pixel every 120 ms.** Only the frame's four strips are repainted on a tick, so the walk costs nothing to speak of, and a dash turns a corner instead of restarting. |
| 2026-09-14 | host | **The frame is tuned to Rotem's numbers: 2 pixels thick, blue #2D41D7 dashes of 12 on dark gaps of 8, one pixel every 256 ms.** The band sits just inside the lit area, the top and bottom bands taking the corners. |
| 2026-09-14 | host | **The frame's dashes are #00B9F7 now, at Rotem's word.** |
| 2026-09-14 | host | **The dashes walk at 1.48 pixels a second now, one pixel every 676 ms, at Rotem's word.** |
| 2026-09-14 | host | **The dashes walk at 8 pixels a second now, one pixel every 125 ms, at Rotem's word: at 1.48 they did not seem to move.** |
| 2026-09-14 | host | **The dashes glide now instead of stepping, at 12 pixels a second: the walk is read off the clock about sixty times a second, and the pixel at every dash edge is blended by the fraction in between.** Rotem saw the pixel steps at 8 a second and asked for smooth. |
| 2026-09-14 | host | **The dashes glide at 24 pixels a second now, at Rotem's word.** |
| 2026-09-14 | host, editor | **The window's top bar is Recon's own now: Windows' frame is gone, and the page draws a dark bar with the file's name on the left, the tool buttons in it, and minimize, maximize and close on the right.** The bar drags the window, a double-click on it maximizes, close hides as it did, and fullscreen puts the bar away. Rotem's call, between recolouring Windows' bar and drawing one. |
| 2026-09-14 | editor | **The top bar has no colour of its own and no line under it now: it sits on the page's own background.** Rotem's word, by eye, on the first look. |
| 2026-09-14 | editor | **The timeline runs newest first now: the latest picture is at the left.** Rotem's word. |
| 2026-09-14 | editor | **The app's background is #111217, at Rotem's word.** The page ground behind the picture and the panels changed from #101214; nothing else moved. |
| 2026-09-14 | editor | **A picture opens with no tool in hand now: the ordinary pointer over the whole window, and a click on the picture creates nothing until a tool is chosen.** A chosen tool works as before, crosshair included, until the next picture is shown; viewing shows the ordinary pointer too. Rotem's call. |
| 2026-09-14 | editor | **The wheel zooms in and out around the pointer now, with Ctrl or without; only a sideways wheel pans.** Rotem's call. In annotation the arrow keys are the pan that is left. |
| 2026-09-14 | plan | **The timeline has a spec for scale and for growing: S2.8, unbuilt.** Today the strip fetches every thumbnail at startup and keeps every one, so memory grows with the library against §3.8; the spec answers with a windowed strip that holds a screenful, no View More button, and a drag on the strip's top edge that enlarges thumbnails to 320 wide and then wraps rows, with the thumbnail file made at 320 once and served at the size shown. |
| 2026-09-14 | editor | **With no tool in hand a drag on the picture pans it now, and holding Space gives any tool the ordinary pointer and the same pan; letting go gives the tool back.** Space typed into a note is still a space. Rotem's call. |
| 2026-09-14 | host, editor | **S2.8 is built: the timeline holds a screenful of cells whatever the library holds, and its top edge drags it taller, the thumbnails growing to 320 wide and then wrapping into rows.** Cells are placed by arithmetic and only the ones near the screen exist, their pictures dropped when they scroll away, at most eight fetched at once; 96 is the floor and the default, 216 one row of 320 by 200, then whole rows up to 60% of the window, a double-click resets, the height is remembered. The host makes the thumbnail file at 320 by 200 and remakes an old 160 one once. Thirty thousand documents scan in about two seconds at startup, so the whole-list read stays, with the number in the plan for Rotem's call. |
| 2026-09-15 | editor | **The timeline may take up to 96% of the window now, at Rotem's word; it was 60%.** Nothing else in the strip moved. |
| 2026-09-15 | host | **Recon can start with Windows now: a "Start with Windows" tick in the tray menu, and started that way it waits in the tray with no window.** The tick writes one value under the user's own Run key and unticking removes exactly that; a start by hand still opens the window. Rotem's ask; the tray-only start at logon is the assistant's call, his to veto. |
| 2026-09-15 | host | **The release at `host/target/release/recon-host.exe` carries the Start with Windows tick now, built from the commit alone.** Rotem's pick of the two next steps. |
| 2026-09-15 | editor | **A drag moves the picture while the pointer moves now, not only on release: what is painted slides with the pointer at once and the fresh picture fills in behind it.** Rotem's report. |
| 2026-09-15 | host, editor | **Recon starts at once at any library size: the newest fifty records are read before the window shows and the latest reopens, the rest of the store is read on a thread, and the page takes the whole list when it lands.** Not the read-by-month that was named: Annotate's resume must see every document, so the list is read whole, in the background. Found and fixed on the way: the storage figure in the HUD walked every folder and file on every timeline refresh; it comes from a ledger now, corrected by each write, move or removal. |
| 2026-09-15 | editor | **The timeline's handle follows the hand exactly now, and a row of thumbnails enters the moment there is room for it; the snap to whole rows, which jumped the handle away from the pointer, is gone at Rotem's word.** A single row never grows past 320 by 200; the slack below it waits for the next row. |
| 2026-09-15 | project-os | **The assistant no longer narrates between edits: one line at pickup, then the report, at Rotem's word.** The rule sits in the reply rules' in-flight narration section. |
| 2026-09-15 | editor | **An edge a drag uncovers shows the picture at once now: a small copy of the whole picture lies under the sharp one while the drag lasts, and the edge sharpens as the fresh picture lands.** Rotem's report. |
| 2026-09-15 | host | **The S0.6 reference environment records the picture area as 1280 by 768 CSS pixels now, as a check run measured it; the reference images did not change.** Found uncommitted at Go commit. |
| 2026-09-15 | host | **The release at `host/target/release/recon-host.exe` carries the drag's edge fix now, built from the commit alone and swapped in the moment Rotem quit Recon.** |
| 2026-09-15 | editor, project-os | **The controls are a sidebar of icon buttons down the left edge now: the six tools, Copy, Save As and the mode as 32 by 32 squares with a 20 by 20 icon, 8 px apart, no fill of their own, and a hover fills the square with #21222C, fading in over 144 ms.** Rotem's spec; the fade is the design system's first token, A1, in the new `project-os/Design.md`. Found and fixed on the way: the page measured its pixel ratio against the picture area's width, which the sidebar narrows, so every copy came out 4% wrong until the ratio was measured against the whole page. |
| 2026-09-15 | host | **The release is rebuilt with the controls sidebar, from the commit alone, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon that is running.** Rotem's word, BUILD. |
| 2026-09-15 | editor | **When the timeline's thumbnails wrap into rows, its vertical scroller is Rotem's own now: a 4 px thumb of #21222C, fully rounded, on no track, 4 px clear of the window's bottom and of the strip's top.** The one-row strip's sideways scroller is still the system's. |
| 2026-09-15 | host | **The sidebar release is in place: the swap ran at 18:48:42 when Rotem quit Recon, the file reads back identical to the build, and his Recon opened on it one second later.** |
| 2026-09-15 | editor | **The sidebar's nine buttons sit in one container centred across the sidebar, and hovering a button opens its name in a tooltip on its right.** Rotem's word. The tooltip's look is provisional until he states one; the key hints that used to show in the system's own tooltip are gone with it. |
| 2026-09-15 | editor | **The one-row timeline's sideways scroller is Rotem's own now too: a 4 px thumb of #21222C, fully rounded, 4 px clear of the window's bottom and 4 px clear above.** His spec was this scroller all along; the rows scroller keeps the same look. |
| 2026-09-15 | host | **The release at `host/target/release/recon-host.exe` carries both timeline scrollers now, built from the commit alone and put in place at once, since no Recon was running.** Rotem's word, BUILD. |
| 2026-09-15 | editor | **Zooming out stops at the whole picture now: its actual size when it fits the window, the whole of it when it is larger; and when the window, full screen or the timeline changes the space, a picture shown whole grows or shrinks to the new whole view.** Rotem's word, both; the wheel and the - key used to go down to a quarter of the fit. |
| 2026-09-15 | editor | **The sidebar's button group is centred in the sidebar's height now, between the top bar and the timeline, instead of sitting at its top.** Rotem's correction: centring it across the width, which it already filled, was the wrong reading of his words; the slip's second time, so it is a rule in `CLAUDE.md` rule 1 now. |
| 2026-09-15 | project-os, host | **Every committed code change is built into the release without asking now, at Rotem's word; the release for the centred sidebar group is built and replaces the running one the moment Rotem quits Recon.** The rule sits in the workflow's delivery step. |
| 2026-09-15 | host | **The release with the centred sidebar group is in place: the copy ran at 20:39:51 when Rotem quit Recon, it reads back identical to the build, and his Recon opened on it a second later.** |
| 2026-09-15 | host | **Recon's window opens in the middle of the screen now, across and in height, instead of near the left edge.** Rotem's word. On a display smaller than the window, the window is shrunk to fit first, so its top bar is never off the screen. |
| 2026-09-15 | host | **The release with the centred window is built from the commit alone, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon that is running.** |
| 2026-09-15 | host | **The release with the centred window is in place: the copy ran at 21:29:44 when Rotem quit Recon, it reads back identical to the build, and his Recon opened on it at 21:29:58.** The first waiting job copied nothing, since Rotem's Recon was back within a second. |
| 2026-09-15 | plan | **Rotem's hand on the 21:29 release: the window opens in the middle of the screen, and it works.** |
| 2026-09-15 | editor | **A timeline dragged taller covers the sidebar now: the buttons below its top edge sit under it instead of over its thumbnails, and the handle on its edge stays on top.** Rotem's report, with a screenshot. |
| 2026-09-15 | host | **The release with the timeline over the sidebar is built from the commit alone, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon that is running.** |
| 2026-09-15 | host | **The release with the timeline over the sidebar is not in place: the waiting job that copies it in was refused by the session's permission check, so no copy will run when Rotem quits Recon.** Correcting the row above. |
| 2026-09-15 | host | **The release with the zoom-out floor, and the timeline over the sidebar with it, is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** No waiting job: the one for the previous release was refused by the session's permission check. |
| 2026-09-15 | host | **The release with the zoom-out floor is in place: copied over `host/target/release/recon-host.exe` at 23:11:01 once Rotem quit Recon, the file reading back identical to the build.** Rotem's word, DONE. |
| 2026-09-15 | editor | **A press anywhere on the one-row timeline and a move of the mouse left or right scrolls it now, following the hand; a press that moves less than 4 px is still a click and shows its thumbnail.** Rotem's word. The rows the timeline wraps into when dragged taller scroll as before. |
| 2026-09-15 | host | **The release with the timeline's press-and-drag scroll is built from the commit alone and waits in `.tmp/release-target/`; it is not copied over `host/target/release/recon-host.exe`, since the Recon from that path is running and the last waiting copy was refused by the session's permission check.** |
| 2026-09-15 | editor | **A picture opened while the timeline fills the window no longer vanishes at the next zoom key: it opens at the fit kept from before, and takes its own whole picture the first time the space returns.** Rotem's word, FIX, on a bug the zoom-out review found. |
| 2026-09-15 | host | **The release with the fix for a picture opened with no room, and the timeline's drag scroll with it, is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-15 | plan | **Rotem's hand on the timeline: a press and a sideways move scrolls it, and dragged taller it covers the sidebar; he called it perfect.** The release he ran was the one copied in at 23:31:30, built from `9adad68`, which carries both changes; the build from `73d51f4` it replaced was never copied. |
| 2026-09-15 | host | **Recon's window opens 1600 wide and 1160 tall now, centred as before; it was 1280 by 800.** Rotem's word. |
| 2026-09-15 | host | **The release with the window opening at 1600 by 1160 is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with the window opening at 1600 by 1160 is in place: copied over `host/target/release/recon-host.exe` at 00:14:39 once Rotem quit Recon, the file reading back identical to the build.** Rotem's word, GO. |
| 2026-09-16 | plan | **Undo of a deletion has a spec, S2.9, nothing built: Ctrl+Z brings back the picture just deleted from the timeline, the last thing done being what undo takes, and Ctrl+Y deletes it again.** Rotem's pick over undo buttons in the sidebar and over undo that survives a restart; three calls in the step are his. |
| 2026-09-16 | project-os | **A choice put to Rotem is shown as the scenes he would see, never as a rule's name: the slip's second time, so it is a line in the reply rules now.** He could not tell what the S2.9 step's three calls asked. |
| 2026-09-16 | plan | **S2.9's three calls are Rotem's now: Ctrl+Z undoes the last thing done, a deletion included; Ctrl+Shift+Z deletes it again, no Ctrl+Y named for it; and no notice when the picture comes back.** Still a spec, nothing built. |
| 2026-09-16 | host, editor | **S2.9 is built: Ctrl+Z brings back the picture just deleted from the timeline, with its notes and their own undo, the last thing done first; Ctrl+Shift+Z deletes it again; from the empty state too; nothing is said on screen.** Two things the review caught are fixed: a refused restore no longer blocks every later Ctrl+Z, and the empty path no longer tries a save into the trash. |
| 2026-09-16 | host | **The window opens at 1600 by 1160: measured on the release Rotem opened at 00:33, not only read in the code.** |
| 2026-09-16 | editor | **The timeline's thumbnails are 112 px tall by default now, the timeline 128 px; the floor a drag can reach stays 96.** Rotem's word. A height once dragged is still remembered, and a double-click on the top edge returns to the new default. |
| 2026-09-16 | host | **The release with the 112 px thumbnails is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with the 112 px thumbnails is in place: copied over `host/target/release/recon-host.exe` at 00:41:08 once Rotem quit Recon, the file reading back identical to the build.** Rotem's word, GO. |
| 2026-09-16 | review | **The whole repository reviewed again, Stage 3 and the timeline in: twelve findings, none fixed, every one waiting on Rotem's verdict.** The one that matters: a character typed during the few milliseconds a save is on its way can be left off the disk while the HUD says saved. Three important: a capture deleted while its image is still being encoded loses that image for the trash; the store's writes are never flushed before the rename, so a power cut can leave an empty record; closing the editor after any capture brings the last capture's application to the front, however the editor was opened. Eight nits with their homes. The document is `notes/2026-09-16-code-review-3.md`. |
| 2026-09-16 | editor | **The picture's size in pixels sits in a container across the whole window above the timeline now, 16 px clear above and below it, on the app's own background, written as 1200x1500.** Rotem's spec; the text's look is provisional until he states one. Side effect: the sidebar ends above the container, so its buttons sit 24 px higher. |
| 2026-09-16 | host | **The release with the image size container is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with the image size container is in place: copied over `host/target/release/recon-host.exe` at 00:57:14 once Rotem quit Recon, the file reading back identical to the build.** Rotem's word, GO. |
| 2026-09-16 | host, editor | **The twelve findings of review 3 are fixed at Rotem's FIX ALL, T12 apart, which is his hand: a character typed while a save is on its way now reaches the disk; a capture deleted while its image encodes keeps the image in the trash and comes back whole; every document write and every export is flushed before it is named; a hide returns the focus once, so an editor opened from the tray closes without a jump; Save As names no format and refuses a second dialog; a delete that cannot move says NOT DELETED and keeps the document.** Found on the way: a delete refused while a thumbnail held a file in the folder, now retried for a second. |
| 2026-09-16 | editor | **The mouse wheel over the one-row timeline scrolls it left and right now, down to the right; once the timeline wraps into rows the wheel scrolls it up and down as before.** Rotem's word. |
| 2026-09-16 | host | **The release at `host/target/release/recon-host.exe` carries the timeline's wheel scroll now, built from the commit alone and put in place at once, since no Recon was running.** |
| 2026-09-16 | plan | **Rotem's hand on the 01:30 release: the wheel over the one-row timeline scrolls it sideways, and it works great.** |
| 2026-09-16 | project-os | **The old build folders in `.tmp/` are cleared at Rotem's word, about 14 GB; the drive had filled to 2 MB free during the wheel task. `.tmp/release-target/`, where each release is built, is kept.** |
| 2026-09-16 | project-os | **Rotem asked why the replies broke the reply rules: status lines between steps, and reports too long, some without headings. One row in the mistakes list records it.** |
| 2026-09-16 | project-os | **The S2.9 commit `de9feb5` undid another session's review-3 commit `40eb3ec` by mistake; `9488cd3` put every line of it back, and the tree with both passes every check.** The assistant's slip is in Mistakes: it committed the shared index while the other session had just committed from the same working tree. |
| 2026-09-16 | host | **The release with undo of a deletion and the review-3 fixes is built from `2639eec` alone and in place: copied over `host/target/release/recon-host.exe` at 01:42:28, no Recon running from that path, the file reading back identical to the build.** |
| 2026-09-16 | editor | **The picture's size is centred in the window now, in Google Sans at 14 px, medium, the font bundled with the page from Google Fonts.** Rotem's spec; only the Latin file, 23 KB, with its Open Font License. The colour and the 16 px line stay provisional. |
| 2026-09-16 | project-os | **Decisions carries each entry once again: the Google Sans commit had copied the focus-return entry in a second time beside its own.** Found reading that commit back. |
| 2026-09-16 | host | **The release with Google Sans in the image size is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with Google Sans in the image size is in place: copied over `host/target/release/recon-host.exe` at 02:11:42 once Rotem quit Recon, the file reading back identical to the build.** Rotem's word, GO. |
| 2026-09-16 | editor, project-os | **Go commit: the docs rotated (111 items moved into the archives), and the one line left uncommitted in the tree, Save As answering true when its dialog opened, landed with them.** No behaviour changed beyond what the review-3 row already records. |
| 2026-09-16 | editor | **A capture is on the clipboard the moment the editor opens with it, with no key pressed: the same copy as Ctrl+C, so the HUD says copied, or NOT COPIED with the reason.** Rotem's word. A file opened, a frame stepped or an older picture shown from the timeline is never copied unasked. |
| 2026-09-16 | host | **The release with the capture's automatic copy is built from the commit alone; it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with the capture's automatic copy is in place: copied over `host/target/release/recon-host.exe` at 19:25:41 once Rotem quit Recon, the file reading back identical to the build.** Rotem's word, BUILD, found it already done. |
| 2026-09-16 | plan | **Rotem's hand on the 19:25 release: a capture lands on the clipboard by itself, and it works perfectly.** |
| 2026-09-16 | host, editor | **The timeline has tabs now: a plus at the left of the picture's size band makes Main and New tab, and another New tab at every press; a capture taken on a tab lands in Main and in that tab's own feed; a tab is deleted by its ×, never Main, and dragged into another place after Main; the list survives a restart.** Rotem's ask, for the feeds of tasks a client's remarks become; the plus is his spec, the tabs' look provisional. Found and fixed on the way: with a tab whose feed was empty selected, leaving fullscreen hid the timeline. |
| 2026-09-16 | host | **A capture brings a minimized editor back to the front now: the window is restored before it is shown and focused, as a click on its taskbar button would.** Rotem's report: since the automatic copy he leaves the editor minimized instead of hiding it, and the next capture left it in the taskbar. Side effect: the tray's click and Open, and a second instance, restore a minimized window the same way. |
| 2026-09-16 | host | **The release with the minimized editor's return is built from the commit alone; it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with the timeline's tabs is built from the commit alone, `d356b38`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** The tabs row above names `1bd859e` as its rollback target; the commit before it was `950e855`, another session's, and reverting to that one keeps that session's work. |
| 2026-09-16 | editor, project-os | **The tabs are finished to Rotem's spec: 14 px text on 10 px corners; a click on the selected tab's name edits it and Enter keeps it; the × pressed once turns into a ✓ and pressed again deletes; in a tab every thumbnail carries a round done mark, shown on hover and kept once pressed; and adding, deleting and renaming a tab and the mark are in Ctrl+Z and Ctrl+Shift+Z.** New rule 23 in `CLAUDE.md`: every new action joins undo and redo. Found and fixed on the way: the undo list dropped every act it was handed, and a tab bar reloaded from disk kept stale tabs behind its elements. |
| 2026-09-16 | host | **The release with the timeline's tabs is in place: copied over `host/target/release/recon-host.exe` at 22:00:35 once Rotem quit Recon, the file reading back identical to the build; his Recon opened on it six seconds later.** The other session's waiting job for the minimized editor's return copied nothing, its build having been replaced; that change is inside this one. |
| 2026-09-16 | host | **The release with the finished tabs, rename, the ✓, the done mark and their undo, is built from the commit alone, `40cfadf`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-16 | editor | **The done mark on a thumbnail is a 24 px circle with a 2 px white border now, its 20 px tick filling the inside with no gap, and the tick fades in by A1 when pressed.** Rotem's spec, correcting the 20 px mark with a ringed icon. |
| 2026-09-16 | host | **The release with the finished tabs is in place: copied over `host/target/release/recon-host.exe` at 22:27:48 once Rotem quit Recon, the file reading back identical to the build; his Recon opened on it two seconds later.** |
| 2026-09-16 | host | **The release with the 24 px done mark is built from the commit alone, `7390281`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with the 24 px done mark is in place: copied over `host/target/release/recon-host.exe` at 22:38:14 once Rotem quit Recon, the file reading back identical to the build.** His first look at it was at the release before, hence his repeat of the spec; the mark there matched it. |
| 2026-09-16 | editor | **The done mark's tick is a 1 px line now; it was 1.5.** Rotem's word. |
| 2026-09-16 | host | **The release with the 1 px tick is built from the commit alone, `2af8d24`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-16 | editor | **The sidebar's buttons are 48 by 48 with 32 by 32 icons now, and the sidebar 64 px wide, at Rotem's word: the picture starts 16 px further right.** His pick, option 2 of two, over buttons filling a 48 px sidebar edge to edge. |
| 2026-09-16 | editor | **The callout takes two clicks now: the first marks the point the line ends at, and the bubble follows the pointer from its automatic place, keeping that offset; the second locks it where it is and the typing begins.** Rotem's spec. Between the clicks Escape, Ctrl+Z, a change of tool, leaving annotation or another picture arriving drops the unplaced bubble and gives its number back; the text tool still types at one click. The code landed inside `2af8d24`, the other session's tick commit made from the shared tree at 22:44 while this change sat uncommitted in the same files, and the release copied in at 22:45:41 carries it. |
| 2026-09-16 | editor | **The sidebar's icons are drawn with a 2 px line in #C3C6CA now, at Rotem's word.** From the 2.4 px the enlargement left, in the buttons' #f2f2f2. |
| 2026-09-16 | host | **The release with the 1 px tick is in place: copied over `host/target/release/recon-host.exe` at 22:45:41 once Rotem quit Recon, the file reading back identical to the build.** |
| 2026-09-16 | editor, project-os | **The done mark's tick is a 2 px line and the circle's border 1 px white now, at Rotem's word: "the line" he asked to thin was the border, and the tick had been thinned instead.** The fill-clip line that had slipped into the tick commit unasked is gone. Two rules from it: a word that can name two elements is asked about first (Mistakes), and a commit from the shared tree carries only its own hunks (`CLAUDE.md` rule 22), promoted after a second slip. |
| 2026-09-16 | host | **The release with the 2 px tick in a 1 px border is built from the commit alone, `cf55206`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-16 | editor | **A ruler tool marks an area and writes its size in pixels, width and height, in a bubble beside the pointer the whole time the drag lasts; released, the box and its size stay on the picture and in every copy, and hovering it shows a round 16 px × with a 12 px X that deletes it.** Rotem's ask, his pick of two readings (a mark that stays, over a measurement that leaves nothing), and his spec for the ×; M and the button after Blur. In undo, redo, save and the copy like the other shapes; the look is provisional. |
| 2026-09-16 | host | **The release with the ruler is built from the commit alone, `d26a596`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** The other session's waiting job for `cf55206` will find the build's hash changed and copy nothing; that change is inside this one. |
| 2026-09-16 | host, editor | **The timeline's thumbnails show the notes now: after every save of a picture's notes the page draws its layer at the thumbnail's own scale, the host writes `thumb.png` anew with it composed over the picture, margin and all, and the strip's cell takes the new picture.** Rotem's ask, from a screenshot of a bare thumbnail under an annotated capture. Only the picture on screen; a note that grows the margin makes its thumbnail the whole composition fitted into the box. |
| 2026-09-16 | host | **The release with the thumbnails' notes is built from the commit alone, `6550906`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** The ruler's waiting job, for `d26a596`, will find another build in the folder and copy nothing; this one carries the ruler too. |
| 2026-09-16 | host | **The release with the thumbnails' notes is in place: copied over `host/target/release/recon-host.exe` at 23:49:50 once Rotem quit Recon, the file reading back identical to the build; his Recon opened on it one second later.** Rotem's word, DONE. The two older waiting jobs, for `cf55206` and `d26a596`, found this build in the folder and copied nothing. |
| 2026-09-17 | project-os | **Five old build folders in `.tmp/` are cleared at Rotem's word, about 18.5 GB: `s29-target`, `target-scroller2`, `front-target`, `check-target` and `check-target-tick2`, each left by a check run and regenerable. `.tmp/release-target/`, where each release is built, is kept.** The drive went from 11 GB free to 29 GB. |
| 2026-09-17 | host, editor | **The picture can be cropped in the editor now: a crop button after Ruler in the sidebar, on K, and with it in hand handles at the picture's corners and sides, dragged inward, cut the picture at the release; the screen, the size band, every copy and the thumbnail are the crop, the notes keep their place, Ctrl+Z gives the pixels back, and the source is never touched.** Rotem's word, over the v1 scope line that kept crop out. The look and the key are provisional. |
| 2026-09-17 | host | **The release with the crop is built from the commit alone; it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-17 | host | **The crop's release is in place: copied over `host/target/release/recon-host.exe` at 02:05:19 once Rotem quit Recon, the file reading back identical to the build, and his Recon opened on it the same second.** |
| 2026-09-17 | review | **The whole repository reviewed a fourth time, the crop in: seven findings, none fixed, every one waiting on Rotem's verdict.** The one that matters: a note whose save failed reads "saved" once its picture is left and returned to, and a quit then loses it. Two important: a capture deleted and undone while its image still encodes loses the image; the strip's thumbnail writer can recreate a deleted document's folder and block its restore. Four nits with their homes. The document is `notes/2026-09-17-code-review-4.md`. |
| 2026-09-17 | host, editor | **The seven findings of review 4 are fixed at Rotem's FIX ALL: a save refused before a switch of picture comes back as NOT SAVED and lands once the store is back; a capture deleted and undone inside its own encode comes back whole; no thread ever makes a document's folder again; the empty editor draws no shapes of the picture just deleted; an empty bubble after the second click gives its number back; two more decodes run outside the documents lock; a delete whose neighbour cannot be shown still deletes, into the trash.** Two new atlas rows and two decisions with it. |
| 2026-09-17 | host | **The release with the review 4 fixes is built from the commit alone, `c587272`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-17 | editor | **The picture's size above the timeline is written 2 px larger now, 16 px, from 14; the band around it stays 48 px tall.** Rotem's word. |
| 2026-09-17 | host | **The release with the 16 px size text is built from the commit alone, `a7dee9e`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-17 | editor | **Every button in the controls sidebar sits on 13 px corners now, from square, so its hover fill and the lit tool are rounded.** Rotem's word, option 1 of two: the button's own square, not the box around all of them. |
| 2026-09-17 | host | **The release with the sidebar's 13 px corners is built from the commit alone, `dd01da6`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-17 | editor | **The timeline's thumbnails end above the sideways scroller now: 112 px thumbnails, 8 px clear above them under the top line, and the scroller's 12 px band below, so the timeline is 133 px tall by default; every thumbnail's bottom 4 px, the selected one's blue border with them, sat under the scroller before.** Rotem's report with a screenshot, his pick of option 1 of three. The reply's option said 132; the timeline's 1 px top line, inside its height, makes it 133. |
| 2026-09-17 | host | **The release with the thumbnails ending above the scroller is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** The older jobs waiting on the same exit will find the newer hash and copy nothing. |
| 2026-09-17 | host | **Correcting the row above: the older jobs for `a7dee9e`, `dd01da6` and `c587272` had already run at 21:08, when Rotem quit Recon; `dd01da6` was copied in and the two others copied nothing. Only the `5ab0d95` job waits now.** |
| 2026-09-17 | editor | **The app's background is #0D0E12 now, at Rotem's word: the page ground behind the picture, the size band and the sidebar, from #111217.** Nothing else moved. |
| 2026-09-17 | host | **The release with the #0D0E12 background is built from the commit alone, `6a95d49`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-17 | editor | **The timeline's thumbnails sit on 13 px corners now, 12 px apart, with 12 px above them and at the row's two ends; the timeline is 137 px tall by default. Everything was 8 px and the corners 4 px.** Rotem's word. The scroller's 12 px band under the row is unchanged. Committed as `f2a314b` with no History row, this row added after. |
| 2026-09-17 | host | **The release with the 12 px air and the 13 px corners is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-17 | editor | **The timeline's thumbnails sit on 8 px corners now, from 13.** Rotem's word. |
| 2026-09-17 | project-os | **Design.md's Callout section, which another session is still writing, was swept into `a12fc2d` by a blind `git add` and is taken back out of the committed file here; it stays in the working tree, uncommitted, as that session left it.** The shared-tree rule, `CLAUDE.md` rule 22, broken a third time. |
| 2026-09-17 | host | **The release with the 8 px corners is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-17 | editor | **The callout bubble is #2D41D7 now, with 32 px of padding, 15 px corners and Rotem's shadow, no number in it, its text box 36 px tall and unmarked while typed in; the line is 4 px thick and ends in an open arrowhead at the point it names; Enter keeps the note and leaves the typing, Ctrl+Enter breaks the line.** Rotem's spec, from his Figma. The number stays in the document; Ctrl+Enter's copy and return works outside typing only now, and Shift+Enter still breaks the line. The six export references are remade from the new look and looked at. |
| 2026-09-17 | host | **The release with Rotem's callout is built from the commit alone, `49392fc`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** The other session's waiting job for `0e35d26` will find the build's hash changed and copy nothing; that change is inside this one. |
| 2026-09-17 | host | **The callout's release did not land: Rotem quit Recon at 22:10:31 and was back a second later, the shared build folder held another session's newer build by then, so the `49392fc` job would have copied nothing, and the other session's job missed the gap.** That waiting build, `18FC...5F76`, was made from a tree that carries the callout commit and its #0A0A0B background, so the next quit, with a few seconds before the restart, lands both. Correcting the row above. |
| 2026-09-17 | host | **The stale `49392fc` copy job was not stopped after all: the kill was refused, so it still waits, and at the next quit it finds the folder's hash changed and copies nothing.** Correcting the row above; harmless either way. |
| 2026-09-17 | editor | **The three window buttons at the top right are 48 px wide now, from 46, with 12 by 12 icons, from 10 by 10; the bar stays 32 px tall and so does each button.** Rotem's word, twice: the first round made the bar and the buttons 48 square, and he kept the bar as it was, so only the width and the icon changed.
| 2026-09-17 | host | **The release with the 48 px wide window buttons is in place: built from the commit alone and copied over `host/target/release/recon-host.exe` at once, since no Recon was running from it.** The copy reads back identical to the build.
| 2026-09-17 | editor, project-os | **The three window buttons are 48 px tall as well as wide now, and the top bar is 48 px tall to hold them, from 32.** Rotem's correction: the round before had read "the bar stays the same" as cancelling the button's height, which was his ask, so the button had gone back to 32. The picture, the sidebar and the picture's size band all start 16 px lower. `CLAUDE.md` rule 1 gained the rule this slip promoted.
| 2026-09-17 | host | **The release with the 48 px tall bar and buttons is in place, copied over the running path at 23:44:47 while no Recon was running from it.** The copy reads back identical to the build.
| 2026-09-17 | editor | **The minimize, maximize and close icons are 10 by 10 again, from 12, inside their unchanged 48 px buttons.** Rotem's word. The bar, the button box and every other icon in the app are untouched.
| 2026-09-17 | host | **The release with the 10 px icons is built from the commit alone and replaces the running copy the moment Rotem quits the Recon now running.** A hidden job waits for that exit; it copies nothing if the build's hash has changed or a Recon from that path is back by then.
| 2026-09-18 | host | **While a capture drag lasts, the dragged area's size in pixels sits in a bubble beside the crosshair now: 14 px text on the app's background, 8 px corners, 8 px of padding at the sides and 4 above and below, 8 px right of and below the pointer and on its other side at a display's edge.** Rotem's spec; the gap from the pointer and the text's colour and face are provisional. |
