# Recon — History

The change log. Every completed change lands here, in two layers: a scan table
you always read, and an appendix you read only when digging.

The scan table answers "what happened lately". The appendix answers "what exactly
did that change do, and how do I undo it".

## How you maintain this file

- **After every completed change**, add one scan row AND one appendix row. Both,
  in the same change that did the work.
- **One change = one row.** Not one row per file, not one row per session. A log
  that has to be reassembled from fragments is a log nobody reads.
- Write the scan row for a reader who was not there. Name the behavior that
  changed, not the files.
- Keep appendix rows short — a few lines, not an essay. The full story is in the
  commit diff; a real decision belongs in `project-os/Decisions.md`.
- Record the commit SHA from **before** the change. That is the rollback target.
- Never rewrite or delete a past row. Correct a wrong one by adding a new row —
  an edited log cannot be trusted about anything.
- A `medium` or `high` risk row names its review result under **What was
  checked**: findings found, findings fixed, pre-existing ones flagged. A
  medium-or-higher row without that is a task that is not finished.
- Never write "tested" or "QA passed". Those phrases record nothing. Name the
  input, the screen, and what happened.
- When this file gets long, move the oldest rows into an archive file beside it.
  `project-os/rotate.ps1` does exactly that at `Go commit`, and creates
  the archive the first time it is needed. Rows move **verbatim** — never
  rewritten, never summarized, never merged, because the detail you drop is the
  one the next reader needed. Never hand-move rows: the script dedups, so it is
  safe to run every time, and a hand-move breaks that guarantee.

## Risk scale

The scale's one home is `project-os/Workflow.md` step 2 — read it there, so the
two files can never disagree. State the level at task pickup; the owner's
override wins.

## Scan log

_Older scan rows archived -> see `History-scan-archive.md` (moved by project-os/rotate.ps1, not rewritten)._

Newest at the bottom.

| Date | Area | What changed |
|---|---|---|
| 2026-09-18 | host | **Correcting the row above: the job waiting to copy the `ca5c936` build in died with the machine's restart at 17:49, before Rotem quit Recon, so his two reopenings at 18:27 ran the 16:03 build, the size still under the circle; the job is restarted at 18:28 and copies the build the moment he quits again.** |
| 2026-09-18 | plan | **Rotem's hand on the `ca5c936` release, copied in once he quit Recon after the restart: the magnifier right of the pointer with the size above its circle, and it works great.** |
| 2026-09-18 | editor, host | **Recon has Settings: three dots left of minimize, in the window buttons' own size, open a modal where the shortcut that opens Recon and the shortcut that starts a capture are picked by pressing them.** Rotem's ask. Opening Recon has no shortcut until one is picked; a shortcut another application holds is refused in words under its field and the old one keeps working; both are saved and read back at the next start. The modal's look is provisional, from values the page already has. |
| 2026-09-18 | host | **The release with Settings is built from the commit alone and replaces the running copy the moment Rotem quits the Recon now running.** A hidden job waits for that exit; it copies nothing if the build's hash has changed or a Recon from that path is back by then.
| 2026-09-18 | project-os | **A change made in Settings stays outside Ctrl+Z: Rotem's call, written into rule 23 and recorded as a decision.** No code changed; Settings was built without an undo, which is now the decided behaviour and no longer an open question.
| 2026-09-18 | notes | **Ten ideas for a Recon logo in Rogers' graphic language, on one page Rotem opens in a browser.** Each is the blue disc with the light ring and 3.2 lines from `rogers_logo.svg`, shown large, beside a Recon wordmark built from Rogers' own letters, and at 64, 32 and 16. Nothing in the product changed.
| 2026-09-18 | host, editor | **The Open Recon shortcut, pressed again while Recon is the window in front, minimizes it; and the Settings button's icon is a chevron pointing down, from three dots.** Rotem's two asks. Behind another application, minimized or in the tray, the press still brings Recon forward.
| 2026-09-18 | host | **The Settings release landed at 18:42:59 when Rotem quit Recon, reading back identical to its build; the release with the toggling shortcut and the chevron is built and waits for his next quit the same way.**
| 2026-09-18 | host | **While capturing, two thin blue lines run through the pointer, one across and one down, over the whole screen whatever the capture area is: 1 px, the blue of the lines inside the magnifier's circle, at 72%.** Rotem's word. The screen is repainted in pieces now, so a move of the mouse still redraws two strips and the magnifier, not every pixel. |
| 2026-09-18 | host | **The release with the pointer's guide lines is built from the commit alone, the toggling shortcut and the chevron inside it; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** The job waiting with the build before it copies nothing now, since the shared build is this one. |
| 2026-09-18 | host, editor | **An SVG being viewed is sharp at every zoom above its own size now: the part on screen is drawn from the file itself at that zoom, where it used to be an enlargement of one small render. A picture with see-through parts is laid over the app's background before it is painted, the intended cure for the earlier sizes staying on screen while zooming in; that half is not yet seen on a screen.** Rotem's report, with `rogers_logo.svg`, 157 by 37. An SVG being annotated keeps its fixed pixels, which are what it copies and saves. |
| 2026-09-18 | host | **The release with the sharp SVG is built from the commit alone, `6b19599`, and is NOT in place: the hidden job that copies it when Rotem quits Recon was refused by this session's permissions, so the copy waits on one command from him.** It carries the guide lines of `5f397ec`. |
| 2026-09-18 | host | **The release with the pointer's guide lines is not in place: when Rotem quit Recon at 19:54 the shared build was already the newer one from `6b19599`, which carries the lines, so the waiting job copied nothing, and a copy by hand was refused by the session's permission check.** `host/target/release/recon-host.exe` is still the Settings build. |
| 2026-09-18 | plan | **Rotem's hand on the `6b19599` release, copied in by him: an SVG opens sharp, stays sharp zoomed in, and no earlier size stays on screen; flawless, in his word.** The see-through pixels laid over the app's background were the cure, seen on his screen where the automated screen check could not run. |
| 2026-09-18 | notes | **Ten more Recon logo ideas, grown from the two Rotem picked, the radar and the capture guides with their lit area, on a second page.** Numbered 11 to 20 with the two sources first, in the same layout as the first page. Nothing in the product changed.
| 2026-09-18 | notes | **Twenty more Recon logo ideas on a third page, numbered 21 to 40.** Wider than round two: the radar and the capture guides again, and beside them an R on the guides, the numbered callout, stacked captures, magnified pixels, binoculars and a scan line. Nothing in the product changed.
| 2026-09-18 | host, editor | **A file opened by name, from the picker, "Open with" or the command line, sits in the timeline now like a capture, as a pointer to where it lives: nothing of it is copied, its x takes it off and never touches the file, Ctrl+Z puts it back, and it leaves by itself when the file is gone.** Rotem's ask and his two calls: a pointer over a kept copy, and only the file opened, never one stepped onto in its folder. Annotate turns the pointer into the file's document. On a tab after Main it joins that tab, as a capture does. |
| 2026-09-18 | host | **The capture's guide lines are at 48% now, from 72%, and they stop 12 px short of the pointer on every side, so the plus there is white again.** Rotem's word, with a picture. Windows draws the plus as the inverse of what is under it, and over the blue lines that came out red. |
| 2026-09-18 | host | **The release with opened files in the timeline is built from the commit alone, `bcd65a5`, and is not in place: this session may not start the hidden job that copies it, so the copy is one command Rotem runs after quitting Recon.** It carries the sharp SVG of `6b19599`. |
| 2026-09-18 | host | **The release with the guide lines at 48% and the white plus is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-18 | host | **The release with the guide lines at 48% and the white plus is in place: copied over `host/target/release/recon-host.exe` at 20:29:40 once Rotem quit Recon, the file reading back identical to the build.** |
| 2026-09-18 | host | **Save As on an annotated PNG or JPEG opens on the file itself now and writes the picture with its notes over it, where it used to offer a new name beside it; every other existing file is still never written over, and an annotated SVG, GIF or other type keeps the new annotated PNG.** Rotem's exception to his own rule 11, asked for and confirmed as the clean original gone from disk; the rule's text carries it. |
| 2026-09-18 | host | **The release with Save As over the annotated file is built from the commit alone, `db497b3`, and is not in place: this session may not start the hidden job that copies it, so the copy is one command Rotem runs after quitting Recon.** It carries the guide lines at 48% of `85e3a8b` and the opened files of `bcd65a5`. |
| 2026-09-18 | host | **The two blue lines inside the magnifier's circle sit on the middle of the centre pixel now; they were 1 px right of and below it.** Rotem's report, with two pictures. The pixel's last row and column are the grid's line, and the middle was counted with them. |
| 2026-09-18 | editor | **The mode switches by itself now: picking a tool over a viewed file starts its annotation, a finished note or a drawn shape puts the tool down so a drag pans again, and a click on a note edits it; the Annotate button and the A key are gone.** Rotem's word, with his two choices: a tool picked again for every note, and no manual override. |
| 2026-09-18 | host | **The release with the circle's blue lines on the centre pixel is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-18 | host | **The release with the mode switching by itself is built from commit `38db43c` alone and waits in `.tmp/release-target/`; it is not copied over `host/target/release/recon-host.exe`, since Rotem's Recon runs from there.** The change's own two rows above went in with `9fdcd59`, another session's commit from the shared tree; the change itself is `38db43c`. |
| 2026-09-18 | host | **The release with the circle's lines on the centre pixel was not copied in: when Rotem quit Recon at 20:46 the shared build was already the newer one from `38db43c`, which carries the fix, so the job copied nothing; a job for that newer build waits now for his Recon to exit.** The release path still holds the 48% lines build. |
| 2026-09-18 | host | **Correcting the row above: the first job for the `38db43c` build did not wait, it tried the copy at 20:46:50 while Rotem's Recon was still letting go of the file, and failed; his Recon was back at 20:47:02 on the 48% lines build. The job is started again and waits for that Recon to exit.** |
| 2026-09-18 | host | **The release from `38db43c` is in place, the circle's lines on the centre pixel and the mode switching by itself inside it: copied over `host/target/release/recon-host.exe` at 20:50:08 once Rotem quit Recon, the file reading back identical to the build.** |
| 2026-09-18 | editor | **The top bar is 64 px tall now, from 40, with the Recon logo at its left, 16 px above it and to its left; minimize, maximize, close and the settings chevron keep their 48 by 40 and sit at the bar's top.** Rotem's word. The picture, the sidebar and the timeline's ceiling start 24 px lower with it. |
| 2026-09-18 | host | **The release with the 64 px top bar and the logo is built from commit `2a66c2e` alone and waits in `.tmp/release-target/`; it is not copied over `host/target/release/recon-host.exe`, because Rotem's Recon runs from there and the waiting copy job is refused to this session.** The copy is his one command, after quitting Recon. |
| 2026-09-18 | editor | **A tab no longer carries a ×: a right press on a tab after Main opens a small menu with Delete, which deletes it, and two new tabs now sit as far apart as Main and the first one.** Rotem's word; the hidden × was holding 18 px of empty room in every new tab. Ctrl+Z still brings a deleted tab back. |
| 2026-09-18 | editor | **The top bar no longer writes the picture's name and "Recon" beside the logo; the bar's middle is empty and still drags the window.** Rotem's word. The window's own title, the one the taskbar shows, still names the file. |
| 2026-09-18 | host | **The release with the tab's right-press menu is built from commit `c54e948` alone and waits in `.tmp/release-target/`; it is not copied over `host/target/release/recon-host.exe`, since Recon is running from there.** The copy is Rotem's, after he quits Recon. |
| 2026-09-18 | editor | **Correcting the row above: the commit that removed the bar's title line, `533447a`, also undid the tab menu commit `c54e948` in the two editor files, for one commit; `ebec0dd` has those lines back, and the working files never lost them.** The assistant's slip: its copy of the files was cut before `c54e948` landed, and HEAD was not read again before committing. |
| 2026-09-18 | host | **The capture frame's dashes walk the other way now: rightwards along the top and clockwise round the frame, from leftwards.** Rotem's word. Speed, dash, gap and colours unchanged. |
| 2026-09-18 | host | **The release to use is the one built from commit `ebec0dd`, which holds both the tab's right-press menu and the top bar without its title line; it waits in `.tmp/release-target/`, not copied, since Recon is running from the release path.** It replaces the `c54e948` build, which lacked the title change. |
| 2026-09-18 | Host | The Recon logo is the application's icon: the exe's file icon, the taskbar button and the tray, in place of the grey placeholder square. |
| 2026-09-18 | Host | The release with the logo icon is built from commit `52fbe55` and waits in `.tmp/release-target/`, not copied, since Recon runs from the release path. |
| 2026-09-18 | host | **The release with the bare top bar, the logo alone at its left, and the tab's right-press menu and the logo icon is built from commit `57f9794` alone and waits in `.tmp/release-target/`; it is not copied over `host/target/release/recon-host.exe` while Rotem's Recon runs from there.** The copy is his one command, after quitting Recon. |
| 2026-09-18 | host | **The two blue lines inside the magnifier's circle run on the top and left edges of the pointer's pixel now, crossing at its corner, and the pixel itself is whole; they cut through its middle before.** Rotem's word, with a picture from another tool, after the mid-square fix read his "on the pixel" wrong. |
| 2026-09-18 | host | **The release with the circle's lines on the pixel's edges is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running, and the copy job now tries again while the exiting Recon still holds its file.** |
| 2026-09-18 | host | **The release with the circle's lines on the pixel's edges is in place: copied over `host/target/release/recon-host.exe` at 21:28:32 once Rotem quit Recon, the file reading back identical to the build.** |
| 2026-09-18 | host | **Recon's window opens 1800 wide and 1390 tall now, centred as before; it was 1600 by 1160.** Rotem's word. His display leaves 1392 px of height above the taskbar, so the window fills it nearly whole, and Windows' unseen resize edge may trim a few pixels off the 1390. |
| 2026-09-18 | host | **The release with the window opening at 1800 by 1390 is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-18 | host | **The release with the window opening at 1800 by 1390 is in place, copied at 21:34:37 once Rotem quit Recon and reading back identical to its build; the Recon he opened on it measures 1800 by 1390 inside, nothing trimmed.** |
| 2026-09-18 | editor, host | **The ruler has the capture's zoom circle now: the whole time the ruler tool is in hand it sits beside the pointer showing the picture's own pixels, and while a ruler is dragged out its size sits above the circle, returning beside the ruler's corner at the release.** Rotem's word, both choices his. The circle is a view, not an act, so there is nothing for Ctrl+Z to take back. |
| 2026-09-18 | host | **The release with the ruler's zoom circle is built from commit `a25408f` alone and is in place: copied over `host/target/release/recon-host.exe` at 21:48:12, no Recon running from there, the file reading back identical to the build.** |
| 2026-09-18 | editor | **The left sidebar holds seven tools: Highlight, Copy and Save As are off it.** The highlight is gone whole, Rotem's choice of two: its button, its H key, and any highlight drawn before, which no longer shows on the picture or in a copy or a save. Copy and Save As stay on Ctrl+C and Ctrl+S. |
| 2026-09-18 | host | **The release at `host/target/release/recon-host.exe` has the seven-tool sidebar, built from `4607db4` alone and copied in at 21:52:47, no Recon running from that path.** |
| 2026-09-18 | editor | **The plus button left of the tabs has the tabs' own rounded corners now, 10 px, seen in its fill under the pointer; it was square.** Rotem's word. |
| 2026-09-18 | editor | **The Callout and Rectangle buttons in the left sidebar carry Rotem's own drawings now: a rounded speech bubble with its tail at the bottom, 28 by 25, and a rectangle on 4 px corners, 24 by 22, both a 2 px line in the icon line colour.** Each sits at its true size in the middle of the 32 px icon box. |
| 2026-09-18 | host | **The release with Rotem's Callout and Rectangle icons is built from `e8af5ac` alone, but it is not in place: Recon was running from the release path, and the waiting job that copies it in was refused by the session's permission check.** The copy is one command, Rotem's to run after quitting Recon. |
| 2026-09-18 | editor, host | **The left sidebar has a colour picker now, an eyedropper after Crop, on the I key: the zoom circle follows the pointer with the HEX of the pixel under it above it, a click copies that HEX to the clipboard as text, and the tool is put down.** Rotem's ask, and his two calls: put down after the click, and the zoom circle. It works over a file only viewed and makes no document; nothing for Ctrl+Z to take back. |
| 2026-09-18 | host | **The release with the colour picker is built from commit `fda1383` alone and waits in `.tmp/release-target/`; it is not copied over `host/target/release/recon-host.exe`, since Rotem's Recon runs from there and the copy was refused to this session.** The copy is his one command, after quitting Recon. It carries the Callout and Rectangle icons of `e8af5ac`. |
| 2026-09-18 | host | **The magnifier's panel no longer flickers while capturing: it is put together off the screen, with what is under it, and lands on the screen in one step.** Rotem's report. Before, every move of the mouse first painted the screen back where the panel is and then the panel over it, and the eye caught the moment in between. |
| 2026-09-18 | host | **The release with the magnifier that does not flicker is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-18 | editor | **The Color picker button in the left sidebar carries Rotem's own drawing of a dropper now, 21 by 21 at its true size in the middle of the 32 px icon box.** His report: the old drawing's head sat off the tube's line, by about a pixel. |
| 2026-09-18 | host | **The release with Rotem's dropper icon is built from commit `fecefad` alone and waits in `.tmp/release-target/`; it is not copied in, since the release in place is another session's fast-mode build with the uncommitted #2554FB blue, which this build would take away.** The copy is Rotem's call. |
| 2026-09-18 | host | **A release with both the dropper icon and the uncommitted #2554FB blue is built from the working tree and waits in `.tmp/release-target/`; the copy over the release was refused to this session, so it is Rotem's one command.** His word, continue: built this way so the dropper arrives without taking the blue away. |
| 2026-09-18 | host, editor | **The magnifier's circle, in a capture and in the editor's Ruler and Color picker: its two lines are #2554FB and 1.64 px wide, centred on the grid, and the grid between the pixels is #808080 at 48% over them, where it was each pixel darkened by half. The capture's full-screen guide lines take the same #2554FB and 1.64 px, still at 48%.** Rotem's words, tuned by eye in fast mode: the grid through #A8A8A8, #8C8C8C, #707070 and 64%, the lines through 1.44 px. |
| 2026-09-18 | editor, host | **In the editor, the zoom circle's two blue lines cross at the circle's centre now, as in the capture, where they crossed half a pixel off it; the ruler and the colour picker share the circle. The releases with the plus button's rounded corners and with this fix are both in place.** Rotem's word, with a picture, in fast mode. |
| 2026-09-19 | host | **The release from `46b66cf`, the magnifier's colours and widths, is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** The fast-mode build in place already carries the same product code. |
| 2026-09-19 | host, project-os | **Correcting the row above: the `46b66cf` release was already in place when it was written, copied at 00:17:02 with no Recon running, reading back identical to the build. And that History commit, `cd09966`, carried another session's staged rows with it: its two rows for the circle's centre, committed as `85b6713`, and a rotation of two of my rows.** Nothing of theirs was lost; the rows are in HEAD. |
| 2026-09-19 | host, editor | **Settings has a second capture shortcut: a Capture 2 row under Capture, empty until one is pressed there, cleared by its ×, starting the same capture as the first.** Rotem's ask, his choice of a second row. No two of the three shortcuts may be the same; the file keeps it as `second_hotkey`. |
| 2026-09-19 | host | **The release with the second capture shortcut is in place: built from `3ac1ea5` plus the page's uncommitted #0C0C0E background, so the look Rotem runs does not step back, and copied at 00:22:49 with no Recon running.** |
| 2026-09-19 | editor | **The app's background is #0C0C0E in the commit now, Rotem's value from a fast-mode burst on 2026-09-17 that never reached a commit; it has been in every release he has run since that evening.** The committed page said #09090C and Design.md #0D0E12; both say #0C0C0E now. The magnifier's panel in a capture, which Design.md fills with the app background, still carries #0D0E12 in the host, left as it is. |
| 2026-09-19 | editor, host | **From the Zoom circle session's fast-mode burst, committed from another session at Rotem's GO COMMIT ALL OPENED: a tool icon in the left sidebar turns #E7E8E9 under the pointer, fading in by A1; the Callout icon is a 2 px outline like the others, the Ruler's rectangle rounder and the Crop's two corners rounded; and a capture can pick Recon's own editor window like any other, only the overlay itself left out.** Rotem's asks in that session; he runs the release built with all of it since 01:50. |
| 2026-09-19 | editor, host | **A fast spin of the wheel zooms the picture under the finger now: the picture already on screen and the notes take each notch at once, a little soft, and the sharp picture lands when the wheel rests, where the picture used to stand still and then jump.** Rotem's report. An opened file with no see-through pixel is also painted without being looked through pixel by pixel at every paint. |
| 2026-09-19 | host | **The release with the wheel's zoom in a fast spin is built from the commit alone, `d094955`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** A job waits for that exit; it copies nothing if the shared build has changed or a Recon from that path is back by then. |
| 2026-09-19 | host | **The demo run of the editor can load an unpacked Chrome extension: with `RECON_EXTENSIONS` naming a folder of extensions, `--editor-demo` opens its window with them, in a profile of its own beside the executable, and stays open.** Rotem's trial of his Rocket Inspector on Recon's own page, then his word to land it. Only in a build with the checks; the product has none of it. |
| 2026-09-19 | host, editor | **The zoom circle with the text above it is one unit now: one piece of code draws it for the capture, the Ruler and the Color picker, so a value changed once changes in all three.** Rotem's ask, his choice of one drawing over one list of values. On it, his values from the same fast-mode burst: 8 px of padding around the circle from 4, the text in Google Sans medium from Segoe UI, and 10 px from the panel's top to the digits and from the digits to the circle, measured from the digits themselves. In the editor the circle shows a moment after the pointer enters the picture. |
| 2026-09-19 | host | **The release with the one-unit zoom circle is in place: built from the commit alone, `ef7a6e2`, and copied at 18:17:01 once no Recon from that path was running, reading back identical to the build.** |
| 2026-09-19 | host | **Rotem's own builds of Recon can load his inspector in the everyday editor window: a build made with the switch `own-extensions`, started with `RECON_PRODUCT_EXTENSIONS` naming the extensions folder, opens the editor with them, in a profile of its own; a build without the switch, the public release, holds none of it.** Rotem's GO, after the costs were put to him. The release he runs is built with the switch from now on. |
| 2026-09-19 | host | **The release with Rotem's inspector is in place, built from the commit alone, `b8f708c`, with the switch, and the inspector was seen working in it: the row above said the product start was not seen, and it was, since no Recon was open when the build landed.** |
| 2026-09-19 | host | **Rotem's hand on the `b8f708c` release: with the variable set for his user, his everyday Recon loads the inspector, and it works.** |
| 2026-09-19 | editor | **A thumbnail on the timeline has no × any more: a right press on it opens the tabs' dropdown with Delete, which deletes it as the × did.** Rotem's ask. The delete itself is unchanged: into the trash for 30 days, an opened file only taken off the timeline, Ctrl+Z bringing it back. |
| 2026-09-19 | host | **The release with the thumbnail's right press and Delete is built from the commit alone, `62a8a41`, with the switch, and is NOT in place yet: Rotem's Recon was running from that path, and the job that waits for its exit was refused by the assistant's permissions, so no job waits.** The build sits in `.tmp/release-target/release/`; it is copied over by hand once Recon is quit. |
| 2026-09-19 | editor, host | **A timeline thumbnail that is not the selected one takes the selected one's border under the pointer, in `#323A43`, fading in over 144 ms, ease-out; the release with it, and with the right press and Delete the row above left out, is in place.** Rotem's ask, tuned by his eye in fast mode: `#21222C`, then `#536070`, `#414C58`, and `#323A43`. The waiting job now runs as a background task of the session, which the permissions allow. |
| 2026-09-19 | editor | **While a crop handle is dragged, the picture's size above the timeline follows the frame, where it used to change only at the release.** Rotem's ask. The cut itself, its undo and the release are as they were. |
| 2026-09-19 | host | **The release with the size following a crop drag is built from the commit alone, `634f2d8`, with the switch, and is NOT in place: Rotem's Recon was running from that path, and the job that waits for its exit was refused by the session's permissions, so no job waits.** The build sits in `.tmp/release-target/release/`; it is copied over by hand once Recon is quit. |
| 2026-09-19 | project-os | **A rule: a build reaches Rotem's Recon without a command from him. The reply asks him to quit Recon, the waiting job, run as a background task of the session, copies the build in, and he reopens.** His correction, after a session handed him copy commands each round; written into `project-os/Workflow.md` step 16. |
| 2026-09-19 | editor | **A click on the sidebar button of the tool already in hand puts the tool down; one more click takes it up again.** Rotem's ask. The keys are as they were: pressing a tool's key again keeps it in hand. Picking a tool was never a step of Ctrl+Z, and putting it down is none either. |
| 2026-09-19 | host | **The release with the second click putting a tool down, and with the size following a crop drag that the `634f2d8` row left out, is built from the commit alone, `6b73225`, with the switch; it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** The job waits as a background task of the session; it copies nothing if the shared build has changed or a Recon from that path is back by then. |
| 2026-09-19 | host | **The `6b73225` release is in place: copied at 19:39:05, the moment Rotem quit Recon, reading back identical to the build.** |
| 2026-09-19 | editor, plan | **Escape with a tool in hand puts the tool down and Recon stays on screen, where it used to hide the window.** Rotem's ask. A note being placed, typed or selected still takes Escape first; with no tool in hand, Escape leaves fullscreen and then hides the editor as before. The plan's key table has the row. |
| 2026-09-19 | host | **The release with Escape putting a tool down is built from the commit alone, `0249c3f`, with the switch; it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** The job waits as a background task of the session, for ten minutes at most; it copies nothing if the shared build has changed or a Recon from that path is back by then. |
| 2026-09-19 | host | **The `0249c3f` release is in place: copied at 19:57:22, the moment Rotem quit Recon, reading back identical to the build. The job outlived the ten minutes the row above gave it.** |
| 2026-09-19 | host | **The editor window's title reads "Recon Editor", so the Windows taskbar's label beside the icon says Recon Editor; with a file open it reads "name - Recon Editor".** Rotem's ask, only where the taskbar needs it: the tray, the file types' "Open with" name, the executable's product name and the data folders still say Recon. |
| 2026-09-19 | host | **The release with the Recon Editor title is built from the commit alone, `f6d97f2`, with the switch, and is not in place: Rotem's Recon was open, and the session's permission check refused the job that waits for its quit.** The release at `host/target/release/` is still the `0249c3f` one. |
| 2026-09-19 | editor | **The timeline along the bottom has no colour and no top line of its own now: the thumbnails sit on the app's background, 16 px under the tabs, and the timeline is 132 px tall by default, from 137.** Rotem's ask. The thumbnails moved up 4 px to make the 16; the tabs and the size stay where they were. The trash keeps its dark red ground. A height remembered from a drag gives thumbnails 5 px taller until the top edge is double-clicked. |
| 2026-09-19 | host | **The release with the timeline on the app's background is in place: built from the commit alone, `46803b4`, with the switch, and copied at 21:00:07 with no Recon running, reading back identical to the build. It carries the Recon Editor title of `f6d97f2`, which was not in place before.** |

## Appendix — deep rows

_Older rows archived -> see `History-archive.md` (moved by project-os/rotate.ps1, not rewritten)._

Newest at the bottom, same as the scan log.

| Date | Task | What changed | What was checked | Result | Risk | Commit before | Rollback |
|---|---|---|---|---|---|---|---|
| 2026-09-18 | The dropper and the blue in one release | `cargo build --release` in `host/` on the working tree at `4cc6296` plus the other session's uncommitted edits (`editor/index.html`, `editor/editor-checks.js`, `host/src/overlay.rs`, `host/src/selftest.rs`), into `.tmp/release-target/`. Not from the commit alone, on purpose: the release in place is already that session's fast-mode build, and a commit-only build would drop its blue. The copy over `host/target/release/recon-host.exe` was refused by the session's permission check. No source changed. | Built in 21.7 s; SHA-256 `21EADFEF...026C6A41`. The release in place is still `3985BCEC...00533DE5`. | Built, not in place | low | `4cc6296` | Nothing to undo: the release path is untouched. |
| 2026-09-18 | The magnifier's colours and widths, fast mode | `host/src/overlay.rs`: `MAG_LINE_RGB` #2554FB for the circle's lines, from the frame's #00B9F7; `MAG_GRID_RGB` #808080 laid at `MAG_GRID_STRENGTH` 0.48 over the pixel, where the grid was the pixel halved; `MAG_LINE_SIDE` 0.32, the pixel on each side of a line taking that much of it, for 1.64 px; `CROSS_RGB` is `MAG_LINE_RGB` and `cross` gives each guide line its two side strips, blended at `CROSS_ALPHA` times `MAG_LINE_SIDE`. `editor/index.html`: `MAG_BLUE` #2554FB, `MAG_GRID` #808080 at 48%, the same side pixels. `host/src/selftest.rs` and `editor/editor-checks.js`: the blue they look for is #2554FB. `project-os/Design.md` Magnifier, `project-os/QA.md` the capture overlay row. Each fast round was built from the last commit plus these files only, and copied in when Rotem quit Recon, the hash read back each time. | Before fast mode, on the #2554FB round: fmt, clippy, build, 78 tests; `--selftest` passed whole; `--editor-check` read the circle's line as 37,84,251, with two failures, the HUD's document count and the tab order on disk, taken by me as another session's run in the same store, not proven. In fast mode Rotem looked at each round. At `Go commit`: fmt, clippy, build, 78 tests pass; two `--selftest` runs, the first broken by a hand on the mouse, the second C and H passing with the magnifier's cells and the guide lines, F failing on a stray click. No review ran on the burst: in fast mode Rotem is the reviewer. | Tuned by Rotem's eye | medium | `2f18d7c` | revert the commit |
| 2026-09-18 | Fast mode: the circle's centre and two releases | `editor/index.html` `paintMag`: the pixels are placed so the lines' crossing, not the pointer's pixel, lands on the circle's centre, the capture's own rule; `editor/editor-checks.js` section 43 reads the cells by the same place. Commit `85b6713`. Releases built from `f15fa84` (`A79B9F5F...6E5B0417`, copied 22:00:28) and from `85b6713` (`863FBECB...DA44411E`, copied 22:50:44), each from its commit alone into `.tmp/release-target/`, each reading back identical. | In fast mode Rotem is the reviewer: no automated run per round. The four project checks before each commit. `.tmp/swap-release.log` carries both copies. | In place; the centring is Rotem's to look at | low | `f15fa84` | `git revert 85b6713` |
| 2026-09-19 | The magnifier burst's release built | A release built from commit `46b66cf` archived into `.tmp/release-46b66cf/`, into the private target folder `.tmp/release-target/`, so the other session's uncommitted app background and settings edits stayed out. Its sources match the fast-mode build in place (`.tmp/release-fast-grid48/`) but for line endings and the two check-only files. `.tmp/swap-release-46b66cf.ps1`, the retrying job, waits for the Recon at the release path to exit, copies, and reads the hash back into `.tmp/swap-release.log`. | `cargo build --release` finished in 23 s; the build's SHA256 is `0E9A48A3...82274351`; the log's last line is the `46b66cf` job. | Built, the copy is the job's | low | `46b66cf` | none needed |
| 2026-09-19 | The 46b66cf copy, and a commit that took another session's staged rows | Nothing built. `.tmp/swap-release.log` 00:17:02: the `46b66cf` job copied its build and read it back as `0E9A48A3...82274351`, True. `cd09966` was meant to carry my two rows; the index already held another session's `project-os/History.md`, `History-archive.md` and `History-scan-archive.md`, staged by it, and `git add` of the file joined mine to them. Their rows describe `85b6713`, already committed, so HEAD is consistent; no history was rewritten to take them out. | `git show cd09966`: my two rows, their two rows, two of mine moved to the archives. `sha256sum` of the release path begins `0e9a48a3`. | Release in place; the stray rows left in, reported | low | `cd09966` | none needed |
| 2026-09-19 | A second capture shortcut | `host/src/settings.rs`: a third slot, `Capture2`, with the same try-first change, never shared with the other two, let go while one is being pressed, dropped at startup when a hand-edited file names it twice; a press of it starts a capture (`host/src/main.rs`). `host/src/config.rs` reads and writes `second_hotkey`. `editor/index.html`: the Capture 2 row with its ×. `editor/editor-checks.js`: five checks in the Settings section. Design, Map, QA and a Decisions entry. | fmt, clippy, build, 76 host and 4 pixel tests pass, 2 ignored by design, three of them new: the second shortcut picked, refused as any of the other two, cleared; either capture shortcut a capture; the file writing and reading all three. `--editor-check`, once: all five new Settings checks pass (rows at 368, 408 and 448, Ctrl+Alt+P in the file as `second_hotkey`, refusals "already Capture" and "already Capture 2", cleared out of the file). Five failures elsewhere, none in Settings: the kept fit on a stage with no area, the edge a drag uncovers, the strip's wheel, the done mark's look and its press; not chased, not proven another session's. Review: one finding introduced and fixed, a hand-edited file naming one shortcut twice would have shown the second as taken by another application; the startup guard for it is not under a test, since it needs the running app. A real press of Print Screen as the second shortcut is Rotem's hand. | pass | medium | `f1c0f6d` | revert the commit |
| 2026-09-19 | The second capture shortcut's release | `3ac1ea5` archived into `.tmp/release-3ac1ea5/`, the working tree's `editor/index.html` laid over it, its one difference the other session's uncommitted `--paper: #0C0C0E`, as the release before it carried; `cargo build --release` into `.tmp/release-target/`. Copied over `host/target/release/recon-host.exe` with no Recon running. | Build finished in 22 s, SHA256 `FDE55524...0A0F9E40`; the copy reads back the same, logged in `.tmp/swap-release.log` at 00:22:49. | pass | low | `3ac1ea5` | copy an earlier build back |
| 2026-09-19 | The app background, a closed fast-mode burst's leftover | `editor/index.html` `:root`: `--paper: #0C0C0E`, from the committed #09090C. `project-os/Design.md` App background: #0C0C0E, from #0D0E12. Rotem's word in the bottom bar's border session, in fast mode on 2026-09-17; that session never committed it, and every commit since left it out as another session's hunk. `SIZE_FILL_RGB` in `host/src/overlay.rs`, the magnifier panel's fill, which Design.md calls a second home of the app background, stays #0D0E12. | Fast mode: Rotem the reviewer. The value is in every release in place since the `fast-0C0C0E` build, copied at 23:08:03 on 2026-09-17 (`.tmp/swap-release.log`). The checks at this commit are on the row below. | Tuned by Rotem's eye | low | `f4d0216` | revert the commit |
| 2026-09-19 | Fast mode, the Zoom circle session: the icons' hover, three drawings, and Recon's own window in a capture | `editor/index.html`: `#controls svg` strokes in `currentColor`, #C3C6CA at rest and #E7E8E9 under the pointer (`#controls button:hover svg`), the colour fading by A1 and not at all under reduced motion; parts filled in #C3C6CA take the same colour (the dropper's). The Callout icon is one outline path with a 2 px line in a `-2 -3 32 32` box, where it was a masked fill; the Ruler's rectangle takes `rx="3"`, from 1; the Crop's two lines turn their corners on a 3.125 radius. `host/src/overlay.rs` `collect_window`: a window is left out of the capture's list when its class is `ReconOverlay`, where it was any window of Recon's process; `GetWindowThreadProcessId` leaves the imports. `project-os/Design.md`: the Icon line hover colour, A1 and the icon button's hover. `project-os/Decisions.md`: the entry superseding the 2026-09-14 clause that left Recon's own windows out. Committed from another session at Rotem's GO COMMIT ALL OPENED; the burst's own session wrote no row. | Fast mode: Rotem looked at each round, and the release built at 01:46:32 with all of it runs since 01:50:55. No review ran on the burst: in fast mode Rotem is the reviewer. At this commit: fmt, clippy, build, 76 host and 4 pixel tests pass, 2 ignored by design. `host/references/s06/environment.txt`, the editor check's rewrite of the reference environment at 225%, left out as the commits before it left it. | Tuned by Rotem's eye | medium | `f4d0216` | revert the commit |
| 2026-09-19 | The wheel's zoom in a fast spin, and the see-through check once per picture | `editor/index.html`: the wheel calls `zoomWhileWheeling`, which sets the zoom and the pan as `setZoomAround` does, then at once places the composition, stretches the canvas over its region's place at the new zoom (`stretchCanvas`), shows the copy of the whole picture under it, lays the notes out, and asks for one region at a time through `paintWhileDragging`; `paintRegion` records the zoom and pan it asked at, stretches a region that lands after the wheel moved on, and ends `body.zooming` (the smoothed stretch) and `body.panning` only on a paint of the view as it is; `lastRegion.zoom` is the zoom asked at. `setZoomAround` and the keys are unchanged. `host/pixels/src/lib.rs` `is_opaque`; `host/src/editor.rs`: `Image::is_opaque`, read once per image, and `ImageInfo.opaque`, never true for a vector; the page skips `flattenOntoPaper` in `paintRegion` when it is true. `editor/editor-checks.js`: four checks after the wheel's. BugAtlas row 1, QA.md and a decision updated. | fmt, clippy, build pass; 76 host and 5 pixel tests pass, 2 ignored by design, the new one: one pixel at alpha 254 is not opaque. `--editor-check`, debug, once and once more after the review fix, every check that ran passed, 560 pass lines: five wheel notches sent in one turn from zoom 6 leave the canvas 399.11 css px wide for the 399.11 its held region takes at zoom 1.966, read before any region could land, `body.zooming` on; the scene's scale 1.96608; at rest no `zooming` or `panning`, the region at zoom 1.966, the canvas 1215 px for 1215; `opaque` true for `reference-scene.png`. Console: Passed: known existing errors only, the refusals the checks provoke themselves (saves into a blocked store, copies with a wrong layer size, one clipboard held). Review, this diff: 1 found and fixed, a wheel notch during a note's or a shape's drag left `zooming` on for good, now ended by any paint of the view as it is; reach is shared, `paintRegion` serves every paint, and the pan drag's checks, the loads and the keys pass unchanged; pre-existing, reported: a region the host refuses leaves `panning` on until the next paint. Not seen: a real wheel on a real large picture, which is Rotem's hand; no timing was measured. | pass | medium | `0880bea` | revert the commit |
| 2026-09-19 | The release from `d094955`, the wheel's zoom | `git archive d094955` into `.tmp/release-d094955/`, `cargo build --release` there into `.tmp/release-target/`. A Recon from the release path was running, so `.tmp/swap-release-d094955.ps1` waits for its exit and copies the build over `host/target/release/recon-host.exe`. No file of the repository changed but this row. | The build finished in 32 s; its SHA-256 is C47FDE06268E4B1165E4631F812F83946D7EC25FB23029E74E878F3EB19822D7. `.tmp/swap-release.log` reads `d094955 waiting for the Recon at the release path to exit` at 15:48:05. The copy and its hash read back are not yet seen: they land in that log at Rotem's next quit. | built, the copy waiting | low | `d094955` | copy an earlier build back |
| 2026-09-19 | Rocket Inspector in the demo editor | `host/src/main.rs` `editor_run`: when the run is the demo and `RECON_EXTENSIONS` is set, the window's builder gets `browser_extensions_enabled(true)`, `extensions_path` of that folder and `data_directory` of `s-webview-ext` beside the executable. `host/src/editor.rs` `editor_exit`: a demo with that variable set logs its result and stays open, a rider the ask did not name, since the demo closes itself after three seconds. Both behind `stage0-checks`. `project-os/QA.md`: one row. Tried first on a private copy of `d094955` in `.tmp/inspector-trial/`. | On that copy: fmt, clippy, build pass; 76 host and 5 pixel tests pass, 2 ignored by design. `--editor-demo` with `RECON_EXTENSIONS=J:ProjectsRocket Editor	ools`, driven over the WebView2 debugging port by `.tmp/inspector-trial-drive.mjs`: the extension's service worker listed; the browser process's profile under `s-webview-ext`, not `com.rotem.recon`; Backquote held and the pointer on the picture put up the bubble `Canvas · No class, 1216 × 342` with the spacing bands, on a sidebar icon `Svg · No class, 32 × 32`; a click turned it to `Specs copied!`, the clipboard read back as the card with `attrs: id=canvas`, the notes still 2; the key let go hid both layers. Console: Passed: known existing errors only, 19 style-policy refusals from the page's own `exportLayer` and `inlineStyles` and one at line 394, the same 19 in a control run with an empty extensions folder; the service worker's console empty. Rotem tried it by hand and pasted the sidebar's card. Review, this diff: 0 found; reach is local, `editor_run` and `editor_exit` serve the check runs alone and `--editor-check` never takes the branch. Not seen: the hover over a note, where the card named the canvas under it. No release build: the product build holds none of this code. | pass | medium | `d094955` | revert the commit |
| 2026-09-19 | Fast mode, the Zoom circle session: the magnifier as one unit, its padding, face and spacing | New `host/src/magnifier.rs`: every number, `layout`, `render`, `place_panel`, `cell_count`, `corner_coverage`, the face, moved out of `host/src/overlay.rs`, which now hands it the frozen pixels one per cell (`cells_around`) and blends the panel. `host/src/editor.rs`: `region://?panel&...` answers the whole panel, nine numbers, the label, RGBA, on its own thread. `editor/index.html`: `paintMag` and the page's constants gone; `askMag` asks for the panel and paints it, `placeMag` places it by the host's gap, the panel's numbers on `#mag`'s dataset. Then Rotem's values: `INSET_PX` 8 from 4, `PAD_Y_PX` and `TEXT_GAP_PX` 10 (after 8, from 4), the face Google Sans medium from `editor/fonts/google-sans-medium-latin.ttf`, unpacked from the bundled .woff2 with fontTools and given to the process by `AddFontMemResourceEx`, the text placed by a digit's glyph box since the face's line is 34 px tall at 14 px. `editor/editor-checks.js` sections 43 and 45 and `host/src/selftest.rs` read the new panel. Design, QA, Map, a Decisions entry. | fmt, clippy, build, host and pixel tests pass. `--editor-check` once, after the unit and before the padding: every Ruler and Color picker magnifier check passed (panel 120x120 at 8,8 from the pointer, the three cells equal to the host's pixels, the blue 37,84,251, "100x70" above the circle, the HEX equal to the picture's); one failure elsewhere, Settings' "the two never share a shortcut", not this change's and not chased. `--selftest` once per later round: the magnifier's drag and hover looks passed with the 8 px padding's probes (ring 9 px in, cells at 67), and the pictures show Google Sans with 8 then the digits' own spacing; in each run one other case failed with Rotem at the machine, the pointer at 0,0 during a drag, an Escape that selected on the second display, the focus return to the stand-in, none of them the panel's and none chased. The editor checks were not run after the padding, face and spacing rounds; Rotem saw each round in his own Recon and called the last one balanced. No review: fast mode. | pass, with the limits named | medium | `f1e3cbf` | revert the commit |
| 2026-09-19 | The release from `ef7a6e2`, the magnifier as one unit | `git archive ef7a6e2` into `.tmp/release-ef7a6e2/`, `cargo build --release` there into `.tmp/release-target/`; a waiting job copied it over `host/target/release/recon-host.exe`. | Build finished in 25 s, SHA256 `C6D44E60...0CCB7AD2`; the copy at 18:17:01 reads back the same. | pass | low | `ef7a6e2` | copy an earlier build back |
| 2026-09-19 | Rotem's inspector in his own builds | `host/Cargo.toml`: the switch `own-extensions`, off by default. `host/src/editor.rs`: `create_hidden` passes its builder through `with_own_extensions`, compiled only with the switch, which turns extensions on, names the folder from `RECON_PRODUCT_EXTENSIONS` and sets the profile `%LOCALAPPDATA%\Recon\webview-ext`; with no variable the builder is as before. `project-os/Workflow.md` step 16: the release build carries the switch. A decision, and a QA row. Written first on a private copy of `ef7a6e2` in `.tmp/inspector-own/`; the real files read back identical to it. | On that copy: fmt, clippy with every switch, the plain build without the switch, 76 host and 5 pixel tests pass, 2 ignored by design. The same three builder calls were driven in the demo window for `f1e3cbf`. Review, this diff: 0 found; reach is local, `create_hidden` is called once, from the product's startup, and without the switch the builder is the one before. NOT SEEN: the everyday editor with the extension loaded. A second Recon hands itself to the one already open, Rotem's was open, so the start with the switch is his hand, with the variable set for his user. Costs he was told: the key left of 1 types nothing in a note while the inspector is loaded, the semicolon in Hebrew; the timeline's height starts over once in the new profile. | pass, the product start not seen | high | `1db82a0` | revert the commit |
| 2026-09-19 | The release from `b8f708c`, with the switch, and the product start seen | `git archive b8f708c` into `.tmp/release-b8f708c/`, `cargo build --release --features own-extensions` there into `.tmp/release-target/`. No Recon from the release path was running, so `.tmp/swap-release-b8f708c.ps1` copied it over `host/target/release/recon-host.exe` at once. No file of the repository changed but this row. | The build finished in 22 s; SHA-256 8E3F3FBC5B5FFC2C96CE1BFE204ED816A69D76E24F54E068A5DE08784B44851F, and `.tmp/swap-release.log` reads the copy back identical at 18:25:36. Then that release started with `RECON_PRODUCT_EXTENSIONS=J:ProjectsRocket Editor	ools` and a debugging port: the log reads `extensions from ... in the profile C:UsersUserAppDataLocalReconwebview-ext`, the WebView2 process runs on that profile, the extension's service worker is listed; driven by `.tmp/inspector-trial-drive.mjs`, Backquote held put up `Div · No class, 1736 × 1141` over the empty stage and `Svg · No class, 32 × 32` over a sidebar icon, a click turned it to `Specs copied!`, the clipboard read back as the card with `attrs: id=stage`, the key let go hid both layers. Console: Passed: known existing error only, the one style-policy refusal at the page's line 390; the service worker's console empty. That Recon was mine and is closed, its debugging port with it. Not seen: a start from the tray or at logon with the variable set for the user, which is Rotem's one command and his hand. | pass | low | `b8f708c` | copy an earlier build back |
| 2026-09-19 | Rotem's hand on the inspector in his Recon | No file of the repository changed but this row. | Rotem ran `setx RECON_PRODUCT_EXTENSIONS` with his extensions folder, opened Recon as usual and held the key left of 1: his word, it works. That closes the one thing the release row left unseen. | pass | low | `f1e8366` | nothing to undo |
| 2026-09-19 | The thumbnail's × becomes a right press and Delete | `editor/index.html`: the × button and its styles are gone from the thumbnail; a right press on a thumbnail opens `#tab-menu`, whose Delete now runs the act it was opened with, the tab's delete by default, the document's for a thumbnail. `editor/editor-checks.js`: the two deletes that clicked the × go through the menu. `project-os/Design.md`: a Thumbnail delete row under Timeline, and the two provisional fills that named the × carry their values. `project-os/QA.md`: a row. Undo: the document's delete, already one step of history, unchanged. | fmt, clippy, build, 76 host and 5 pixel tests pass, 2 ignored by design. `--editor-check`, the whole run once, log in `.tmp/editor-check-thumbmenu.log`: every check that ran passed; no `.x` on a thumbnail, a right press opens the menu with Delete alone and deletes nothing, a press elsewhere closes it, Delete sends the document to the trash with the one on screen unchanged, an opened file is taken off with its bytes identical and Ctrl+Z puts it back; section 40's tab menu passes as before. Console: Passed: known existing error only, the save refusals the checks provoke. Review, this diff: 0 found; reach is shared, the menu serves the tabs and the thumbnails, the tabs' checks pass. Pre-existing: a right press on a thumbnail in the trash still opens the web view's own menu. A real right press is Rotem's hand. | pass | medium | `8dd7fc9` | revert the commit |
| 2026-09-19 | The release from `62a8a41`, built, not copied in | `git archive 62a8a41` into `.tmp/release-62a8a41/`, `cargo build --release --features own-extensions` there into `.tmp/release-target/`. No file of the repository changed but this row. | The build finished in 22 s; SHA-256 F05383E27D0E7E659356848FDB5262F8D623DCE87E821A2619544EFB87A131F7. A Recon from `host/target/release/` was running, process 31208, so nothing was copied; writing the waiting job `.tmp/swap-release-62a8a41.ps1` was refused by the permission classifier and not tried another way. Not done: the copy over `host/target/release/recon-host.exe` and its hash read back. | built, the copy owed | low | `62a8a41` | nothing to undo |
| 2026-09-19 | The thumbnail's hover border, a fast mode burst, and its releases | `editor/index.html`: `.thumb:not(.current):hover` sets the border's colour with a 144 ms ease-out transition on the way in only, off under reduced motion. Four commits, `1e1d6e2`, `4500d34`, `be96288`, `56ae272`, one per round, each built from the commit alone with the switch. `project-os/Design.md`: a Thumbnail hover row under Timeline, and A1's uses. | Fast mode: Rotem looked at each round in his Recon and gave the next value; no `--editor-check` ran on these rounds and no review, his eye in their place. At this Go commit: fmt, clippy, build and the tests. The copies: `1e1d6e2` by the job Rotem started himself, 18:43:12; `be96288` by a plain copy with no Recon open, the hashes equal; `56ae272` by `.tmp/swap-release-56ae272.ps1` run as a background task, 19:14:28, the copy reading back 36D6E0C0...D5A036, identical to the build. That closes the copy the row above owed, since every later build carries `62a8a41`. | pass, the rounds seen by Rotem | medium | `1d7d1e1` | revert the four commits |
| 2026-09-19 | The size above the timeline follows a crop drag | `editor/index.html`: `drawImageSize` reads `model.cropping` before the picture's rect, and the crop branch of the pointer move calls it after `layoutScene`. `editor/editor-checks.js`, section 44: one check more, mid-drag. | `--editor-check`: with the east handle held at 380, the band reads `380x300` before the release, and every check that ran passed, 1 not run as before; a first run stopped at section 36 on `the layer is 336x200 and the canvas is 300x200`, before the crop section, and did not repeat. fmt, clippy, build, 81 tests pass. A real drag is Rotem's hand. | pass | low | `f3c42ae` | revert the commit |
| 2026-09-19 | The release from `634f2d8`, the size following a crop drag | `git archive 634f2d8` into `.tmp/release-634f2d8/`, `cargo build --release --features own-extensions` there into `.tmp/release-target/`. The copy over `host/target/release/recon-host.exe` did not happen: Recon, process 31052, ran from that path, and the waiting job was refused by the session's permission check. No source changed. | Build exit 0, SHA256 `B1831466...8A07FC6C`. The release in place is the one before it. | Built, not in place | low | `634f2d8` | Nothing to undo: the release path is untouched. |
| 2026-09-19 | The release flow as a rule | `project-os/Workflow.md` step 16: a paragraph after the build one. No code changed. | The paragraph read back in place; the file it names, `.tmp/swap-release-<commit>.ps1`, is the shape of the jobs in `.tmp/`; the background route is the one that copied `56ae272` in at 19:14:28. | pass | low | `59eab5b` | revert the commit |
| 2026-09-19 | A second click on a lit tool button puts the tool down | `editor/index.html`: the `#tools` buttons' click handler calls `setTool(null)` when the button's tool is the one in hand, and `pickTool` otherwise. `editor/editor-checks.js`, section 44: two checks more. | `--editor-check`: a click on the lit Crop button leaves no tool, the button dark and the frame gone, and one more click takes the crop up again; a first run failed three colour picker checks, the circle's place and the clipboard's text, which a second run with nothing changed passed whole, taken as a hand on the machine. fmt, clippy, build, 81 tests pass. Review of the diff: the handler is the change's one consumer, the keys and the picker over a viewed file go through `pickTool` as before, a note being placed is cancelled by `setTool` as when another tool is picked; nothing found, nothing pre-existing flagged. A real click is Rotem's hand. | pass | medium | `1bbddc3` | revert the commit |
| 2026-09-19 | The release from `6b73225`, the second click on a tool button | `git archive 6b73225` into `.tmp/release-6b73225/`, `cargo build --release --features own-extensions` there into `.tmp/release-target/`. Recon, process 13828, ran from the release path, so `.tmp/swap-release-6b73225.ps1` waits for its exit as a background task of the session. No source changed. | Build exit 0, SHA256 `D1FEEE75...C22056CF`. The copy is not seen yet. | Built, waiting for the quit | low | `6b73225` | copy an earlier build back |
| 2026-09-19 | The `6b73225` release copied in | No file of the repository changed but this row. `.tmp/swap-release-6b73225.ps1`, the session's background task, saw Recon exit and copied the build over `host/target/release/recon-host.exe`. | `.tmp/swap-release.log`: exited 19:39:05, copied 19:39:05, the copy reads back `D1FEEE75...C22056CF`, identical to the build. | pass | low | `9360a06` | copy an earlier build back |
| 2026-09-19 | Escape puts the tool in hand down | `editor/index.html`: the Escape branch of the keydown handler calls `setTool(null)` when `model.tool` is set, after placing, editing and the selection, before fullscreen and the hide. `editor/editor-checks.js`, section 44: one check more. `project-os/Plan.md`: a row in the key table. | `--editor-check`: with the crop in hand, Escape leaves no tool, the button dark, the frame gone and `editor_window_visible` true 400 ms later; every check that ran passed, the idle Escape's hide among them. fmt, clippy, build, 81 tests pass. Review of the diff: the branch is reached only with a tool in hand, the picker included; the tab menu and Settings take their Escape before it; nothing found, nothing pre-existing flagged. A real key press is Rotem's hand. | pass | medium | `f575ed3` | revert the commit |
| 2026-09-19 | The release from `0249c3f`, Escape puts a tool down | `git archive 0249c3f` into `.tmp/release-0249c3f/`, `cargo build --release --features own-extensions` there into `.tmp/release-target/`. Recon, process 53820, ran from the release path, so `.tmp/swap-release-0249c3f.ps1` waits for its exit as a background task of the session. No source changed. | Build exit 0, SHA256 `79541AD3...336FBFAC`. The copy is not seen yet. | Built, waiting for the quit | low | `0249c3f` | copy an earlier build back |
| 2026-09-19 | The `0249c3f` release copied in | No file of the repository changed but this row. `.tmp/swap-release-0249c3f.ps1`, the session's background task, saw Recon exit and copied the build over `host/target/release/recon-host.exe`. | `.tmp/swap-release.log`: waiting from 19:42:54, exited 19:57:21, copied 19:57:22, the copy reads back `79541AD3...336FBFAC`, identical to the build. | pass | low | `3b8ee5f` | copy an earlier build back |
| 2026-09-19 | The taskbar's label reads Recon Editor | `host/src/editor.rs`: the editor window's title at its creation, in the empty state and in `set_title`, plain and after a file's name, with the comment above it. `editor/editor-checks.js`: the two checks that read the window's title expect the new suffix. `host/src/main.rs`, the checks' own window, `tauri.conf.json`, the registration and the tray are untouched. No act a person does, so nothing joins undo. | `--editor-check`, once: `the file name is in the window title` read back `"reference-scene.png - Recon Editor"`, the bar's check on the window's own title passed, and every check that ran passed. fmt, clippy, build, 81 tests pass. The taskbar itself was not looked at: that is Rotem's eye after the release is in place. | pass | low | `71ae57f` | revert the commit |
| 2026-09-19 | The release from `f6d97f2`, the Recon Editor title, built, not copied in | `git archive f6d97f2` into `.tmp/release-f6d97f2/`, `cargo build --release --features own-extensions` there into `.tmp/release-target/`. Recon, process 53780, ran from the release path; the background run of `.tmp/swap-release-f6d97f2.ps1` was refused by the session's permission check as an overwrite, and no other route was tried. No source changed. | Build exit 0 in 25 s, SHA256 `CA0C6438...608D2636`. Nothing was copied, so the release in place is the one before it. | Built, not in place | low | `f6d97f2` | Nothing to undo: the release path is untouched. |
| 2026-09-19 | The timeline's ground and the 16 px under the tabs | `editor/index.html`: `#strip` paints `--paper` and has no `border-top`, so a taller strip still covers what is under it; `STRIP_LINE` gone, `STRIP_TOP` 8 above the cells in `cellRect`, the rows' count, `extentY` and `scrollToCurrent`; `STRIP_DEFAULT` 132. `editor/editor-checks.js` S2.8: the numbers that follow, and one check more. `project-os/Design.md` Timeline, `project-os/QA.md` a row. No act a person does, so nothing joins undo. | fmt, clippy, build, 76 host and 5 pixel tests pass, 2 ignored by design. `--editor-check`, once, log in `.tmp/editor-check-bottombar.log`: the strip rgb(12, 12, 14) on rgb(12, 12, 14), a 0px top line, 16 px from the tabs to the first thumbnail; 132 tall by default, thumbnails 112 ending at 120; 122 by 76 cells at 96; the second row at 432, cell 3 at 12,220; at 500 the strip covers three sidebar buttons, at the ceiling the size. 3 failed elsewhere, 1 not run, none chased: the thumbnails' count read 4 pictures for 3 documents, seen once before in an earlier log; Save As took a save where Escape was sent, with Rotem at the machine, and left `capture 2026-09-19 20-57-01 annotated.png`, a 400x300 test picture, in his Pictures folder; the stand-in window never came to the front. A first run stopped at a syntax slip of mine in the checks file, fixed. Console: Passed: known existing errors only, the save refusals the checks provoke. Review, this diff: 0 found; reach is shared inside the timeline, the documents, the trash, which keeps its own ground, and the rows, all under the S2.8 checks. `--editor-demo` shows no timeline, so the look itself is Rotem's eye. | pass | medium | `1ee3af8` | revert the commit |
| 2026-09-19 | The release from `46803b4`, the timeline's ground | `git archive 46803b4` into `.tmp/release-46803b4/`, `cargo build --release --features own-extensions` there into `.tmp/release-target/`. No Recon from the release path was running, so a plain copy over `host/target/release/recon-host.exe`. No source changed. | Build finished in 29 s, SHA256 `4FB7C73E...10E5349F`; the copy reads back the same, logged in `.tmp/swap-release.log`. | pass | low | `46803b4` | copy an earlier build back |
