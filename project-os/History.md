# Recon — History

The change log. Every completed change lands here, in two layers: a scan table
you always read, and an appendix you read only when digging.

The scan table answers "what happened lately". The appendix answers "what exactly
did that change do, and how do I undo it".

## How you maintain this file

- **After every completed change**, add one scan row AND one appendix row. Both,
  in the same change that did the work.
- **One change = one row.** Not one row per file, not one row per session. A log
  that has to be reassembled from fragments is a log nobody reads.
- Write the scan row for a reader who was not there. Name the behavior that
  changed, not the files.
- Keep appendix rows short — a few lines, not an essay. The full story is in the
  commit diff; a real decision belongs in `project-os/Decisions.md`.
- Record the commit SHA from **before** the change. That is the rollback target.
- Never rewrite or delete a past row. Correct a wrong one by adding a new row —
  an edited log cannot be trusted about anything.
- A `medium` or `high` risk row names its review result under **What was
  checked**: findings found, findings fixed, pre-existing ones flagged. A
  medium-or-higher row without that is a task that is not finished.
- Never write "tested" or "QA passed". Those phrases record nothing. Name the
  input, the screen, and what happened.
- When this file gets long, move the oldest rows into an archive file beside it.
  `project-os/rotate.ps1` does exactly that at `Go commit`, and creates
  the archive the first time it is needed. Rows move **verbatim** — never
  rewritten, never summarized, never merged, because the detail you drop is the
  one the next reader needed. Never hand-move rows: the script dedups, so it is
  safe to run every time, and a hand-move breaks that guarantee.

## Risk scale

The scale's one home is `project-os/Workflow.md` step 2 — read it there, so the
two files can never disagree. State the level at task pickup; the owner's
override wins.

## Scan log

_Older scan rows archived -> see `History-scan-archive.md` (moved by project-os/rotate.ps1, not rewritten)._

Newest at the bottom.

| Date | Area | What changed |
|---|---|---|
| 2026-09-14 | plan | **Firefox needs no support, at Rotem's word: the whole browser lit there is final, and nothing further is planned for it.** |
| 2026-09-14 | host | **The frame around the lit area is dashed now and walks slowly: light dashes and dark gaps, six pixels each, one pixel every 120 ms.** Only the frame's four strips are repainted on a tick, so the walk costs nothing to speak of, and a dash turns a corner instead of restarting. |
| 2026-09-14 | host | **The frame is tuned to Rotem's numbers: 2 pixels thick, blue #2D41D7 dashes of 12 on dark gaps of 8, one pixel every 256 ms.** The band sits just inside the lit area, the top and bottom bands taking the corners. |
| 2026-09-14 | host | **The frame's dashes are #00B9F7 now, at Rotem's word.** |
| 2026-09-14 | host | **The dashes walk at 1.48 pixels a second now, one pixel every 676 ms, at Rotem's word.** |
| 2026-09-14 | host | **The dashes walk at 8 pixels a second now, one pixel every 125 ms, at Rotem's word: at 1.48 they did not seem to move.** |
| 2026-09-14 | host | **The dashes glide now instead of stepping, at 12 pixels a second: the walk is read off the clock about sixty times a second, and the pixel at every dash edge is blended by the fraction in between.** Rotem saw the pixel steps at 8 a second and asked for smooth. |
| 2026-09-14 | host | **The dashes glide at 24 pixels a second now, at Rotem's word.** |
| 2026-09-14 | host, editor | **The window's top bar is Recon's own now: Windows' frame is gone, and the page draws a dark bar with the file's name on the left, the tool buttons in it, and minimize, maximize and close on the right.** The bar drags the window, a double-click on it maximizes, close hides as it did, and fullscreen puts the bar away. Rotem's call, between recolouring Windows' bar and drawing one. |
| 2026-09-14 | editor | **The top bar has no colour of its own and no line under it now: it sits on the page's own background.** Rotem's word, by eye, on the first look. |
| 2026-09-14 | editor | **The timeline runs newest first now: the latest picture is at the left.** Rotem's word. |
| 2026-09-14 | editor | **The app's background is #111217, at Rotem's word.** The page ground behind the picture and the panels changed from #101214; nothing else moved. |
| 2026-09-14 | editor | **A picture opens with no tool in hand now: the ordinary pointer over the whole window, and a click on the picture creates nothing until a tool is chosen.** A chosen tool works as before, crosshair included, until the next picture is shown; viewing shows the ordinary pointer too. Rotem's call. |
| 2026-09-14 | editor | **The wheel zooms in and out around the pointer now, with Ctrl or without; only a sideways wheel pans.** Rotem's call. In annotation the arrow keys are the pan that is left. |
| 2026-09-14 | plan | **The timeline has a spec for scale and for growing: S2.8, unbuilt.** Today the strip fetches every thumbnail at startup and keeps every one, so memory grows with the library against §3.8; the spec answers with a windowed strip that holds a screenful, no View More button, and a drag on the strip's top edge that enlarges thumbnails to 320 wide and then wraps rows, with the thumbnail file made at 320 once and served at the size shown. |
| 2026-09-14 | editor | **With no tool in hand a drag on the picture pans it now, and holding Space gives any tool the ordinary pointer and the same pan; letting go gives the tool back.** Space typed into a note is still a space. Rotem's call. |
| 2026-09-14 | host, editor | **S2.8 is built: the timeline holds a screenful of cells whatever the library holds, and its top edge drags it taller, the thumbnails growing to 320 wide and then wrapping into rows.** Cells are placed by arithmetic and only the ones near the screen exist, their pictures dropped when they scroll away, at most eight fetched at once; 96 is the floor and the default, 216 one row of 320 by 200, then whole rows up to 60% of the window, a double-click resets, the height is remembered. The host makes the thumbnail file at 320 by 200 and remakes an old 160 one once. Thirty thousand documents scan in about two seconds at startup, so the whole-list read stays, with the number in the plan for Rotem's call. |
| 2026-09-15 | editor | **The timeline may take up to 96% of the window now, at Rotem's word; it was 60%.** Nothing else in the strip moved. |
| 2026-09-15 | host | **Recon can start with Windows now: a "Start with Windows" tick in the tray menu, and started that way it waits in the tray with no window.** The tick writes one value under the user's own Run key and unticking removes exactly that; a start by hand still opens the window. Rotem's ask; the tray-only start at logon is the assistant's call, his to veto. |
| 2026-09-15 | host | **The release at `host/target/release/recon-host.exe` carries the Start with Windows tick now, built from the commit alone.** Rotem's pick of the two next steps. |
| 2026-09-15 | editor | **A drag moves the picture while the pointer moves now, not only on release: what is painted slides with the pointer at once and the fresh picture fills in behind it.** Rotem's report. |
| 2026-09-15 | host, editor | **Recon starts at once at any library size: the newest fifty records are read before the window shows and the latest reopens, the rest of the store is read on a thread, and the page takes the whole list when it lands.** Not the read-by-month that was named: Annotate's resume must see every document, so the list is read whole, in the background. Found and fixed on the way: the storage figure in the HUD walked every folder and file on every timeline refresh; it comes from a ledger now, corrected by each write, move or removal. |
| 2026-09-15 | editor | **The timeline's handle follows the hand exactly now, and a row of thumbnails enters the moment there is room for it; the snap to whole rows, which jumped the handle away from the pointer, is gone at Rotem's word.** A single row never grows past 320 by 200; the slack below it waits for the next row. |
| 2026-09-15 | project-os | **The assistant no longer narrates between edits: one line at pickup, then the report, at Rotem's word.** The rule sits in the reply rules' in-flight narration section. |
| 2026-09-15 | editor | **An edge a drag uncovers shows the picture at once now: a small copy of the whole picture lies under the sharp one while the drag lasts, and the edge sharpens as the fresh picture lands.** Rotem's report. |
| 2026-09-15 | host | **The S0.6 reference environment records the picture area as 1280 by 768 CSS pixels now, as a check run measured it; the reference images did not change.** Found uncommitted at Go commit. |
| 2026-09-15 | host | **The release at `host/target/release/recon-host.exe` carries the drag's edge fix now, built from the commit alone and swapped in the moment Rotem quit Recon.** |
| 2026-09-15 | editor, project-os | **The controls are a sidebar of icon buttons down the left edge now: the six tools, Copy, Save As and the mode as 32 by 32 squares with a 20 by 20 icon, 8 px apart, no fill of their own, and a hover fills the square with #21222C, fading in over 144 ms.** Rotem's spec; the fade is the design system's first token, A1, in the new `project-os/Design.md`. Found and fixed on the way: the page measured its pixel ratio against the picture area's width, which the sidebar narrows, so every copy came out 4% wrong until the ratio was measured against the whole page. |
| 2026-09-15 | host | **The release is rebuilt with the controls sidebar, from the commit alone, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon that is running.** Rotem's word, BUILD. |
| 2026-09-15 | editor | **When the timeline's thumbnails wrap into rows, its vertical scroller is Rotem's own now: a 4 px thumb of #21222C, fully rounded, on no track, 4 px clear of the window's bottom and of the strip's top.** The one-row strip's sideways scroller is still the system's. |
| 2026-09-15 | host | **The sidebar release is in place: the swap ran at 18:48:42 when Rotem quit Recon, the file reads back identical to the build, and his Recon opened on it one second later.** |
| 2026-09-15 | editor | **The sidebar's nine buttons sit in one container centred across the sidebar, and hovering a button opens its name in a tooltip on its right.** Rotem's word. The tooltip's look is provisional until he states one; the key hints that used to show in the system's own tooltip are gone with it. |
| 2026-09-15 | editor | **The one-row timeline's sideways scroller is Rotem's own now too: a 4 px thumb of #21222C, fully rounded, 4 px clear of the window's bottom and 4 px clear above.** His spec was this scroller all along; the rows scroller keeps the same look. |
| 2026-09-15 | host | **The release at `host/target/release/recon-host.exe` carries both timeline scrollers now, built from the commit alone and put in place at once, since no Recon was running.** Rotem's word, BUILD. |
| 2026-09-15 | editor | **Zooming out stops at the whole picture now: its actual size when it fits the window, the whole of it when it is larger; and when the window, full screen or the timeline changes the space, a picture shown whole grows or shrinks to the new whole view.** Rotem's word, both; the wheel and the - key used to go down to a quarter of the fit. |
| 2026-09-15 | editor | **The sidebar's button group is centred in the sidebar's height now, between the top bar and the timeline, instead of sitting at its top.** Rotem's correction: centring it across the width, which it already filled, was the wrong reading of his words; the slip's second time, so it is a rule in `CLAUDE.md` rule 1 now. |
| 2026-09-15 | project-os, host | **Every committed code change is built into the release without asking now, at Rotem's word; the release for the centred sidebar group is built and replaces the running one the moment Rotem quits Recon.** The rule sits in the workflow's delivery step. |
| 2026-09-15 | host | **The release with the centred sidebar group is in place: the copy ran at 20:39:51 when Rotem quit Recon, it reads back identical to the build, and his Recon opened on it a second later.** |
| 2026-09-15 | host | **Recon's window opens in the middle of the screen now, across and in height, instead of near the left edge.** Rotem's word. On a display smaller than the window, the window is shrunk to fit first, so its top bar is never off the screen. |
| 2026-09-15 | host | **The release with the centred window is built from the commit alone, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon that is running.** |
| 2026-09-15 | host | **The release with the centred window is in place: the copy ran at 21:29:44 when Rotem quit Recon, it reads back identical to the build, and his Recon opened on it at 21:29:58.** The first waiting job copied nothing, since Rotem's Recon was back within a second. |
| 2026-09-15 | plan | **Rotem's hand on the 21:29 release: the window opens in the middle of the screen, and it works.** |
| 2026-09-15 | editor | **A timeline dragged taller covers the sidebar now: the buttons below its top edge sit under it instead of over its thumbnails, and the handle on its edge stays on top.** Rotem's report, with a screenshot. |
| 2026-09-15 | host | **The release with the timeline over the sidebar is built from the commit alone, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon that is running.** |
| 2026-09-15 | host | **The release with the timeline over the sidebar is not in place: the waiting job that copies it in was refused by the session's permission check, so no copy will run when Rotem quits Recon.** Correcting the row above. |
| 2026-09-15 | host | **The release with the zoom-out floor, and the timeline over the sidebar with it, is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** No waiting job: the one for the previous release was refused by the session's permission check. |
| 2026-09-15 | host | **The release with the zoom-out floor is in place: copied over `host/target/release/recon-host.exe` at 23:11:01 once Rotem quit Recon, the file reading back identical to the build.** Rotem's word, DONE. |
| 2026-09-15 | editor | **A press anywhere on the one-row timeline and a move of the mouse left or right scrolls it now, following the hand; a press that moves less than 4 px is still a click and shows its thumbnail.** Rotem's word. The rows the timeline wraps into when dragged taller scroll as before. |
| 2026-09-15 | host | **The release with the timeline's press-and-drag scroll is built from the commit alone and waits in `.tmp/release-target/`; it is not copied over `host/target/release/recon-host.exe`, since the Recon from that path is running and the last waiting copy was refused by the session's permission check.** |
| 2026-09-15 | editor | **A picture opened while the timeline fills the window no longer vanishes at the next zoom key: it opens at the fit kept from before, and takes its own whole picture the first time the space returns.** Rotem's word, FIX, on a bug the zoom-out review found. |
| 2026-09-15 | host | **The release with the fix for a picture opened with no room, and the timeline's drag scroll with it, is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-15 | plan | **Rotem's hand on the timeline: a press and a sideways move scrolls it, and dragged taller it covers the sidebar; he called it perfect.** The release he ran was the one copied in at 23:31:30, built from `9adad68`, which carries both changes; the build from `73d51f4` it replaced was never copied. |
| 2026-09-15 | host | **Recon's window opens 1600 wide and 1160 tall now, centred as before; it was 1280 by 800.** Rotem's word. |
| 2026-09-15 | host | **The release with the window opening at 1600 by 1160 is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with the window opening at 1600 by 1160 is in place: copied over `host/target/release/recon-host.exe` at 00:14:39 once Rotem quit Recon, the file reading back identical to the build.** Rotem's word, GO. |
| 2026-09-16 | plan | **Undo of a deletion has a spec, S2.9, nothing built: Ctrl+Z brings back the picture just deleted from the timeline, the last thing done being what undo takes, and Ctrl+Y deletes it again.** Rotem's pick over undo buttons in the sidebar and over undo that survives a restart; three calls in the step are his. |
| 2026-09-16 | project-os | **A choice put to Rotem is shown as the scenes he would see, never as a rule's name: the slip's second time, so it is a line in the reply rules now.** He could not tell what the S2.9 step's three calls asked. |
| 2026-09-16 | plan | **S2.9's three calls are Rotem's now: Ctrl+Z undoes the last thing done, a deletion included; Ctrl+Shift+Z deletes it again, no Ctrl+Y named for it; and no notice when the picture comes back.** Still a spec, nothing built. |
| 2026-09-16 | host, editor | **S2.9 is built: Ctrl+Z brings back the picture just deleted from the timeline, with its notes and their own undo, the last thing done first; Ctrl+Shift+Z deletes it again; from the empty state too; nothing is said on screen.** Two things the review caught are fixed: a refused restore no longer blocks every later Ctrl+Z, and the empty path no longer tries a save into the trash. |
| 2026-09-16 | host | **The window opens at 1600 by 1160: measured on the release Rotem opened at 00:33, not only read in the code.** |
| 2026-09-16 | editor | **The timeline's thumbnails are 112 px tall by default now, the timeline 128 px; the floor a drag can reach stays 96.** Rotem's word. A height once dragged is still remembered, and a double-click on the top edge returns to the new default. |
| 2026-09-16 | host | **The release with the 112 px thumbnails is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with the 112 px thumbnails is in place: copied over `host/target/release/recon-host.exe` at 00:41:08 once Rotem quit Recon, the file reading back identical to the build.** Rotem's word, GO. |
| 2026-09-16 | review | **The whole repository reviewed again, Stage 3 and the timeline in: twelve findings, none fixed, every one waiting on Rotem's verdict.** The one that matters: a character typed during the few milliseconds a save is on its way can be left off the disk while the HUD says saved. Three important: a capture deleted while its image is still being encoded loses that image for the trash; the store's writes are never flushed before the rename, so a power cut can leave an empty record; closing the editor after any capture brings the last capture's application to the front, however the editor was opened. Eight nits with their homes. The document is `notes/2026-09-16-code-review-3.md`. |
| 2026-09-16 | editor | **The picture's size in pixels sits in a container across the whole window above the timeline now, 16 px clear above and below it, on the app's own background, written as 1200x1500.** Rotem's spec; the text's look is provisional until he states one. Side effect: the sidebar ends above the container, so its buttons sit 24 px higher. |
| 2026-09-16 | host | **The release with the image size container is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with the image size container is in place: copied over `host/target/release/recon-host.exe` at 00:57:14 once Rotem quit Recon, the file reading back identical to the build.** Rotem's word, GO. |
| 2026-09-16 | host, editor | **The twelve findings of review 3 are fixed at Rotem's FIX ALL, T12 apart, which is his hand: a character typed while a save is on its way now reaches the disk; a capture deleted while its image encodes keeps the image in the trash and comes back whole; every document write and every export is flushed before it is named; a hide returns the focus once, so an editor opened from the tray closes without a jump; Save As names no format and refuses a second dialog; a delete that cannot move says NOT DELETED and keeps the document.** Found on the way: a delete refused while a thumbnail held a file in the folder, now retried for a second. |
| 2026-09-16 | editor | **The mouse wheel over the one-row timeline scrolls it left and right now, down to the right; once the timeline wraps into rows the wheel scrolls it up and down as before.** Rotem's word. |
| 2026-09-16 | host | **The release at `host/target/release/recon-host.exe` carries the timeline's wheel scroll now, built from the commit alone and put in place at once, since no Recon was running.** |
| 2026-09-16 | plan | **Rotem's hand on the 01:30 release: the wheel over the one-row timeline scrolls it sideways, and it works great.** |
| 2026-09-16 | project-os | **The old build folders in `.tmp/` are cleared at Rotem's word, about 14 GB; the drive had filled to 2 MB free during the wheel task. `.tmp/release-target/`, where each release is built, is kept.** |
| 2026-09-16 | project-os | **Rotem asked why the replies broke the reply rules: status lines between steps, and reports too long, some without headings. One row in the mistakes list records it.** |
| 2026-09-16 | host, editor | **The twelve findings of review 3 are fixed at Rotem's FIX ALL, T12 apart, which is his hand: a character typed while a save is on its way now reaches the disk; a capture deleted while its image encodes keeps the image in the trash and comes back whole; every document write and every export is flushed before it is named; a hide returns the focus once, so an editor opened from the tray closes without a jump; Save As names no format and refuses a second dialog; a delete that cannot move says NOT DELETED and keeps the document.** Found on the way: a delete refused while a thumbnail held a file in the folder, now retried for a second. |
| 2026-09-16 | project-os | **The S2.9 commit `de9feb5` undid another session's review-3 commit `40eb3ec` by mistake; `9488cd3` put every line of it back, and the tree with both passes every check.** The assistant's slip is in Mistakes: it committed the shared index while the other session had just committed from the same working tree. |
| 2026-09-16 | host | **The release with undo of a deletion and the review-3 fixes is built from `2639eec` alone and in place: copied over `host/target/release/recon-host.exe` at 01:42:28, no Recon running from that path, the file reading back identical to the build.** |
| 2026-09-16 | editor | **The picture's size is centred in the window now, in Google Sans at 14 px, medium, the font bundled with the page from Google Fonts.** Rotem's spec; only the Latin file, 23 KB, with its Open Font License. The colour and the 16 px line stay provisional. |
| 2026-09-16 | project-os | **Decisions carries each entry once again: the Google Sans commit had copied the focus-return entry in a second time beside its own.** Found reading that commit back. |
| 2026-09-16 | host | **The release with Google Sans in the image size is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with Google Sans in the image size is in place: copied over `host/target/release/recon-host.exe` at 02:11:42 once Rotem quit Recon, the file reading back identical to the build.** Rotem's word, GO. |

| 2026-09-16 | editor, project-os | **Go commit: the docs rotated (111 items moved into the archives), and the one line left uncommitted in the tree, Save As answering true when its dialog opened, landed with them.** No behaviour changed beyond what the review-3 row already records. |
| 2026-09-16 | editor | **A capture is on the clipboard the moment the editor opens with it, with no key pressed: the same copy as Ctrl+C, so the HUD says copied, or NOT COPIED with the reason.** Rotem's word. A file opened, a frame stepped or an older picture shown from the timeline is never copied unasked. |
| 2026-09-16 | host | **The release with the capture's automatic copy is built from the commit alone; it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with the capture's automatic copy is in place: copied over `host/target/release/recon-host.exe` at 19:25:41 once Rotem quit Recon, the file reading back identical to the build.** Rotem's word, BUILD, found it already done. |
| 2026-09-16 | plan | **Rotem's hand on the 19:25 release: a capture lands on the clipboard by itself, and it works perfectly.** |
| 2026-09-16 | host, editor | **The timeline has tabs now: a plus at the left of the picture's size band makes Main and New tab, and another New tab at every press; a capture taken on a tab lands in Main and in that tab's own feed; a tab is deleted by its ×, never Main, and dragged into another place after Main; the list survives a restart.** Rotem's ask, for the feeds of tasks a client's remarks become; the plus is his spec, the tabs' look provisional. Found and fixed on the way: with a tab whose feed was empty selected, leaving fullscreen hid the timeline. |
| 2026-09-16 | host | **A capture brings a minimized editor back to the front now: the window is restored before it is shown and focused, as a click on its taskbar button would.** Rotem's report: since the automatic copy he leaves the editor minimized instead of hiding it, and the next capture left it in the taskbar. Side effect: the tray's click and Open, and a second instance, restore a minimized window the same way. |
| 2026-09-16 | host | **The release with the minimized editor's return is built from the commit alone; it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with the timeline's tabs is built from the commit alone, `d356b38`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** The tabs row above names `1bd859e` as its rollback target; the commit before it was `950e855`, another session's, and reverting to that one keeps that session's work. |
| 2026-09-16 | editor, project-os | **The tabs are finished to Rotem's spec: 14 px text on 10 px corners; a click on the selected tab's name edits it and Enter keeps it; the × pressed once turns into a ✓ and pressed again deletes; in a tab every thumbnail carries a round done mark, shown on hover and kept once pressed; and adding, deleting and renaming a tab and the mark are in Ctrl+Z and Ctrl+Shift+Z.** New rule 23 in `CLAUDE.md`: every new action joins undo and redo. Found and fixed on the way: the undo list dropped every act it was handed, and a tab bar reloaded from disk kept stale tabs behind its elements. |
| 2026-09-16 | host | **The release with the timeline's tabs is in place: copied over `host/target/release/recon-host.exe` at 22:00:35 once Rotem quit Recon, the file reading back identical to the build; his Recon opened on it six seconds later.** The other session's waiting job for the minimized editor's return copied nothing, its build having been replaced; that change is inside this one. |
| 2026-09-16 | host | **The release with the finished tabs, rename, the ✓, the done mark and their undo, is built from the commit alone, `40cfadf`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-16 | editor | **The done mark on a thumbnail is a 24 px circle with a 2 px white border now, its 20 px tick filling the inside with no gap, and the tick fades in by A1 when pressed.** Rotem's spec, correcting the 20 px mark with a ringed icon. |

## Appendix — deep rows

_Older rows archived -> see `History-archive.md` (moved by project-os/rotate.ps1, not rewritten)._

Newest at the bottom, same as the scan log.

| Date | Task | What changed | What was checked | Result | Risk | Commit before | Rollback |
|---|---|---|---|---|---|---|---|
| 2026-09-16 | S2.9's calls settled | `project-os/Plan.md`, the S2.9 step: the rule marked as Rotem's "A"; redo by Ctrl+Shift+Z alone; the notice gone from the coming-back and from the checks; the open-calls line closed; the check sequence rewritten to match. No code changed. | The step read back; every Ctrl+Y in it is gone, the failure line kept. Risk low, a plan step. | Pass | low | `b62353d` | Revert the step's four edits. |
| 2026-09-16 | The thumbnail-height release swapped in | `host/target/release/recon-host.exe`: the build from `f49447f` in `.tmp/release-target/` copied over it at 00:41:08, after Rotem said GO; no Recon process was running, checked right before the copy, and no code was committed after `f49447f`. It replaced the `61a3ea8` build. No source changed. | SHA-256 of the build and of the release read back identical, `d79fc8a4...0a4a5a62`. Not run: the thumbnails under Rotem's eye, and a height he may have dragged before, which his Recon still remembers. Risk low, a copy. | Pass | low | `4cba482` | Rebuild the release from `b62353d`, whose thumbnails are 80 px by default. |
| 2026-09-16 | Code review 3, the whole repository | `notes/2026-09-16-code-review-3.md`, new: twelve findings at `965b53b`, one blocking (T1), three important (T2 to T4), eight nits (T5 to T12), each with where, the problem, the fix and the check that proves it; the four open findings of the 2026-09-13 pass re-read, R6 closed by S1.1, R8 to R10 still open. No code changed. The working tree's uncommitted image-size band was read with the rest and raised nothing. | Every file under `host/`, `editor/`, `project-os/guards/` and the two scripts read in full, one agent and no sub-agents at Rotem's word; the always-check list walked row by row. No check run: nothing changed. Risk low, a document. | Pass | low | `965b53b` | Delete the document and these two rows. |
| 2026-09-16 | The image size container | `editor/index.html`: `#image-size`, a block across the window, 16 px tall with a 16 px margin above and below, on `var(--paper)`, `z-index: 4` like the timeline, its text provisional: 12 px on a 16 px line in `#cfd4da`, 12 px in, as the top bar's title; `body.sized`, set by `applyMode` while a picture is shown and before any fit is measured, shows it, and fullscreen hides it; `body.sized:not(.fullscreen)` sets `--size-band: 48px`, which `#stage` and `#controls` take off their height; over a timeline its margin sits on the timeline's top, capped at `100vh - 80px` so it never rises into the top bar; `drawHud` writes `WxH` through `drawImageSize`. Side effects, required: the sidebar ends above the container, its button group centred 24 px higher; with no timeline the HUD sits 48 px higher (`body:not(.strip) #hud`), since the container covered its last line, where the notices are, found in review. `editor/editor-checks.js`: section 27, the stage height less 48 and five new checks (the place and the colour, the HUD with no timeline, the size following the picture, fullscreen away and back); section 29, one (the empty state); section 37, its four stage heights less 48 and one check at the ceiling. `project-os/Design.md`: the Image size component, and the button group's placement ends at it. `host/references/s06/environment.txt`: the picture area is 1232x720 css, as the check run measured it; no reference image changed. | fmt, clippy with -D warnings and --all-features, the product build with no warning, 55 host and 4 pixel tests, and the checks build, all on a copy of `965b53b` plus this change in `.tmp/imagesize-src/`, since another session committed its timeline work in the same files meanwhile. Editor checks from that build: every check that ran passed, none not run, 342 pass; the new ones read "640x200" at 0-1280 by 672-688, the stage to 656, the strip from 704, rgb(17, 18, 23); the HUD to 742 above the size from 768 with no timeline; "400x300" after a thumbnail click; none in fullscreen and block after, the stage 624 of 800; "" and none once the last document was deleted; the stage 420 under a 300 strip; the size from 48 under a strip at the ceiling, the strip hit there; the double-click's 128 default with the stage 48 shorter. Browser: the page from `.tmp/imagesize-harness.html`, the host stubbed with one 1200x1500 capture and thirty documents, in the in-app browser at 1280 by 800: "1200x1500" at 0-1280 by 672-688, the stage and the sidebar ending at 656, the button group centred at 344 as the sidebar is; fullscreen hid it with the stage 0-800; with no timeline it sat at 768-784; at the ceiling it stopped at 48 while the close button and the title still took the pointer; at 800 by 600 it ran 0-800 at 472-488; console clean; tab closed. Review (rule 17, medium), this diff: blast radius shared, the stage's height being read by the fit, the pan, the pointer and the region request, all driven by the checks, and the sidebar and the HUD sharing the window's bottom with it; one finding introduced and fixed, the HUD's last line under the container with no timeline; a copy never carries the container, since the export clones the notes' layer only. Pre-existing, reported not fixed: whenever the timeline shows it covers the HUD, so the HUD's notices, NOT SAVED and NOT COPIED among them, cannot be seen; the HUD's key line runs 2209 px wide, past the window. Not run by a machine: Rotem's eye on the real window. | Pass | medium | `1721ef7` | Revert this commit. |
| 2026-09-16 | The release built for the image size container | A release built from commit `31f8d88` archived into `.tmp/release-31f8d88/`, into the private target folder `.tmp/release-target/`, so the other sessions' uncommitted work in the same files stayed out. Not copied: Recon was running from `host/target/release/recon-host.exe` since 00:42:07 (PID 53456), which holds the file. No source changed. | `cargo build --release` from that checkout: finished in 25.0 s at 00:51. The build: 14,703,616 bytes, SHA-256 `1a5fb835...b7d478ab`. Not run: the copy and its hash read back, waiting on Rotem quitting Recon. The project checks were not rerun: the code is `31f8d88`'s, whose change passed them on `965b53b`. Risk low, a build. | Pass, copy pending | low | `31f8d88` | Rebuild the release from `1721ef7`, which has no image size container. |
| 2026-09-16 | The image size release copied in | `host/target/release/recon-host.exe`: the build from `31f8d88` in `.tmp/release-target/` copied over it at 00:57:14, after Rotem said GO; no Recon process was running, checked in the same command as the copy, and the build's hash was read first, so no other session's build went in by mistake. It replaced the build carrying the 112 px thumbnails, which the new one contains. No source changed. | SHA-256 of the build and of the release read back identical, `1a5fb835...b7d478ab`, 14,703,616 bytes. Not run: the container on the real window, Rotem's eye. Risk low, a copy. | Pass | low | `b72d3bc` | Rebuild the release from `1721ef7`, which has no image size container. |
| 2026-09-16 | S2.9, undo of a deletion | `editor/index.html`: `actionSeq`, `deletions`, `undoneStack` and `newAction` beside `record`, which stamps `history.seq`; `undoDeletion`; `undo` choosing by number and `redo` by the undone stack; `deleteDocument` recording the deletion and keeping the stash, `stashCurrent(save)` for the empty path; `showEmpty` resetting the history; `loadImage` seeding `seq`; `deletionsOf` exported. `host/src/editor.rs`: `trash_rejoin` and the command `editor_trash_rejoin`, `editor_trash_restore` built on it, registered in both handler lists. `editor/editor-checks.js`: section 39. `project-os/Plan.md`: S2.9 built, the §3.6 undo row's clause. Decisions: the entry. | Another session was editing the same three code files, uncommitted, so this task's own versions of them, the last commit plus only these edits, were staged by hand (`.tmp/s29-mine.py`) and every check ran on exactly that staged tree, built into a private target folder: fmt, clippy with -D warnings and --all-features, the product build, 55 host and 4 pixel tests, and the editor checks on the debug checks build, 356 pass, 0 fail, 4 not run (the live compares), section 39 all 21 green. Earlier runs on the working tree, with the other session's edits in, showed the same 21 green and two failures in the Ctrl+S Save As checks that tree's dialog edits were changing; they are not in this commit. Review of the diff, high risk: two findings introduced, both fixed and re-run (a refused restore stuck Ctrl+Z; the empty path's save into the trash); blast radius shared, `editor_trash_restore` now built on `trash_rejoin` and its callers, the Restore button and sections 31 and 38, pass; `deleteDocument`'s callers, the ×, Ctrl+Delete and sections 29, 31 and 38, pass. Not looked at by eye in the release. | Pass, with the two Save As lines open | high | `ce57511` | Revert the S2.9 hunks of the three code files and the plan step; drop the Decisions entry. |
| 2026-09-16 | Review 3 fixed, T1 to T11 | `editor/index.html`: saveNow clears the dirty flag before waiting on an earlier save (T1); saveAs holds one dialog at a time and returns false when refused (T9); deleteDocument shows NOT DELETED on a refused move (T10). `host/src/editor.rs`: the preserve thread writes the image under the list's lock, into the trash when the document was deleted meanwhile (T2); the delete moves the folder under the lock and keeps the document on a refused move (T2, T10); show_document decodes outside the lock through `preserved_frame` (T7); `editor_trash_break` for the checks. `host/src/store.rs`: `write_atomic` flushes before the rename and names temporaries with a counter (T3, T8); `trash` retries a refused move for a second. `host/src/export.rs`: `write_new` flushes (T3). `host/src/focus.rs`: the target is taken on use (T4). `host/src/dialog.rs`: `save_as`, titled "Save As, a new file" (T5). `host/src/compose.rs`: `blur_line`, a running sum, with a test against the direct sum (T6). `host/src/source/report.rs`: both Recon data folders hashed, whole (T11). `host/src/selftest.rs`: section G checks a second return activates nothing. `editor/editor-checks.js`: five new checks, and section 22 no longer presses Ctrl+S, which left a dialog open. `notes/2026-09-16-code-review-3.md`: the statuses. | On a clean copy of HEAD plus these changes alone, since the working tree carries another session's S2.9 draft: fmt, clippy with -D warnings and --all-features, both builds, 56 unit tests with the blur test among them, the 347 editor checks all green with the five new ones, `--selftest` green with the new line in section G, `--decode-report` on the 28 real files green with both folders watched. Review: the diff read back against T1 to T11 and the always-check list; the delete's refusal under a thumbnail was found and fixed on the way; nothing else moved. Not run: a logoff (T12, Rotem's hand); the release copy, a Recon is running. Risk high, the save and store paths. | Pass | high | `ce57511` | Revert the commit; the store's files stay readable, the format is unchanged. |
| 2026-09-16 | The wheel scrolls the one-row timeline sideways | `editor/index.html`: after the press-drag block, a `wheel` listener on `#strip`, not passive, in one row only (`layout.rows === 1`): an up or down wheel adds its `deltaY` to `scrollLeft`, 40 px a line when the wheel counts in lines, the same step the picture's sideways wheel uses; a wheel moving more sideways than up or down keeps the system's own scroll. `editor/editor-checks.js`: section 37, one check. | fmt, clippy with -D warnings and --all-features, the product build with no warning, 56 host and 4 pixel tests, in `.tmp/target-scroller2/` on the live tree, all green. Editor checks: the new check passed twice, on the live tree and on `de9feb5` with only this change, reading 120 after a wheel of 120 down, 70 after 50 up, and in two rows the wheel left to the strip. Each run failed one other check, a different one each time: on the live tree the trash's thumbnail, on the isolated tree the picture staying sharp after a pan; a third run was discarded, since another session rebuilt the shared checks binary under it; `de9feb5` alone stopped with an error at the folder section. Neither failing check dispatches a wheel on the timeline; their cause is not looked into. Browser, real mouse: `.tmp/dragscroll-harness.html` at 1280 by 800, three notches down over the timeline scrolled it to 300, one up back to 200; tab closed. Review, risk medium: the listener sits on the timeline only, and the picture's wheel zoom is on the stage, which does not contain it; the rows keep the system's scroll. Impact local. On the way: the disk had 2 MB free when two private build folders of this task failed to build; the task's own two, 2.2 GB, were removed. Not run: Rotem's hand on the real window. | Pass | medium | `de9feb5` | Remove the wheel listener and the check. |
| 2026-09-16 | The release with the timeline's wheel scroll | A release built from commit `9882dc6` archived into `.tmp/release-9882dc6/`, into the private target folder `.tmp/release-target/`, so other sessions' uncommitted and staged work stayed out; copied over `host/target/release/recon-host.exe` at 01:30:55, after checking that the build's hash had not changed and no Recon from that path was running. No source changed. | `cargo build --release` from that checkout: 1 m 52 s, no warning, 14,708,224 bytes. SHA-256 of the build and of the release read back identical, `3605E91A...44D76370`. Not run: the wheel on the real window, Rotem's hand. Risk low, a build and a copy. | Pass | low | `9882dc6` | Rebuild the release from `de9feb5`. |
| 2026-09-16 | Rotem's hand on the wheel scroll | No file changed but this one. Rotem opened the release copied in at 01:30:55 and wrote that the wheel works great. | Rotem's eye on the real window, the check the wheel row left open. Risk low, a record. | Pass | low | `f902d05` | Remove these rows. |
| 2026-09-16 | Old builds cleared from the scratch folder | Removed at Rotem's word: the build folders `.tmp/target`, `.tmp/target-center`, `.tmp/target-recon62`, `.tmp/target-scroller` and `.tmp/target-scroller2`; the release source copies `.tmp/release-<commit>/` for thirteen commits, `.tmp/release-checkout` and two `.tar` copies; this session's four isolated source copies. Kept: `.tmp/release-target/`, the workflow's release build folder, and every log and script. No source changed. | Before: no cargo or rustc running, the only Recon from `host/target/release/`; the newest use of a removed folder was a checks build at 01:27. J: read 11 GB free before and 25 GB after; the free figure moves with other work on the drive. A later checks build in one of those folders compiles from nothing, about two minutes. Risk low, regenerable build output. | Pass | low | `f902d05` | Nothing to restore; any removed folder rebuilds with `cargo build`. |
| 2026-09-16 | The reply rules broken, recorded | `project-os/Mistakes.md`: one Open row, the narration between tool calls and the reports past the ceiling, home `project-os/Conversations.md` if it repeats. | Read back: the row sits last in the Open table, before Promoted. Risk low, docs. | Pass | low | `98ecea9` | Remove the row. |
| 2026-09-16 | Review 3 fixed, T1 to T11 | `editor/index.html`: saveNow clears the dirty flag before waiting on an earlier save (T1); saveAs holds one dialog at a time and returns false when refused (T9); deleteDocument shows NOT DELETED on a refused move (T10). `host/src/editor.rs`: the preserve thread writes the image under the list's lock, into the trash when the document was deleted meanwhile (T2); the delete moves the folder under the lock and keeps the document on a refused move (T2, T10); show_document decodes outside the lock through `preserved_frame` (T7); `editor_trash_break` for the checks. `host/src/store.rs`: `write_atomic` flushes before the rename and names temporaries with a counter (T3, T8); `trash` retries a refused move for a second. `host/src/export.rs`: `write_new` flushes (T3). `host/src/focus.rs`: the target is taken on use (T4). `host/src/dialog.rs`: `save_as`, titled "Save As, a new file" (T5). `host/src/compose.rs`: `blur_line`, a running sum, with a test against the direct sum (T6). `host/src/source/report.rs`: both Recon data folders hashed, whole (T11). `host/src/selftest.rs`: section G checks a second return activates nothing. `editor/editor-checks.js`: five new checks, and section 22 no longer presses Ctrl+S, which left a dialog open. `notes/2026-09-16-code-review-3.md`: the statuses. First landed as `40eb3ec`, undone by `de9feb5`, which was made from an older copy, and re-applied on top of `9882dc6`, the conflicts in the delete function, the History appendix and the Decisions index resolved by hand. | On a clean copy of HEAD plus these changes alone, since the working tree carries another session's S2.9 draft: fmt, clippy with -D warnings and --all-features, both builds, 56 unit tests with the blur test among them, the 347 editor checks all green with the five new ones, `--selftest` green with the new line in section G, `--decode-report` on the 28 real files green with both folders watched. Review: the diff read back against T1 to T11 and the always-check list; the delete's refusal under a thumbnail was found and fixed on the way; nothing else moved. Not run: a logoff (T12, Rotem's hand); the release copy, a Recon is running. Risk high, the save and store paths. | Pass | high | `9882dc6` | Revert the commit; the store's files stay readable, the format is unchanged. |
| 2026-09-16 | The S2.9 commit's revert, recorded | No code: `de9feb5` was made from an index whose entries for ten files still held `ce57511`'s content after another session committed `40eb3ec` from the same working tree with a pathspec, so `de9feb5` reverted those ten files and the review-3 Decisions, BugAtlas, Code_review and notes changes; that session re-landed them as `9488cd3`, which Rotem moved main to by hand, and the shared index was lined up with it after. `project-os/Mistakes.md`: the row. This row and the scan row. | The tree of `40eb3ec` plus S2.9's edits, built into a private target folder before the re-land: fmt, clippy with -D warnings and --all-features, the product build, 56 host and 4 pixel tests, and the editor checks 368 pass, 0 fail, 0 not run, section 39 all 21 green, on the third run; the first two runs failed the Ctrl+C readback once each (0x0), which `40eb3ec` alone also failed on other timing checks in the same minutes, three sessions building at once. Not re-run on `9488cd3` itself; its own session ran 369 green on it. Risk low, two doc rows. | Pass | low | `9488cd3` | Remove the two rows. |
| 2026-09-16 | The S2.9 release built and copied | A release built from commit `2639eec` archived into `.tmp/release-2639eec/`, into the private target folder `.tmp/release-target/`, made afresh since the folder had been removed to free the full drive; copied over `host/target/release/recon-host.exe` at 01:42:28 after a check found no Recon running from that path. It replaces the `9882dc6` build another session put there, which lacked the review-3 fixes. No source changed. | `cargo build --release` from that checkout: finished in 54.5 s, no warning. SHA-256 of the build and of the copy read back identical, `F61CFE1A...1069DBF65A`, 14,743,552 bytes. Not run: Rotem's hand on the new release. Risk low, a build and a copy. | Pass | low | `2639eec` | Rebuild the release from `9882dc6` and copy it back. |
| 2026-09-16 | The image size in Google Sans | `editor/fonts/google-sans-medium-latin.woff2`, new: the Latin file of Google Sans at weight 500 as Google Fonts' stylesheet serves it (`fonts.gstatic.com/s/googlesans/v70/`), 23,336 bytes, SHA-256 `7ee2b232...5b19bd7fa`; `editor/fonts/OFL.txt`, new: the SIL Open Font License text from Google Fonts' own file list for the family. `editor/index.html`: an `@font-face` for "Google Sans", weight 500, from that file with the stylesheet's Latin `unicode-range`; `#image-size` takes `text-align: center` and `font: 500 14px/16px "Google Sans", ...` in place of 12 px on the same 16 px line, and its comment says so. `editor/editor-checks.js`: section 27, one check: the face loaded, 14 px, 500, the family first, the text's middle within 1 px of the window's. `project-os/Design.md`: a Fonts section and the Image size text row. `project-os/Map.md`: `editor/fonts/`. `project-os/Decisions.md`: one entry, the Latin file bundled. | fmt, clippy with -D warnings and --all-features, the product build, 56 host and 4 pixel tests, and the checks build, on a copy of `9488cd3` plus this change in `.tmp/imagesize-src/`, the code this commit sits on, since other sessions' work sat uncommitted in the same files. A first commit attempt at 01:16 stopped on its own comparison of the staged patch, which counted hunk line numbers that HEAD's two new commits had moved; its clean-up then failed on the font paths not yet staged and left the four patched files staged in the shared index until they were unstaged by hand a few minutes later; nothing was committed, the script now compares without line numbers and unstages path by path, and the change was checked again on the new HEAD. Editor checks from that build: every check that ran passed, none not run, 370 pass; the new one read loaded, 500 14px "Google Sans", the text 610.7-669.3 in a window 1280 wide. Browser: the page from `.tmp/imagesize-harness-font.html`, the host stubbed and the same font bytes inline, since the pane opens a file as a snapshot that cannot reach a file beside it, at 1280 by 800: the face loaded, `"Google Sans"` first, 14 px, 500, "1200x1500" drawn from 604.1 to 675.8, its middle at 640.0 in a window 1280 wide, and 71.7 px wide as Google Sans measures it where Segoe UI would take 64.9, so the face draws it; the container still 0-1280 by 672-688, the stage to 656, the timeline from 704; console clean; tab closed. The first try linked the file and its load failed with a network error, which is how the snapshot came to light. Review (rule 17, medium), this diff: blast radius local, the face is named only by `#image-size`, whose box, margins and line are unchanged, so the stage, the sidebar and the HUD did not move and every check reading them passed; the notes and the export keep the system font; the policy's `font-src 'self'` lets the file load in the web view, as the real run's loaded face shows. Nothing found. Not run by a machine: Rotem's eye on the real window. | Pass | medium | `223f01a` | Revert this commit. |
| 2026-09-16 | Decisions entry once | `project-os/Decisions.md`: the second copy of the entry "The focus return target is used once" removed. `a7773b2`'s commit script took this change's entry from the live file up to the next separator, and another session's entry had been appended there with none between; the Google Sans entry and its index line stay. The live Decisions, whose lines matched the corrected file, is written to it; the live History, which lacked six rows another worktree had committed, is written to HEAD's. | The three 2026-09-16 entries counted once each in the corrected file; the live Decisions held the same non-blank lines as it before being overwritten; the live History differed from HEAD by six missing rows and no extra one. Risk low, docs. | Pass | low | `a7773b2` | Put the duplicate back from `a7773b2`. |
| 2026-09-16 | The release built for Google Sans | A release built from commit `a7773b2` archived into `.tmp/release-a7773b2/`, into `.tmp/release-target/`, after recon-8c's release build there had finished. Not copied: Recon was running from `host/target/release/recon-host.exe` since 01:42:32 (PID 66928), which holds the file. No source changed. | `cargo build --release` from that checkout: finished in 25.1 s at 01:46. The build: 14,769,664 bytes, SHA-256 `e5ba072d...e954e36c`. Not run: the copy and its hash read back, waiting on Rotem quitting Recon. The project checks were not rerun: the code is `a7773b2`'s, whose change passed them on `9488cd3`, the same code. Risk low, a build. | Pass, copy pending | low | `a7773b2` | Rebuild the release from `223f01a`, which has no Google Sans. |
| 2026-09-16 | The Google Sans release copied in | `host/target/release/recon-host.exe`: the build from `a7773b2` in `.tmp/release-target/` copied over it at 02:11:42, after Rotem said GO; no Recon process was running, checked in the same command as the copy, and the build's hash was read first, so no other session's build went in by mistake. It replaced the `2639eec` build, whose code this one contains. No source changed. | SHA-256 of the build and of the release read back identical, `e5ba072d...e954e36c`, 14,769,664 bytes. Not run: the centred size in Google Sans on the real window, Rotem's eye. Risk low, a copy. | Pass | low | `7241781` | Rebuild the release from `223f01a`, which has no Google Sans. |
| 2026-09-16 | Go commit, rotation and the Save As leftover | `project-os/rotate.ps1` run: 111 items moved verbatim into `History-archive.md`, `History-scan-archive.md` and `Decisions-archive.md`. `editor/index.html`: `saveAs` ends with `return true`, the half of the review-3 T9 fix (false when a press was ignored, true when the dialog opened) that the re-land had lost; found as the tree's only uncommitted line at Go commit. | The four project checks in order: fmt clean, clippy clean, build, 56 + 4 tests with 2 ignored by design. The rotation's own report: every target resolved, nothing edited. Not run: the editor checks, for one return value no check reads. Risk low. | Pass | low | `a056967` | Drop the `return true` line; the archives are move-only, so a rotation is undone by moving the rows back. |
| 2026-09-16 | A capture copies itself to the clipboard | `editor/index.html`: the `capture-ready` listener calls `copyComposed` after `loadImage`, gated on `source === 'capture'` and no file, not awaited, a failure logged; the notice and the NOT COPIED line come from the copy path itself. `project-os/Plan.md` §3.2, one bullet; `project-os/Decisions.md`, the entry on the page's copy over the host's; `project-os/QA.md`, one row. | The four checks green (56, 4 and 0 tests). `--editor-check` twice with the change: every `editor_capture_probe` was followed by the host's `copied WxH to the clipboard` at the probe's size, 115 copies in each log, the S1.10 held-clipboard check, the Ctrl+C check and the Copy button check passed; one failure both times, "still crisp after a pan, 93 flips", and the same failure with the same number on the committed page with the edit stashed, so pre-existing, in a section that runs before any capture is announced; the not-run lines were the live-editor screen copies and the Save As Escape, refused because another window was in front. Review per `project-os/Code_review.md`: the diff read, impact local, the one shared consumer being the listener itself, which files and frames reach under another source label; nothing introduced, the one pre-existing failure flagged. Not run: a real hotkey capture and a paste, Rotem's hand. The environment record the check run rewrote, `host/references/s06/environment.txt`, is left unstaged. Side effect: the "copied" line shows for 1.5 s after every capture. | Pass, one pre-existing failure flagged | medium | `701f14d` | Remove the seven lines in the listener. |
| 2026-09-16 | The release built for the capture's automatic copy | A release built from commit `bb6ec47` archived into `.tmp/release-bb6ec47/`, into the private target folder `.tmp/release-target/`, so the check run's rewrite of `host/references/s06/environment.txt` and any other session's work stayed out. Not copied: Recon was running from `host/target/release/recon-host.exe` since 05:48:16 (PID 18132), which holds the file. A detached job, `.tmp/swap-release.ps1`, started at 19:24:02, copies the build over it the moment that Recon exits, copies nothing if the build's hash has changed or a Recon from that path is back by then, and writes what it did to `.tmp/swap-release.log`. No source changed. | `cargo build --release` from that checkout: finished in 32.8 s, no warning. The build: 14,769,664 bytes, SHA-256 `58ad615d...f881999d`. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `bb6ec47`'s, whose tree passed them. Risk low, a build. | Pass, copy pending | low | `bb6ec47` | Rebuild the release from `701f14d`, whose capture copies nothing by itself. |
| 2026-09-16 | The automatic-copy release copied in | `host/target/release/recon-host.exe`: the waiting job of the row above copied the build from `bb6ec47` in `.tmp/release-target/` over it at 19:25:41, after the release running since 05:48:16 exited; the job checked the build's hash first and that no Recon from that path was back. Rotem's BUILD at 19:27 found the copy already made. No source changed. | SHA-256 of the build and of the release read back identical, `58AD615D...F881999D`, 14,769,664 bytes; read again at the BUILD, still identical, no Recon running. Not run: a capture on the new release and a paste, Rotem's hand. Risk low, a file copy. | Pass | low | `a4b0b63` | Rebuild the release from `701f14d` and copy it back. |
| 2026-09-16 | Rotem's hand on the automatic copy | No file changed but this one. Rotem opened the release copied in at 19:25:41, built from `bb6ec47`, took a capture and wrote that it works perfectly. | Rotem's eye and paste on the real window, the check the copy row left open. Risk low, a record. | Pass | low | `fb36251` | Remove these rows. |
| 2026-09-16 | The editor comes to the front after a capture | `host/src/editor.rs`: `show` restores the window when `is_minimized` says so, before `show` and `set_focus`; nothing else moved. `editor/editor-checks.js`: section 36, one check, the window minimized by its own button, then not minimized and visible after `editor_show`. `project-os/QA.md`: the row. `project-os/Plan.md` §3.1: the line. Cause: show changes nothing on a window that is already visible, and the runtime's focus call skips a minimized one; the editor of Rotem's running release was visible and minimized when this task began, read off the window itself. | On a worktree of `1bd859e` plus these edits alone (`.tmp/front-src`, another session's tabs work being uncommitted in the same files), built into `.tmp/front-target/`: fmt, clippy with -D warnings and --all-features, the product build, 56 host and 4 pixel tests, the checks build, all green; editor checks 371 pass, 0 fail, 0 not run, the new one reading "minimized true, then minimized false". Review, medium: blast radius shared, `show` serving the capture, the tray's click and Open, a second instance and the checks, and every one wants the same restore; no finding introduced. Pre-existing, reported not fixed: from the capture thread the runtime's focus call reads the window's flags before the posted show has run, so the focus after a capture rests on the show itself activating the window. Not run: a real hotkey capture with the editor minimized, Rotem's hand. | Pass | medium | `1bd859e` | Revert the commit. |
| 2026-09-16 | The release built for the minimized editor's return | A release built from commit `41caa2b` archived into `.tmp/release-41caa2b/`, into the private target folder `.tmp/release-target/`, so another session's uncommitted tabs work in the same files stayed out. Not copied: Recon has been running from `host/target/release/recon-host.exe` since 21:23:34 (PID 39280), which holds the file; a hidden job (`.tmp/swap-release.ps1`) waits for it to exit, checks the build's hash, and copies it in unless a Recon from that path is back by then. No source changed. | `cargo build --release` from that checkout: finished in 34.2 s at 21:44. The build: 14,793,216 bytes, SHA-256 `91d227b0...aad9baa1`. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the project checks were not rerun, the code being `41caa2b`'s, whose change passed them on the worktree of `1bd859e`. Risk low, a build. | Pass, copy pending | low | `41caa2b` | Rebuild the release from `bb6ec47`, where a minimized editor stays minimized after a capture. |
| 2026-09-16 | The timeline's tabs | `editor/index.html`: `#tabbar` in the size band, left 8 px, 32 px tall, hidden with the band and in fullscreen, `#tab-add` an icon button with a round-ended plus, `.tab` elements with a `.x` on every tab after Main; the tabs block: `tabs` (list and selected id, Main being 0), `loadTabs`, `saveTabs` (one write at a time, a snapshot each), `renderTabs`, `selectTab`, `addTab`, `deleteTab`, `moveTab`, `captureIntoTab`, the pointer drag with a 4 px slop that reorders live and follows the hand; `refreshStrip` filters the library by the selected tab's feed; the `capture-ready` listener puts a capture into the selected tab before it loads; `boot` loads the tabs before the first picture; `stripWanted` replaces `cells.length` in `setFullscreen`, so an empty feed does not hide the timeline on the way back; eight exports. `host/src/store.rs`: `tabs_path`, `tabs.json` beside the documents and the trash. `host/src/editor.rs`: `editor_tabs` (a file that does not parse is set aside as `tabs.broken-<ms>.json`) and `editor_save_tabs` (through `write_atomic`), in both handler lists. `editor/editor-checks.js`: section 40, 21 checks. `project-os/Design.md`: the Tab bar component. `project-os/Map.md`: the tabs.json data row. `project-os/Plan.md` §3.8: one bullet. `project-os/QA.md`: one row. `project-os/Decisions.md`: the entry. | fmt clean, clippy with -D warnings and --all-features clean, the product build with no warning, 56 host and 4 pixel tests, and the checks build, on the live tree. `--editor-check` three times: the first run failed one new check, the bar gone after fullscreen with an empty feed selected (the strip toggled on `cells.length`), fixed with `stripWanted`, plus the pre-existing "still crisp after a pan, 93 flips"; the second and third runs, the third with the review fixes in, every check that ran passed, 394 then 395 pass, 0 failed, 0 not run, section 40 all 21 green: the plus at 8,664 32x32 with a 20x20 icon in the band from 656, Main|New tab after the first press with New tab selected and 0 thumbnails, the probe capture in the feed and on disk as `{"selected":<tab>,"tabs":[{"docs":[<id>]}]}`, 3 thumbnails on Main, the list read back from disk, the pointer drag giving the order 3,1,2 with the tab following, on disk, Main refusing a drag and a move, the × on the selected tab leaving 2 tabs and Main with 3 thumbnails, the bar to 696 above the strip from 704 after fullscreen. Browser, the page from `.tmp/tabs-harness.html` with the host stubbed and the tabs kept in memory, in the in-app browser at 1280 by 800: the plus at 8,664 32x32, the icon 20x20, the band 656-704; a real click on it gave Main at 48 and New tab at 103, 32 tall, New tab selected with a ×, Main with none, 0 thumbnails; a stubbed capture landed as the feed's one thumbnail and in the saved list; a click on Main showed 4; two presses made two more tabs; a real drag of the last tab to before the first put it right after Main, saved, no transform left, the selection unchanged; a real drag on Main changed nothing; a click on the selected tab's × left Main selected with 4 thumbnails and the capture still in the library. Console: Passed, no new console errors. Tab closed, viewport reset. Review (rule 17, high): blast radius shared, `refreshStrip` and its callers, the fullscreen toggle and the capture listener; three findings introduced and fixed, then re-run green: two saves in flight could land in the wrong order (now one at a time, a snapshot each); a corrupt tabs.json would have been written over on the next change (now set aside); a drag ended off the bar would have swallowed the next tab click (the guard resets on every press). Pre-existing, reported: the pan check's 93 flips, as before. Not run by a machine: a real hotkey capture on a tab and a drag by hand, Rotem's; a tab list wider than half the window is cut, not scrolled. | Pass | high | `1bd859e` | Revert this commit; a `tabs.json` left on disk is ignored by the page before it. |
| 2026-09-16 | The release built for the timeline's tabs | A release built from commit `d356b38` archived into `.tmp/release-d356b38/`, into the private target folder `.tmp/release-target/`, so the check run's rewrite of `host/references/s06/environment.txt` stayed out; it carries `41caa2b`, the minimized editor's return, which another session committed under this task. Not copied: Recon was running from `host/target/release/recon-host.exe` since 21:23:34 (PID 39280), which holds the file. A detached job, `.tmp/swap-release-d356b38.ps1`, started at 21:49:04, copies the build over it the moment that Recon exits, copies nothing if the build's hash has changed or a Recon from that path is back by then, and writes what it did to `.tmp/swap-release.log`. The other session's job for `41caa2b`, waiting since 21:44:27, will find the build's hash changed and copy nothing; this build contains its change. Correction: the tabs row's rollback target is `950e855`, the commit before it, not `1bd859e`. No source changed. | `cargo build --release` from that checkout: finished in 32.4 s, no warning. The build: 14,822,912 bytes, SHA-256 `2891cee7...7c619daa`. The job's log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `d356b38`'s, whose tree passed them. Risk low, a build. | Pass, copy pending | low | `d356b38` | Rebuild the release from `950e855`, whose timeline has no tabs. |
| 2026-09-16 | The tabs finished: rename, the ✓, the done mark, undo | `editor/index.html`: `.tab` 14 px on a 10 px radius; the × an icon with a `.tick` twin, `armed` on the first press, disarmed when the pointer leaves the tab; `startRename` and `endRename` on the name span (contenteditable, all selected, Enter or blur keeps a non-empty trimmed name, Main never), keys stopped from the page while it is edited, no drag meanwhile; the undo list `acts` beside `deletions`, `recordAct` numbering on the same counter, `undo` and `redo` taking an act by number like a deletion; `putTab` and `takeTab` as the two moves, `addTab`, `deleteTab`, `renameTab` and `toggleDone` recording their undo and redo; `loadTabs` rebuilds the bar's elements; a tab's `done` list, the strip's cells in a tab keyed by tab and carrying it, `.thumb .done` a round 20 by 20 button with a 16 by 16 icon at the top left, `checked` on the cell; two exports. `editor/editor-checks.js`: section 40 rewritten, 45 checks. `CLAUDE.md`: rule 23. `project-os/Plan.md`: §3.6's undo row and §3.8's tabs bullet. `project-os/Design.md`: the tab bar's four new rows and the Done mark component. `project-os/QA.md`: the tabs row. | fmt, clippy with -D warnings and --all-features, the product build, 56 host and 4 pixel tests, the checks build, on the live tree. `--editor-check` four times: the first threw in section 40, since `acts.push(newAction())` pushed onto the list `newAction` had just replaced, so no act was ever kept and Ctrl+Z reached section 39's stale deletions; the second failed the rename, whose element still held the tab object a reload had replaced, and an addition's undo, whose check clicked a tab by position after the drag had moved it; the third failed only that check, which read the strip before it refreshed; the fourth: every check that ran passed, 419 pass, 0 failed, 0 not run, section 40 all 45 green, among them 14px on 10px, editable and focused after the click on the name, "Acme remarks" on screen and on disk after Enter, Main not edited, the rename undone and redone, the done mark none until hover at 20x20 radius 10 at 2,2 with a 16 px icon, grid and ticked and on disk after the press, cleared by Ctrl+Z and back by Ctrl+Shift+Z, no mark in Main, the × armed after one press with the ✓ shown and the tab kept, disarmed on leaving, deleted after two, the deleted tab back by Ctrl+Z in its place with its name, feed and selection and on disk, deleted again by Ctrl+Shift+Z, the added tab taken by Ctrl+Z with the selection back on the tab before it, a note then a tab deletion undone in that order. Browser, `.tmp/tabs-harness.html` with the host stubbed, in-app at 1280 by 800, real pointer and keyboard: the plus made Main and New tab at 14px on a 10px radius, 32 tall; a stubbed capture in the tab; hovering its thumbnail showed the done mark (grid), a real click on it ticked it and put the id in the tab's done list; one real click on the × armed it (✓ shown, × hidden, tab kept), moving to Main disarmed it, two clicks deleted the tab; a real click on the selected tab's name made it editable with the whole name selected, typing "Acme remarks" and Enter kept it on screen and on disk on one line, and undo and redo through the page's own functions renamed it back and forth, and brought the deleted tab back with its feed, mark and selection. Console: Passed, no new console errors. Not proved in the browser: the real Ctrl+Z and Return keys, which the pane's key tool sends with no key code, so the page's physical-key match never sees them; the editor checks cover the key path with a coded event. Tab closed, viewport reset. Review (rule 17, high): blast radius shared, undo and redo with the notes' steps and the document deletions, the strip's cell keys, `refreshStrip`'s callers; two findings introduced and fixed on the way, above, then re-run green; nothing else found. Pre-existing: none new. | Pass | high | `2d4022f` | Revert this commit; a `tabs.json` with `done` lists is read by the page before it, which ignores the field. |
| 2026-09-16 | The tabs release copied in | `host/target/release/recon-host.exe`: the waiting job of the d356b38 build row copied the build from `.tmp/release-target/` over it at 22:00:35, after the release running since 21:23:34 exited; the job checked the build's hash first and that no Recon from that path was back. The other session's job for `41caa2b`, polling the same file, found the hash changed and copied nothing, as that row said it would. Rotem's Recon started from the new file at 22:00:41. No source changed. | The job's log: the copy reads back `2891CEE7...7C619DAA`, identical to the build. Not run: the tabs under Rotem's eye on that release. Risk low, a file copy. | Pass | low | `40cfadf` | Rebuild the release from `950e855` and copy it back. |
| 2026-09-16 | The release built for the finished tabs | A release built from commit `40cfadf` archived into `.tmp/release-40cfadf/`, into the private target folder `.tmp/release-target/`, so the check run's rewrite of `host/references/s06/environment.txt` stayed out. Not copied: Recon was running from `host/target/release/recon-host.exe` since 22:00:41 (PID 62444), which holds the file. A detached job, `.tmp/swap-release-40cfadf.ps1`, started at 22:26:07, copies the build over it the moment that Recon exits, copies nothing if the build's hash has changed or a Recon from that path is back by then, and writes what it did to `.tmp/swap-release.log`. No source changed. | `cargo build --release` from that checkout: finished in 22.0 s. The build: 14,826,496 bytes, SHA-256 `0b492f9c...1adca085`. The job's log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `40cfadf`'s, whose tree passed them. Risk low, a build. | Pass, copy pending | low | `40cfadf` | Rebuild the release from `d356b38`, whose tabs have no rename, ✓, done mark or undo. |
| 2026-09-16 | The done mark to Rotem's spec | `editor/index.html`: `.thumb .done` 24 by 24 with a 12 px radius, `border: 2px solid #fff`, border-box; its svg 20 by 20; the ring circle gone from the markup, the tick redrawn in a 20 box; `.tick` at opacity 0, and under `.thumb.checked` opacity 1 with `transition: opacity 144ms ease-out` (A1) on the marking only, so clearing is instant; a reduced-motion rule beside it. `editor/editor-checks.js`: section 40's mark checks read the new sizes, the border, the absence of the ring, the tick clear before the press, the transition `opacity 0.144s ease-out`, opacity 1 after 300 ms, and the mark's box 4 px inside the thumbnail's edge (its 2 px border plus 2). `project-os/Design.md`: the Done mark rows and A1's second use. | fmt, clippy with -D warnings and --all-features, the product build, 56 host and 4 pixel tests, the checks build, on the live tree. `--editor-check` twice: the first failed only the new geometry check, which measured the mark from the thumbnail's outer edge where its 2 px border sits between, fixed in the check; the second: every check that ran passed, 412 pass, 0 failed, 5 not run, the three live-editor screen copies and the two dialog Escapes, refused because the browser pane was in front of the checks window meanwhile; they passed in the first run and no line of this change reaches them. Browser, `.tmp/tabs-harness.html` regenerated, in-app at 1280 by 800, real pointer: after a tab and a stubbed capture, hovering the thumbnail showed the mark as a grid at 12,717, 24 by 24, radius 12, border 2px rgb(255, 255, 255), the icon 20 by 20 at 14,719, so 2 px in on every side and nothing between; a real click ticked it, checked on the cell, the transition read `opacity 0.144s ease-out`, the id on the tab's done list; the screenshot shows the green circle with its white border and tick. Not readable there: the tick's opacity after 300 ms stayed 0 in the pane, which freezes animations while it is hidden behind another window (Visual QA §3); the editor check read 1 in the real window. Console: Passed, no new console errors. Tab closed, viewport reset. Risk low, styling and one motion; no review armed. Side effect, named: clearing a mark is instant, since the fade is on the marking alone. | Pass, 5 unrelated checks not run | low | `590580c` | Revert this commit. |
