# Recon — History

The change log. Every completed change lands here, in two layers: a scan table
you always read, and an appendix you read only when digging.

The scan table answers "what happened lately". The appendix answers "what exactly
did that change do, and how do I undo it".

## How you maintain this file

- **After every completed change**, add one scan row AND one appendix row. Both,
  in the same change that did the work.
- **One change = one row.** Not one row per file, not one row per session. A log
  that has to be reassembled from fragments is a log nobody reads.
- Write the scan row for a reader who was not there. Name the behavior that
  changed, not the files.
- Keep appendix rows short — a few lines, not an essay. The full story is in the
  commit diff; a real decision belongs in `project-os/Decisions.md`.
- Record the commit SHA from **before** the change. That is the rollback target.
- Never rewrite or delete a past row. Correct a wrong one by adding a new row —
  an edited log cannot be trusted about anything.
- A `medium` or `high` risk row names its review result under **What was
  checked**: findings found, findings fixed, pre-existing ones flagged. A
  medium-or-higher row without that is a task that is not finished.
- Never write "tested" or "QA passed". Those phrases record nothing. Name the
  input, the screen, and what happened.
- When this file gets long, move the oldest rows into an archive file beside it.
  `project-os/rotate.ps1` does exactly that at `Go commit`, and creates
  the archive the first time it is needed. Rows move **verbatim** — never
  rewritten, never summarized, never merged, because the detail you drop is the
  one the next reader needed. Never hand-move rows: the script dedups, so it is
  safe to run every time, and a hand-move breaks that guarantee.

## Risk scale

The scale's one home is `project-os/Workflow.md` step 2 — read it there, so the
two files can never disagree. State the level at task pickup; the owner's
override wins.

## Scan log

_Older scan rows archived -> see `History-scan-archive.md` (moved by project-os/rotate.ps1, not rewritten)._

Newest at the bottom.

| Date | Area | What changed |
|---|---|---|
| 2026-09-15 | editor | **The one-row timeline's sideways scroller is Rotem's own now too: a 4 px thumb of #21222C, fully rounded, 4 px clear of the window's bottom and 4 px clear above.** His spec was this scroller all along; the rows scroller keeps the same look. |
| 2026-09-15 | host | **The release at `host/target/release/recon-host.exe` carries both timeline scrollers now, built from the commit alone and put in place at once, since no Recon was running.** Rotem's word, BUILD. |
| 2026-09-15 | editor | **Zooming out stops at the whole picture now: its actual size when it fits the window, the whole of it when it is larger; and when the window, full screen or the timeline changes the space, a picture shown whole grows or shrinks to the new whole view.** Rotem's word, both; the wheel and the - key used to go down to a quarter of the fit. |
| 2026-09-15 | editor | **The sidebar's button group is centred in the sidebar's height now, between the top bar and the timeline, instead of sitting at its top.** Rotem's correction: centring it across the width, which it already filled, was the wrong reading of his words; the slip's second time, so it is a rule in `CLAUDE.md` rule 1 now. |
| 2026-09-15 | project-os, host | **Every committed code change is built into the release without asking now, at Rotem's word; the release for the centred sidebar group is built and replaces the running one the moment Rotem quits Recon.** The rule sits in the workflow's delivery step. |
| 2026-09-15 | host | **The release with the centred sidebar group is in place: the copy ran at 20:39:51 when Rotem quit Recon, it reads back identical to the build, and his Recon opened on it a second later.** |
| 2026-09-15 | host | **Recon's window opens in the middle of the screen now, across and in height, instead of near the left edge.** Rotem's word. On a display smaller than the window, the window is shrunk to fit first, so its top bar is never off the screen. |
| 2026-09-15 | host | **The release with the centred window is built from the commit alone, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon that is running.** |
| 2026-09-15 | host | **The release with the centred window is in place: the copy ran at 21:29:44 when Rotem quit Recon, it reads back identical to the build, and his Recon opened on it at 21:29:58.** The first waiting job copied nothing, since Rotem's Recon was back within a second. |
| 2026-09-15 | plan | **Rotem's hand on the 21:29 release: the window opens in the middle of the screen, and it works.** |
| 2026-09-15 | editor | **A timeline dragged taller covers the sidebar now: the buttons below its top edge sit under it instead of over its thumbnails, and the handle on its edge stays on top.** Rotem's report, with a screenshot. |
| 2026-09-15 | host | **The release with the timeline over the sidebar is built from the commit alone, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon that is running.** |
| 2026-09-15 | host | **The release with the timeline over the sidebar is not in place: the waiting job that copies it in was refused by the session's permission check, so no copy will run when Rotem quits Recon.** Correcting the row above. |
| 2026-09-15 | host | **The release with the zoom-out floor, and the timeline over the sidebar with it, is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** No waiting job: the one for the previous release was refused by the session's permission check. |
| 2026-09-15 | host | **The release with the zoom-out floor is in place: copied over `host/target/release/recon-host.exe` at 23:11:01 once Rotem quit Recon, the file reading back identical to the build.** Rotem's word, DONE. |
| 2026-09-15 | editor | **A press anywhere on the one-row timeline and a move of the mouse left or right scrolls it now, following the hand; a press that moves less than 4 px is still a click and shows its thumbnail.** Rotem's word. The rows the timeline wraps into when dragged taller scroll as before. |
| 2026-09-15 | host | **The release with the timeline's press-and-drag scroll is built from the commit alone and waits in `.tmp/release-target/`; it is not copied over `host/target/release/recon-host.exe`, since the Recon from that path is running and the last waiting copy was refused by the session's permission check.** |
| 2026-09-15 | editor | **A picture opened while the timeline fills the window no longer vanishes at the next zoom key: it opens at the fit kept from before, and takes its own whole picture the first time the space returns.** Rotem's word, FIX, on a bug the zoom-out review found. |
| 2026-09-15 | host | **The release with the fix for a picture opened with no room, and the timeline's drag scroll with it, is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-15 | plan | **Rotem's hand on the timeline: a press and a sideways move scrolls it, and dragged taller it covers the sidebar; he called it perfect.** The release he ran was the one copied in at 23:31:30, built from `9adad68`, which carries both changes; the build from `73d51f4` it replaced was never copied. |
| 2026-09-15 | host | **Recon's window opens 1600 wide and 1160 tall now, centred as before; it was 1280 by 800.** Rotem's word. |
| 2026-09-15 | host | **The release with the window opening at 1600 by 1160 is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with the window opening at 1600 by 1160 is in place: copied over `host/target/release/recon-host.exe` at 00:14:39 once Rotem quit Recon, the file reading back identical to the build.** Rotem's word, GO. |
| 2026-09-16 | plan | **Undo of a deletion has a spec, S2.9, nothing built: Ctrl+Z brings back the picture just deleted from the timeline, the last thing done being what undo takes, and Ctrl+Y deletes it again.** Rotem's pick over undo buttons in the sidebar and over undo that survives a restart; three calls in the step are his. |
| 2026-09-16 | project-os | **A choice put to Rotem is shown as the scenes he would see, never as a rule's name: the slip's second time, so it is a line in the reply rules now.** He could not tell what the S2.9 step's three calls asked. |
| 2026-09-16 | plan | **S2.9's three calls are Rotem's now: Ctrl+Z undoes the last thing done, a deletion included; Ctrl+Shift+Z deletes it again, no Ctrl+Y named for it; and no notice when the picture comes back.** Still a spec, nothing built. |
| 2026-09-16 | host, editor | **S2.9 is built: Ctrl+Z brings back the picture just deleted from the timeline, with its notes and their own undo, the last thing done first; Ctrl+Shift+Z deletes it again; from the empty state too; nothing is said on screen.** Two things the review caught are fixed: a refused restore no longer blocks every later Ctrl+Z, and the empty path no longer tries a save into the trash. |
| 2026-09-16 | host | **The window opens at 1600 by 1160: measured on the release Rotem opened at 00:33, not only read in the code.** |
| 2026-09-16 | editor | **The timeline's thumbnails are 112 px tall by default now, the timeline 128 px; the floor a drag can reach stays 96.** Rotem's word. A height once dragged is still remembered, and a double-click on the top edge returns to the new default. |
| 2026-09-16 | host | **The release with the 112 px thumbnails is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with the 112 px thumbnails is in place: copied over `host/target/release/recon-host.exe` at 00:41:08 once Rotem quit Recon, the file reading back identical to the build.** Rotem's word, GO. |
| 2026-09-16 | review | **The whole repository reviewed again, Stage 3 and the timeline in: twelve findings, none fixed, every one waiting on Rotem's verdict.** The one that matters: a character typed during the few milliseconds a save is on its way can be left off the disk while the HUD says saved. Three important: a capture deleted while its image is still being encoded loses that image for the trash; the store's writes are never flushed before the rename, so a power cut can leave an empty record; closing the editor after any capture brings the last capture's application to the front, however the editor was opened. Eight nits with their homes. The document is `notes/2026-09-16-code-review-3.md`. |
| 2026-09-16 | editor | **The picture's size in pixels sits in a container across the whole window above the timeline now, 16 px clear above and below it, on the app's own background, written as 1200x1500.** Rotem's spec; the text's look is provisional until he states one. Side effect: the sidebar ends above the container, so its buttons sit 24 px higher. |
| 2026-09-16 | host | **The release with the image size container is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with the image size container is in place: copied over `host/target/release/recon-host.exe` at 00:57:14 once Rotem quit Recon, the file reading back identical to the build.** Rotem's word, GO. |
| 2026-09-16 | host, editor | **The twelve findings of review 3 are fixed at Rotem's FIX ALL, T12 apart, which is his hand: a character typed while a save is on its way now reaches the disk; a capture deleted while its image encodes keeps the image in the trash and comes back whole; every document write and every export is flushed before it is named; a hide returns the focus once, so an editor opened from the tray closes without a jump; Save As names no format and refuses a second dialog; a delete that cannot move says NOT DELETED and keeps the document.** Found on the way: a delete refused while a thumbnail held a file in the folder, now retried for a second. |
| 2026-09-16 | editor | **The mouse wheel over the one-row timeline scrolls it left and right now, down to the right; once the timeline wraps into rows the wheel scrolls it up and down as before.** Rotem's word. |
| 2026-09-16 | host | **The release at `host/target/release/recon-host.exe` carries the timeline's wheel scroll now, built from the commit alone and put in place at once, since no Recon was running.** |
| 2026-09-16 | plan | **Rotem's hand on the 01:30 release: the wheel over the one-row timeline scrolls it sideways, and it works great.** |
| 2026-09-16 | project-os | **The old build folders in `.tmp/` are cleared at Rotem's word, about 14 GB; the drive had filled to 2 MB free during the wheel task. `.tmp/release-target/`, where each release is built, is kept.** |
| 2026-09-16 | project-os | **Rotem asked why the replies broke the reply rules: status lines between steps, and reports too long, some without headings. One row in the mistakes list records it.** |
| 2026-09-16 | host, editor | **The twelve findings of review 3 are fixed at Rotem's FIX ALL, T12 apart, which is his hand: a character typed while a save is on its way now reaches the disk; a capture deleted while its image encodes keeps the image in the trash and comes back whole; every document write and every export is flushed before it is named; a hide returns the focus once, so an editor opened from the tray closes without a jump; Save As names no format and refuses a second dialog; a delete that cannot move says NOT DELETED and keeps the document.** Found on the way: a delete refused while a thumbnail held a file in the folder, now retried for a second. |
| 2026-09-16 | project-os | **The S2.9 commit `de9feb5` undid another session's review-3 commit `40eb3ec` by mistake; `9488cd3` put every line of it back, and the tree with both passes every check.** The assistant's slip is in Mistakes: it committed the shared index while the other session had just committed from the same working tree. |
| 2026-09-16 | host | **The release with undo of a deletion and the review-3 fixes is built from `2639eec` alone and in place: copied over `host/target/release/recon-host.exe` at 01:42:28, no Recon running from that path, the file reading back identical to the build.** |
| 2026-09-16 | editor | **The picture's size is centred in the window now, in Google Sans at 14 px, medium, the font bundled with the page from Google Fonts.** Rotem's spec; only the Latin file, 23 KB, with its Open Font License. The colour and the 16 px line stay provisional. |
| 2026-09-16 | project-os | **Decisions carries each entry once again: the Google Sans commit had copied the focus-return entry in a second time beside its own.** Found reading that commit back. |
| 2026-09-16 | host | **The release with Google Sans in the image size is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with Google Sans in the image size is in place: copied over `host/target/release/recon-host.exe` at 02:11:42 once Rotem quit Recon, the file reading back identical to the build.** Rotem's word, GO. |
| 2026-09-16 | editor, project-os | **Go commit: the docs rotated (111 items moved into the archives), and the one line left uncommitted in the tree, Save As answering true when its dialog opened, landed with them.** No behaviour changed beyond what the review-3 row already records. |
| 2026-09-16 | editor | **A capture is on the clipboard the moment the editor opens with it, with no key pressed: the same copy as Ctrl+C, so the HUD says copied, or NOT COPIED with the reason.** Rotem's word. A file opened, a frame stepped or an older picture shown from the timeline is never copied unasked. |
| 2026-09-16 | host | **The release with the capture's automatic copy is built from the commit alone; it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with the capture's automatic copy is in place: copied over `host/target/release/recon-host.exe` at 19:25:41 once Rotem quit Recon, the file reading back identical to the build.** Rotem's word, BUILD, found it already done. |
| 2026-09-16 | plan | **Rotem's hand on the 19:25 release: a capture lands on the clipboard by itself, and it works perfectly.** |
| 2026-09-16 | host, editor | **The timeline has tabs now: a plus at the left of the picture's size band makes Main and New tab, and another New tab at every press; a capture taken on a tab lands in Main and in that tab's own feed; a tab is deleted by its ×, never Main, and dragged into another place after Main; the list survives a restart.** Rotem's ask, for the feeds of tasks a client's remarks become; the plus is his spec, the tabs' look provisional. Found and fixed on the way: with a tab whose feed was empty selected, leaving fullscreen hid the timeline. |
| 2026-09-16 | host | **A capture brings a minimized editor back to the front now: the window is restored before it is shown and focused, as a click on its taskbar button would.** Rotem's report: since the automatic copy he leaves the editor minimized instead of hiding it, and the next capture left it in the taskbar. Side effect: the tray's click and Open, and a second instance, restore a minimized window the same way. |
| 2026-09-16 | host | **The release with the minimized editor's return is built from the commit alone; it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with the timeline's tabs is built from the commit alone, `d356b38`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** The tabs row above names `1bd859e` as its rollback target; the commit before it was `950e855`, another session's, and reverting to that one keeps that session's work. |
| 2026-09-16 | editor, project-os | **The tabs are finished to Rotem's spec: 14 px text on 10 px corners; a click on the selected tab's name edits it and Enter keeps it; the × pressed once turns into a ✓ and pressed again deletes; in a tab every thumbnail carries a round done mark, shown on hover and kept once pressed; and adding, deleting and renaming a tab and the mark are in Ctrl+Z and Ctrl+Shift+Z.** New rule 23 in `CLAUDE.md`: every new action joins undo and redo. Found and fixed on the way: the undo list dropped every act it was handed, and a tab bar reloaded from disk kept stale tabs behind its elements. |
| 2026-09-16 | host | **The release with the timeline's tabs is in place: copied over `host/target/release/recon-host.exe` at 22:00:35 once Rotem quit Recon, the file reading back identical to the build; his Recon opened on it six seconds later.** The other session's waiting job for the minimized editor's return copied nothing, its build having been replaced; that change is inside this one. |
| 2026-09-16 | host | **The release with the finished tabs, rename, the ✓, the done mark and their undo, is built from the commit alone, `40cfadf`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-16 | editor | **The done mark on a thumbnail is a 24 px circle with a 2 px white border now, its 20 px tick filling the inside with no gap, and the tick fades in by A1 when pressed.** Rotem's spec, correcting the 20 px mark with a ringed icon. |
| 2026-09-16 | host | **The release with the finished tabs is in place: copied over `host/target/release/recon-host.exe` at 22:27:48 once Rotem quit Recon, the file reading back identical to the build; his Recon opened on it two seconds later.** |
| 2026-09-16 | host | **The release with the 24 px done mark is built from the commit alone, `7390281`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-16 | host | **The release with the 24 px done mark is in place: copied over `host/target/release/recon-host.exe` at 22:38:14 once Rotem quit Recon, the file reading back identical to the build.** His first look at it was at the release before, hence his repeat of the spec; the mark there matched it. |
| 2026-09-16 | editor | **The done mark's tick is a 1 px line now; it was 1.5.** Rotem's word. |
| 2026-09-16 | host | **The release with the 1 px tick is built from the commit alone, `2af8d24`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-16 | editor | **The sidebar's buttons are 48 by 48 with 32 by 32 icons now, and the sidebar 64 px wide, at Rotem's word: the picture starts 16 px further right.** His pick, option 2 of two, over buttons filling a 48 px sidebar edge to edge. |
| 2026-09-16 | editor | **The callout takes two clicks now: the first marks the point the line ends at, and the bubble follows the pointer from its automatic place, keeping that offset; the second locks it where it is and the typing begins.** Rotem's spec. Between the clicks Escape, Ctrl+Z, a change of tool, leaving annotation or another picture arriving drops the unplaced bubble and gives its number back; the text tool still types at one click. The code landed inside `2af8d24`, the other session's tick commit made from the shared tree at 22:44 while this change sat uncommitted in the same files, and the release copied in at 22:45:41 carries it. |
| 2026-09-16 | editor | **The sidebar's icons are drawn with a 2 px line in #C3C6CA now, at Rotem's word.** From the 2.4 px the enlargement left, in the buttons' #f2f2f2. |
| 2026-09-16 | host | **The release with the 1 px tick is in place: copied over `host/target/release/recon-host.exe` at 22:45:41 once Rotem quit Recon, the file reading back identical to the build.** |
| 2026-09-16 | editor, project-os | **The done mark's tick is a 2 px line and the circle's border 1 px white now, at Rotem's word: "the line" he asked to thin was the border, and the tick had been thinned instead.** The fill-clip line that had slipped into the tick commit unasked is gone. Two rules from it: a word that can name two elements is asked about first (Mistakes), and a commit from the shared tree carries only its own hunks (`CLAUDE.md` rule 22), promoted after a second slip. |
| 2026-09-16 | host | **The release with the 2 px tick in a 1 px border is built from the commit alone, `cf55206`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-16 | editor | **A ruler tool marks an area and writes its size in pixels, width and height, in a bubble beside the pointer the whole time the drag lasts; released, the box and its size stay on the picture and in every copy, and hovering it shows a round 16 px × with a 12 px X that deletes it.** Rotem's ask, his pick of two readings (a mark that stays, over a measurement that leaves nothing), and his spec for the ×; M and the button after Blur. In undo, redo, save and the copy like the other shapes; the look is provisional. |
| 2026-09-16 | host | **The release with the ruler is built from the commit alone, `d26a596`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** The other session's waiting job for `cf55206` will find the build's hash changed and copy nothing; that change is inside this one. |
| 2026-09-16 | host, editor | **The timeline's thumbnails show the notes now: after every save of a picture's notes the page draws its layer at the thumbnail's own scale, the host writes `thumb.png` anew with it composed over the picture, margin and all, and the strip's cell takes the new picture.** Rotem's ask, from a screenshot of a bare thumbnail under an annotated capture. Only the picture on screen; a note that grows the margin makes its thumbnail the whole composition fitted into the box. |
| 2026-09-16 | host | **The release with the thumbnails' notes is built from the commit alone, `6550906`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** The ruler's waiting job, for `d26a596`, will find another build in the folder and copy nothing; this one carries the ruler too. |
| 2026-09-16 | host | **The release with the thumbnails' notes is in place: copied over `host/target/release/recon-host.exe` at 23:49:50 once Rotem quit Recon, the file reading back identical to the build; his Recon opened on it one second later.** Rotem's word, DONE. The two older waiting jobs, for `cf55206` and `d26a596`, found this build in the folder and copied nothing. |
| 2026-09-17 | project-os | **Five old build folders in `.tmp/` are cleared at Rotem's word, about 18.5 GB: `s29-target`, `target-scroller2`, `front-target`, `check-target` and `check-target-tick2`, each left by a check run and regenerable. `.tmp/release-target/`, where each release is built, is kept.** The drive went from 11 GB free to 29 GB. |
| 2026-09-17 | host, editor | **The picture can be cropped in the editor now: a crop button after Ruler in the sidebar, on K, and with it in hand handles at the picture's corners and sides, dragged inward, cut the picture at the release; the screen, the size band, every copy and the thumbnail are the crop, the notes keep their place, Ctrl+Z gives the pixels back, and the source is never touched.** Rotem's word, over the v1 scope line that kept crop out. The look and the key are provisional. |
| 2026-09-17 | host | **The release with the crop is built from the commit alone; it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-17 | host | **The crop's release is in place: copied over `host/target/release/recon-host.exe` at 02:05:19 once Rotem quit Recon, the file reading back identical to the build, and his Recon opened on it the same second.** |
| 2026-09-17 | review | **The whole repository reviewed a fourth time, the crop in: seven findings, none fixed, every one waiting on Rotem's verdict.** The one that matters: a note whose save failed reads "saved" once its picture is left and returned to, and a quit then loses it. Two important: a capture deleted and undone while its image still encodes loses the image; the strip's thumbnail writer can recreate a deleted document's folder and block its restore. Four nits with their homes. The document is `notes/2026-09-17-code-review-4.md`. |
| 2026-09-17 | host, editor | **The seven findings of review 4 are fixed at Rotem's FIX ALL: a save refused before a switch of picture comes back as NOT SAVED and lands once the store is back; a capture deleted and undone inside its own encode comes back whole; no thread ever makes a document's folder again; the empty editor draws no shapes of the picture just deleted; an empty bubble after the second click gives its number back; two more decodes run outside the documents lock; a delete whose neighbour cannot be shown still deletes, into the trash.** Two new atlas rows and two decisions with it. |
| 2026-09-17 | host | **The release with the review 4 fixes is built from the commit alone, `c587272`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-17 | editor | **The picture's size above the timeline is written 2 px larger now, 16 px, from 14; the band around it stays 48 px tall.** Rotem's word. |

| 2026-09-17 | host | **The release with the 16 px size text is built from the commit alone, `a7dee9e`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |

| 2026-09-17 | editor | **Every button in the controls sidebar sits on 13 px corners now, from square, so its hover fill and the lit tool are rounded.** Rotem's word, option 1 of two: the button's own square, not the box around all of them. |

| 2026-09-17 | host | **The release with the sidebar's 13 px corners is built from the commit alone, `dd01da6`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-17 | editor | **The timeline's thumbnails end above the sideways scroller now: 112 px thumbnails, 8 px clear above them under the top line, and the scroller's 12 px band below, so the timeline is 133 px tall by default; every thumbnail's bottom 4 px, the selected one's blue border with them, sat under the scroller before.** Rotem's report with a screenshot, his pick of option 1 of three. The reply's option said 132; the timeline's 1 px top line, inside its height, makes it 133. |
| 2026-09-17 | host | **The release with the thumbnails ending above the scroller is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** The older jobs waiting on the same exit will find the newer hash and copy nothing. |
| 2026-09-17 | host | **Correcting the row above: the older jobs for `a7dee9e`, `dd01da6` and `c587272` had already run at 21:08, when Rotem quit Recon; `dd01da6` was copied in and the two others copied nothing. Only the `5ab0d95` job waits now.** |

| 2026-09-17 | editor | **The app's background is #0D0E12 now, at Rotem's word: the page ground behind the picture, the size band and the sidebar, from #111217.** Nothing else moved. |
| 2026-09-17 | host | **The release with the #0D0E12 background is built from the commit alone, `6a95d49`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-17 | editor | **The timeline's thumbnails sit on 13 px corners now, 12 px apart, with 12 px above them and at the row's two ends; the timeline is 137 px tall by default. Everything was 8 px and the corners 4 px.** Rotem's word. The scroller's 12 px band under the row is unchanged. Committed as `f2a314b` with no History row, this row added after. |
| 2026-09-17 | host | **The release with the 12 px air and the 13 px corners is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-17 | editor | **The timeline's thumbnails sit on 8 px corners now, from 13.** Rotem's word. |
| 2026-09-17 | project-os | **Design.md's Callout section, which another session is still writing, was swept into `a12fc2d` by a blind `git add` and is taken back out of the committed file here; it stays in the working tree, uncommitted, as that session left it.** The shared-tree rule, `CLAUDE.md` rule 22, broken a third time. |
| 2026-09-17 | host | **The release with the 8 px corners is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-17 | editor | **The callout bubble is #2D41D7 now, with 32 px of padding, 15 px corners and Rotem's shadow, no number in it, its text box 36 px tall and unmarked while typed in; the line is 4 px thick and ends in an open arrowhead at the point it names; Enter keeps the note and leaves the typing, Ctrl+Enter breaks the line.** Rotem's spec, from his Figma. The number stays in the document; Ctrl+Enter's copy and return works outside typing only now, and Shift+Enter still breaks the line. The six export references are remade from the new look and looked at. |
| 2026-09-17 | host | **The release with Rotem's callout is built from the commit alone, `49392fc`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** The other session's waiting job for `0e35d26` will find the build's hash changed and copy nothing; that change is inside this one. |
| 2026-09-17 | host | **The callout's release did not land: Rotem quit Recon at 22:10:31 and was back a second later, the shared build folder held another session's newer build by then, so the `49392fc` job would have copied nothing, and the other session's job missed the gap.** That waiting build, `18FC...5F76`, was made from a tree that carries the callout commit and its #0A0A0B background, so the next quit, with a few seconds before the restart, lands both. Correcting the row above. |
| 2026-09-17 | host | **The stale `49392fc` copy job was not stopped after all: the kill was refused, so it still waits, and at the next quit it finds the folder's hash changed and copies nothing.** Correcting the row above; harmless either way. |
| 2026-09-17 | editor | **The three window buttons at the top right are 48 px wide now, from 46, with 12 by 12 icons, from 10 by 10; the bar stays 32 px tall and so does each button.** Rotem's word, twice: the first round made the bar and the buttons 48 square, and he kept the bar as it was, so only the width and the icon changed.
| 2026-09-17 | host | **The release with the 48 px wide window buttons is in place: built from the commit alone and copied over `host/target/release/recon-host.exe` at once, since no Recon was running from it.** The copy reads back identical to the build.
| 2026-09-17 | editor, project-os | **The three window buttons are 48 px tall as well as wide now, and the top bar is 48 px tall to hold them, from 32.** Rotem's correction: the round before had read "the bar stays the same" as cancelling the button's height, which was his ask, so the button had gone back to 32. The picture, the sidebar and the picture's size band all start 16 px lower. `CLAUDE.md` rule 1 gained the rule this slip promoted.
| 2026-09-17 | host | **The release with the 48 px tall bar and buttons is in place, copied over the running path at 23:44:47 while no Recon was running from it.** The copy reads back identical to the build.
| 2026-09-17 | editor | **The minimize, maximize and close icons are 10 by 10 again, from 12, inside their unchanged 48 px buttons.** Rotem's word. The bar, the button box and every other icon in the app are untouched.
| 2026-09-17 | host | **The release with the 10 px icons is built from the commit alone and replaces the running copy the moment Rotem quits the Recon now running.** A hidden job waits for that exit; it copies nothing if the build's hash has changed or a Recon from that path is back by then.
| 2026-09-18 | host | **While a capture drag lasts, the dragged area's size in pixels sits in a bubble beside the crosshair now: 14 px text on the app's background, 8 px corners, 8 px of padding at the sides and 4 above and below, 8 px right of and below the pointer and on its other side at a display's edge.** Rotem's spec; the gap from the pointer and the text's colour and face are provisional. |
| 2026-09-18 | host | **The release with the capture drag's size bubble is built from the commit alone, `c3727cd`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-18 | host | **The capture overlay has a magnifier now: a 112 px circle beside the crosshair showing the pixels around the pointer 8 px each with a grid between them and the frame's blue through the pointer's own, up the whole time the overlay is, in a dark panel below and left of the pointer with the selection's size under the circle, the lit window's while hovering and the dragged area's while dragging.** Rotem's ask with a picture of another tool; the circle is his number, the rest is the picture's reading and provisional. The size bubble of the day before moved under the circle. |
| 2026-09-18 | host | **The release with the capture drag's size bubble is in place: copied over `host/target/release/recon-host.exe` at 01:36:10 once Rotem quit Recon, the file reading back identical to the build; his Recon opened on it at 01:48:37.** |
| 2026-09-18 | host | **The release with the magnifier is built from the commit alone, `e658def`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-18 | host | **When the pointer moves from one window to the next on the capture overlay, the lit area and its dashed frame glide from the one to the other now, over 144 ms, instead of jumping; a click mid-glide still picks the window under the pointer, and with Windows' animation effects off the area jumps as before.** Rotem's ask; the timing is A1's, provisional. The code landed inside `e658def`, the other session's magnifier commit made from the shared tree at 01:56 while this change sat uncommitted in the same two files, so the magnifier's release, waiting for Rotem's Recon to exit, carries the glide; this commit is the docs. |
| 2026-09-18 | host | **The lit area's glide between windows on the capture overlay is 256 ms with an ease-in now, slow to set out and quick to arrive, from A1's 144 ms ease-out.** Rotem's values. The self test looks at it 200 and 450 ms after the move now, from 40 and 300. |
| 2026-09-18 | host | **The release with the 256 ms ease-in glide is built from the commit alone, `9c51d08`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** The magnifier's release, with the 144 ms glide, was copied in at 05:30:41 when he quit the one before, and his Recon opened on it at 05:31:06. |
| 2026-09-18 | host | **The lit area's glide between windows on the capture overlay is 164 ms with an ease-out now, quick to set out and slow to arrive, from 256 ms with an ease-in.** Rotem's values. The self test looks at it 80 and 300 ms after the move now, from 200 and 450. |
| 2026-09-18 | host | **The release with the 164 ms ease-out glide is built from the commit alone, `b9d0a9b`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** The 256 ms release was copied in at 06:36:24 when he quit the one before; his Recon opened on it at 06:46:18 and again at 06:49:11. |
| 2026-09-18 | host | **The magnifier sits on the pointer's right now, its left edge 8 px right of the pointer, and moves to the pointer's left only where the right would leave the display.** Rotem's word; the picture had it on the left. |
| 2026-09-18 | plan | **Escape during a capture was already built: it cancels the capture while a window is lit and in the middle of a drag alike, nothing captured, and the plan's keyboard table says so now.** Rotem asked for it; no code changed. |
| 2026-09-18 | host | **The release with the magnifier on the pointer's right is built from the commit alone, `47b0369`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** The build carries the committed 164 ms glide, not the 192 ms another session is still tuning uncommitted. |
| 2026-09-18 | editor | **The minimize, maximize and close buttons are 40 px tall now, from 48, still 48 wide, and the top bar is 40 tall with them.** Rotem's word: 48 was a little tall. The picture, the sidebar and the picture's size band all start 8 px higher than yesterday.
| 2026-09-18 | host | **The release with the 40 px buttons is built from the commit alone and replaces the running copy the moment Rotem quits the Recon now running.** It carries the magnifier commit before it, so that session's waiting job finds a different build and copies nothing.
| 2026-09-18 | host | **The lit area's glide between windows on the capture overlay is 192 ms now, the ease-out kept, from 164 ms.** Rotem's value. The self test's mid-glide look is inconclusive past 140 ms now, from 120; that line of `project-os/QA.md` went in with the other session's `47b0369`, made from the shared tree. |
| 2026-09-18 | host | **The release with the 192 ms glide is built from the commit alone, `369296b`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** It carries the 40 px buttons of `502821a` too, whose own waiting job will find this newer build in the shared folder and copy nothing. |
| 2026-09-18 | host | **The magnifier's size text sits above its circle now, at the top of the panel, where the picture had it under.** Rotem's word. |
| 2026-09-18 | host | **The release with the magnifier on the pointer's right was in place at 15:45:14, copied once Rotem quit Recon, the file reading back identical to the build; two later releases of other sessions carry it too, and his Recon opened on the 16:03 one.** |
| 2026-09-18 | host | **The release with the size above the magnifier's circle is built from the commit alone, `ca5c936`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** |
| 2026-09-18 | host | **Correcting the row above: the job waiting to copy the `ca5c936` build in died with the machine's restart at 17:49, before Rotem quit Recon, so his two reopenings at 18:27 ran the 16:03 build, the size still under the circle; the job is restarted at 18:28 and copies the build the moment he quits again.** |
| 2026-09-18 | plan | **Rotem's hand on the `ca5c936` release, copied in once he quit Recon after the restart: the magnifier right of the pointer with the size above its circle, and it works great.** |
| 2026-09-18 | editor, host | **Recon has Settings: three dots left of minimize, in the window buttons' own size, open a modal where the shortcut that opens Recon and the shortcut that starts a capture are picked by pressing them.** Rotem's ask. Opening Recon has no shortcut until one is picked; a shortcut another application holds is refused in words under its field and the old one keeps working; both are saved and read back at the next start. The modal's look is provisional, from values the page already has. |
| 2026-09-18 | host | **The release with Settings is built from the commit alone and replaces the running copy the moment Rotem quits the Recon now running.** A hidden job waits for that exit; it copies nothing if the build's hash has changed or a Recon from that path is back by then.
| 2026-09-18 | project-os | **A change made in Settings stays outside Ctrl+Z: Rotem's call, written into rule 23 and recorded as a decision.** No code changed; Settings was built without an undo, which is now the decided behaviour and no longer an open question.
| 2026-09-18 | notes | **Ten ideas for a Recon logo in Rogers' graphic language, on one page Rotem opens in a browser.** Each is the blue disc with the light ring and 3.2 lines from `rogers_logo.svg`, shown large, beside a Recon wordmark built from Rogers' own letters, and at 64, 32 and 16. Nothing in the product changed.
| 2026-09-18 | host, editor | **The Open Recon shortcut, pressed again while Recon is the window in front, minimizes it; and the Settings button's icon is a chevron pointing down, from three dots.** Rotem's two asks. Behind another application, minimized or in the tray, the press still brings Recon forward.
| 2026-09-18 | host | **The Settings release landed at 18:42:59 when Rotem quit Recon, reading back identical to its build; the release with the toggling shortcut and the chevron is built and waits for his next quit the same way.**
| 2026-09-18 | host | **While capturing, two thin blue lines run through the pointer, one across and one down, over the whole screen whatever the capture area is: 1 px, the blue of the lines inside the magnifier's circle, at 72%.** Rotem's word. The screen is repainted in pieces now, so a move of the mouse still redraws two strips and the magnifier, not every pixel. |
| 2026-09-18 | host | **The release with the pointer's guide lines is built from the commit alone, the toggling shortcut and the chevron inside it; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** The job waiting with the build before it copies nothing now, since the shared build is this one. |
| 2026-09-18 | host, editor | **An SVG being viewed is sharp at every zoom above its own size now: the part on screen is drawn from the file itself at that zoom, where it used to be an enlargement of one small render. A picture with see-through parts is laid over the app's background before it is painted, the intended cure for the earlier sizes staying on screen while zooming in; that half is not yet seen on a screen.** Rotem's report, with `rogers_logo.svg`, 157 by 37. An SVG being annotated keeps its fixed pixels, which are what it copies and saves. |
| 2026-09-18 | host | **The release with the sharp SVG is built from the commit alone, `6b19599`, and is NOT in place: the hidden job that copies it when Rotem quits Recon was refused by this session's permissions, so the copy waits on one command from him.** It carries the guide lines of `5f397ec`. |
| 2026-09-18 | host | **The release with the pointer's guide lines is not in place: when Rotem quit Recon at 19:54 the shared build was already the newer one from `6b19599`, which carries the lines, so the waiting job copied nothing, and a copy by hand was refused by the session's permission check.** `host/target/release/recon-host.exe` is still the Settings build. |
| 2026-09-18 | plan | **Rotem's hand on the `6b19599` release, copied in by him: an SVG opens sharp, stays sharp zoomed in, and no earlier size stays on screen; flawless, in his word.** The see-through pixels laid over the app's background were the cure, seen on his screen where the automated screen check could not run. |
| 2026-09-18 | notes | **Ten more Recon logo ideas, grown from the two Rotem picked, the radar and the capture guides with their lit area, on a second page.** Numbered 11 to 20 with the two sources first, in the same layout as the first page. Nothing in the product changed.
| 2026-09-18 | notes | **Twenty more Recon logo ideas on a third page, numbered 21 to 40.** Wider than round two: the radar and the capture guides again, and beside them an R on the guides, the numbered callout, stacked captures, magnified pixels, binoculars and a scan line. Nothing in the product changed.
| 2026-09-18 | host, editor | **A file opened by name, from the picker, "Open with" or the command line, sits in the timeline now like a capture, as a pointer to where it lives: nothing of it is copied, its x takes it off and never touches the file, Ctrl+Z puts it back, and it leaves by itself when the file is gone.** Rotem's ask and his two calls: a pointer over a kept copy, and only the file opened, never one stepped onto in its folder. Annotate turns the pointer into the file's document. On a tab after Main it joins that tab, as a capture does. |
| 2026-09-18 | host | **The capture's guide lines are at 48% now, from 72%, and they stop 12 px short of the pointer on every side, so the plus there is white again.** Rotem's word, with a picture. Windows draws the plus as the inverse of what is under it, and over the blue lines that came out red. |
| 2026-09-18 | host | **The release with opened files in the timeline is built from the commit alone, `bcd65a5`, and is not in place: this session may not start the hidden job that copies it, so the copy is one command Rotem runs after quitting Recon.** It carries the sharp SVG of `6b19599`. |
| 2026-09-18 | host | **The release with the guide lines at 48% and the white plus is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-18 | host | **The release with the guide lines at 48% and the white plus is in place: copied over `host/target/release/recon-host.exe` at 20:29:40 once Rotem quit Recon, the file reading back identical to the build.** |
| 2026-09-18 | host | **Save As on an annotated PNG or JPEG opens on the file itself now and writes the picture with its notes over it, where it used to offer a new name beside it; every other existing file is still never written over, and an annotated SVG, GIF or other type keeps the new annotated PNG.** Rotem's exception to his own rule 11, asked for and confirmed as the clean original gone from disk; the rule's text carries it. |
| 2026-09-18 | host | **The release with Save As over the annotated file is built from the commit alone, `db497b3`, and is not in place: this session may not start the hidden job that copies it, so the copy is one command Rotem runs after quitting Recon.** It carries the guide lines at 48% of `85e3a8b` and the opened files of `bcd65a5`. |
| 2026-09-18 | host | **The two blue lines inside the magnifier's circle sit on the middle of the centre pixel now; they were 1 px right of and below it.** Rotem's report, with two pictures. The pixel's last row and column are the grid's line, and the middle was counted with them. |
| 2026-09-18 | editor | **The mode switches by itself now: picking a tool over a viewed file starts its annotation, a finished note or a drawn shape puts the tool down so a drag pans again, and a click on a note edits it; the Annotate button and the A key are gone.** Rotem's word, with his two choices: a tool picked again for every note, and no manual override. |
| 2026-09-18 | host | **The release with the circle's blue lines on the centre pixel is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-18 | host | **The release with the mode switching by itself is built from commit `38db43c` alone and waits in `.tmp/release-target/`; it is not copied over `host/target/release/recon-host.exe`, since Rotem's Recon runs from there.** The change's own two rows above went in with `9fdcd59`, another session's commit from the shared tree; the change itself is `38db43c`. |
| 2026-09-18 | host | **The release with the circle's lines on the centre pixel was not copied in: when Rotem quit Recon at 20:46 the shared build was already the newer one from `38db43c`, which carries the fix, so the job copied nothing; a job for that newer build waits now for his Recon to exit.** The release path still holds the 48% lines build. |
| 2026-09-18 | host | **Correcting the row above: the first job for the `38db43c` build did not wait, it tried the copy at 20:46:50 while Rotem's Recon was still letting go of the file, and failed; his Recon was back at 20:47:02 on the 48% lines build. The job is started again and waits for that Recon to exit.** |
| 2026-09-18 | host | **The release from `38db43c` is in place, the circle's lines on the centre pixel and the mode switching by itself inside it: copied over `host/target/release/recon-host.exe` at 20:50:08 once Rotem quit Recon, the file reading back identical to the build.** |
| 2026-09-18 | editor | **The top bar is 64 px tall now, from 40, with the Recon logo at its left, 16 px above it and to its left; minimize, maximize, close and the settings chevron keep their 48 by 40 and sit at the bar's top.** Rotem's word. The picture, the sidebar and the timeline's ceiling start 24 px lower with it. |
| 2026-09-18 | host | **The release with the 64 px top bar and the logo is built from commit `2a66c2e` alone and waits in `.tmp/release-target/`; it is not copied over `host/target/release/recon-host.exe`, because Rotem's Recon runs from there and the waiting copy job is refused to this session.** The copy is his one command, after quitting Recon. |
| 2026-09-18 | editor | **A tab no longer carries a ×: a right press on a tab after Main opens a small menu with Delete, which deletes it, and two new tabs now sit as far apart as Main and the first one.** Rotem's word; the hidden × was holding 18 px of empty room in every new tab. Ctrl+Z still brings a deleted tab back. |
| 2026-09-18 | editor | **The top bar no longer writes the picture's name and "Recon" beside the logo; the bar's middle is empty and still drags the window.** Rotem's word. The window's own title, the one the taskbar shows, still names the file. |
| 2026-09-18 | host | **The release with the tab's right-press menu is built from commit `c54e948` alone and waits in `.tmp/release-target/`; it is not copied over `host/target/release/recon-host.exe`, since Recon is running from there.** The copy is Rotem's, after he quits Recon. |
| 2026-09-18 | editor | **Correcting the row above: the commit that removed the bar's title line, `533447a`, also undid the tab menu commit `c54e948` in the two editor files, for one commit; `ebec0dd` has those lines back, and the working files never lost them.** The assistant's slip: its copy of the files was cut before `c54e948` landed, and HEAD was not read again before committing. |
| 2026-09-18 | host | **The capture frame's dashes walk the other way now: rightwards along the top and clockwise round the frame, from leftwards.** Rotem's word. Speed, dash, gap and colours unchanged. |
| 2026-09-18 | host | **The release to use is the one built from commit `ebec0dd`, which holds both the tab's right-press menu and the top bar without its title line; it waits in `.tmp/release-target/`, not copied, since Recon is running from the release path.** It replaces the `c54e948` build, which lacked the title change. |

## Appendix — deep rows

_Older rows archived -> see `History-archive.md` (moved by project-os/rotate.ps1, not rewritten)._

Newest at the bottom, same as the scan log.

| Date | Task | What changed | What was checked | Result | Risk | Commit before | Rollback |
|---|---|---|---|---|---|---|---|
| 2026-09-16 | The done mark to Rotem's spec | `editor/index.html`: `.thumb .done` 24 by 24 with a 12 px radius, `border: 2px solid #fff`, border-box; its svg 20 by 20; the ring circle gone from the markup, the tick redrawn in a 20 box; `.tick` at opacity 0, and under `.thumb.checked` opacity 1 with `transition: opacity 144ms ease-out` (A1) on the marking only, so clearing is instant; a reduced-motion rule beside it. `editor/editor-checks.js`: section 40's mark checks read the new sizes, the border, the absence of the ring, the tick clear before the press, the transition `opacity 0.144s ease-out`, opacity 1 after 300 ms, and the mark's box 4 px inside the thumbnail's edge (its 2 px border plus 2). `project-os/Design.md`: the Done mark rows and A1's second use. | fmt, clippy with -D warnings and --all-features, the product build, 56 host and 4 pixel tests, the checks build, on the live tree. `--editor-check` twice: the first failed only the new geometry check, which measured the mark from the thumbnail's outer edge where its 2 px border sits between, fixed in the check; the second: every check that ran passed, 412 pass, 0 failed, 5 not run, the three live-editor screen copies and the two dialog Escapes, refused because the browser pane was in front of the checks window meanwhile; they passed in the first run and no line of this change reaches them. Browser, `.tmp/tabs-harness.html` regenerated, in-app at 1280 by 800, real pointer: after a tab and a stubbed capture, hovering the thumbnail showed the mark as a grid at 12,717, 24 by 24, radius 12, border 2px rgb(255, 255, 255), the icon 20 by 20 at 14,719, so 2 px in on every side and nothing between; a real click ticked it, checked on the cell, the transition read `opacity 0.144s ease-out`, the id on the tab's done list; the screenshot shows the green circle with its white border and tick. Not readable there: the tick's opacity after 300 ms stayed 0 in the pane, which freezes animations while it is hidden behind another window (Visual QA §3); the editor check read 1 in the real window. Console: Passed, no new console errors. Tab closed, viewport reset. Risk low, styling and one motion; no review armed. Side effect, named: clearing a mark is instant, since the fade is on the marking alone. | Pass, 5 unrelated checks not run | low | `590580c` | Revert this commit. |
| 2026-09-16 | The finished-tabs release copied in | `host/target/release/recon-host.exe`: the waiting job of the 40cfadf build row copied the build from `.tmp/release-target/` over it at 22:27:48, after the release running since 22:00:41 exited; the job checked the build's hash first and that no Recon from that path was back. Rotem's Recon started from the new file at 22:27:50, and his word on the done mark came from it. No source changed. | The job's log: the copy reads back `0B492F9C...1ADCA085`, identical to the build. Risk low, a file copy. | Pass | low | `7390281` | Rebuild the release from `d356b38` and copy it back. |
| 2026-09-16 | The release built for the done mark | A release built from commit `7390281` archived into `.tmp/release-7390281/`, into the private target folder `.tmp/release-target/`, so the check run's rewrite of `host/references/s06/environment.txt` stayed out. Not copied: Recon was running from `host/target/release/recon-host.exe` since 22:27:50 (PID 21764), which holds the file. A detached job, `.tmp/swap-release-7390281.ps1`, started at 22:34:49, copies the build over it the moment that Recon exits, copies nothing if the build's hash has changed or a Recon from that path is back by then, and writes what it did to `.tmp/swap-release.log`. No source changed. | `cargo build --release` from that checkout: finished in 21.4 s, no warning. The build: 14,826,496 bytes, SHA-256 `4fb3a474...4f436fa8`. The job's log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `7390281`'s, whose tree passed them. Risk low, a build. | Pass, copy pending | low | `7390281` | Rebuild the release from `40cfadf`, whose mark is 20 px with a ringed icon and no fade. |
| 2026-09-16 | The done-mark release copied in | `host/target/release/recon-host.exe`: the waiting job of the 7390281 build row copied the build from `.tmp/release-target/` over it at 22:38:14, after the release running since 22:27:50 exited; the job checked the build's hash first and that no Recon from that path was back. Rotem's screenshot of a 20 px mark with green past its ring, sent before the swap, was the 40cfadf release; he called it his mistake once told. No source changed. | The job's log: the copy reads back `4FB3A474...4F436FA8`, identical to the build. Risk low, a file copy. | Pass | low | `c704e13` | Rebuild the release from `40cfadf` and copy it back. |
| 2026-09-16 | The tick's line 1 px | `editor/index.html`: `.thumb .done svg` stroke-width 1, from 1.5. `editor/editor-checks.js`: section 40's mark check reads the icon's stroke width, 1px. `project-os/Design.md`: the Done mark's icon row. | fmt, clippy with -D warnings and --all-features, the product build, 56 host and 4 pixel tests, the checks build, on the live tree. `--editor-check`: every check that ran passed, 435 pass, 0 failed, 1 not run, the Save As Escape, another application in front; unrelated to this line. Browser, `.tmp/tabs-harness.html` regenerated, in-app at 1280 by 800, real pointer: a tab, a stubbed capture, hovering the thumbnail and clicking the mark: checked, the icon's stroke 1px, the box 24, the border 2px. Console: Passed, no new console errors. The pane's screenshot timed out twice with the window hidden, so the 1 px line was read from the style, not looked at; the editor check reads the same. Tab closed, viewport reset. Risk low, one value. | Pass, one unrelated check not run | low | `c704e13` | Put stroke-width back to 1.5. |
| 2026-09-16 | The release built for the 1 px tick | A release built from commit `2af8d24` archived into `.tmp/release-2af8d24/`, into the private target folder `.tmp/release-target/`, so the other session's uncommitted edits in the checks, the plan, QA and Decisions, and the check run's rewrite of `host/references/s06/environment.txt`, stayed out. Not copied: Recon was running from `host/target/release/recon-host.exe`, which holds the file. A detached job, `.tmp/swap-release-2af8d24.ps1`, started at 22:44:54, copies the build over it the moment that Recon exits, copies nothing if the build's hash has changed or a Recon from that path is back by then, and writes what it did to `.tmp/swap-release.log`. No source changed. | `cargo build --release` from that checkout: finished in 23.5 s, no warning. The build: 14,828,544 bytes, SHA-256 `b2c4ffb4...b8f2c8ae`. The job's log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `2af8d24`'s, whose tree passed them. Risk low, a build. | Pass, copy pending | low | `2af8d24` | Rebuild the release from `7390281`, whose tick is 1.5 px. |
| 2026-09-16 | The sidebar's buttons and icons enlarged | `editor/index.html`: `#controls button` 48 by 48 from 32, `#controls svg` 32 by 32 from 20, `#controls` 64 px wide from 48, `#stage` starting at 64 px from 48; two comments. The plus in the size band, its own rules, unchanged; the icons' 1.5 stroke now draws 2.4 px wide, scaled with the 20-unit drawings, a side effect. `editor/editor-checks.js`: the sidebar section's numbers, 64, 48, 32, the container 48 wide, the tooltip at 56 px. `project-os/Design.md`: the icon button's two sizes restated, the sidebar's width added to the button group. | `--editor-check` from a private build (`.tmp/check-target/`, Recon running from the debug exe), three runs: the first found the two checks that still carried the old container width and tooltip offset, fixed; the second stopped early in the timeline's navigation section, `model.callouts[0].text` undefined at line 1607, a section this change does not touch, not seen in the runs before or after; the third, every check that ran passed, the sidebar 0-64 from 32, nine buttons 48 by 48 with 32 px icons 8 px apart, container 8-56 centred, the stage from 64, back from fullscreen from 64,32, the plus still 32 by 32. Console: Passed, known existing errors only, the save and copy failures the checks provoke. `cargo fmt --check` and clippy in the same private folder. Not run: cargo test, no Rust changed. Rotem's eye on the real window is open. Risk low, a value. Note: another session's `2af8d24`, the done mark's 1 px tick, committed `editor/index.html`, `project-os/Design.md` and the first check numbers from the shared working tree while this task ran, so the page's half of this change landed under that message; this commit carries the last two check numbers and these rows. | Pass | low | `c704e13` | Restore the four values in `#controls`, `#controls button`, `#controls svg` and `#stage`, and the five check numbers. |
| 2026-09-16 | The callout in two clicks | `editor/index.html`: `model.placing`; `beginPlacing`, `followPointer` and `cancelPlacing` above the stage's pointerdown; the pointerdown's callout branch creates the note and begins placing instead of editing, a pointerdown while placing locks it and starts the editing wherever it lands, the text tool unchanged; pointermove follows while placing and no drag runs; Escape and Ctrl+Z cancel while placing, Ctrl+Shift+Z is left to redo; `setTool` on a change of tool, `setMode` to view and `stashCurrent` drop it, `restore`, `showEmpty` and `loadImage` clear it. `editor/editor-checks.js`: sections 20, 32 and 34 give their pointer-made notes the second click; section 41, new, seventeen checks. `project-os/Plan.md` §3.5, the callout's steps; `project-os/QA.md`, the row; `project-os/Decisions.md`, the offset and the number. | fmt, clippy with -D warnings and --all-features, the product build, 56 host and 4 pixel tests: pass. `--editor-check`, the final page, one binary run twice: run 4, 431 pass, section 41 all seventeen, 1 failed, the typing autosave's three-second wait in section 23, untouched by this change and passing in every other run, and the three live-editor screen copies not run, another window in front; run 5, 426 pass, every check that ran, 7 not run for windows in front: the three screen copies, the two dialog Escapes and the two focus-return stand-ins, all but the screen copies passed in run 2 of this change, and no run of this session could take the screen copies, which this change does not reach. Browser: the page from `.tmp/tabs-harness.html` regenerated, in-app at 1280 by 800, real pointer: the sidebar's Callout button, then a click on the picture at image 194,256 made note 1 anchored there, its bubble at 222,280, nothing typed; a move carried the bubble to 478,475 with the line from the anchor to its edge; a click there locked it with the focus in the bubble, "placed in two clicks" typed and kept, the bubble still at 478,475 after a further move; Escape committed it as step 1; a first click and a move then Escape dropped note 2, the next number back to 2, the history untouched; the same for Ctrl+Z through a keydown carrying `code`, since the pane's own key action sends none, so its C and Ctrl+Z reach nothing on a page that routes by physical key. Console: no errors. Review, medium, this change's diff: one finding introduced and fixed, a capture arriving between the clicks stashed the unplaced bubble with the old picture, now dropped at the stash with a check for it; impact local, the callout tool's click path and the places that clear the editing; nothing pre-existing raised. | Pass | medium | `c704e13` for the code, which sits inside `2af8d24`; `9d91643` for the docs | The code: revert its hunks of `2af8d24` in `editor/index.html` and `editor/editor-checks.js`, keeping the tick's lines; the docs: `git revert` this commit. |
| 2026-09-16 | The sidebar's icon line: 2 px, #C3C6CA | `editor/index.html`: `#controls svg` stroke-width 1.25 from 1.5, which is 2 px on screen at 32 px over the 20-unit drawings, and stroke `#C3C6CA` from currentColor; the buttons' own text colour untouched, so the tooltip stays as it was. `editor/editor-checks.js`: the nine-buttons check asserts the computed stroke width scaled to 2 and the stroke `rgb(195, 198, 202)`. `project-os/Design.md`: the icon line colour token, the icon button's icon row. | `--editor-check` from the private build in `.tmp/check-target/`, three runs after the two edits: the first, with the width only, failed one trash check, Restore finding 0 in the trash, a section this change does not touch; the second, with the colour, ended with no report after a copy, the editor hidden with no application to return to; the third, every check that ran passed, 435 checks, the nine buttons with a 32 px icon drawn with a 2 px #C3C6CA line. The two stray runs are the store and the window state a run leaves for the next, seen twice today in unrelated sections; recorded here, not fixed. Console: Passed, known existing errors only, the failures the checks provoke. Not run: cargo checks, no Rust changed. Rotem's eye on the real window is open. Risk low, two values. | Pass | low | `5fdeea7` | Restore `stroke: currentColor; stroke-width: 1.5` in `#controls svg`, the check's two terms and the Design rows. |
| 2026-09-16 | The 1 px tick release copied in | `host/target/release/recon-host.exe`: the waiting job of the 2af8d24 build row copied the build from `.tmp/release-target/` over it at 22:45:41, after the release running since 22:43:26 exited; the job checked the build's hash first and that no Recon from that path was back. No source changed. | The job's log: the copy reads back `B2C4FFB4...B8F2C8AE`, identical to the build. Risk low, a file copy. | Pass | low | `9d91643` | Rebuild the release from `7390281` and copy it back. |
| 2026-09-16 | The tick 2 px, the border 1 px, the clip line out | `editor/index.html`: `.thumb .done` border 1px solid #fff, from 2px; its svg stroke-width 2, from 1; the `background-clip: padding-box` line removed: it had landed in `2af8d24` although Rotem stopped the tool call that wrote it, and the checked state's own `background` shorthand reset it anyway, so it did nothing where it was meant to. `editor/editor-checks.js`: section 40's mark check reads 1px border, 2px stroke, no clip clause. `project-os/Design.md`: the Done mark's two rows. `project-os/Mistakes.md`: one Open row, the ambiguous "line"; the shared-tree row promoted. `CLAUDE.md` rule 22: the paragraph on a commit from a shared tree carrying only its own hunks. Staged by `.tmp/tick2-stage.py`: HEAD's copy of the three shared files plus these edits alone, since another session's sidebar-icon edits sat uncommitted in the same files; CLAUDE.md and Mistakes.md whole, their diffs read and all mine. | fmt, clippy with -D warnings and --all-features, the product build, 56 host and 4 pixel tests, on the live tree. `--editor-check` five times on the live tree, which carries the other session's uncommitted sidebar edits: the first (before the clip line went) failed the S2.7 restore check once; the second never booted, a run started while another session's check run held the same folder, and was left behind since stopping it was refused; the third, from a private checks build in `.tmp/check-target-tick2/`, failed the HUD storage line once and the thumbnail-bubble pixel; the fourth failed the thumbnail-bubble pixel alone, 442 pass, 0 not run. Section 40 green in every run that booted: the mark 24 by 24 with a 1px rgb(255, 255, 255) border, the icon 20 with a 2px stroke, the tick fading in. The thumbnail-bubble pixel at 78,144 reads the host's thumbnail PNG after a note is placed; no line of this change reaches the thumbnail or the note, and the callout's two-click placement landed in the tree under this task from the other session; reported, not fixed, theirs to look at. Browser, `.tmp/tabs-harness.html` regenerated, in-app at 1280 by 800: the pane could not draw for a screenshot, so the mark was driven through the page's own functions and read: shown as a grid once marked, 24 wide, border 1px rgb(255, 255, 255), the icon 20 with a 2px stroke, the tick at opacity 1, the id on the tab's done list; before the clip line went, the checked state read background-clip border-box, which is how the line was found inert. Console: Passed, no new console errors. Tab closed, viewport reset. Not looked at by eye: the 2 px tick in the 1 px ring, Rotem's. Risk low, two values. | Pass, one pre-existing failure flagged | low | `690c134` | Put the border back to 2px and the stroke to 1. |
| 2026-09-16 | The release built for the 2 px tick | A release built from commit `cf55206` archived into `.tmp/release-cf55206/`, into the private target folder `.tmp/release-target/`, so the other session's uncommitted work and the check run's rewrite of `host/references/s06/environment.txt` stayed out. Not copied: Recon was running from `host/target/release/recon-host.exe` since 22:57:59 (PID 49552), which holds the file. A detached job, `.tmp/swap-release-cf55206.ps1`, started at 23:04:42, copies the build over it the moment that Recon exits, copies nothing if the build's hash has changed or a Recon from that path is back by then, and writes what it did to `.tmp/swap-release.log`. Left behind: a `--editor-check` process from 22:52:22 (PID 60804) that never booted, since stopping it was refused; it holds `host/target/debug/recon-host.exe`, so the next checks build there waits on it, or builds into a private folder as this task did. No source changed. | `cargo build --release` from that checkout: finished in 26.1 s, no warning. The build: 14,829,056 bytes, SHA-256 `70d6a4e2...efcf6236`. The job's log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `cf55206`'s, whose tree passed them. Risk low, a build. | Pass, copy pending | low | `cf55206` | Rebuild the release from `9d91643`, whose tick is 1 px in a 2 px border. |
| 2026-09-16 | The ruler | `editor/index.html`: a `ruler` tool after Blur in the sidebar, on M; a shape `{ kind: 'ruler', a, b }` made by the shapes' drag; `layoutScene` lays each out as a `.ruler` box in the scene like the blur, a 2 px #7aa7ff border, with a `.size` label reading `WxH` in image pixels 8 px right of and below the corner b, where the pointer is, and a `.x` button, `data-ui`, hidden while the ruler is drawn and shown on hover; the scene's transform sets `--k`, its own scale, and the × is counter-scaled by it to 16 screen pixels, centred on the outer top right corner; a pointerdown on the × removes the shape, settles and records; a move or draw drag lays the ruler out like a blur; `exportLayer` drops every `[data-ui]` element from the clone. `editor/editor-checks.js`: section 43, twenty checks; the sidebar section counts ten buttons. `project-os/Plan.md`: S3.5. `project-os/Design.md`: the Ruler component, the seven tools. `project-os/QA.md`: the row. `project-os/Decisions.md`: the entry. | On a worktree of `cf55206` plus these edits alone (`.tmp/ruler-src`), other sessions' work being uncommitted in the same files, built into `.tmp/ruler-target/`: fmt clean, clippy with -D warnings and --all-features clean, the product build, 56 host and 4 pixel tests, the checks build. `--editor-check` there, three runs: the first was the product binary by mistake, the project checks' build having replaced the checks build in the same folder, and it exited as a second instance, showing the Recon already running; the second, 449 pass, section 43 all twenty, and the sidebar count failed on the tenth button, fixed in the check; the third, 449 pass, 0 failed in what this change touches, 5 failed elsewhere, 1 not run: the picker's Escape with another application in front. The same binary of `cf55206` alone, without the ruler, run once for the comparison: 429 pass, the same five failed, the picture opened with no room, the pan's whole-picture copy, the one-row wheel and the two done-mark checks, none of which dispatches anything at a ruler, and the same one not run, so all five are pre-existing and none is looked into here. Section 43 read: the ruler at 100,80 160x120 with "160x120" at 168,128 after the drag and "100x70" at 108,78 midway, the × hidden while drawing and 16.0 by 16.0 centred on the corner within 0.2 px at zoom 1 and 2, the export holding the size and no button, the move to 110,100 undone, the click making nothing, the ruler back from the disk, the × press deleting it and Ctrl+Z, Ctrl+Shift+Z, Ctrl+Z walking it back, gone and back. Browser, the page from `.tmp/ruler-harness.html` with the host stubbed, in-app at 1280 by 800, real pointer: the Ruler button lit the tool; a drag on the picture made the box with "399x300" beside the release corner; the × 16 by 16 at zoom 0.74 and 2, its centre 0.5 px off the corner at most once the border was allowed for (the first cut sat 2 px in, found here, fixed); hovering off the box hid the ×, hovering on it showed it; a real click on the × deleted the ruler, undo and redo through the page's own functions walked it back; the export markup held the size and no button. Console: Passed, no new console errors. Tab closed, viewport reset. Review (rule 17, medium), this diff: impact shared, `layoutScene`, the stage's pointer handlers and the export being read by every tool; nothing found beyond the × offset above, fixed and re-run; pre-existing, reported not fixed: a ruler whose label lies past the picture's edge is cut in the copy, since the margin follows the notes alone, as a rectangle past the edge already is. Not run by a machine: a real hover and × press on the real window, Rotem's hand. Side effects, named: M is taken as a key in annotation; the ruler counts as a shape everywhere shapes are counted. | Pass, 5 pre-existing failures flagged | medium | `9e48b4e` | Revert this commit; a `ruler` shape in a saved document is ignored by the page before it. |
| 2026-09-16 | The release built for the ruler | A release built from commit `d26a596` archived into `.tmp/release-d26a596/`, into the private target folder `.tmp/release-target/`, so the other sessions' uncommitted work in the same files stayed out. Not copied: Recon was running from `host/target/release/recon-host.exe` (PID 49552, since 22:57:59), which holds the file. A hidden job, `.tmp/swap-release-d26a596.ps1`, started at 23:17:43, copies the build over it the moment that Recon exits, copies nothing if the build's hash has changed or a Recon from that path is back by then, and writes what it did to `.tmp/swap-release.log`. The other session's job for `cf55206`, waiting since 23:04:42, will find the hash changed and copy nothing; this build contains its change. No source changed. On the way: the drive filled during this task's two private check builds, 6.6 GB in them, and the first commit attempt stopped on it before touching the index; both build folders and the comparison worktree were removed, this task's own. | `cargo build --release` from that checkout: finished in 25.2 s, no warning. The build: 14,831,104 bytes, SHA-256 `10857e99...fb12ba6a`. The job's log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `d26a596`'s, whose change passed them on the worktree of `cf55206`. Risk low, a build. | Pass, copy pending | low | `d26a596` | Rebuild the release from `9e48b4e`, which has no ruler. |
| 2026-09-16 | The thumbnails carry the notes | `host/src/editor.rs`: `editor_thumbnail`, a command taking the page's layer as PNG bytes with the margin, the blur and the document id in headers, refusing a layer whose document is not on screen or whose size is not the thumbnail of the composition, resampling the picture (a blurred copy when blur rects are named) to that scale, composing the layer over it with the margin scaled and the rounding taken by the margin's far sides, and writing `thumb.png` atomically; `thumb_size`, the one rule for the box, used by `thumbnail` too; the command in both handler lists. `editor/index.html`: `exportLayer(scale)` draws the layer into a canvas that much smaller; `refreshThumbnail` after `editor_save_notes` succeeds, not awaited, one at a time with one more queued, dropping the cached picture and reloading the strip's cell; `thumbnailDone` and `compositionSize` exported for the checks. `editor/editor-checks.js`: section 42, eight checks. `project-os/Plan.md` S2.1, `project-os/Map.md` the store row, `project-os/QA.md` the row, `project-os/Decisions.md` the entry. | fmt, clippy with -D warnings and --all-features, the product build and 56 host and 4 pixel tests in a private target folder, `.tmp/check-target-thumb/`, since another session's debug Recon held `host/target/debug/recon-host.exe` for the whole task: pass. `--editor-check` from that folder, the final code on the tree with the ruler commit `d26a596` under it: 458 pass, section 42 all eight: a 320 by 200 capture's thumbnail read back 320 by 200; a note typed and saved followed by a refresh the host accepted, the number badge's blue (122,167,255) at 78,144 where the picture had white and the bubble's ground (27,31,36) at 302,164 where it had 20,20,20, the pixel at 5,5 unchanged, the strip cell's picture replaced; the note deleted and saved, the pixel back to white; a note at the right edge growing the margin to 0,0,0,4 gave a 314 by 200 thumbnail of the 320 by 204 composition. 5 failed, none reached by this change, five checks red on the display the machine has now, one 3840 by 2160 TV at 225% where every earlier run had a 100% display: the no-area fit, the drag's edge copy, the wheel over the strip, the done mark's border read as 0.889 px and its press; the same five were red in the first run of this code and the section-24 crash of one run, a capture probe's notes missing, was the atlas's double-load class and did not repeat. Browser: none; the host composes the thumbnail, which a stubbed host cannot, so the check run through the real binary is the driven app for this change. Review, medium, this change's diff: one finding introduced and fixed, a picture shown between the layer's drawing and its arrival could have taken the other picture's notes on its thumbnail, now refused by the document header; impact shared, `exportLayer` also serves Copy, Save As and the export references, all of whose checks passed at the default scale of 1; the five scale-red checks are pre-existing, reported, not fixed. | Pass | medium | `3a0d4de` | `git revert` the commit; `thumb.png` files already rewritten keep their notes until the document's next save, and a `thumb.png` deleted is made again from the picture. |
| 2026-09-16 | The release built for the thumbnails' notes | A release built from commit `6550906` archived into `.tmp/release-6550906/`, into the private target folder `.tmp/release-target/`, so the check run's rewrite of `host/references/s06/environment.txt` stayed out; not copied: Rotem's Recon was running from `host/target/release/`, so `.tmp/swap-release-6550906.ps1` waits, hidden, for it to exit, checks the build's hash, copies it over the release and reads the copy back; its first log line is in `.tmp/swap-release.log` at 23:22:16. | `cargo build --release` from that checkout: 22.5 s, no warning. The build: 14,882,304 bytes, SHA-256 `bc06c27c...eb912c2`. Not run: the copy and its hash read back, waiting on Rotem quitting Recon. The project checks were not rerun: the code is `6550906`'s, whose tree passed them. Risk low, a build. | Pass | low | `6550906` | Delete `.tmp/release-target/release/recon-host.exe` before Rotem quits, or stop the waiting PowerShell; a copy already made is undone by rebuilding the release from the commit wanted. |
| 2026-09-16 | The thumbnails' release copied in | `host/target/release/recon-host.exe`: the waiting job of the row above copied the build from `6550906` in `.tmp/release-target/` over it at 23:49:50, after the release running since 22:57:59 exited; the job checked the build's hash before the copy and read the copy back; the two older jobs logged a hash mismatch and copied nothing. Rotem's DONE also cleared `host/target/debug`: `host/target` is 3.0 GB now, 19 GB before. | The job's log lines in `.tmp/swap-release.log`; the file's SHA-256 read again at pickup, `BC06C27C...EB912C2`, identical to the build; Rotem's Recon, PID 3780, running from that path since 23:49:51. Not run: the strip with a note on a real capture, Rotem's eye. Risk low, a record. | Pass | low | `1d2f344` | Nothing to undo: a record. The release itself is undone by rebuilding from the commit wanted. |
| 2026-09-17 | Old build folders cleared from the scratch folder | Removed at Rotem's word, option 1 of two: `.tmp/s29-target`, `.tmp/target-scroller2`, `.tmp/front-target`, `.tmp/check-target`, `.tmp/check-target-tick2`, private cargo target folders from earlier check runs. Kept: `.tmp/release-target/` and everything else, `.tmp/` at 2.2 GB after, 21 GB before. No process ran from any of them: the two Recons running were `host/target/debug` and `host/target/release`. | `du` before and after, `df`: 29 GB free after, 11 before. Nothing built or run. Risk low, scratch output. | Pass | low | `10f4e3e` | Nothing to restore: a check run rebuilds a target folder in about a minute and a half. |
| 2026-09-17 | The crop | `editor/index.html`: a `crop` tool after Ruler in the sidebar, on K; `model.crop`, one rect in image pixels or null, and `model.cropping` while a handle is dragged; `pictureRect()`, the crop or the whole source, read by `compositionSize`, `clampPan` (centred, the pan is the crop's origin), `paintRegion` (the region stops at the crop's edges), `placeComposition` (the canvas, the backdrop and the mat placed by the crop's origin), `fitMargin` and `placeCallout` (the margin around the crop) and the size band; a `#crop` frame in the scene with eight `.h` handles counter-scaled by `--k`, shown by `body.cropping` while the tool is in hand, `dragging` with a dimming shadow while a handle moves; `cropFrom` moves the held edge or a corner's two inward only, a side no smaller than `MIN_CROP` 8; the stage's pointerdown starts a `crop` drag on a handle and pans on the picture with the tool in hand, pointermove follows, `endDrag` calls `applyCrop` and records one step; `applyCrop` sets the crop, drops the pan backdrop, follows the fit, lays out, refits the margin and paints; the crop is in `notesOf`, the snapshot, the stash and `loadImage` (`cropFits` drops one that does not fit its picture), and `restore` applies a crop that changed; `exportLayer` places image space at the margin less the crop's origin, leaves the frame out and returns `crop`; the thumbnail, copy and Save As send a `crop` header. `host/src/compose.rs`: `CropRect`, `parse_crop`, `prepare_source` (the blur on a copy, then the crop on a copy, the bytes themselves when neither is named) and a test. `host/src/editor.rs`: `crop_header`; `compose_layer` takes the crop and composes the prepared source; `editor_thumbnail` resamples the prepared source; the export check compares against the source cut to the crop, unblurred, and reports the crop's size; the copy, Save As and the checks' write pass the header. `editor/editor-checks.js`: section 44, twenty-one checks; the sidebar section counts eleven buttons; the export check helper sends the crop. `project-os/Plan.md`: S3.6, and the scope line. `project-os/Design.md`: the Crop component, the eight tools. `project-os/QA.md`: the row. `project-os/Decisions.md`: the entry. | In a private target folder, `.tmp/crop-target/`, since a Recon runs from `host/target/debug/` and one from `host/target/release/`: fmt clean after `cargo fmt`, clippy with -D warnings and --all-features clean, the product build, 57 host and 4 pixel tests (the crop's the new one), the checks build. `--editor-check` from it, two runs: the first, 483 pass, 1 failed, section 44's handle centre 1 px in at zoom 1 and 2 px at zoom 2, the handles placed inside the frame's border; the frame's line made an outline; the second, 484 pass, 0 failed, 0 not run, every section, the five failures the ruler's run reported now passing too. Section 44 read: K lights the button after Ruler, 48 px; the frame block at 0,0 400x300 with 8 handles, 10.0 by 10.0 centred on the right edge within 0.5 px at zoom 1 and 2; the east handle dragged to 380 shows the frame at 380 wide with a half-black shadow and no crop yet, released it is `0,0,380,300`, the band `380x300`, history index 2; the corner to 40,30 gives `40,30,340,270`, the canvas 340x270 at the offset, the pan 40,30, the note's anchor 60,50 and its box 88px unchanged, the margin 0; outward moves nothing; the west handle to 1000 stops at width 8; Ctrl+Z back; the layer `crop 40,30,340,270` with `left:-40px;top:-30px` and the note, no handle; the export check 340x270, 0 source mismatches, its pixel 0,0 equal to the region's pixel 40,30; the thumbnail 252x200; Ctrl+Z twice to 380 and to none with the canvas 400 wide, Ctrl+Shift+Z twice back to `40,30,340,270`; after `editor_store_reload` the crop and the note are back; C hides the frame, K shows it at 40,30 340x270. Console: Passed, known existing error only: WebView2's "Failed to unregister class Chrome_WidgetWin_0" at exit, in the earlier runs too. Browser QA, Path A: the check's real pointer events in the real window are the drag, the computed styles and the DOM are the inspection. Review (rule 17, medium), this working tree against `b3440c3`: impact shared, `pictureRect` read by the fit, the pan, the paint, the placement, the margin, the export and the thumbnail, every consumer exercised by the 484 checks; found and fixed, the handle offset above; reported, not fixed: the handles are pointer only, as every shape's drag is. Not run by a machine: a real drag by hand and a paste of the cropped copy, Rotem's hand. Side effects, named: K is taken as a key in annotation; a click on the picture with the crop tool pans; a note outside the crop takes margin around it. | Pass | medium | `b3440c3` | Revert this commit; a `crop` in a saved document is ignored by the page before it. |
| 2026-09-17 | The release built for the crop | A release built from commit `d01e623` archived into `.tmp/release-d01e623/`, into the private target folder `.tmp/release-target/`, so the check run's rewrite of `host/references/s06/environment.txt` stayed out. Not copied: Rotem's Recon was running from `host/target/release/recon-host.exe` (PID 65276, since 00:03:51), which holds the file. A hidden job, `.tmp/swap-release-d01e623.ps1`, started at 01:38:58, copies the build over it the moment that Recon exits, copies nothing if the build's hash has changed or a Recon from that path is back by then, and writes what it did to `.tmp/swap-release.log`. No source changed. | `cargo build --release` from that checkout: finished in 42.7 s, no warning. The build: 14,865,920 bytes, SHA-256 `8C2076A3...3FEBAC62`. The job's first log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `d01e623`'s, whose change passed them in `.tmp/crop-target/`. Risk low, a build. | Pass, copy pending | low | `d01e623` | Rebuild the release from `b3440c3`, which has no crop. |
| 2026-09-17 | The crop's release copied in | `host/target/release/recon-host.exe`: the waiting job of the row above copied the build from `d01e623` in `.tmp/release-target/` over it at 02:05:19, after the release running since 00:03:51 exited; the job checked the build's hash before the copy and read the copy back. No source changed. | The job's log: the copy reads back `8C2076A3...3FEBAC62`, identical to the build, True. Rotem's Recon (PID 64840) started from that path at 02:05:19, on the new file. Not run: a crop by hand on it, Rotem's hand. | Pass | low | `e130e48` | Rebuild the release from `b3440c3` and copy it in when no Recon runs from that path. |
| 2026-09-17 | Code review 4, the whole repository | `notes/2026-09-17-code-review-4.md`: the review of the whole tree at `a754798`, one agent, read file by file: the product code, the checks code and the process tooling (unchanged since review 3, read again). Seven findings, one block each with where, the problem, the fix and the check that proves it: T1 blocking, the save state not carried in the page's stash, so a failed save reads "saved" after a switch of picture; T2 important, the preserve thread's PNG dropped when a delete and an undo both land inside the encode; T3 important, `thumbnail` writing a document's folder outside the list's lock and `write_atomic` recreating it; T4 to T7 nits: the empty editor keeping the last picture's shapes, an empty bubble after the second click burning its number, two decodes under the documents lock, a delete with an unshowable neighbour reported as NOT DELETED. Nothing in the code changed. | The read itself: every file in `host/src`, `host/pixels`, `editor/` and `project-os/guards` plus the four scripts, against the always-check list row by row; each finding traced to its lines and its consequence reasoned from the code on both sides (the page's `stashCurrent` and `loadImage` for T1; `preserve` and `trash_rejoin` for T2; `thumbnail` and `write_atomic` for T3). No check was run and no browser driven: nothing changed. The document read back, its file names resolving. Risk low, a document. | Pass | low | `a754798` | Delete the document and these two rows. |
| 2026-09-17 | Review 4 fixed, T1 to T7 | `editor/index.html`: `stashCurrent` carries `save: model.save`, the object itself, and `loadImage` restores it and calls `markDirty` when it is dirty (T1); `showEmpty` resets the shapes, the crop, the cropping rect and the margins (T4); `commitEditing` gives a discarded empty bubble's number back when it is the newest, text notes apart (T5). `host/src/store.rs`: `rename_retry`, the second of retries, now under `restore` as well as `trash`; `write_beside`, the atomic write that refuses to make the folder, `write_atomic` sharing `write_into` with it; a unit test for both. `host/src/editor.rs`: the preserve thread writes an unlisted document's image beside its folder wherever it is, documents or trash, with `write_beside` (T2, T3); `trash_rejoin` waits `REJOIN_WAIT`, 1.5 s, for a missing image and puts the folder back into the trash on any failure (T2); `thumbnail` and `editor_thumbnail` write with `write_beside` (T3); `thumbnail` and `editor_annotate` take the handle under the lock and decode after it, `Managed::frame` gone (T6); `editor_delete_document` falls back to `show_nothing` when the neighbour cannot be shown and returns the delete as done (T7); `editor_store_remove_source`, checks only, in the handler list. `editor/editor-checks.js`: five checks, in sections 23, 29 (two), 39 and 41. `project-os/QA.md`: the row. `project-os/BugAtlas.md`: row 4's count and rows 5 and 6. `project-os/Decisions.md`: two entries. `project-os/Code_review.md`: the folder row rewritten, two rows added. `notes/2026-09-17-code-review-4.md`: the seven marked done. | In a private target folder, `.tmp/review4-target/`, since a stuck check run holds `host/target/debug/recon-host.exe` and Rotem's Recon runs from `host/target/release/`: `cargo fmt --check` clean, clippy with -D warnings and --all-features clean, the product build, 57 host and 4 pixel tests (the write-beside test the new one), the checks build. `--editor-check` from it, two runs: the first, 488 pass, 1 failed, section 28's HUD storage line, the ledger against the HUD's figure, a check this change does not reach and one earlier rows record failing on its own (2026-09-16, the tick2 row); the second, 480 pass, 1 failed and 5 not run, all six the dialogs and screen copies with another window in front, and the storage line green. The five new checks passed in both runs: section 23, refused then failed, dirty true, NOT SAVED shown, on disk by the timer, then saved; section 29, 0 shapes, crop null, margin 0,0,0,0, nothing drawn, and the delete with the neighbour's image removed done, width 0, in the trash, no NOT DELETED; section 39, the 4000 by 2500 capture back on screen with json and png in one folder and nothing in the trash, the host's log naming the image joining its folder among the documents; section 41, the number back to 2 and the history at 1. Console: Passed, known existing errors only, the failures the checks provoke. Browser QA, Path A: the checks' real pointer events in the real window are the drive, the DOM and the store on disk the inspection. Review (rule 17, high), this diff: impact shared, `write_beside` under three writers, `rename_retry` under the trash and the restore, the stash and the load under every switch of picture, every consumer exercised by the 488 checks; nothing found beyond the seven. Side effects, named: a restore that fails puts the folder back under a fresh stamp, so its thirty days start again; the wait for a missing image holds the command's thread up to 1.5 s, only then. Not run by a machine: a real Ctrl+Delete then Ctrl+Z on a real capture, Rotem's hand. | Pass, 1 pre-existing flake and 6 window-in-front checks flagged | high | `ccce822` | Revert this commit; a document's record and image are unchanged by it. |
| 2026-09-17 | The release built for the review 4 fixes | A release built from commit `c587272` archived into `.tmp/release-c587272/`, into the private target folder `.tmp/release-target/`, so the check runs' rewrite of `host/references/s06/environment.txt` stayed out. Not copied: Rotem's Recon was running from `host/target/release/recon-host.exe` (PID 64840, since 02:05:19), which holds the file. A hidden job, `.tmp/swap-release-c587272.ps1`, started at 16:23:31, copies the build over it the moment that Recon exits, copies nothing if the build's hash has changed or a Recon from that path is back by then, and writes what it did to `.tmp/swap-release.log`. The first try at starting it, the script written and started in one shell command, was refused by the session's permission check; written as a file and started on its own it ran. No source changed. | `cargo build --release` from that checkout: finished in 33.0 s, no warning. The build: 14,885,888 bytes, SHA-256 `80E5A6A6...5194611C`. The job's first log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `c587272`'s, whose change passed them in `.tmp/review4-target/`. Risk low, a build. | Pass, copy pending | low | `c587272` | Rebuild the release from `a754798`, which has none of the seven fixes. |
| 2026-09-17 | The size text at 16 px | `editor/index.html`: the size band's text is 16 px, from 14, on the same 16 px line in the same 48 px band; `editor/editor-checks.js`: the size band's check expects 16 px; `project-os/Design.md`: Image size, Text, 16 px from 14. Rotem's word, 2 px more. Not touched: the tabs' 14 px text and the ruler's 14 px bubble. | On a private copy of HEAD plus this change alone, another session's uncommitted timeline change left out: fmt, clippy, build and test green (57 and 4 tests pass); `--editor-check` 489 pass, 0 fail, the size check reading 500 16px "Google Sans", the text centred at 606.5 to 673.4 in a 1280 window, the band still 16 px under the stage and 16 px above the timeline. Not run: Rotem's eye on the release. Risk low, a value. | Pass | low | `d491db0` | Revert the commit; the three files carry nothing else of it. |
| 2026-09-17 | The release built for the 16 px size text | A release built from commit `a7dee9e` archived into `.tmp/release-a7dee9e/`, into the private target folder `.tmp/release-target/`, so another session's uncommitted timeline change and the check runs' rewrite of `host/references/s06/environment.txt` stayed out. Not copied: Rotem's Recon was running from `host/target/release/recon-host.exe` (PID 64840, since 02:05:19), which holds the file. A hidden job, `.tmp/swap-release-a7dee9e.ps1`, started at 21:04:14, copies the build over it the moment that Recon exits, copies nothing if the build's hash has changed or a Recon from that path is back by then, and writes what it did to `.tmp/swap-release.log`; the older c587272 job waiting on the same exit will find the newer hash and copy nothing. No source changed. | `cargo build --release` from that checkout: finished in 31 s, no warning. The build: 14,885,888 bytes, SHA-256 `23E8C2E8...3981C525`. The job's first log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon. Risk low, a build. | Pass | low | `a7dee9e` | Nothing to undo: the build waits in `.tmp/`; the copy is undone by rebuilding from the commit wanted. |
| 2026-09-17 | The sidebar buttons on 13 px corners | `editor/index.html`: `#controls button` has a 13 px radius, from 0, so the hover fill and the active tool's fill are rounded; `editor/editor-checks.js`: the icon button check expects the 13 px corners on all eleven; `project-os/Design.md`: Icon button, Container, 13 px corners from square. Rotem's word, option 1 of two (the button's own square; the box around all the buttons has no fill, so a radius there shows nothing). Not touched: the tab bar's plus, still square. | On a private copy of HEAD plus this change alone, another session's uncommitted timeline change left out: fmt, clippy, build and test green (57 and 4 tests pass); `--editor-check` 489 pass, 0 fail, the eleven icon buttons 48 by 48 on 13 px corners with their hover fill by A1. Not run: Rotem's eye on the release. Risk low, a value. | Pass | low | `12fc670` | Revert the commit; the three files carry nothing else of it. |
| 2026-09-17 | The release built for the sidebar's corners | A release built from commit `dd01da6` archived into `.tmp/release-dd01da6/`, into the private target folder `.tmp/release-target/`, so another session's uncommitted timeline change and the check runs' rewrite of `host/references/s06/environment.txt` stayed out. Not copied: Rotem's Recon was running from `host/target/release/recon-host.exe` (PID 64840, since 02:05:19), which holds the file. A hidden job, `.tmp/swap-release-dd01da6.ps1`, started at 21:07:41, copies the build over it the moment that Recon exits, copies nothing if the build's hash has changed or a Recon from that path is back by then, and writes what it did to `.tmp/swap-release.log`; the older a7dee9e and c587272 jobs waiting on the same exit will find the newer hash and copy nothing. No source changed. | `cargo build --release` from that checkout: finished in 22 s, no warning. The build: 14,885,888 bytes, SHA-256 `4EFF6F1D...68500349`. The job's first log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon. Risk low, a build. | Pass | low | `dd01da6` | Nothing to undo: the build waits in `.tmp/`; the copy is undone by rebuilding from the commit wanted. |
| 2026-09-17 | The thumbnails end above the scroller | `editor/index.html`: `STRIP_DEFAULT` 133, from 128; `STRIP_LINE` 1 and `STRIP_SCROLLER` 12; `layoutStrip` takes a cell's height as the strip's less the line, the 8 px above and the scroller's band, where it was the strip's less 8 px above and below, so in one row the band replaces the 8 px below the cells at every height (at the 96 floor the cells are 120 by 75, from 128 by 80); the rows the strip wraps into are unchanged. `editor/editor-checks.js`: section 37, the 96 tall numbers; one new check that the current cell's bottom edge sits inside the strip's content; the default check at 133 reading the cell's end and the content both at 120. `project-os/Design.md`: the Timeline row. `project-os/Decisions.md`: the entry. Side effect, named: 133 and not the 132 the reply offered, since the timeline's 1 px top line is inside its height and 132 would cut the thumbnails to 111. | Built from a worktree of HEAD plus these edits alone (`.tmp/strip132-src`, another session committing in the shared tree meanwhile) into `.tmp/strip132-target/`. `--editor-check` on `12fc670` plus the edits: every check that ran passed, none not run; the new check reads the cell ending at 83 with the content at 83 at 96 tall, and the default reads 133 tall, remembered 133, thumbnails 112, the cell ending at 120 with the content at 120; the first run, before the band replaced the 8 px below, read thumbnails 104 and the content at 119, which is how the 1 px line was found. Rerun on `dd01da6` plus the edits: 1 failed, 3 not run: the HUD's document count and disk size line, which also failed in the review 4 and tick 2 runs on other code and passed in the run above, and the three live-editor screen copies, another window in front. fmt, clippy with -D warnings and --all-features, the product build with no warning, 57 host and 4 pixel tests, 2 ignored by design, on both. The page errors in the check logs are the checks' own provoked ones, a blocked store and a layer wider than its canvas, as in the last runs. Not run: Rotem's eye on the real window; a strip height remembered on his machine from a drag or a double-click stays until the edge is double-clicked again. Risk low, a style. | Pass | low | `17f86a7` | Set `STRIP_DEFAULT` back to 128 and the cell height to `inner`, the two constants out, the check numbers and the Design row back with them. |
| 2026-09-17 | The release built for the thumbnails ending above the scroller | A release built from commit `5ab0d95` archived into `.tmp/release-5ab0d95/`, into the private target folder `.tmp/release-target/`, so the check runs' rewrite of `host/references/s06/environment.txt` stayed out. Not copied: Rotem's Recon was running from `host/target/release/recon-host.exe` (PID 41736), which holds the file. A hidden job, `.tmp/swap-release-5ab0d95.ps1`, started at 21:10:44, copies the build over it the moment that Recon exits, copies nothing if the build's hash has changed or a Recon from that path is back by then, and writes what it did to `.tmp/swap-release.log`; the other sessions' jobs for `a7dee9e` and `dd01da6` waiting on the same exit will find this hash and copy nothing, and this build carries their changes. No source changed. | `cargo build --release` from that checkout: finished in 26.75 s, no warning. The build: 14,886,400 bytes, SHA-256 `5487BB0D...F48BE0E5`. The job's first log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `5ab0d95`'s, whose edits passed them on the worktrees of `12fc670` and `dd01da6`. Risk low, a build. | Pass, copy pending | low | `5ab0d95` | Rebuild the release from `17f86a7`, whose thumbnails sit 4 px under the scroller. |
| 2026-09-17 | The build row corrected | `project-os/History.md`: the row above said the older jobs would find the new hash; `.tmp/swap-release.log` shows they ran at 21:08:03, before this build, when Rotem quit Recon: `dd01da6` copied in, `a7dee9e` and `c587272` copied nothing. Rotem's Recon (PID 41736) runs on the `dd01da6` build; the `5ab0d95` job waits for it. No source changed. | The log read back. Risk low, a record. | Pass | low | `a87ff90` | Nothing to undo: a record. |
| 2026-09-17 | The app background at #0D0E12 | `editor/index.html`: the `--paper` token is #0D0E12, from #111217; the body and the size band, which sit on the token, follow. `project-os/Design.md`: App background, #0D0E12 from #111217. Rotem's word. Not touched: the buttons' and tabs' fills, the tooltip, the margin colour in the composer. | In `host/`: fmt, clippy and test green (57 and 4 tests pass). `cargo build` was refused at `host/target/debug/recon-host.exe`, held by a debug Recon running since 2026-09-16 22:52 (PID 60804), so the build and the stage0 build went into the private `.tmp/paper-target/` and passed; `--editor-check` from there, twice: 486 pass, 1 fail, 2 not run, then 481 pass, 1 fail, 5 not run; the size band read on the app's background as rgb(13, 14, 18) both times. The fails and the not-runs are the checks that need the check window in front, refused as "another window is over ours" while this session's app was: the three screen copies, Escape on the picker and on Save As, and on the second run the HUD's disk count, after the first run left Save As open in the same store. Not run: Rotem's eye on the release. Risk low, a value. | Pass, the front-window checks blocked | low | `6598f48` | Revert the commit; the two files carry nothing else of it. |
| 2026-09-17 | The release built for the #0D0E12 background | A release built from commit `6a95d49` archived into `.tmp/release-6a95d49/`, into the private target folder `.tmp/release-target/`, so the check runs' rewrite of `host/references/s06/environment.txt` stayed out. Not copied: Rotem's Recon was running from `host/target/release/recon-host.exe` (PID 51476, since 21:11:32), which holds the file. A hidden job, `.tmp/swap-release-6a95d49.ps1`, started at 21:18:25, copies the build over it the moment that Recon exits, copies nothing if the build's hash has changed or a Recon from that path is back by then, and writes what it did to `.tmp/swap-release.log`. No source changed. | `cargo build --release` from that checkout: finished in 29.92 s, no warning. The build: 14,886,400 bytes, SHA-256 `C368796E...3DA0BF6`. The job's first log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `6a95d49`'s, whose edits passed them in `.tmp/paper-target/`. Risk low, a build. | Pass, copy pending | low | `6a95d49` | Rebuild the release from `5ab0d95`, whose background is #111217. |
| 2026-09-17 | The thumbnails 12 px apart on 13 px corners | `editor/index.html`: `STRIP_PAD` 12 and `STRIP_GAP` 12, from 8, so the cells sit 12 px below the line, 12 px from the row's ends and 12 px apart, in the rows the strip wraps into as well; `STRIP_DEFAULT` 137, from 133; `.thumb` and `.thumb img` take a 13 px radius, from 4 and none. `editor/editor-checks.js`: section 37's numbers: 114 by 71 cells at 96 tall, 126 px pitch, two rows entering at 436 where it was 424, the second row's first cell at 12,224, the default 137 with the cell and the content ending at 124; the wheel check's rows height 436. `project-os/Design.md`: the Timeline rows. Side effect, named: the rows the strip wraps into take the 12 px too, since one constant places both. The rows here were meant for `f2a314b` and missed it: the insert failed on a marker another session's row had changed, and the commit went in without them. | Built from a worktree of HEAD `c553c14` plus these edits alone (`.tmp/strip132-src`) into `.tmp/strip132-target/`: `--editor-check`, every check that ran passed, none not run; the first run failed the wheel check, whose rows case still set 424, one row now, and passed once it set 436. fmt, clippy with -D warnings and --all-features, the product build with no warning, 57 host and 4 pixel tests, 2 ignored by design. Not run: Rotem's eye on the real window; the corners are read in the CSS, not measured. Risk low, a style. | Pass | low | `c553c14` | Set the two constants and the default back to 8 and 133, the radius to 4 with the img's rule out, and the check numbers with them. |
| 2026-09-17 | The release built for the 12 px air and the 13 px corners | A release built from commit `f2a314b` archived into `.tmp/release-f2a314b/`, into the private target folder `.tmp/release-target/`, so the check runs' rewrite of `host/references/s06/environment.txt` stayed out. Not copied: Rotem's Recon was running from `host/target/release/recon-host.exe` (PID 19152), which holds the file. A hidden job, `.tmp/swap-release-f2a314b.ps1`, started at 21:49:45, copies the build over it the moment that Recon exits, copies nothing if the build's hash has changed or a Recon from that path is back by then, and writes what it did to `.tmp/swap-release.log`. No source changed. | `cargo build --release` from that checkout: finished in 22 s, no warning. The build: 14,886,400 bytes, SHA-256 `E10D1445...B75EC6AE`. The job's first log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `f2a314b`'s, whose edits passed them on the worktree of `c553c14`. Risk low, a build. | Pass, copy pending | low | `f2a314b` | Rebuild the release from `c553c14`, whose thumbnails are 8 px apart on 4 px corners. |
| 2026-09-17 | The thumbnails' corners 8 px | `editor/index.html`: `.thumb` and `.thumb img` border-radius 8px, from 13px. `project-os/Design.md`: the corners row. Staged from HEAD plus these two lines, since another session's note edits sit uncommitted in the same file. | On a worktree of `697f350` plus these edits alone (`.tmp/strip8-src`) into `.tmp/strip132-target/`: fmt, clippy with -D warnings and --all-features, the product build with no warning, 57 host and 4 pixel tests, 2 ignored by design. `--editor-check` there: 1 failed, 5 not run: the Copy button's clipboard check, which read 300x200 where another session's check run and Rotem's Recon shared the clipboard and the screen, and the four screen and dialog checks with another window in front; two reruns exited at startup as a second instance, Rotem's Recon (PID 13680) running from the release since 22:02:18, so the run did not repeat. The radius is read in the CSS, not measured. Not run: Rotem's eye. Risk low, a value. | Pass, one check unrepeated | low | `697f350` | Set the two radii back to 13px. |
| 2026-09-17 | Design.md's swept section taken back out | `project-os/Design.md`: the committed copy is `697f350`'s plus the corners row alone, staged from a blob built that way; the working tree keeps the other session's Callout section untouched. `a12fc2d` had staged the file whole after its diff was printed and not read. | `git diff --cached` on this commit shows the Callout section leaving and nothing else; `git diff` after it shows the section back as an uncommitted edit. Risk low, a record. | Pass | low | `a12fc2d` | Nothing: the other session commits its section when it is done. |
| 2026-09-17 | The release built for the 8 px corners | A release built from commit `0e35d26` archived into `.tmp/release-0e35d26/`, into the private target folder `.tmp/release-target/`, so the other session's uncommitted note edits and the check runs' rewrite of `host/references/s06/environment.txt` stayed out. Not copied: Rotem's Recon was running from `host/target/release/recon-host.exe` (PID 13680, since 22:02:18), which holds the file. A hidden job, `.tmp/swap-release-0e35d26.ps1`, started at 22:04:47, copies the build over it the moment that Recon exits, copies nothing if the build's hash has changed or a Recon from that path is back by then, and writes what it did to `.tmp/swap-release.log`. No source changed. | `cargo build --release` from that checkout, no warning. The build: 14,886,400 bytes, SHA-256 `C5065CA7...AC6F1066`. The job's first log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `a12fc2d`'s page, whose edits passed them on the worktree of `697f350`. Risk low, a build. | Pass, copy pending | low | `0e35d26` | Rebuild the release from `697f350`, whose corners are 13 px. |
| 2026-09-17 | The callout to Rotem's spec | `editor/index.html`: `.callout` padding 1.6em, radius .75em, background #2D41D7, a transparent 1 px border (the selected ring stays #7aa7ff), `box-shadow: 0 8px 16px 6px rgba(0,0,0,.25)`; `.callout .n` hidden; `.callout .t` `min-height: 1.8em`, its focus background and outline gone (the `body.plain .callout .t:focus` line is inert now, left as it was); `.callout.text .t` keeps no floor, so the text note is unchanged; `heightOf` and `createCallout` estimate one line as 5em plus the border (102 at 20); `drawArrows` draws the line at 4 px and an open two-stroke head at the anchor, 16 by 7, `fill="none"`; the Enter branch commits on a bare Enter and inserts the newline on Ctrl+Enter or Shift+Enter, Ctrl+Shift+Enter left to Rogers. `editor/editor-checks.js`: section 9 types Ctrl+Enter for the break and proves a bare Enter keeps the text and ends the editing, the two-line check measures against the line height over the 36 px floor; section 21's long note shortened so it fits the scene dragged back up; section 42's first note anchored at 20,40 so the 102 px bubble stays inside, its pixel checks on the #2D41D7 ground, the margin note anchored at 60,150 where no place fits. `host/references/s06/ref-*.png`: the six S0.6 references rewritten from the new look. `project-os/Plan.md`: §3.5 step 5 and the numbering note, the Ctrl+Enter and Enter key rows. `project-os/Design.md`: the Callout component. Not touched: the line's #7aa7ff, the anchor dot, the text note, the ruler's bubble, the HUD's key hint. | In `host/` with the private target `.tmp/crop-target`: fmt, clippy, build with stage0-checks, test (57, 4 and 0 pass) green; `--editor-check` first run 9 red: the six references differing by 8.7 to 28.8% (the look), a two-line check reading 54 against the 36 floor, the long note no longer fitting the scene once dragged back, the margin note finding a place; the six new outputs looked at (open heads, blue bubbles, no numbers, the long and edge cases cut at the picture's edge as before) and put in place, the three checks corrected; second run 483 pass, 0 FAIL, 5 NOT RUN (the standing foreground ones: another window over ours); third run, on the final tree, 490 pass, 0 NOT RUN and 1 FAIL, the HUD's store size against `editor_storage` (3 documents, 263036 bytes), which passed in the second run on the same code and has failed the same way in four earlier sessions' runs, so it is pre-existing and flaky, reported and not touched; the six references identical, section 9's Enter checks, section 21 and section 42 green. In the in-app browser on a stub harness of the page (`.tmp/callout-look-harness.html`): padding 32px, radius 15px, rgb(45,65,215), the shadow as set, `.n` none, the text box 36 tall with no focus outline or background, two lines and two polylines at stroke 4, and Ctrl+Enter then Shift+Enter then Enter giving "one\ntwo\nthree" with the editing ended and the note kept; console clean; the screenshot showed the open heads and the blue bubbles. Review (medium): one finding introduced and fixed, the 36 px floor leaking into the text note through the shared `.t` class; a stale comment on the old ems removed; impact shared with the text note and the thumbnail layer, both checked. A real note typed by hand, Enter and Ctrl+Enter under Rotem's fingers, and the look on his display are his hand. | Pass | medium | `0ce3772` | Revert the commit; the six references come back with it. |
| 2026-09-17 | The release built for Rotem's callout | A release built from commit `49392fc` archived into `.tmp/release-49392fc/`, into the private target folder `.tmp/release-target/`, so the check runs' rewrite of `host/references/s06/environment.txt` stayed out. Not copied: Rotem's Recon was running from `host/target/release/recon-host.exe` (PID 39800, since 22:08:04, on the other session's `fast-09090C` build), which holds the file. A hidden job, `.tmp/swap-release-49392fc.ps1`, started at 22:08:57, copies the build over it the moment that Recon exits, checks the build's hash first (`4C883624...307EA990`) and reads the copy back; the other session's job for `0e35d26` will see the hash changed and copy nothing, that commit being an ancestor of this one. No source changed. | `cargo build --release` finished in 24 s; the hash read; the job's first log line in `.tmp/swap-release.log`. Risk low, a build. | Pass | low | `49392fc` | Nothing to undo: the copy is the job's, and reverting `49392fc` and rebuilding puts the previous release back. |
| 2026-09-17 | The callout's release row corrected | `.tmp/swap-release.log`: at 22:10:31 Rotem's Recon exited and the `fast-0A0A0B` job found the shared `.tmp/release-target/` holding hash `18FC...5F76`, not its own, and copied nothing; the `49392fc` job (expected `4C88...A990`) and the `fast-0A0A0B-b` job (expected `18FC...5F76`) were between their two-second polls and missed the one-second gap before his Recon (PID 46480) came back at 22:10:32. The file in place is still the 22:06 `fast-09090C` build (`68D0...C0A3`), with the old callout, which is what Rotem saw. `.tmp/strip-fast-src/editor/index.html`, the other session's build source at 22:09:48, carries `#2D41D7` and `#0A0A0B`, so its waiting build carries this change; the stale `49392fc` job was stopped, its build in the shared folder being gone. No source changed. | The log, the hashes of the file in place and of the folder's build, the job list and the source read; risk low, a record. | Pass | low | `ac804cb` | Nothing to undo: a record. |
| 2026-09-17 | The stale job row corrected | `taskkill` on the `49392fc` job (PID 60284) was refused; the job stays, expecting a hash the shared folder no longer holds, so it copies nothing and logs one line at the next quit. No source changed. | The job list read back after the kill. Risk low, a record. | Pass | low | `3aac8f2` | Nothing to undo: a record. |
| 2026-09-17 | The window buttons 48 wide, their icons 12 | `editor/index.html`: `#winbtns button` width 48px, from 46; `#winbtns svg` 12px, from 10; the comment's size line with them. Nothing else in the page changed. The first round of this task also took `#bar` to 48 px and followed it into `#stage`'s top inset, `#controls`' top and heights and the size band's ceiling; Rotem then said the bar stays as it was, so all of that was put back to 32 and `100vh - 80px` before delivery, and this row replaces the one written for it, which recorded a state that never shipped. `editor/editor-checks.js`: the window-button check reads 48 wide, the bar's own height as before; the eight numbers the 48 px bar had needed are back at 32. Not touched: `project-os/Design.md`, which has no window-button row. | In `host/` with the private target `.tmp/bar48-target/`, Rotem's Recon holding the usual one: fmt, clippy with -D warnings and --all-features, the product build, the checks build, 57 host and 4 pixel tests, 2 ignored by design, all clean. `--editor-check` on the rebuilt binary: 490 pass, 0 NOT RUN, 1 FAIL, the HUD's document count and disk size, which failed and then passed on unchanged code earlier in this same task and has failed the same way in four earlier sessions, so it is the known flake, reported and not touched. The button check reads the three at 1136-1184, 1184-1232 and 1232-1280, 32 tall each, against the window's 1280. The 48 px bar's own run, before the revert, read 490 pass and one real failure, the sidebar's eleven buttons short of 16 px in a 752 px window; with the bar back at 32 that check passes again. Not run: Rotem's eye on the release. Risk low, two values. | Pass, 1 known flake | low | `35587b9` | Set the width back to 46px and the icon to 10px, and the check's 48 with them. |
| 2026-09-17 | The release built for the 48 px window buttons | A release built from commit `459cab2` archived into `.tmp/release-459cab2/`, into the private target folder `.tmp/release-target/`, so another session's uncommitted background value and the check runs' rewrite of `host/references/s06/environment.txt` stayed out. Copied over `host/target/release/recon-host.exe` at once at 23:14:28: no Recon was running from that path, only a debug one from `host/target/debug/` (PID 60804, since 2026-09-16), so no waiting job was needed. No source changed. | `cargo build --release` from that checkout: finished in 25.38 s, no warning. The build: 14,886,912 bytes, SHA-256 `AAF0D5E7...EBA2BB90`; the copy in place reads back the same hash, True. The archive's page was read before the build and carries the 48 px width. The project checks were not rerun: the code is `459cab2`'s, whose change passed them in `.tmp/bar48-target/`. Not run: Rotem's eye on the running app. Risk low, a build. | Pass | low | `459cab2` | Rebuild the release from `35587b9`, whose buttons are 46 px wide with 10 px icons, and copy it in. |
| 2026-09-17 | The window buttons and the bar at 48 tall | `editor/index.html`: `#winbtns button` height 48px, from `100%`; `#bar` 48px, from 32; and the four places measured from the bar, `#stage`'s top inset, `#controls`' top and its two heights, and the size band's ceiling in `#image-size` and `#tabbar` (`100vh - 96px`, from `- 80px`, the bar plus the band); three comment lines with them. The width stays 48 and the icon 12, from the commit before. `editor/editor-checks.js`: the bar's height and the stage's top at 48 in four checks, the button check at 48 by 48, the six stage heights from `- 32 - 48` to `- 48 - 48`, the sidebar's top at 48, the size band's ceiling at 64 (16 under the new bar), the zero-area stage set from `innerHeight - 49`. `CLAUDE.md` rule 1: a new paragraph, a word that can name two things on screen is asked about, never assigned, and an answer that would undo the ask it answers is asked about too. `project-os/Mistakes.md`: the 2026-09-16 row moved to Promoted with this second occurrence. Side effects, named: the bar's height and everything measured from it were not in the ask and follow the button, since a 48 px button cannot sit in a 32 px bar. Not touched: `project-os/Design.md`, which has no window-button row; the sidebar's 8 px gaps. | In `host/` with the private target `.tmp/bar48-target/`, Rotem's Recon holding the usual one: fmt, clippy with -D warnings and --all-features, the product build, the checks build, 57 host and 4 pixel tests, 2 ignored by design, all clean. `--editor-check` on the rebuilt binary: 490 pass, 0 NOT RUN, 1 FAIL. The bar reads 48 tall with the stage from 48, and the three buttons 1136-1184, 1184-1232 and 1232-1280, 48 tall each. The failure is the sidebar's eleven buttons: in the check's 752 px window they need 608 px and have 592 between the taller bar and the timeline, so they sit at the top instead of centred; reported, not fixed, the remedy being Rotem's call. The HUD's store-size check, which failed in earlier runs today, passed here. Not run: Rotem's eye on the release. Risk low, a value and what it moves. | Pass, 1 sidebar check failing | low | `0d45737` | Set the button's height back to `100%`, `#bar` to 32, the four dependants back to 32 and `- 80px`, and the eight check numbers with them. |
| 2026-09-17 | The release built for the 48 px tall buttons | A release built from commit `7ef8608` archived into `.tmp/release-7ef8608/`, into the private target folder `.tmp/release-target/`, so another session's uncommitted background value and the check runs' rewrite of `host/references/s06/environment.txt` stayed out. Copied over `host/target/release/recon-host.exe` at 23:44:47: Rotem's Recon from that path had exited, only the 2026-09-16 debug one was running, so no waiting job was needed. No source changed. | `cargo build --release` from that checkout: finished in 25.04 s, no warning. The build: 14,886,912 bytes, SHA-256 `BC610FEC...16BFFDDE`; the copy in place reads back the same hash, True. The archive's page was read before the build: `#bar` and `#winbtns button` both 48 px. The project checks were not rerun: the code is `7ef8608`'s, whose change passed them in `.tmp/bar48-target/`. Not run: Rotem's eye on the running app. Risk low, a build. | Pass | low | `7ef8608` | Rebuild the release from `0d45737`, whose bar is 32 px tall, and copy it in. |
| 2026-09-17 | The window icons back to 10 px | `editor/index.html`: `#winbtns svg` 10px, from 12. One line. Read as the window buttons' own icons, the ones the last three rounds were about, not the sidebar's 32 px ones, which are untouched. No check measures this icon, so no check changed. | In `host/` with the private target `.tmp/bar48-target/`: fmt, clippy with -D warnings and --all-features, the product build, the checks build, 57 host and 4 pixel tests, 2 ignored by design, all clean. `--editor-check` on the rebuilt binary: 482 pass, 1 FAIL, 5 NOT RUN. The failure is the sidebar's eleven buttons short of 16 px under the 48 px bar, carried over from the commit before and still Rotem's to settle. The five not run are the three screen copies and two Escape presses that need the check window in front, with his own app on screen. Not run: Rotem's eye on the release. Risk low, one value. | Pass, 1 carried failure and 5 blocked by the front window | low | `8da6ec9` | Set `#winbtns svg` back to 12px. |
| 2026-09-17 | The release built for the 10 px window icons | A release built from commit `488e8fc` archived into `.tmp/release-488e8fc/`, into the private target folder `.tmp/release-target/`, so another session's uncommitted background value and the check runs' rewrite of `host/references/s06/environment.txt` stayed out. Not copied: Rotem's Recon was running from `host/target/release/recon-host.exe` (PID 35860, since 23:45:26), which holds the file. A hidden job, `.tmp/swap-release-488e8fc.ps1`, started at 23:49:23, copies the build over it the moment that Recon exits, checks the build's hash first and reads the copy back, and writes what it did to `.tmp/swap-release.log`. Its first start, at 23:48:51, died unlogged on a PowerShell parse error, `$want:` inside a string read as a scope prefix; written as `${want}` it ran. No source changed. | `cargo build --release` from that checkout: finished in 30.98 s, no warning. The build: 14,886,912 bytes, SHA-256 `C773E198...0515B6E0`. The archive's page was read before the build: `#winbtns svg` 10 px. The job's first log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `488e8fc`'s, whose change passed them in `.tmp/bar48-target/`. Risk low, a build. | Pass, copy pending | low | `488e8fc` | Rebuild the release from `8da6ec9`, whose window icons are 12 px, and copy it in. |
| 2026-09-18 | The capture drag's size bubble | `host/src/overlay.rs`: seven constants for the bubble (14 px text, 8 px corners, 8 and 4 px of padding, an 8 px gap, the app background #0D0E12 as the fill, #F2F2F2 as the text); each surface makes a Segoe UI face at its display's scale and frees it at teardown; `size_bubble` lays the bubble out beside the pointer while a drag on that window has moved, `place_bubble` putting it on the pointer's other side at a display's edge; `draw_bubble` builds the rounded fill pixel by pixel with the corners' coverage, draws the text into it, puts back the alpha the text call cleared and blends it on in one call; a move invalidates the bubble's old and new places, and the paint draws it as its fourth step wherever the dirty region touches it. `host/src/selftest.rs`: section C copies the screen beside the pointer mid-drag to `s02-drag-size.png` beside the executable and reads the bubble's ground and its text's light pixels. `project-os/Design.md`: the Capture size bubble component. `project-os/QA.md`: the overlay row. `project-os/Plan.md` §3.2: one sentence. `project-os/Decisions.md`: the entry. Not touched: the editor page, the ruler's own bubble, the hover's lit window (no size shown there; not asked). | In `host/` with the private target `.tmp/bar48-target/`, `host/target/debug/` being held by the 2026-09-16 debug Recon: fmt, clippy with -D warnings and --all-features, the product build, the checks build, 61 host tests (4 new: the bubble's place and its flips, the corners' coverage, the scaling) and 4 pixel tests, 2 ignored by design, all clean. `--selftest` from there: every check passed; C read the bubble's ground 12,19 from the pointer as (13, 14, 18) and 97 light pixels of text, and got the dragged rectangle back exactly; D, F and H as before. The file looked at and measured: "160x100" in a dark rounded bubble at 8,8 from the pointer, 70 by 27 (54 px of text plus 16, a 19 px cell plus 8), the corner's edge pixels grading over 8 px, the digits' ink 10 px in and centred in the height. Review (medium): the diff and its reach read; the overlay module is the only reader of a surface and the self test the only other caller of the overlay, so the impact is local; no finding introduced; pre-existing, flagged: the app background now has a second home, this constant beside the page's `--paper`, the class Backlog F68 names for the margin colour. Not run: Rotem's hand on a real hotkey drag; a display at another scale. | Pass | medium | `21f5769` | Revert the commit; the two source files and the five docs carry nothing else of it. |
| 2026-09-18 | The release built for the size bubble | A release built from commit `c3727cd` archived into `.tmp/release-c3727cd/`, into the private target folder `.tmp/release-target/`, so another session's uncommitted background value in the page and the check runs' rewrite of `host/references/s06/environment.txt` stayed out. Not copied: Rotem's Recon was running from `host/target/release/recon-host.exe` (PID 51500, since 2026-09-17 23:54:03), which holds the file. A hidden job, `.tmp/swap-release-c3727cd.ps1` (PID 40244), started at 00:12:01, copies the build over it the moment that Recon exits, checks the build's hash first and reads the copy back, and writes what it did to `.tmp/swap-release.log`. No source changed. | `cargo build --release` from that checkout: finished in 33.52 s, no warning. The build: 14,879,744 bytes, SHA-256 `26D0071C...A675410`. The archive's overlay was read before the build: the fill constant #0D0E12. The job's first log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `c3727cd`'s, which passed them in `.tmp/bar48-target/`. Risk low, a build. | Pass, copy pending | low | `c3727cd` | Rebuild the release from `21f5769`, whose overlay has no size bubble, and copy it in. |
| 2026-09-18 | The capture overlay's magnifier | `host/src/overlay.rs`: six constants for the magnifier (112 px across, 8 px to a source pixel, 4 px around the circle, an 8 px gap from the pointer, a 2 px ring, the ring's white) beside the size label's, whose gap constant went; the state keeps where the pointer last was and on which surface, set at the first look and at every move; `selection_size` is the dragged area's once the drag has moved, a line's included, otherwise the lit window's; `magnifier` lays the panel out beside the pointer, the circle inset in it and the size text centred under the circle, `place_panel` putting it below the pointer with its right edge left of it and on the other side at a display's edge, `cell_count` an odd number of cells covering the circle; `magnified` stretches the display's frozen bitmap with no smoothing into a bitmap of cells, cut to the display, darkens the last pixel of every cell by half and paints the frame's blue through the centre cell; `draw_panel` builds the panel pixel by pixel, the rounded fill, the ring and the cells blended by their coverage, the text under, the alpha put back, one blend onto the window; every move, hovering or dragging, invalidates the panel's old and new places, the drag's own move handling moved unchanged into `drag_move`, re-indented, and the first look invalidates it too; the paint draws it as its fourth step. The size bubble's own functions went. `host/src/selftest.rs`: `pixels_around` reads the nine frozen pixels around a point; `magnifier_seen` copies the screen below and left of the pointer to `s02-magnifier-<name>.png` and reads the ground, the ring, the nine centre cells against the frozen pixels and the text row's light pixels; C looks mid-drag and H while hovering the stand-in's part. `project-os/Design.md`: the Magnifier component, the size bubble's section moved under it. `project-os/QA.md`: the overlay row. `project-os/Plan.md` §3.2: the sentence. `project-os/Decisions.md`: the entry. Not built, from the picture: the full-screen guide lines through the pointer. Not touched: the editor page, the ruler's own bubble. | In `host/` with the private target `.tmp/bar48-target/`: fmt, clippy with -D warnings and --all-features (one round: a chunked fill loop clippy refused, written as a stepped loop), the product build, the checks build, 64 host tests (3 new: the panel's place and its flips, the cell count) and 4 pixel tests, 2 ignored by design, all clean. `--selftest` from there, on the 3840x2160 display at 225% this time, where the bubble's run was on the 5120x1440 one at 100%: every check passed; C read the panel's ground and ring at the scaled probes, the nine centre cells equal to the frozen pixels around the mid-drag point, 618 light pixels of text, and got the dragged rectangle back exactly; H the same while hovering the part with 642 light pixels, then picked the window, the part and the drag as before; C's overlay-in-output comparison was inconclusive, the area under it live, as that check says of itself. The two files looked at and measured: the panel 270 by 324 (120 by 144 at 225%) at 8 and 8 scaled from the pointer's left and bottom, the circle ringed with an 18 px grid, both blue lines through its centre (242 blue pixels down the column and along the row), "160x100" and "200x100" under it, a 4x crop of the centre read for the grid and the lines. Review (medium): the diff and its reach read; the overlay module is the only reader of a surface, the self test the only other caller, the hotkey path unchanged, so the impact is local; no finding introduced; pre-existing, flagged: every hover move now repaints the panel, about 17 thousand pixels at 100% and 87 thousand at 225%, in software; the app background's second home stands. Not run: Rotem's hand on a real hotkey; a second display. | Pass | medium | `241f102` | Revert the commit; the two source files and the five docs carry nothing else of it. |
| 2026-09-18 | The release built for the magnifier | A release built from commit `e658def` archived into `.tmp/release-e658def/`, into the private target folder `.tmp/release-target/`, so another session's uncommitted background value in the page and the check runs' rewrite of `host/references/s06/environment.txt` stayed out. Not copied: Rotem's Recon was running from `host/target/release/recon-host.exe` (PID 55488, since 01:48:37, on the size bubble's build, which the `c3727cd` job copied in at 01:36:10 when he quit the one before, the copy reading back identical). A hidden job, `.tmp/swap-release-e658def.ps1` (PID 40172), started at 01:57:52, copies the build over it the moment that Recon exits, checks the build's hash first and reads the copy back, and writes what it did to `.tmp/swap-release.log`. No source changed. | `cargo build --release` from that checkout: finished in 30.42 s, no warning. The build: 14,890,496 bytes, SHA-256 `A26A4D10...70EFCFDC`. The archive's overlay was read before the build: the 112 px constant. The job's first log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `e658def`'s, which passed them in `.tmp/bar48-target/`. Risk low, a build. | Pass, copy pending | low | `e658def` | Rebuild the release from `c3727cd`, whose overlay has the size bubble beside the pointer and no magnifier, and copy it in. |
| 2026-09-18 | The lit window's glide | `host/src/overlay.rs`: one constant, 144 ms; a `Glide` beside the hover, where the area set out from, where it is going, when, and where it is as painted, and an `animate` flag read once from Windows' animation setting (`windows_animates`); `update_hover` starts a glide from the area as painted when the next window is on the same display, keeps one already heading there, and jumps as before at the first light, a loss, another display or animations off; `advance_glide` on the frame's tick moves the area by the clock, eased, and repaints the union of where it was and where it is; `client_selection` paints the glide's rectangle while one lasts; `take_hover` ends it when a drag takes over; `ease_out` and `glide_rect`, with two tests. `host/src/selftest.rs`: H looks at the frame's left band in a strip between the window's edge and the part's, mid-glide and at 300 ms (`glide_strip`, `glide_seen`, `band_left`). `project-os/Design.md`: the Capture window glide component. `project-os/QA.md`: the overlay row. `project-os/Plan.md` §3.2: one sentence. `project-os/Decisions.md`: the entry. Not touched: the drag's own rectangle, the marching frame's look, the magnifier. | In `.tmp/glide-head/`, an archive of `e658def`, with the private target `.tmp/glide-target/`: fmt --check, clippy with -D warnings and --all-features, the product build, 64 host tests and 4 pixel tests, 2 ignored by design, and the checks build, all clean. `--selftest` from there, the stand-in on the 5120x1440 display at 100%: every check passed; H read the frame's left band 37 px in from the window's edge and 5 px out from the part's 40 ms after the move, and on the window's edge at 317 ms, then picked the window, the part and the drag as before; C's overlay-in-output comparison was inconclusive, the area under it live, as that check says of itself. The 40 ms sample sits early on the curve because the screen's copy is a tick and a composition behind the clock (inferred). Review (medium): the diff and its reach read; `client_selection` is the one shared reader, used by the paint, the tick and the magnifier's size text, and that text reads the hover's own rectangle, so the size never shows an in-between one; the drag's takeover, the click, Escape and a change of display each end or bypass the glide; impact local to the overlay; no finding introduced; nothing pre-existing flagged. Not run: Rotem's hand with real windows; a display at 225%; Windows' animation effects off, which the self test would report as a jump. | Pass | medium | `241f102` for the code, which landed in `e658def`; `ad52623` for the docs | Revert the glide hunks of `e658def`, which shares its two files with the magnifier, and this docs commit. |
| 2026-09-18 | The glide at 256 ms, ease-in | `host/src/overlay.rs`: `GLIDE_MS` 256, from 144; `ease_out` is `ease_in`, cubic, and its test with it. `host/src/selftest.rs`: `glide_seen` looks mid-glide 200 ms after the move, from 40, inconclusive past 230, from 90, and at the arrival at 450 ms, from 300. `project-os/Design.md`: the Capture window glide component's Motion row, the values Rotem's now. `project-os/QA.md`: the overlay row's timings. `project-os/Decisions.md`: a paragraph on the glide entry. Not touched: the glide's mechanism, the magnifier. | In `.tmp/glide-head2/`, an archive of `7cd45a4` with the two source files copied in, with the private target `.tmp/glide-target/`: fmt --check, with fmt changing nothing, clippy with -D warnings and --all-features, the product build, 64 host tests and 4 pixel tests, 2 ignored by design, and the checks build, all clean. `--selftest` from there, the stand-in on the 5120x1440 display at 100%: every check passed; H read the frame's left band 28 px in from the window's edge and 13 px out from the part's 200 ms after the move, and on the window's edge at 453 ms, then picked the window, the part and the drag as before; C's overlay-in-output comparison was inconclusive, the area under it live, as that check says of itself. Not run: Rotem's hand on real windows, which is what the values are for. Risk low, two values the owner named. | Pass | low | `7cd45a4` | Revert the commit; the two source files and the four docs carry nothing else of it. |
| 2026-09-18 | The release built for the 256 ms glide | A release built from commit `9c51d08` archived into `.tmp/release-9c51d08/`, into the private target folder `.tmp/release-target/`, so another session's uncommitted background value in the page and the check runs' rewrite of `host/references/s06/environment.txt` stayed out. Not copied: Rotem's Recon was running from `host/target/release/recon-host.exe` (PID 26288, since 05:31:06, on the magnifier's build, which the `e658def` job copied in at 05:30:41 when he quit the one before, the copy reading back identical). A hidden job, `.tmp/swap-release-9c51d08.ps1` (PID 18020), started at 06:22:45, copies the build over it the moment that Recon exits, checks the build's hash first and reads the copy back, and writes what it did to `.tmp/swap-release.log`. No source changed. | `cargo build --release` from that checkout: finished in 29.45 s, no warning. The build: 14,890,496 bytes, SHA-256 `8C337CB0...03BF949B`. The archive's overlay was read before the build: the 256 ms constant. The job's first log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `9c51d08`'s, which passed them in `.tmp/glide-target/`. Risk low, a build. | Pass, copy pending | low | `9c51d08` | Rebuild the release from `7cd45a4`, whose glide is 144 ms with an ease-out, and copy it in. |
| 2026-09-18 | The glide at 164 ms, ease-out | `host/src/overlay.rs`: `GLIDE_MS` 164, from 256; `ease_in` is `ease_out` again, cubic, and its test with it. `host/src/selftest.rs`: `glide_seen` looks mid-glide 80 ms after the move, from 200, inconclusive past 120, from 230, and at the arrival at 300 ms, from 450. `project-os/Design.md`: the Capture window glide component's Motion row. `project-os/QA.md`: the overlay row's timings. `project-os/Decisions.md`: a line on the glide entry. Not touched: the glide's mechanism, the magnifier, whose move to the pointer's right another session had in the same files uncommitted, left out of this commit by building its copies from HEAD plus these hunks. | In `.tmp/glide-head3/`, an archive of `f94fc57` with the two source files copied in, with the private target `.tmp/glide-target/`: fmt --check, with fmt changing nothing, clippy with -D warnings and --all-features, the product build, 64 host tests and 4 pixel tests, 2 ignored by design, and the checks build, all clean. `--selftest` from there, three runs: the first collided with the other session's own run of it and was stopped; the second, 06:44 to 06:48 with Rotem at the machine, passed every check but C, whose drag came back as another rectangle, the pointer moved mid-drag by a hand or another run (inferred), not the overlay, which the two clean runs earlier tonight and D, F, G and H in this one bear out. H read the frame's left band 8 px in from the window's edge and 33 px out from the part's 80 ms after the move, and on the window's edge at 306 ms, then picked the window, the part and the drag as before. Not run: a quiet rerun of C; Rotem's hand on real windows, which is what the values are for. Risk low, two values the owner named. | Pass, C to rerun quiet | low | `f94fc57` | Revert the commit; the two source files and the four docs carry nothing else of it. |
| 2026-09-18 | The release built for the 164 ms glide | A release built from commit `b9d0a9b` archived into `.tmp/release-b9d0a9b/`, into the private target folder `.tmp/release-target/`, so another session's uncommitted magnifier move and background value, and the check runs' rewrite of `host/references/s06/environment.txt`, stayed out. Not copied: Rotem's Recon was running from `host/target/release/recon-host.exe` (PID 36132, since 06:49:11, on the 256 ms build, which the `9c51d08` job copied in at 06:36:24 when he quit the one before, the copy reading back identical). A hidden job, `.tmp/swap-release-b9d0a9b.ps1` (PID 29876), started at 06:51:12, copies the build over it the moment that Recon exits, checks the build's hash first and reads the copy back, and writes what it did to `.tmp/swap-release.log`. No source changed. | `cargo build --release` from that checkout: finished in 25.89 s, no warning. The build: 14,890,496 bytes, SHA-256 `7448D88F...266D33F3`. The archive's overlay was read before the build: the 164 ms constant. The job's first log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `b9d0a9b`'s, which passed them in `.tmp/glide-target/`. Risk low, a build. | Pass, copy pending | low | `b9d0a9b` | Rebuild the release from `9c51d08`, whose glide is 256 ms with an ease-in, and copy it in. |
| 2026-09-18 | The magnifier on the pointer's right | `host/src/overlay.rs`: `place_panel` puts the panel's left edge the gap right of the pointer and flips it to the pointer's left where that would leave the display, from the mirror of that; the constants' comment; the three placement tests mirrored, the narrow-display case now clamped at 0. `host/src/selftest.rs`: `magnifier_seen` copies the screen right of the pointer and probes the panel from 8 px right of it, and prints the pointer's real place at the copy when it differs from where the script put it. `project-os/Design.md`: the Side row, Rotem's, and the provisional sentence. `project-os/QA.md`: the overlay row. Staged hunk by hunk from a tree holding another session's uncommitted glide tuning (192 ms, the 140 ms look, its Design and Decisions lines), which stayed out. | In `host/` with the private target `.tmp/bar48-target/`: fmt, clippy with -D warnings and --all-features, the product build, the checks build, 64 host and 4 pixel tests, 2 ignored by design, all clean. `--selftest` four times, on the 5120x1440 display at 100%, while Rotem worked at the machine: the first three runs each had a look or a drag disturbed by the mouse moving mid-test (the pointer put at 300,230 found at 304,205; put at 200,193 found at 423,159; a drag returned 3 px off), the new pointer line in the log naming it each time; the fourth run passed both looks, the panel's ground, ring and nine centre cells read below and right of the pointer while dragging (97 light pixels of text) and while hovering (95), the drag exact, the pick and the glide as before, with one failure, F's foreground return, where the overlay stayed up 47 s until a click picked the stand-in, real input in the test's way; C's overlay-in-output comparison inconclusive, the area live. The two pictures looked at: the circle right of and below the pointer, "160x100" and "200x100" under it. Not run: Rotem's hand on a real hotkey. Risk low, a placement value. | Pass, F disturbed | low | `c38dfee` | Mirror `place_panel` back and the self test's copy region with it. |
| 2026-09-18 | Escape during capture, already built | `project-os/Plan.md`: the keyboard table's `Esc` during capture row says built at S0.2. No code changed: `host/src/overlay.rs` ends the selection as cancelled on Escape from any of its windows, a drag under way dropped, and `host/src/main.rs` logs "cancelled: nothing captured, nothing replaced". | Read the key handler and the hotkey path. The self test's section D, a synthesized press, a move, Escape, then the release, through the real overlay: "escape mid-drag cancelled, and nothing was captured" in six runs of the working tree's overlay today, other sessions' runs, `.tmp/glide2-selftest.log` at 06:20 through `.tmp/glide4-selftest.log` at 07:03. The real hotkey: `.tmp/s08-hotkey.log`, a cancel 4.8 s after the overlay showed, on 2026-09-13. Not run: my own `--selftest` from the private target `.tmp/esc-target/`, built clean in 59 s; other sessions' self tests held the screen back to back, and Rotem declined the run when its turn came. The plan row read back. A real hotkey, a drag and Escape by hand is Rotem's hand. | Pass on the runs named, own run not made | low | `47b0369` | Revert the commit; docs only. |
| 2026-09-18 | The release built for the magnifier on the right | A release built from commit `47b0369` archived into `.tmp/release-47b0369/`, into the private target folder `.tmp/release-target/`, so another session's uncommitted glide tuning, the page's background value and the check runs' rewrite of `host/references/s06/environment.txt` stayed out; the archive's overlay reads `GLIDE_MS` 164, the committed value. Not copied: Rotem's Recon was running from `host/target/release/recon-host.exe` (PID 47444, since 06:52:10, on the `b9d0a9b` build), which holds the file. A hidden job, `.tmp/swap-release-47b0369.ps1` (PID 41788), started at 15:44:34, copies the build over it the moment that Recon exits, checks the build's hash first and reads the copy back, and writes what it did to `.tmp/swap-release.log`. No source changed. | `cargo build --release` from that checkout: finished in 29.43 s, no warning. The build: 14,890,496 bytes, SHA-256 `63FC2FFA...01CF5D56`. The archive's `place_panel` read before the build: the panel's left edge at the pointer plus the gap. The job's first log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `47b0369`'s, which passed them in `.tmp/bar48-target/`. Risk low, a build. | Pass, copy pending | low | `47b0369` | Rebuild the release from `c38dfee`, whose magnifier sits on the pointer's left, and copy it in. |
| 2026-09-18 | The window buttons and the bar at 40 tall | `editor/index.html`: `#winbtns button` height 40px, from 48; `#bar` 40px, from 48; the four places measured from the bar, `#stage`'s top inset, `#controls`' top and its two heights, and the size band's ceiling in `#image-size` and `#tabbar` (`100vh - 88px`, from `- 96px`, the bar plus the band); three comment lines with them. Width 48 and icon 10 unchanged. `editor/editor-checks.js`: the bar's height and the stage's top at 40, the button check at 48 wide by 40 tall, the six stage heights `- 40 - 48`, the sidebar's top at 40, the size band's ceiling at 56, the zero-area stage from `innerHeight - 41`. Side effect, named: the bar follows the button, as it did at 48. | Built from `.tmp/bar40-src/`, HEAD `b2d973f` plus this change alone, other sessions' uncommitted overlay, selftest and doc edits left out, into `.tmp/bar48-target/`: fmt, clippy with -D warnings and --all-features, the product build, the checks build, 64 host and 4 pixel tests, 2 ignored by design, all clean. `--editor-check`, three runs: the first two failed 8 and 9 checks, the screen copies and Save As dialogs with other windows in front; a control run of HEAD alone then read 490 pass and 1 fail, and the third run of this build read the same, 490 pass, 1 fail. The bar reads 40 tall with the stage from 40, the buttons 1136-1184, 1184-1232 and 1232-1280, 40 tall each. The failure is the sidebar's eleven buttons, which HEAD fails too: 8 px short now, where the 48 bar left them 16 short. Not run: Rotem's eye on the release. Risk low, a value and what it moves. | Pass, 1 carried failure | low | `b2d973f` | Set the button and the bar back to 48, the dependants to 48 and `- 96px`, and the check numbers with them. |
| 2026-09-18 | The release built for the 40 px buttons | A release built from commit `502821a` archived into `.tmp/release-502821a/`, into the private target folder `.tmp/release-target/`, so other sessions' uncommitted work and the check runs' rewrite of `host/references/s06/environment.txt` stayed out. Not copied: Rotem's Recon was running from `host/target/release/recon-host.exe` (PID 70288, since 15:45:18), which holds the file. A hidden job, `.tmp/swap-release-502821a.ps1`, started at 15:54:27, copies the build over it the moment that Recon exits, checks the build's hash first and reads the copy back, and writes what it did to `.tmp/swap-release.log`. The `47b0369` magnifier build is an ancestor of this one, so its waiting job finds this hash and copies nothing. No source changed. | `cargo build --release` from that checkout: finished in 34.68 s, no warning. The build: 14,890,496 bytes, SHA-256 `D52A2A73...24DF76C9`. The archive's page read before the build: `#bar` 40 px, `#winbtns button` 48 by 40. The job's first log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon. The project checks were not rerun: the code is `502821a`'s, whose change passed them from `.tmp/bar40-src/`. Risk low, a build. | Pass, copy pending | low | `502821a` | Rebuild the release from `b2d973f`, whose buttons are 48 tall, and copy it in. |
| 2026-09-18 | The glide at 192 ms | `host/src/overlay.rs`: `GLIDE_MS` 192, from 164, the ease-out kept. `host/src/selftest.rs`: the mid-glide look inconclusive past 140 ms, from 120; the looks stay at 80 and 300 ms. `project-os/Design.md`: the Motion row. `project-os/Decisions.md`: a line on the glide entry. `project-os/QA.md`: the row's 140 ms, already in HEAD, swept into `47b0369` by the other session's commit from the shared tree. Not touched: the glide's mechanism, the magnifier. | In `.tmp/glide-head5/`, an archive of `b2d973f` with the two source files' copies, HEAD plus these hunks, with the private target `.tmp/glide-target/`: fmt --check, clippy with -D warnings and --all-features, the product build, 64 host tests and 4 pixel tests, 2 ignored by design, and the checks build, all clean; HEAD moved to `4be3064` meanwhile by a change to the page alone. `--selftest`, five runs across the day with Rotem at the machine and the other session running its own: two collided with that session's run; two failed only in the magnifier's hover probe, the pointer found 77 px from where the test had put it, measured in the run's screenshot, a hand on the mouse (inferred); the last, at 15:49, passed C, D, E and H and failed F and G, whose Escape came back as a click on a 1282 by 802 window and whose stand-in never reached the foreground, the desktop in use (inferred). In that run H read the frame's left band 12 px in from the window's edge and 29 px out from the part's 80 ms after the move, and on the window's edge at 302 ms, then picked the window, the part and the drag. Not run: a whole self test on a quiet desktop; Rotem's hand on real windows, which is what the value is for. Risk low, a value the owner named. | Pass, F and G to rerun quiet | low | `4be3064` | Revert the commit; the two source files and the three docs carry nothing else of it. |
| 2026-09-18 | The release built for the 192 ms glide | A release built from commit `369296b` archived into `.tmp/release-369296b/`, into the private target folder `.tmp/release-target/`, so another session's uncommitted background value in the page and the check runs' rewrite of `host/references/s06/environment.txt` stayed out. It replaced the `502821a` build waiting in that folder, the 40 px buttons, which this commit carries as an ancestor; that build's job will find the hash changed and copy nothing. Not copied: Rotem's Recon was running from `host/target/release/recon-host.exe` (PID 70288, since 15:45:18, on the `47b0369` build copied in at 15:45:14). A hidden job, `.tmp/swap-release-369296b.ps1` (PID 59720), started at 15:59:05, copies the build over it the moment that Recon exits, checks the build's hash first and reads the copy back, and writes what it did to `.tmp/swap-release.log`. No source changed. | `cargo build --release` from that checkout: finished in 25.39 s, no warning. The build: 14,890,496 bytes, SHA-256 `3719509D...FABE1AC8`. The archive's overlay was read before the build: the 192 ms constant. The job's first log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the host's code is `369296b`'s, which passed them in `.tmp/glide-target/`. Risk low, a build. | Pass, copy pending | low | `369296b` | Rebuild the release from `4be3064`, whose glide is 164 ms, and copy it in. |
| 2026-09-18 | The size above the magnifier's circle | `host/src/overlay.rs`: `magnifier` lays the text row out first, its padding above and below, then the circle with its inset, the panel's height and the circle's centre following; four comments. `host/src/selftest.rs`: `magnifier_seen` finds the circle's centre row by the frame's blue line through it, read 30 px into the panel, rather than assuming the row, since the text's height is the face's; the ground, ring and cell probes sit on that row, and the text's light pixels are counted between the panel's top and the circle's. `project-os/Design.md`: the Size row, Rotem's, and two sentences. `project-os/QA.md`: the overlay row. `project-os/Plan.md` §3.2: one word. Not touched: the panel's place, size and fill; another session's uncommitted settings work in `config.rs`, `editor.rs`, `main.rs` and `settings.rs`, and its page and reference edits. | In `host/` with the private target `.tmp/bar48-target/`: fmt, clippy with -D warnings and --all-features, the product build, the checks build, 64 host and 4 pixel tests, 2 ignored by design, all clean. `--selftest` twice, on the 5120x1440 display at 100%: both runs read the magnifier while dragging and while hovering below and right of the pointer, the ground and ring on the row found, the nine centre cells the frozen pixels, 97 and 95 light pixels of text above the circle, the drag exact; the first run failed the other session's glide look in H, no frame band in its strip at 300 ms, a strip at the stand-in's left edge that the panel never reaches, and the second run passed every check, the glide read at 80 and 302 ms. The two pictures looked at: "160x100" and "200x100" above the ringed circle, right of and below the pointer. Not run: Rotem's hand on a real hotkey. Risk low, a layout value. | Pass | low | `d776cc6` | Put the text row back under the circle: the height and the centre from `inset + diameter`, the text at `inset + diameter + pad.1`. |
| 2026-09-18 | The release built for the size above the circle | A release built from commit `ca5c936` archived into `.tmp/release-ca5c936/`, into the private target folder `.tmp/release-target/`, so another session's uncommitted settings work, the page's background value and the check runs' rewrite of `host/references/s06/environment.txt` stayed out; the archive holds no `settings.rs`. Not copied: Rotem's Recon was running from `host/target/release/recon-host.exe` (PID 42860, since 16:03:10, on the `369296b` build, which carries the magnifier on the right). A hidden job, `.tmp/swap-release-ca5c936.ps1` (PID 57636), started at 16:12:50, copies the build over it the moment that Recon exits, checks the build's hash first and reads the copy back, and writes what it did to `.tmp/swap-release.log`. The `47b0369` job had copied its build in at 15:45:14, the copy identical. No source changed. | `cargo build --release` from that checkout: finished in 36.20 s, no warning. The build: 14,890,496 bytes, SHA-256 `7A55CF54...5B752DCB`. The archive's overlay read before the build: the text row laid out first. The job's first log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; the log says. The project checks were not rerun: the code is `ca5c936`'s, which passed them in `.tmp/bar48-target/`. Risk low, a build. | Pass, copy pending | low | `ca5c936` | Rebuild the release from `d776cc6`, whose size sits under the circle, and copy it in. |
| 2026-09-18 | The copy job restarted after the restart | Rotem quit and reopened Recon twice at about 18:27 and saw no change. `.tmp/swap-release.log` ends at the `ca5c936` job's first line, 16:12:50, with no copy; the machine's last boot is 17:49:46, which killed the hidden job, the 2026-09-16 debug Recon and every other waiting job; `host/target/release/recon-host.exe` still hashes `37195 09D...FABE1AC8`, the `369296b` build copied in at 16:03, and `.tmp/release-target/` still holds the `ca5c936` build, `7A55CF54...5B752DCB`, untouched. The job `.tmp/swap-release-ca5c936.ps1` is started again (PID 19040) at 18:28:31 and waits for Rotem's Recon (PID 6384, since 18:27:27) to exit; a hidden job does not survive a restart, and nothing here makes it. No source changed. | The log, the two hashes, the boot time and the process list read; the job's new first line read back. Risk low, a record. | Pass, copy pending | low | `e2d95ae` | Nothing to undo: a record. |
| 2026-09-18 | Rotem's hand on the magnifier | Rotem quit and reopened Recon after the restarted job was in place and wrote that it works great: the magnifier on the pointer's right, the size above its circle. `.tmp/swap-release.log` shows the `ca5c936` copy, the file reading back identical to the build. No source changed. | The log's copy line and the hash in place read back; his word. Risk low, a record. | Pass | low | `501cf52` | Nothing to undo: a record. |
| 2026-09-18 | Settings: the two global shortcuts | `host/src/settings.rs`, new: `change`, the swap with Windows behind two closures, the new shortcut registered before the old one is let go, the two never equal, only Open clearable; `start` at startup in place of main's own registration; `fired`, which shortcut a press was; the commands `editor_settings`, `editor_settings_set` and `editor_settings_recording`, the last letting both shortcuts go while a field waits; the lock never held across a registration; two checks-only commands behind the feature. `host/src/config.rs`: `open_hotkey`, `save` keeping every other key, flushed before its rename, and a path the checks and tests point beside their executable. `host/src/main.rs`: the handler asks `fired` and shows the editor for the open shortcut; the tray's Capture line is handed to Settings to rename; a check run gets its own fresh settings file and no shortcut plugin. `host/src/editor.rs`: the commands in both handler lists. `editor/index.html`: `#win-settings`, the modal, its script: the field's waiting state, the shortcut built from the physical key, a capture-phase key listener so the editor under the modal hears nothing, a note being typed committed before it opens, Escape, the veil and the × to close, the window's blur ending a wait. `editor/editor-checks.js`: the button order check with settings first, and a section of nineteen checks. `project-os/Decisions.md`, `Design.md`, `Map.md`: the entry, the component, the rows. Not built: a settings change in Ctrl+Z (rule 23), asked of Rotem. | Built from `.tmp/settings-src/`, HEAD `e2d95ae` plus this change alone, into `.tmp/bar48-target/`: fmt, clippy with -D warnings with and without --all-features, the product build, the checks build, 71 host and 4 pixel tests, 2 ignored by design, seven of them new: six on `change` (free, taken, shared however spelled, cleared, not a shortcut and Win, retried after unavailable) and one on `save` and `load`. `--editor-check`, final code: 509 pass, 0 NOT RUN, 1 FAIL, the sidebar's eleven buttons, carried and dropped by Rotem. The Settings section, all pass: the button at 1088-1136 by 40 with a 10 px icon against minimize from 1136; the panel 430-850 by 316-484 in a 1280 by 800 window; Ctrl+Alt+R pressed with a Hebrew letter kept by its physical key with the file reading back both keys; Ctrl+Alt+T, declared taken, refused with the old shortcut still on screen, in the host and in the file; the same shortcut for both refused; Ctrl+Shift+5 kept and named by the host; the × clearing `open_hotkey` out of the file; A under the modal leaving the mode at view and switching it once closed; a note typed before the click kept with its text. Two earlier runs failed pointer and dialog checks elsewhere with other windows in front and the mouse in use, and passed in the final run on the same code. Review (rule 17, medium): two findings introduced and fixed, the settings file renamed before it was flushed, and a note being typed not committed when the modal opened; one more fixed before it could bite, the lock held across a registration; impact shared with the capture shortcut's startup, the tray's label and the measure run's `HOTKEY_REGISTERED`, each read and kept. NOT run by a machine: the real swap against Windows and the open shortcut showing the window, since a check run has no plugin and Rotem's own Recon was running, which a second product instance hands over to; his hand on the release. | Pass, the live swap by hand | medium | `8feb74c` | Revert the commit; `recon.json` keeps a harmless `open_hotkey` key the old build ignores. |
| 2026-09-18 | The release built for Settings | A release built from commit `68413ac` archived into `.tmp/release-68413ac/`, into the private target folder `.tmp/release-target/`, so other sessions' uncommitted overlay, selftest and background edits and the check runs' rewrite of `host/references/s06/environment.txt` stayed out. Not copied: Rotem's Recon was running from `host/target/release/recon-host.exe` (PID 34716, since 18:29:20), which holds the file. A hidden job, `.tmp/swap-release-68413ac.ps1`, started at 18:39:49, copies the build over it the moment that Recon exits, checks the build's hash first and reads the copy back, and writes what it did to `.tmp/swap-release.log`. The commit's ancestors include the other session's `8feb74c`, so this build carries it. No source changed. | `cargo build --release` from that checkout: finished in 32.31 s, no warning. The build: 14,854,144 bytes, SHA-256 `D2D65B37...601CD7B8`. The archive read before the build: `#win-settings` in the page, `host/src/settings.rs` present. The job's first log line read back. Not run: the copy and its hash read back, waiting on Rotem quitting Recon; and on that release, by his hand, the real swap of a shortcut against Windows. The project checks were not rerun on `68413ac`: its change passed them from `.tmp/settings-src/` on `e2d95ae`. Risk low, a build. | Pass, copy pending | low | `68413ac` | Rebuild the release from `8feb74c`, which has no Settings, and copy it in. |
| 2026-09-18 | Settings outside undo, decided | `CLAUDE.md` rule 23: one paragraph, a change made in the Settings modal is not an act in the undo list. `project-os/Decisions.md`: the entry and its index line, closing the question the Settings entry left open. No code changed. | Both files read back after the edit; the rule's paragraph sits after the 2026-09-16 sentence, the index line after the Settings one. No check run: docs only. Risk low, a record. | Pass | low | `a1831bb` | Remove the paragraph and add a superseding decision. |
| 2026-09-18 | Logo ideas page | New file `notes/2026-09-18-logo-ideas.html`, on the app background `#0D0E12`: Rogers' mark as the source, then ten marks (reticle, viewfinder, radar, an R made of a region and its line, callout, eye, Rogers' triangle turned into a pointer, magnifier, crop, the capture guides and area). The wordmark reuses Rogers' R, e and o outlines; c and n are drawn on the o's and the r's numbers. Ideas only, no icon in the product touched. | The page opened in the built-in browser at 1680 wide: all eleven cards drew, the console was empty. Seen and fixed there: the Rogers lockup was missing its e, the reticle's ticks poked into its circle, the radar's blip merged with its sweep, the capture area touched its guides. No project check run: no code changed. | Pass | low | `a264e5a` | Delete the file. |
| 2026-09-18 | The open shortcut toggles; the Settings icon a chevron | `host/src/settings.rs`: `open_press`, show or minimize from visible, minimized and focused, and its test. `host/src/editor.rs`: `toggle`, which reads the three from the window and minimizes or calls `show`. `host/src/main.rs`: the open shortcut calls `toggle`, and logs what it did. `editor/index.html`: `#win-settings`' icon is the path `M1 3.5l4 4 4-4` on the window icons' 1 px stroke, from three dots; two comments with it. `editor/editor-checks.js`: the section's line and the button check say chevron. `project-os/Design.md`: the Settings button row. `project-os/Decisions.md`: the entry. | Built from `.tmp/settings-src/`, HEAD `9f6fd6c` plus this change alone, into `.tmp/bar48-target/`: fmt, clippy with -D warnings with and without --all-features, the product build, the checks build, 72 host and 4 pixel tests, 2 ignored by design, the new one reading minimize for the window in front and show for the tray, minimized and behind another window. A corrupt cached PDB from an interrupted build was cleared first with `cargo clean -p recon-pixels`. `--editor-check`: 501 pass, 2 FAIL, 4 NOT RUN: the sidebar's eleven buttons, carried and dropped by Rotem; the HUD's store size, the known flake; the three screen copies and one picker Escape with another window in front. The chevron's button reads 1088-1136 by 40 with a 10 px icon. Review (rule 17, medium): nothing found; impact contained, the tray's click and a capture still call `show` alone. NOT run by a machine: the press itself, since a check run has no shortcut plugin and Rotem's Recon was running; his hand on the release. | Pass, the press by hand | medium | `9f6fd6c` | Revert the commit. |
| 2026-09-18 | The Settings release copied in; the toggle's release built | `.tmp/swap-release.log`: the `68413ac` job copied its build over `host/target/release/recon-host.exe` at 18:42:59 and read it back as `D2D65B37...601CD7B8`, same as the build, True; Rotem's Recon (PID 46348) started on it at 18:43:04. Then a release built from commit `fbc2693` archived into `.tmp/release-fbc2693/`, into the private target folder `.tmp/release-target/`, other sessions' uncommitted edits left out. Not copied: that Recon holds the file. A hidden job, `.tmp/swap-release-fbc2693.ps1`, started at 18:49:09, copies the build the moment it exits, checks the build's hash first and reads the copy back. No source changed. | `cargo build --release`: finished in 23.22 s, no warning. The build: 14,856,704 bytes, SHA-256 `8EB39557...F91DFB85`. The archive read before the build: the chevron's path in the page, `toggle` in `editor.rs`. The job's first log line read back. Not run: the copy, and the press itself on it, Rotem's hand. Risk low, a build. | Pass, copy pending | low | `fbc2693` | Rebuild the release from `a264e5a`, whose open shortcut only shows the window, and copy it in. |
| 2026-09-18 | The pointer's guide lines on the capture overlay | `host/src/overlay.rs`: `CROSS_RGB` and `CROSS_ALPHA` (184 of 255), a 1x1 blue bitmap beside the dim's, `State::cross` giving the row and the column above and below it on the pointer's display (a moved drag's clamped point, otherwise the pointer), `invalidate_cross` before and after every move and at the first pointer read, the lines blended after the dim and under the dashed frame. `paint` now reads the update region's rectangles (`update_rects`) and paints each (`paint_piece`), the magnifier once after them (`paint_magnifier`): a row plus a column has the whole display as its bounding box, which the old paint would have redrawn at every move. `host/src/selftest.rs`: `line_probes` and the line check in `magnifier_seen`. `project-os/Design.md` Magnifier, `project-os/QA.md` the capture overlay row. | fmt, clippy, build, 76 tests pass. `--selftest` on the build before the magnifier moved to once a paint: passed whole twice with the line check in C and H, and `s02-window-lit.png` shows both lines edge to edge on the 5120 by 1440 display, over the lit part and the dim, under the panel. On the final build C passed with the line check; the run then hung in F when another session's Recon window took the keyboard from the stand-in, and was ended by hand. Three runs after that stopped at `freeze FAILED: BitBlt failed`, the display having changed to 3840 by 2160 at 225% with the screen not readable, so H is not run on the final build. Other failed runs in between were F, G and H's foreground steps with Rotem at the machine, none the line check. Review (medium): one finding, introduced and fixed, the magnifier rebuilt once per dirty piece, three or four times a move; pre-existing, inferred and not reproduced on one display: a pointer that moves to another display leaves the magnifier's last panel painted on the first. | Lines as asked; H on the final build and a hand drag are open | medium | `ef20086` | revert the commit |
| 2026-09-18 | The guide lines' release built | A release built from commit `5f397ec` archived into `.tmp/release-5f397ec/`, into the private target folder `.tmp/release-target/`, so other sessions' uncommitted editor edits stayed out; `fbc2693` is its ancestor, so the toggle and the chevron are in it. Not copied: Rotem's Recon (PID 46348) runs from `host/target/release/`. `.tmp/swap-release-5f397ec.ps1` waits for it to exit, copies, and reads the hash back into `.tmp/swap-release.log`; the `fbc2693` job checks the shared build's hash first and will copy nothing. | `cargo build --release` finished in 21 s; the build's SHA256 is `675DEAFC...F1F92886`; the log's last line is the `5f397ec` job waiting. | Built, waiting on Rotem's Recon to exit | low | `5f397ec` | none needed, nothing is replaced yet |
| 2026-09-18 | An SVG sharp at every zoom; see-through pixels flattened | `host/src/source/svg.rs`: the tree is shared (`Arc`), `Drawing::region` draws a rectangle of the scale-one render at any scale; `source/mod.rs`: `FrameSource::drawing`, `Opened::drawing`. `host/src/editor.rs`: `region_bytes_inner` sends an enlarging request to `viewed_drawing()` when the file on screen is a vector and the document is not managed; the clone is taken under the document lock and the draw runs after it. `editor/index.html`: `flattenOntoPaper`, run in `paintRegion` for a picture that came from a file, since a pixel with alpha under 255 written into the `alpha: false` canvas is the suspected cause of earlier paints staying on screen. Checks: `editor_screen_compare` (feature-gated) and the section 'A vector enlarged' in `editor/editor-checks.js`. | fmt, clippy, build, 76 tests pass. `--editor-check` before the fix: 'hard edges, not a ramp' FAILED with 12 part-way pixels on row 240 of `svg-css-style-block.svg` asked at 4x; after: pass, 0. The screen check, canvas against screen after zooms 1.5, 2.2, 3.3, was NOT RUN in every pass: another window was over the check's, Rotem working on the machine; so the flattening is unproven and waits on his eye. Six other checks fail the same before and after this change (stage with no area, the eleven buttons' container, the drag's copy, the strip's wheel, the done mark twice), not this task's. Review: no finding introduced; consumers of the region service are the page's paint and the checks, and only an enlarging request for a viewed vector takes the new path. | Pass in part, the screen half unverified | medium | `20965b9` | Revert the commit. |
| 2026-09-18 | The sharp SVG's release built, not copied | A release built from commit `6b19599` archived into `.tmp/release-6b19599/`, into the private target folder `.tmp/release-target/`, other sessions' uncommitted edits left out; `5f397ec` is its ancestor. Not copied: Rotem's Recon runs from `host/target/release/`, and starting the hidden waiting job was denied by the session's permission classifier, so no job waits. The copy is one command Rotem runs after quitting Recon. No source changed. | `cargo build --release` finished in 20.82 s. The build: 14,938,112 bytes, SHA-256 `B3596B1E...7938B12B`. Not run: the copy, and Rotem's look at an SVG zoomed in on it. | Built, not copied | low | `6b19599` | none needed, nothing is replaced yet |
| 2026-09-18 | The guide lines' release not copied | Nothing changed on disk. `.tmp/swap-release.log` 19:54:26: the `5f397ec` job found the shared build `B3596B1E...7938B12B`, the `6b19599` build, not its own `675DEAFC...`, and copied nothing, as written; the `fbc2693` job the same a second later. `5f397ec` is an ancestor of `6b19599`, so that build has the lines. A copy of it over the release path with no Recon running was refused twice: the path guard on a runtime path, then the session's permission check on the literal one. | `tasklist` shows no Recon running; the release path's SHA256 begins `D2D65B37`, the `68413ac` Settings build. | Waiting on Rotem to run the copy, or to allow it | low | `fb3b0fb` | none needed |
| 2026-09-18 | Rotem's look at the sharp SVG release | No source changed. Rotem copied the `6b19599` build over `host/target/release/recon-host.exe` himself and opened `rogers_logo.svg`. | His report in chat: flawless, sharp at the opening size and zoomed in, no copies of earlier sizes. This closes the half the History row for `6b19599` left unverified. Not run by me: the copy's hash read back. | Pass, by Rotem's eye | low | `fb3b0fb` | none needed |
| 2026-09-18 | Logo ideas, round two | New file `notes/2026-09-18-logo-ideas-2.html`, the first page's layout and wordmark with a new set: 03 and 10 as the sources, then 11 a lit radar sector, 12 guides with a round area, 13 horizon and sweep, 14 quadrant radar, 15 guides with an outlined area, 16 lit quadrant, 17 the ring as the radar, 18 range rings with the sweep cut out, 19 marching frame, 20 scope with a lit quarter. The first page is untouched. | The page opened in the built-in browser at 1680 wide: all twelve cards drew, the console was empty. Seen and fixed there: 19's dashes read as a grid of dots, lengthened to 4 on 2; 11's sector drew with its arc bulging inward in that browser, redrawn as a polygon under the circle's line and seen again at 300 px as a plain wedge. No project check run: no code changed. | Pass | low | `4ac8584` | Delete the file. |
| 2026-09-18 | Logo ideas, round three | New file `notes/2026-09-18-logo-ideas-3.html`, the same layout and wordmark with twenty new marks, 21 Ping to 40 Guides and a blip. The first two pages are untouched. | The page opened in the built-in browser: all twenty cards drew as meant, the console was empty. Noted, not changed: 21 Ping reads close to the common feed icon. No project check run: no code changed. | Pass | low | `375ea0e` | Delete the file. |
| 2026-09-18 | An opened file in the timeline, as a pointer | `host/src/editor.rs`: `Link`, `LINKS`, `opened.json` read in `load_store` and written whole through `store::write_atomic`; `open_file` beside `open_path` (the picker and `main.rs` use it, folder steps do not), a pointer made or reused before the frame is presented so the page hears `linked`; `history_ids` and `editor_documents` list documents and pointers by creation, a pointer whose file is gone dropped there; `show_document`, `thumbnail`, `editor_delete_document` and `trash_rejoin` each branch for a pointer; `preserve` drops the pointer the document replaces. `host/src/store.rs`: `opened_path`. `editor/index.html`: the tab join for `linked`, the removal's notice, a notice when a pointed file will not open. `project-os/Plan.md` S2.10, `Map.md` Data, `Decisions.md`. Rule 11 touched and kept: no pixel and no thumbnail of the file is written anywhere. Undo (rule 23): opening is not an act, as a capture is not; the removal is, through the deletion path. | fmt, clippy, build, 76 tests pass. `--editor-check`, section 'A file opened by name', nine checks, all pass on a build in `.tmp/dev-target/`: `img2.png` opened by name gives one thumbnail marked a pointer, in viewing, the store empty before and after its thumbnail draws; a folder step joins nothing and a second open reuses the number; chosen from the timeline it reopens; `opened.json` read back gives two files and the capture; its x leaves 2 thumbnails, 0 in the trash, the file `958 bytes, 8d102896d3f492ce` before and after, the notice 'untouched'; Ctrl+Z and Ctrl+Shift+Z put it back and take it off; Annotate leaves one thumbnail for the file, not a pointer, same number, now in the store; `b.jpg` removed from its folder leaves the list. Other checks in those runs failed differently run to run (the eleven buttons' container every time; 'still crisp after a pan' once; the HUD's storage line and six Settings checks once) while Rotem worked on the machine; none touches this change, and one earlier run ended mid-way with exit unknown. Not run: the picker and 'Open with' by hand. Review: no finding introduced; consumers of `editor_documents` and `history_ids` are the strip, the keys' history walk, the delete's neighbour and the checks, all passing; lock order kept, the documents' lock and the pointers' never held together. Known: each refresh asks whether every pointed file exists, slow on a dead network path; a pointer's thumbnail is not remade when its file changes until a restart. | Pass | high | `9649093` | Revert the commit; an `opened.json` left behind is ignored by the older build. |
| 2026-09-18 | Guide lines at 48%, the plus white | `host/src/overlay.rs`: `CROSS_ALPHA` 122 of 255 from 184; `CROSS_GAP_PX` 12, scaled by the display, and `State::cross` gives four strips, the row left and right of the pointer and the column above and below, each the gap short of it, so nothing blue is under the system's cross. The cursor itself is untouched, `IDC_CROSS` as before. `host/src/selftest.rs`: the line check at 48%. `project-os/Design.md` Magnifier, `project-os/QA.md` the capture overlay row. The 12 px is mine, by eye from the cross's arm at 100%, provisional. | fmt, clippy, build, 76 tests pass. `--selftest`: C passed with the 48% line check mid-drag. D, F, G and H failed in a run with Rotem at the machine: another Recon window held the foreground, and H's own note says the pointer was put at 278,271 and was at 269,265 when the screen was copied, so its line probe read a dimmed pixel with no line. Two earlier runs were cut by my own time limit at H. The white plus over the gap is not read by any check: a screen copy does not carry the cursor. Review (medium): no finding; the gap is on the lines only, the magnifier and the frame untouched. | 48% and the gap in place; the plus's colour and H are Rotem's hand | medium | `bcd65a5` | revert the commit |
| 2026-09-18 | The opened files' release built, not copied | A release built from commit `bcd65a5` archived into `.tmp/release-bcd65a5/`, into the private target folder `.tmp/release-target/`, other sessions' uncommitted edits left out. Not copied: Rotem's Recon runs from `host/target/release/`, and the hidden waiting job is refused to this session, as at `6b19599`. No source changed. | `cargo build --release` finished in 32.97 s. The build: 14,995,968 bytes, SHA-256 `AFB21BDE...E5A87F4F`. Not run: the copy, and Rotem's hand on the picker and 'Open with'. | Built, not copied | low | `bcd65a5` | none needed, nothing is replaced yet |
| 2026-09-18 | The 48% lines' release built | A release built from commit `85e3a8b` archived into `.tmp/release-85e3a8b/`, into the private target folder `.tmp/release-target/`, so other sessions' uncommitted editor and store edits stayed out. Not copied: a Recon runs from `host/target/release/`. `.tmp/swap-release-85e3a8b.ps1` waits for it to exit, copies, and reads the hash back into `.tmp/swap-release.log`; it copies nothing if the shared build is another by then. | `cargo build --release` finished in 22 s; the build's SHA256 is `B317CE25...46C50CFB`; the log's last line is the `85e3a8b` job waiting. | Built, waiting on Rotem's Recon to exit | low | `85e3a8b` | none needed, nothing is replaced yet |
| 2026-09-18 | The 48% lines' release copied in | Nothing built. `.tmp/swap-release.log` 20:29:40: the `85e3a8b` job copied its build over `host/target/release/recon-host.exe` and read it back as `B317CE25...46C50CFB`, same as the build, True. | `sha256sum` of the release path begins `b317ce25`, the build's. | In place | low | `2609684` | copy an earlier `.tmp/release-target` build back |
| 2026-09-18 | Save As over the annotated file | `host/src/export.rs`: `replaceable`, `write` with `Written::Replaced`, the original written through `store::write_atomic`; `write_new` unchanged for every other path. `host/src/editor.rs`: `save_as_plan` (a managed document's own PNG or JPEG gives its folder, its name and itself as the one file that may be written over), `note_own_save` (the document's stamp of its file follows the save), the dialog's title 'Save As' in that case. `host/src/dialog.rs`: the title is a parameter. `CLAUDE.md` rule 11's exception, `Decisions.md`, `Plan.md` S2.11. Rule 11 is the thing changed, by Rotem's word. Undo (rule 23): a save to disk is not an act in the list, as Save As never was. | On a clean copy, the commit before plus these files only, since another session's unfinished page edit stops the check run in the shared tree: fmt, clippy, build, 77 tests pass, the new unit test among them (the original named in another case is written over; another existing file, and the original with none named, are not). `--editor-check`, section 'Save As on an annotated PNG or JPEG', seven checks pass: viewed only, `img2 annotated.png` and nothing replaceable; annotated, the plan is the file's own folder, `img2.png`, and itself; `IMG1.png` chosen gives Exists and keeps its bytes; `img2.png` chosen gives Replaced, `958 bytes, 8d102896d3f492ce` then `8280 bytes, f1442b7d545a8212`, a PNG of 336 by 174 that opens; no other name left in the folder; `source_changed` false with the note kept; an annotated GIF keeps `a annotated.png`. Two other checks failed in that run, the picker's Escape and the eleven buttons' container, neither this change's. Not run: the dialog itself by hand. Review: no finding introduced; the only callers of the write are Save As and its check; the narrow door is one comparison under a unit test. | Pass | high | `2fd2f19` | Revert the commit; files already written over stay written over. |
| 2026-09-18 | The save-over release built, not copied | A release built from commit `db497b3` archived into `.tmp/release-db497b3/`, into the private target folder `.tmp/release-target/`, other sessions' uncommitted edits left out. Not copied: Rotem's Recon runs from `host/target/release/`, and the hidden waiting job is refused to this session. No source changed. | `cargo build --release` finished in 29.50 s. The build: 15,011,328 bytes, SHA-256 `504E44ED...C53E0984`. Not run: the copy, and Rotem's hand on Save As over a real file. | Built, not copied | low | `db497b3` | none needed, nothing is replaced yet |
| 2026-09-18 | The circle's centre lines on the pixel | `host/src/overlay.rs`: the centre line's place in `magnified` and the matching one in `draw_panel` is `(cell - 1) / 2` into the centre cell, from `cell / 2`: a cell's last pixel is the grid's line, so its own pixels are one fewer. The circle's centre stays on the line, so the grid moves 1 px, not the lines. `host/src/selftest.rs`: the guide lines' check reads nothing when the pointer is not where the script put it and says so, where it failed the run; a hand on the mouse did that twice today. | fmt, clippy, build, 77 tests pass. `s02-magnifier-dragging.png` read pixel by pixel at 100%: grid lines at x 88 and 96, the blue at 92, the middle of 89 to 95; before the fix the grid was at 67 and 75 and the blue at 72, one right of the middle 71. `--selftest`: C and H passed, the magnifier's cells and the guide lines included; F and G failed on the foreground, other sessions' Recon windows taking it, as in every run this evening. Not measured at 225%, where the cell is 18 and the middle is by the same arithmetic. Review (medium): no finding; the two formulas are the only users of the centre. | Lines on the pixel at 100%, 225% by arithmetic only | medium | `cb6476b` | revert the commit |
| 2026-09-18 | The mode follows what is done | `editor/index.html`: `pickTool` behind the tool buttons and the eight tool keys, which asks the host for the document when the picture is only viewed, once per burst, and arms the tool only if that picture is still shown; `commitEditing` and the release of a draw that made a shape put the tool down; the `#mode` button, its styles, its listener and the `KeyA` handler removed; the HUD's mode line reworded. The crop tool stays in hand, and `setMode('view')` stays for the checks only. `editor/editor-checks.js`: section 20 rewritten around the new route, section 36 counts ten buttons, the Settings section proves the keys with T instead of A. `project-os/Plan.md` §3.3 and the key table, `Design.md`, `QA.md`, `Decisions.md`. Undo (rule 23): no new act, the tool in hand was never in the list. | fmt, clippy, build, 77 tests pass, in a private target folder since another session's selftest held `host/target/debug`. `--editor-check` from `.tmp/mode-target`: first run one failure, the vector's screen copy with another window over ours, 4 not run for the same reason; second run every check passed, none unrun, the new section 20 lines among them. Review (medium): one finding fixed, a tool armed over another picture shown while the host answered; reach: `commitEditing`'s twelve callers all tolerate the tool going down, `loadImage` already put it down. Known: a tool key over a viewed file makes its document, where only A did; a file annotated before shows its notes only once a tool is picked. Not run: real clicks by hand. | Pass | medium | `293396a` | revert the commit |
| 2026-09-18 | The centred lines' release built | A release built from commit `293396a` archived into `.tmp/release-293396a/`, into the private target folder `.tmp/release-target/`, so other sessions' uncommitted editor, design and plan edits stayed out. Not copied: a Recon runs from `host/target/release/`. `.tmp/swap-release-293396a.ps1` waits for it to exit, copies, and reads the hash back into `.tmp/swap-release.log`; it copies nothing if the shared build is another by then. | `cargo build --release` finished in 26 s; the build's SHA256 is `0386B5AD...9B2B67D1`; the log's last line is the `293396a` job waiting, 20:43:36. | Built, waiting on Rotem's Recon to exit | low | `293396a` | none needed, nothing is replaced yet |
| 2026-09-18 | The automatic mode's release built, not copied | A release built from commit `38db43c` archived into `.tmp/release-38db43c/`, into the private target folder `.tmp/release-target/`, other sessions' uncommitted top bar edits left out. Not copied: a Recon runs from `host/target/release/`, and no waiting job was started, since another session's job for `293396a` may still be waiting on the same file. The commit's copy of the two editor files was built by hand from HEAD plus this task's edits (`.tmp/mode-commit/apply.py`); the editor checks ran on the working tree, which also held the other session's top bar edits, not on that exact copy. | `cargo build --release` finished in 23.51 s; the build's SHA-256 begins `d9cb78391b32e1eb`, the file in place begins `b317ce257b19c4f7`. Not run: the copy, and Rotem's hand on a note finished and clicked again. | Built, not copied | low | `38db43c` | none needed, nothing is replaced yet |
| 2026-09-18 | The centred lines' release superseded | Nothing built. `.tmp/swap-release.log` 20:46:26: the `293396a` job found the shared build `D9CB7839...C0229D5D`, the `38db43c` build, and copied nothing, as written. `293396a` is an ancestor of `38db43c`, whose History row says its build was not copied and names no waiting job, so `.tmp/swap-release-38db43c.ps1` was written from mine with that build's hash and started. | `sha256sum` of the shared build is `D9CB7839...`, the hash the job waits with; the release path begins `b317ce25`, the `85e3a8b` build; a Recon (PID 8692) is running; the log's last line is the `38db43c` job waiting. | Waiting on Rotem's Recon to exit | low | `0b2e10f` | none needed, nothing is replaced yet |
| 2026-09-18 | The 38db43c copy job run again | Nothing built. `.tmp/swap-release.log` 20:46:50: the `38db43c` job saw no Recon at the release path, tried the copy and failed, the file still in use by the Recon that was exiting; nothing was replaced. Rotem's Recon (PID 50484) started from the release path at 20:47:02. The same job started again. | The release path's hash still begins `b317ce25`; the log's last line is the `38db43c` job waiting. | Waiting on Rotem's Recon to exit | low | `f504600` | none needed |
| 2026-09-18 | The 38db43c release copied in | Nothing built. `.tmp/swap-release.log` 20:50:08: the `38db43c` job copied the shared build over `host/target/release/recon-host.exe` and read it back as `D9CB7839...C0229D5D`, same as the build, True. | `sha256sum` of the release path and of the shared build both begin `d9cb7839`; no Recon was running at the read. | In place | low | `14f82dd` | copy an earlier `.tmp/release-target` build back |
| 2026-09-18 | Top bar 64 px, the logo | `editor/index.html`: `#bar` 64 px; `#stage`, `#controls` and the timeline's ceiling (`100vh - 112px`) follow; `#winbtns button` untouched at 48 by 40, at the bar's top; `#logo`, Rotem's SVG inline before the title, 16 px left margin, centred so 16 px above, `pointer-events: none` so a press on it drags the window (side effect, named). The title line unchanged, right of the logo. `editor/editor-checks.js`: the bar's numbers, and a new logo check. `project-os/Design.md`: Window gets Top bar and Logo rows. | A clean copy of `cd47941` plus this change only, `.tmp/bar64-src`, built with `stage0-checks`, `--editor-check` (`.tmp/bar64-check5.log`): bar 64 tall, stage from 64, the four window buttons 48 by 40 at top 0, the logo at 16,16, 103 by 32, the title from 119, maximize and fullscreen and back, the sidebar's ten buttons centred from 64 down, the size line stopping at 80 at the strip's ceiling. One failure, the machine's: `300 ms after the show` read another window over ours, Rotem at work on the same screen; four not run for the same reason (three live text compares, the picker's Escape). Not seen by eye: a screen capture caught Rotem's desktop, not the editor, and was deleted. fmt, clippy, build, test on the same copy. | Done | low | `cd47941` | `git revert` this commit |
| 2026-09-18 | The 64 px bar's release built, not copied | A release built from commit `2a66c2e` archived into `.tmp/release-2a66c2e/`, into the private target folder `.tmp/release-target/`, another session's uncommitted `--paper` edit left out. Not copied: Recon 42768 runs from `host/target/release/`, and the waiting job `.tmp/swap-release-2a66c2e.ps1` was refused to this session. No source changed. | `cargo build --release` finished; `sha256sum` of the build begins `dfbabfe8`. | Built, waiting on Rotem's copy | low | `2a66c2e` | copy an earlier `.tmp/release-target` build back |
| 2026-09-18 | The tab's × out, a right-press menu with Delete | `editor/index.html`: the `.tab .x` button, its rules and its two-press arming removed; `#tab-menu`, one fixed element outside the clipping bar, opened by `contextmenu` on a tab after Main above the bar at the pointer, Delete calling the same `deleteTab`, so undo and redo are unchanged; closed by a press elsewhere, Escape, the window's blur; Main's right press opens nothing and the system menu is kept away. The menu's look is provisional. One press deletes now, where the × took two: the menu is the first step. `editor/editor-checks.js` section 40, `project-os/Design.md` Tab bar, `project-os/QA.md` follow. | `--editor-check` from the working tree, built with `stage0-checks` into `.tmp/dev-target` (`.tmp/tabmenu-check.log`): gaps Main to first and first to second both 8 px, a name ending 10 px before its tab; right press on Main opens nothing; on a tab the menu shows, Delete alone, its bottom left at the pointer, 3 tabs still; a press on the body closes it; Escape closes it; Delete leaves 2 tabs, Main selected, 3 thumbnails, deletion on disk; Ctrl+Z and Ctrl+Shift+Z over the deletion as before. Every check that ran passed, 2 not run, the machine's. First run failed on my own check comparing a fractional left by rounding; compared within 1 px now. Review: no other consumer of `.tab .x`; the drag ignores the right button already; nothing pre-existing found. A real right press by hand is Rotem's. | Done | medium | `2c30ec4` | `git revert` this commit |
| 2026-09-18 | The bar's title line removed | `editor/index.html`: `#title` stays as the bar's empty middle, a drag region that keeps the window buttons right, with its text styles, `titleLine` and `drawTitle` gone; the host's `set_title` untouched. `editor/editor-checks.js`: the title check now wants the line empty and the window's title naming the file. `project-os/Design.md`: the Logo row says so. The `#cfd4da` other parts borrowed by the name "top bar title's" is unchanged in them. | A clean copy of `2c30ec4` plus this change only, `--editor-check` (`.tmp/bar64-check6.log`): every check that ran passed, the bar's line empty, `editor_window_title` still `reference-scene.png - Recon`, the logo at 16,16, the window buttons at the right edge. fmt, clippy, build, test on the same copy. Not seen by eye. | Done | low | `2c30ec4` | `git revert` this commit |
| 2026-09-18 | The tab menu's release built, not copied | A release built from commit `c54e948` archived into `.tmp/release-c54e948/`, into the private target folder `.tmp/release-target/`, another session's uncommitted edits left out. Not copied: Recon 36892 and 47892 run from `host/target/release/`. No source changed. | `cargo build --release` finished; `sha256sum` of the build begins `449eca16`. | Built, waiting on Rotem's copy | low | `c54e948` | copy an earlier `.tmp/release-target` build back |
| 2026-09-18 | The tab menu's lines put back after `533447a` | `editor/index.html` and `editor/editor-checks.js` rebuilt by hand in the shared index as `c54e948` plus the title change only; the other session's next commit, `ebec0dd`, carried them in from there, so the lines are back as of `ebec0dd`. No working file changed: the shared tree held the right text throughout. `project-os/Mistakes.md`: the slip's row. | `git diff --stat c54e948 ebec0dd -- editor`: 6 and 14 lines, the title change alone. The `--editor-check` of the title change ran on `2c30ec4`, before the tab menu; it has not run on this pair together. | Done | low | `533447a` | `git revert` this commit and `533447a` |
| 2026-09-18 | The capture frame's dashes walk rightwards | `host/src/overlay.rs`: `ant_runs` subtracts the phase where it added it, and `phase()` hands out one pixel more with the edge blend's fraction turned round, so the glide between whole pixels runs the new way too. The unit test of one phase step says the new direction. | `cargo fmt --check`, clippy with `-D warnings`, `cargo test --workspace --all-features`: 73 and 4 passed. Not run: the overlay on screen; the direction and the smoothness of the glide are Rotem's eye on the next release. | Pass, screen unseen | low | this commit | Put the `+` back in `ant_runs`, and `phase()` and the test as they were. |
| 2026-09-18 | The release rebuilt from ebec0dd, not copied | A release built from commit `ebec0dd` archived into `.tmp/release-ebec0dd/`, into `.tmp/release-target/`; the archive's `editor/index.html` holds `tab-menu` 7 times and the empty title. It supersedes the `c54e948` build. Not copied: Recon runs from `host/target/release/`. No source changed. | `cargo build --release` finished; `sha256sum` of the build begins `faa98fb5`. | Built, waiting on Rotem's copy | low | `21d85bd` | copy an earlier `.tmp/release-target` build back |
