# Recon — History

The change log. Every completed change lands here, in two layers: a scan table
you always read, and an appendix you read only when digging.

The scan table answers "what happened lately". The appendix answers "what exactly
did that change do, and how do I undo it".

## How you maintain this file

- **After every completed change**, add one scan row AND one appendix row. Both,
  in the same change that did the work.
- **One change = one row.** Not one row per file, not one row per session. A log
  that has to be reassembled from fragments is a log nobody reads.
- Write the scan row for a reader who was not there. Name the behavior that
  changed, not the files.
- Keep appendix rows short — a few lines, not an essay. The full story is in the
  commit diff; a real decision belongs in `project-os/Decisions.md`.
- Record the commit SHA from **before** the change. That is the rollback target.
- Never rewrite or delete a past row. Correct a wrong one by adding a new row —
  an edited log cannot be trusted about anything.
- A `medium` or `high` risk row names its review result under **What was
  checked**: findings found, findings fixed, pre-existing ones flagged. A
  medium-or-higher row without that is a task that is not finished.
- Never write "tested" or "QA passed". Those phrases record nothing. Name the
  input, the screen, and what happened.
- When this file gets long, move the oldest rows into an archive file beside it.
  `project-os/rotate.ps1` does exactly that at `Go commit`, and creates
  the archive the first time it is needed. Rows move **verbatim** — never
  rewritten, never summarized, never merged, because the detail you drop is the
  one the next reader needed. Never hand-move rows: the script dedups, so it is
  safe to run every time, and a hand-move breaks that guarantee.

## Risk scale

The scale's one home is `project-os/Workflow.md` step 2 — read it there, so the
two files can never disagree. State the level at task pickup; the owner's
override wins.

## Scan log

_Older scan rows archived -> see `History-scan-archive.md` (moved by project-os/rotate.ps1, not rewritten)._

Newest at the bottom.

| Date | Area | What changed |
|---|---|---|
| 2026-09-18 | host | **The release with the circle's blue lines on the centre pixel is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-18 | host | **The release with the mode switching by itself is built from commit `38db43c` alone and waits in `.tmp/release-target/`; it is not copied over `host/target/release/recon-host.exe`, since Rotem's Recon runs from there.** The change's own two rows above went in with `9fdcd59`, another session's commit from the shared tree; the change itself is `38db43c`. |
| 2026-09-18 | host | **The release with the circle's lines on the centre pixel was not copied in: when Rotem quit Recon at 20:46 the shared build was already the newer one from `38db43c`, which carries the fix, so the job copied nothing; a job for that newer build waits now for his Recon to exit.** The release path still holds the 48% lines build. |
| 2026-09-18 | host | **Correcting the row above: the first job for the `38db43c` build did not wait, it tried the copy at 20:46:50 while Rotem's Recon was still letting go of the file, and failed; his Recon was back at 20:47:02 on the 48% lines build. The job is started again and waits for that Recon to exit.** |
| 2026-09-18 | host | **The release from `38db43c` is in place, the circle's lines on the centre pixel and the mode switching by itself inside it: copied over `host/target/release/recon-host.exe` at 20:50:08 once Rotem quit Recon, the file reading back identical to the build.** |
| 2026-09-18 | editor | **The top bar is 64 px tall now, from 40, with the Recon logo at its left, 16 px above it and to its left; minimize, maximize, close and the settings chevron keep their 48 by 40 and sit at the bar's top.** Rotem's word. The picture, the sidebar and the timeline's ceiling start 24 px lower with it. |
| 2026-09-18 | host | **The release with the 64 px top bar and the logo is built from commit `2a66c2e` alone and waits in `.tmp/release-target/`; it is not copied over `host/target/release/recon-host.exe`, because Rotem's Recon runs from there and the waiting copy job is refused to this session.** The copy is his one command, after quitting Recon. |
| 2026-09-18 | editor | **A tab no longer carries a ×: a right press on a tab after Main opens a small menu with Delete, which deletes it, and two new tabs now sit as far apart as Main and the first one.** Rotem's word; the hidden × was holding 18 px of empty room in every new tab. Ctrl+Z still brings a deleted tab back. |
| 2026-09-18 | editor | **The top bar no longer writes the picture's name and "Recon" beside the logo; the bar's middle is empty and still drags the window.** Rotem's word. The window's own title, the one the taskbar shows, still names the file. |
| 2026-09-18 | host | **The release with the tab's right-press menu is built from commit `c54e948` alone and waits in `.tmp/release-target/`; it is not copied over `host/target/release/recon-host.exe`, since Recon is running from there.** The copy is Rotem's, after he quits Recon. |
| 2026-09-18 | editor | **Correcting the row above: the commit that removed the bar's title line, `533447a`, also undid the tab menu commit `c54e948` in the two editor files, for one commit; `ebec0dd` has those lines back, and the working files never lost them.** The assistant's slip: its copy of the files was cut before `c54e948` landed, and HEAD was not read again before committing. |
| 2026-09-18 | host | **The capture frame's dashes walk the other way now: rightwards along the top and clockwise round the frame, from leftwards.** Rotem's word. Speed, dash, gap and colours unchanged. |
| 2026-09-18 | host | **The release to use is the one built from commit `ebec0dd`, which holds both the tab's right-press menu and the top bar without its title line; it waits in `.tmp/release-target/`, not copied, since Recon is running from the release path.** It replaces the `c54e948` build, which lacked the title change. |
| 2026-09-18 | Host | The Recon logo is the application's icon: the exe's file icon, the taskbar button and the tray, in place of the grey placeholder square. |
| 2026-09-18 | Host | The release with the logo icon is built from commit `52fbe55` and waits in `.tmp/release-target/`, not copied, since Recon runs from the release path. |
| 2026-09-18 | host | **The release with the bare top bar, the logo alone at its left, and the tab's right-press menu and the logo icon is built from commit `57f9794` alone and waits in `.tmp/release-target/`; it is not copied over `host/target/release/recon-host.exe` while Rotem's Recon runs from there.** The copy is his one command, after quitting Recon. |
| 2026-09-18 | host | **The two blue lines inside the magnifier's circle run on the top and left edges of the pointer's pixel now, crossing at its corner, and the pixel itself is whole; they cut through its middle before.** Rotem's word, with a picture from another tool, after the mid-square fix read his "on the pixel" wrong. |
| 2026-09-18 | host | **The release with the circle's lines on the pixel's edges is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running, and the copy job now tries again while the exiting Recon still holds its file.** |
| 2026-09-18 | host | **The release with the circle's lines on the pixel's edges is in place: copied over `host/target/release/recon-host.exe` at 21:28:32 once Rotem quit Recon, the file reading back identical to the build.** |
| 2026-09-18 | host | **Recon's window opens 1800 wide and 1390 tall now, centred as before; it was 1600 by 1160.** Rotem's word. His display leaves 1392 px of height above the taskbar, so the window fills it nearly whole, and Windows' unseen resize edge may trim a few pixels off the 1390. |
| 2026-09-18 | host | **The release with the window opening at 1800 by 1390 is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-18 | host | **The release with the window opening at 1800 by 1390 is in place, copied at 21:34:37 once Rotem quit Recon and reading back identical to its build; the Recon he opened on it measures 1800 by 1390 inside, nothing trimmed.** |
| 2026-09-18 | editor, host | **The ruler has the capture's zoom circle now: the whole time the ruler tool is in hand it sits beside the pointer showing the picture's own pixels, and while a ruler is dragged out its size sits above the circle, returning beside the ruler's corner at the release.** Rotem's word, both choices his. The circle is a view, not an act, so there is nothing for Ctrl+Z to take back. |
| 2026-09-18 | host | **The release with the ruler's zoom circle is built from commit `a25408f` alone and is in place: copied over `host/target/release/recon-host.exe` at 21:48:12, no Recon running from there, the file reading back identical to the build.** |
| 2026-09-18 | editor | **The left sidebar holds seven tools: Highlight, Copy and Save As are off it.** The highlight is gone whole, Rotem's choice of two: its button, its H key, and any highlight drawn before, which no longer shows on the picture or in a copy or a save. Copy and Save As stay on Ctrl+C and Ctrl+S. |
| 2026-09-18 | host | **The release at `host/target/release/recon-host.exe` has the seven-tool sidebar, built from `4607db4` alone and copied in at 21:52:47, no Recon running from that path.** |
| 2026-09-18 | editor | **The plus button left of the tabs has the tabs' own rounded corners now, 10 px, seen in its fill under the pointer; it was square.** Rotem's word. |
| 2026-09-18 | editor | **The Callout and Rectangle buttons in the left sidebar carry Rotem's own drawings now: a rounded speech bubble with its tail at the bottom, 28 by 25, and a rectangle on 4 px corners, 24 by 22, both a 2 px line in the icon line colour.** Each sits at its true size in the middle of the 32 px icon box. |
| 2026-09-18 | host | **The release with Rotem's Callout and Rectangle icons is built from `e8af5ac` alone, but it is not in place: Recon was running from the release path, and the waiting job that copies it in was refused by the session's permission check.** The copy is one command, Rotem's to run after quitting Recon. |
| 2026-09-18 | editor, host | **The left sidebar has a colour picker now, an eyedropper after Crop, on the I key: the zoom circle follows the pointer with the HEX of the pixel under it above it, a click copies that HEX to the clipboard as text, and the tool is put down.** Rotem's ask, and his two calls: put down after the click, and the zoom circle. It works over a file only viewed and makes no document; nothing for Ctrl+Z to take back. |
| 2026-09-18 | host | **The release with the colour picker is built from commit `fda1383` alone and waits in `.tmp/release-target/`; it is not copied over `host/target/release/recon-host.exe`, since Rotem's Recon runs from there and the copy was refused to this session.** The copy is his one command, after quitting Recon. It carries the Callout and Rectangle icons of `e8af5ac`. |
| 2026-09-18 | host | **The magnifier's panel no longer flickers while capturing: it is put together off the screen, with what is under it, and lands on the screen in one step.** Rotem's report. Before, every move of the mouse first painted the screen back where the panel is and then the panel over it, and the eye caught the moment in between. |
| 2026-09-18 | host | **The release with the magnifier that does not flicker is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** |
| 2026-09-18 | editor | **The Color picker button in the left sidebar carries Rotem's own drawing of a dropper now, 21 by 21 at its true size in the middle of the 32 px icon box.** His report: the old drawing's head sat off the tube's line, by about a pixel. |
| 2026-09-18 | host | **The release with Rotem's dropper icon is built from commit `fecefad` alone and waits in `.tmp/release-target/`; it is not copied in, since the release in place is another session's fast-mode build with the uncommitted #2554FB blue, which this build would take away.** The copy is Rotem's call. |
| 2026-09-18 | host | **A release with both the dropper icon and the uncommitted #2554FB blue is built from the working tree and waits in `.tmp/release-target/`; the copy over the release was refused to this session, so it is Rotem's one command.** His word, continue: built this way so the dropper arrives without taking the blue away. |
| 2026-09-18 | host, editor | **The magnifier's circle, in a capture and in the editor's Ruler and Color picker: its two lines are #2554FB and 1.64 px wide, centred on the grid, and the grid between the pixels is #808080 at 48% over them, where it was each pixel darkened by half. The capture's full-screen guide lines take the same #2554FB and 1.64 px, still at 48%.** Rotem's words, tuned by eye in fast mode: the grid through #A8A8A8, #8C8C8C, #707070 and 64%, the lines through 1.44 px. |
| 2026-09-18 | editor, host | **In the editor, the zoom circle's two blue lines cross at the circle's centre now, as in the capture, where they crossed half a pixel off it; the ruler and the colour picker share the circle. The releases with the plus button's rounded corners and with this fix are both in place.** Rotem's word, with a picture, in fast mode. |
| 2026-09-19 | host | **The release from `46b66cf`, the magnifier's colours and widths, is built from the commit alone; it replaces `host/target/release/recon-host.exe` when Rotem quits the Recon now running.** The fast-mode build in place already carries the same product code. |
| 2026-09-19 | host, project-os | **Correcting the row above: the `46b66cf` release was already in place when it was written, copied at 00:17:02 with no Recon running, reading back identical to the build. And that History commit, `cd09966`, carried another session's staged rows with it: its two rows for the circle's centre, committed as `85b6713`, and a rotation of two of my rows.** Nothing of theirs was lost; the rows are in HEAD. |
| 2026-09-19 | host, editor | **Settings has a second capture shortcut: a Capture 2 row under Capture, empty until one is pressed there, cleared by its ×, starting the same capture as the first.** Rotem's ask, his choice of a second row. No two of the three shortcuts may be the same; the file keeps it as `second_hotkey`. |
| 2026-09-19 | host | **The release with the second capture shortcut is in place: built from `3ac1ea5` plus the page's uncommitted #0C0C0E background, so the look Rotem runs does not step back, and copied at 00:22:49 with no Recon running.** |
| 2026-09-19 | editor | **The app's background is #0C0C0E in the commit now, Rotem's value from a fast-mode burst on 2026-09-17 that never reached a commit; it has been in every release he has run since that evening.** The committed page said #09090C and Design.md #0D0E12; both say #0C0C0E now. The magnifier's panel in a capture, which Design.md fills with the app background, still carries #0D0E12 in the host, left as it is. |
| 2026-09-19 | editor, host | **From the Zoom circle session's fast-mode burst, committed from another session at Rotem's GO COMMIT ALL OPENED: a tool icon in the left sidebar turns #E7E8E9 under the pointer, fading in by A1; the Callout icon is a 2 px outline like the others, the Ruler's rectangle rounder and the Crop's two corners rounded; and a capture can pick Recon's own editor window like any other, only the overlay itself left out.** Rotem's asks in that session; he runs the release built with all of it since 01:50. |
| 2026-09-19 | editor, host | **A fast spin of the wheel zooms the picture under the finger now: the picture already on screen and the notes take each notch at once, a little soft, and the sharp picture lands when the wheel rests, where the picture used to stand still and then jump.** Rotem's report. An opened file with no see-through pixel is also painted without being looked through pixel by pixel at every paint. |
| 2026-09-19 | host | **The release with the wheel's zoom in a fast spin is built from the commit alone, `d094955`, and it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** A job waits for that exit; it copies nothing if the shared build has changed or a Recon from that path is back by then. |
| 2026-09-19 | host | **The demo run of the editor can load an unpacked Chrome extension: with `RECON_EXTENSIONS` naming a folder of extensions, `--editor-demo` opens its window with them, in a profile of its own beside the executable, and stays open.** Rotem's trial of his Rocket Inspector on Recon's own page, then his word to land it. Only in a build with the checks; the product has none of it. |
| 2026-09-19 | host, editor | **The zoom circle with the text above it is one unit now: one piece of code draws it for the capture, the Ruler and the Color picker, so a value changed once changes in all three.** Rotem's ask, his choice of one drawing over one list of values. On it, his values from the same fast-mode burst: 8 px of padding around the circle from 4, the text in Google Sans medium from Segoe UI, and 10 px from the panel's top to the digits and from the digits to the circle, measured from the digits themselves. In the editor the circle shows a moment after the pointer enters the picture. |
| 2026-09-19 | host | **The release with the one-unit zoom circle is in place: built from the commit alone, `ef7a6e2`, and copied at 18:17:01 once no Recon from that path was running, reading back identical to the build.** |
| 2026-09-19 | host | **Rotem's own builds of Recon can load his inspector in the everyday editor window: a build made with the switch `own-extensions`, started with `RECON_PRODUCT_EXTENSIONS` naming the extensions folder, opens the editor with them, in a profile of its own; a build without the switch, the public release, holds none of it.** Rotem's GO, after the costs were put to him. The release he runs is built with the switch from now on. |
| 2026-09-19 | host | **The release with Rotem's inspector is in place, built from the commit alone, `b8f708c`, with the switch, and the inspector was seen working in it: the row above said the product start was not seen, and it was, since no Recon was open when the build landed.** |
| 2026-09-19 | host | **Rotem's hand on the `b8f708c` release: with the variable set for his user, his everyday Recon loads the inspector, and it works.** |
| 2026-09-19 | editor | **A thumbnail on the timeline has no × any more: a right press on it opens the tabs' dropdown with Delete, which deletes it as the × did.** Rotem's ask. The delete itself is unchanged: into the trash for 30 days, an opened file only taken off the timeline, Ctrl+Z bringing it back. |
| 2026-09-19 | host | **The release with the thumbnail's right press and Delete is built from the commit alone, `62a8a41`, with the switch, and is NOT in place yet: Rotem's Recon was running from that path, and the job that waits for its exit was refused by the assistant's permissions, so no job waits.** The build sits in `.tmp/release-target/release/`; it is copied over by hand once Recon is quit. |
| 2026-09-19 | editor, host | **A timeline thumbnail that is not the selected one takes the selected one's border under the pointer, in `#323A43`, fading in over 144 ms, ease-out; the release with it, and with the right press and Delete the row above left out, is in place.** Rotem's ask, tuned by his eye in fast mode: `#21222C`, then `#536070`, `#414C58`, and `#323A43`. The waiting job now runs as a background task of the session, which the permissions allow. |
| 2026-09-19 | editor | **While a crop handle is dragged, the picture's size above the timeline follows the frame, where it used to change only at the release.** Rotem's ask. The cut itself, its undo and the release are as they were. |
| 2026-09-19 | host | **The release with the size following a crop drag is built from the commit alone, `634f2d8`, with the switch, and is NOT in place: Rotem's Recon was running from that path, and the job that waits for its exit was refused by the session's permissions, so no job waits.** The build sits in `.tmp/release-target/release/`; it is copied over by hand once Recon is quit. |
| 2026-09-19 | project-os | **A rule: a build reaches Rotem's Recon without a command from him. The reply asks him to quit Recon, the waiting job, run as a background task of the session, copies the build in, and he reopens.** His correction, after a session handed him copy commands each round; written into `project-os/Workflow.md` step 16. |
| 2026-09-19 | editor | **A click on the sidebar button of the tool already in hand puts the tool down; one more click takes it up again.** Rotem's ask. The keys are as they were: pressing a tool's key again keeps it in hand. Picking a tool was never a step of Ctrl+Z, and putting it down is none either. |
| 2026-09-19 | host | **The release with the second click putting a tool down, and with the size following a crop drag that the `634f2d8` row left out, is built from the commit alone, `6b73225`, with the switch; it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** The job waits as a background task of the session; it copies nothing if the shared build has changed or a Recon from that path is back by then. |
| 2026-09-19 | host | **The `6b73225` release is in place: copied at 19:39:05, the moment Rotem quit Recon, reading back identical to the build.** |
| 2026-09-19 | editor, plan | **Escape with a tool in hand puts the tool down and Recon stays on screen, where it used to hide the window.** Rotem's ask. A note being placed, typed or selected still takes Escape first; with no tool in hand, Escape leaves fullscreen and then hides the editor as before. The plan's key table has the row. |
| 2026-09-19 | host | **The release with Escape putting a tool down is built from the commit alone, `0249c3f`, with the switch; it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** The job waits as a background task of the session, for ten minutes at most; it copies nothing if the shared build has changed or a Recon from that path is back by then. |
| 2026-09-19 | host | **The `0249c3f` release is in place: copied at 19:57:22, the moment Rotem quit Recon, reading back identical to the build. The job outlived the ten minutes the row above gave it.** |
| 2026-09-19 | host | **The editor window's title reads "Recon Editor", so the Windows taskbar's label beside the icon says Recon Editor; with a file open it reads "name - Recon Editor".** Rotem's ask, only where the taskbar needs it: the tray, the file types' "Open with" name, the executable's product name and the data folders still say Recon. |
| 2026-09-19 | host | **The release with the Recon Editor title is built from the commit alone, `f6d97f2`, with the switch, and is not in place: Rotem's Recon was open, and the session's permission check refused the job that waits for its quit.** The release at `host/target/release/` is still the `0249c3f` one. |
| 2026-09-19 | editor | **The timeline along the bottom has no colour and no top line of its own now: the thumbnails sit on the app's background, 16 px under the tabs, and the timeline is 132 px tall by default, from 137.** Rotem's ask. The thumbnails moved up 4 px to make the 16; the tabs and the size stay where they were. The trash keeps its dark red ground. A height remembered from a drag gives thumbnails 5 px taller until the top edge is double-clicked. |
| 2026-09-19 | host | **The release with the timeline on the app's background is in place: built from the commit alone, `46803b4`, with the switch, and copied at 21:00:07 with no Recon running, reading back identical to the build. It carries the Recon Editor title of `f6d97f2`, which was not in place before.** |
| 2026-09-19 | editor | **The band above the timeline, with the tabs and the picture's size, is 64 px tall now, from 48, and both are centred in its height: 16 px above and below the tabs, 24 above and below the size.** Rotem's ask. To keep his 16 px from the tabs to the thumbnails, the thumbnails start at the timeline's top edge, and the timeline is 124 px tall by default, from 132; the picture's area is 8 px shorter. The edge that resizes the timeline now lies over the thumbnails' top 3 px. |
| 2026-09-19 | host | **The release with the 64 px band is built from the commit alone, `9a965af`, with the switch; it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** The job waits as a background task of the session; it copies nothing if the shared build has changed or a Recon from that path is back by then. |
| 2026-09-19 | host | **The `9a965af` release, the 64 px band, is in place: copied at 21:25:43, the moment Rotem quit Recon, reading back identical to the build.** |
| 2026-09-19 | editor | **The picture's size sits 1 px lower in its 64 px band, so its digits are in the band's middle: 26 px above them and 26 below, where Rotem's screenshot had 25 and 27.** His report that the number and the tabs were off centre. Google Sans' digits sit high on their line, so a line centred in the band left them high. The tabs were not moved: in the same screenshot the selected tab has 16 px above and 16 below, and its text 27 and 27. |
| 2026-09-19 | host | **The release with the size's digits in the band's middle is in place: built from the commit alone, `250c868`, with the switch, and copied at 21:33:05, reading back identical to the build.** |
| 2026-09-20 | plan | **The plan has a part 14 now, screen recording as Stage 5: what Rotem would see from the shortcut to the pasted file, the seven calls that are his before anything is built, the route, ten steps with their evidence and models, and the risks.** Rotem's ask. One finding he should hear first: the video type that plays everywhere is commonly limited to 4096 px across, and his whole screen is 5120. Nothing in the product changed. |
| 2026-09-21 | plans | **Screen recording has its complete plan as a file of its own, `plans/2026-09-21-screen-recording-plan.md`, at Rotem's word: what he would see, his three answers, twenty-one open calls put as scenes, the route, the risks, and fifteen steps of which the first six are gates.** Written from eight parallel readers, six of them re-checked by skeptics, and revised after four reviewers' seventy-nine findings. Part 14 of the main plan, its first draft, now says in its first line that it is superseded. Findings that change the picture: Claude takes no video at all; Recon would not start on a Windows N edition once it imports Media Foundation; locking the computer has to end a recording; Escape cannot be Recon's key while recording. Nothing in the product changed. |
| 2026-09-21 | editor | **The X that closes Recon, at the top bar's right, is 2 px bigger: a 12 px mark where minimize and maximize keep theirs at 10.** Rotem's ask, his choice of the two readings. The button's own 48 by 40 box is untouched, so the four window buttons still sit in one row.
| 2026-09-21 | host | **The release with the bigger close X is in place: built from the commit alone, `194b1f7`, and copied over `host/target/release/recon-host.exe` while no Recon was running.** The copy reads back identical to the build.
| 2026-09-21 | checks | **The editor's test run reads the tabs as Rotem tuned them, the plus 12 px in and 16 px tab text, so those two pass again; its maximize step still fails, and the cause is outside Recon's code: the test window is maximized for a fiftieth of a second and then put back as a left-third snap.** Rotem's fix all. The third failure, the timeline over the sidebar, only follows from the window left at the wrong size. Nothing in the product changed. |
| 2026-09-21 | checks | **Correcting the row above: what put the test window on the left third is Rotem's own new tool that splits his screen into three; with it paused, the editor's test run passes whole, maximize included.** Nothing in Recon was at fault and nothing changed. A run of the checks needs that tool paused. |
| 2026-09-21 | editor, project-os | **The record of the fast rounds of 2026-09-20, written at Go commit: the tabs' text is Google Sans medium at 16 px, the plus sits after the last tab, and the tabs' row starts 12 px from the left, on the first thumbnail's line.** Rotem's three asks, each checked by his own eye as it landed; the design file now carries them. |
| 2026-09-22 | host, editor | **A capture takes the mouse pointer with it: when the pointer stood inside the captured area as the shortcut was pressed, it arrives on top of the capture as an element of its own, which a drag moves and the x on hover, or Delete, deletes; Settings has a switch, Mouse pointer in captures, on until switched off.** Rotem's ask, both choices his. The capture's own pixels never hold the pointer; it is in every copy, save and thumbnail until deleted, moving and deleting it join Ctrl+Z, and the switch stays outside it like the rest of Settings. |
| 2026-09-22 | host | **The release with the mouse pointer in a capture is built from the commit alone, `f15823c`, with the switch for Rotem's inspector; it replaces `host/target/release/recon-host.exe` the moment Rotem quits the Recon now running.** The job waits as a background task of the session; it copies nothing if the shared build has changed or a Recon from that path is back by then. |
| 2026-09-22 | host | **The `f15823c` release, the mouse pointer in a capture, is in place: copied at 01:16:32, the moment Rotem quit Recon, reading back identical to the build.** |
| 2026-09-22 | host, editor | **The mouse pointer is part of the captured picture now, embedded where it stood when the shortcut was pressed; the element on top that could be moved and deleted is gone. The frozen screen shows the pointer while the area is chosen, and the switch in Settings still turns it off.** Rotem's word, in place of his choice of the day before. A capture made in between keeps its pixels, which never held the pointer, and no longer shows one. |
| 2026-09-22 | host | **The release with the pointer embedded in the capture is in place: built from the commit alone, `a5cde12`, with the switch for Rotem's inspector, and copied over `host/target/release/recon-host.exe` while no Recon was running.** The copy reads back identical to the build. |
| 2026-09-22 | host, plan | **A capture can take a whole long page: over an area that scrolls, the capture shows a round button with an arrow down near the frame's bottom, and a press on it lets Rotem scroll the page himself while Recon joins what passes into one tall picture, which arrives in the editor as any capture does.** His ask and his two calls: he scrolls, and the 56 px button 24 px up from the frame's bottom. A bar where the button was says what to do and holds Done and cancel; Enter and Escape do the same. The plan's scope line no longer keeps scrolling capture out. |
| 2026-09-22 | host | **The release with the scrolling capture is built from the commit alone, `39c670b`, with the switch, and is NOT in place: Rotem's Recon was running from that path, and the job that waits for its quit was refused by the session's permission check, so no job waits.** The build sits in `.tmp/release-target/release/`; it is copied over once Recon is quit. |
| 2026-09-22 | host | **A scrolling capture's live area is the lit frame, whole: it no longer shrinks to the part of the frame its application says scrolls.** Rotem's bug: pressed on a window in the right part of his screen, the area came up exactly half as tall as the frame. What stands still inside the frame, a title bar or a panel, is in the picture once. |
| 2026-09-22 | host | **The mouse pointer lands in a capture exactly where the mouse was at the selection, the release that ends a drag or the click on a lit area, and no longer where it stood when the shortcut was pressed; it is the ordinary arrow.** Rotem's word, after the first way read as the pointer freezing in place. After a drag the mouse is on the area's corner, so only the part of the arrow inside the area is kept: none of it after a drag down and to the right. |

## Appendix — deep rows

_Older rows archived -> see `History-archive.md` (moved by project-os/rotate.ps1, not rewritten)._

Newest at the bottom, same as the scan log.

| Date | Task | What changed | What was checked | Result | Risk | Commit before | Rollback |
|---|---|---|---|---|---|---|---|
| 2026-09-19 | The `6b73225` release copied in | No file of the repository changed but this row. `.tmp/swap-release-6b73225.ps1`, the session's background task, saw Recon exit and copied the build over `host/target/release/recon-host.exe`. | `.tmp/swap-release.log`: exited 19:39:05, copied 19:39:05, the copy reads back `D1FEEE75...C22056CF`, identical to the build. | pass | low | `9360a06` | copy an earlier build back |
| 2026-09-19 | Escape puts the tool in hand down | `editor/index.html`: the Escape branch of the keydown handler calls `setTool(null)` when `model.tool` is set, after placing, editing and the selection, before fullscreen and the hide. `editor/editor-checks.js`, section 44: one check more. `project-os/Plan.md`: a row in the key table. | `--editor-check`: with the crop in hand, Escape leaves no tool, the button dark, the frame gone and `editor_window_visible` true 400 ms later; every check that ran passed, the idle Escape's hide among them. fmt, clippy, build, 81 tests pass. Review of the diff: the branch is reached only with a tool in hand, the picker included; the tab menu and Settings take their Escape before it; nothing found, nothing pre-existing flagged. A real key press is Rotem's hand. | pass | medium | `f575ed3` | revert the commit |
| 2026-09-19 | The release from `0249c3f`, Escape puts a tool down | `git archive 0249c3f` into `.tmp/release-0249c3f/`, `cargo build --release --features own-extensions` there into `.tmp/release-target/`. Recon, process 53820, ran from the release path, so `.tmp/swap-release-0249c3f.ps1` waits for its exit as a background task of the session. No source changed. | Build exit 0, SHA256 `79541AD3...336FBFAC`. The copy is not seen yet. | Built, waiting for the quit | low | `0249c3f` | copy an earlier build back |
| 2026-09-19 | The `0249c3f` release copied in | No file of the repository changed but this row. `.tmp/swap-release-0249c3f.ps1`, the session's background task, saw Recon exit and copied the build over `host/target/release/recon-host.exe`. | `.tmp/swap-release.log`: waiting from 19:42:54, exited 19:57:21, copied 19:57:22, the copy reads back `79541AD3...336FBFAC`, identical to the build. | pass | low | `3b8ee5f` | copy an earlier build back |
| 2026-09-19 | The taskbar's label reads Recon Editor | `host/src/editor.rs`: the editor window's title at its creation, in the empty state and in `set_title`, plain and after a file's name, with the comment above it. `editor/editor-checks.js`: the two checks that read the window's title expect the new suffix. `host/src/main.rs`, the checks' own window, `tauri.conf.json`, the registration and the tray are untouched. No act a person does, so nothing joins undo. | `--editor-check`, once: `the file name is in the window title` read back `"reference-scene.png - Recon Editor"`, the bar's check on the window's own title passed, and every check that ran passed. fmt, clippy, build, 81 tests pass. The taskbar itself was not looked at: that is Rotem's eye after the release is in place. | pass | low | `71ae57f` | revert the commit |
| 2026-09-19 | The release from `f6d97f2`, the Recon Editor title, built, not copied in | `git archive f6d97f2` into `.tmp/release-f6d97f2/`, `cargo build --release --features own-extensions` there into `.tmp/release-target/`. Recon, process 53780, ran from the release path; the background run of `.tmp/swap-release-f6d97f2.ps1` was refused by the session's permission check as an overwrite, and no other route was tried. No source changed. | Build exit 0 in 25 s, SHA256 `CA0C6438...608D2636`. Nothing was copied, so the release in place is the one before it. | Built, not in place | low | `f6d97f2` | Nothing to undo: the release path is untouched. |
| 2026-09-19 | The timeline's ground and the 16 px under the tabs | `editor/index.html`: `#strip` paints `--paper` and has no `border-top`, so a taller strip still covers what is under it; `STRIP_LINE` gone, `STRIP_TOP` 8 above the cells in `cellRect`, the rows' count, `extentY` and `scrollToCurrent`; `STRIP_DEFAULT` 132. `editor/editor-checks.js` S2.8: the numbers that follow, and one check more. `project-os/Design.md` Timeline, `project-os/QA.md` a row. No act a person does, so nothing joins undo. | fmt, clippy, build, 76 host and 5 pixel tests pass, 2 ignored by design. `--editor-check`, once, log in `.tmp/editor-check-bottombar.log`: the strip rgb(12, 12, 14) on rgb(12, 12, 14), a 0px top line, 16 px from the tabs to the first thumbnail; 132 tall by default, thumbnails 112 ending at 120; 122 by 76 cells at 96; the second row at 432, cell 3 at 12,220; at 500 the strip covers three sidebar buttons, at the ceiling the size. 3 failed elsewhere, 1 not run, none chased: the thumbnails' count read 4 pictures for 3 documents, seen once before in an earlier log; Save As took a save where Escape was sent, with Rotem at the machine, and left `capture 2026-09-19 20-57-01 annotated.png`, a 400x300 test picture, in his Pictures folder; the stand-in window never came to the front. A first run stopped at a syntax slip of mine in the checks file, fixed. Console: Passed: known existing errors only, the save refusals the checks provoke. Review, this diff: 0 found; reach is shared inside the timeline, the documents, the trash, which keeps its own ground, and the rows, all under the S2.8 checks. `--editor-demo` shows no timeline, so the look itself is Rotem's eye. | pass | medium | `1ee3af8` | revert the commit |
| 2026-09-19 | The release from `46803b4`, the timeline's ground | `git archive 46803b4` into `.tmp/release-46803b4/`, `cargo build --release --features own-extensions` there into `.tmp/release-target/`. No Recon from the release path was running, so a plain copy over `host/target/release/recon-host.exe`. No source changed. | Build finished in 29 s, SHA256 `4FB7C73E...10E5349F`; the copy reads back the same, logged in `.tmp/swap-release.log`. | pass | low | `46803b4` | copy an earlier build back |
| 2026-09-19 | The tabs' and size's band at 64 px | `editor/index.html`: `--size-band` 64px, `#image-size` margin 24px, `#tabbar` 16 px above the strip, the ceiling `100vh - 128px`; `STRIP_TOP` 0 and `STRIP_DEFAULT` 124, so the 16 px from the tabs to the thumbnails holds. `editor/editor-checks.js`: the numbers that follow, and the S2.1 size check also reads the band's 64 and the tabs' 16 and 16. `project-os/Design.md` Image size and Timeline, `project-os/QA.md`. No act a person does, so nothing joins undo. | fmt, clippy, build, 76 host and 5 pixel tests pass, 2 ignored by design. `--editor-check`, once, log in `.tmp/editor-check-band64.log`: every check that ran passed; the size at 664-680 between the stage's 640 and the strip's 704; 16 px from the tabs to the first thumbnail; 124 tall by default, thumbnails 112 ending at 112; at the ceiling the size 24 px under the top bar. Console: Passed: known existing errors only, the save refusals the checks provoke. Reach: `--size-band` is read by the stage, the sidebar and the HUD, all under passing checks. The look itself is Rotem's eye. | pass | low | `2be8c79` | revert the commit |
| 2026-09-19 | The release from `9a965af`, the 64 px band | `git archive 9a965af` into `.tmp/release-9a965af/`, `cargo build --release --features own-extensions` there into `.tmp/release-target/`. Recon, process 48404, ran from the release path, so `.tmp/swap-release-9a965af.ps1` waits for its exit as a background task of the session. No source changed. | Build finished in 23 s, SHA256 `D99639C0...C87F8D11`. The copy is not seen yet. | Built, waiting for the quit | low | `9a965af` | copy an earlier build back |
| 2026-09-19 | The `9a965af` release copied in | No file of the repository changed but this row. `.tmp/swap-release-9a965af.ps1`, the session's background task, saw Recon exit and copied the build over `host/target/release/recon-host.exe`. | `.tmp/swap-release.log`: waiting from 21:09:23, exited 21:25:43, copied 21:25:43, the copy reads back `D99639C0...C87F8D11`, identical to the build. | pass | low | `f2a9d80` | copy an earlier build back |
| 2026-09-19 | The size's digits in the band's middle | `editor/index.html`: `#image-size` margin `25px 0 23px`, from 24 and 24. `editor/editor-checks.js`: the size's two checks read 25 and 23, the ceiling 89; the plus button's check measured the band from the size's old 16 px margin, right by coincidence since the 64 px band, and now reads the band itself. `project-os/Design.md` Image size. Nothing joins undo. | Rotem's screenshot read row by row: the band rows 124 to 187, the digits 149 to 160 with a faint row at 161, the Main tab 140 to 171, its text 151 to 160. fmt, clippy, build, 76 host and 5 pixel tests pass, 2 ignored by design. `--editor-check`, twice, log in `.tmp/editor-check-size1px.log`: the first failed the plus button's check, the stale measure above, fixed; the second, every check that ran passed, the size at 665-681 between 640 and 704, the plus at 656 in a band from 640; 3 not run, another window was over the checks' own, Rotem at the machine. The digits' place on a real screen is Rotem's eye. | pass | low | `5fbcae8` | revert the commit |
| 2026-09-19 | The release from `250c868`, the size 1 px lower | `git archive 250c868` into `.tmp/release-250c868/`, `cargo build --release --features own-extensions` there into `.tmp/release-target/`; `.tmp/swap-release-250c868.ps1`, a background task of the session, copied it over `host/target/release/recon-host.exe`. No source changed. | Build finished in 23 s, SHA256 `F424E117...7A87ECE7`; `.tmp/swap-release.log`: copied 21:33:05, the copy reads back the same. | pass | low | `250c868` | copy an earlier build back |
| 2026-09-20 | Part 14 of the plan, screen recording | `project-os/Plan.md`: part 14 appended after the sources, one line in How to read it, one Stage 5 paragraph in part 10 pointing at it. §3.10 untouched, video stays out of v1. No code, no decision taken: R1 to R7 are Rotem's. | The part read against the plan it joins: the capture interface's header in `host/src/capture/mod.rs` and part 6a already leave the video source open; part 11's 41 to 50 ms freeze is the figure the new route is argued from; the display is S0.8's 5120x1440. The documented claims read on Microsoft Learn that day (border switch from build 20348, H.264 encoder's input types and rate control, the decoder's 4096 by 2304); what was not read is listed as assumed until S5.1. No long dash in the file. Docs only, so no cargo check and no build. | pass | low | `2626b04` | `git revert` the commit |
| 2026-09-21 | The complete screen recording plan, in `plans/` | New `plans/2026-09-21-screen-recording-plan.md`, 1101 lines. `project-os/Plan.md`: part 14 opens with a superseded line, and part 10's Stage 5 paragraph names the new file. `project-os/Map.md`: the `plans/` folder in the tree and a row in Ownership. `project-os/Mistakes.md`: one row, for having begun reading the code to build the first step when Rotem had asked for a plan only; nothing was built or changed by that. Research and reviews kept as scratch in `.tmp/rec-research/`, not committed. No code. | The draft read by four independent reviewers, against the research files claim by claim, against `CLAUDE.md` and the main plan, as the engineer who builds from it, and as Rotem; 79 findings, 2 blockers (the drift proof could pass with no corrector; the file sat beside part 14 with nothing saying which one to build from), folded into version 2. `grep` for a long dash in the new file: none. Docs only, so no cargo check and no build. Not checked: nothing in the plan is measured on this machine beyond what Plan.md part 11 and S0.8 already hold; every such item is marked untested and owned by a gate. | pass | low | `2b324e0` | `git revert` the commit |
| 2026-09-21 | The close X 2 px bigger | `editor/index.html`: one rule after `#winbtns svg`, `#winbtns #win-close svg { width: 12px; height: 12px; }`, with its comment. The shared 10 px rule, the 48 by 40 boxes, the settings chevron, minimize, maximize and the modal's own close are untouched. `project-os/Design.md` Window: a Window button icons row. No act a person does, so nothing joins undo. | fmt, clippy, build, 76 host and 5 pixel tests pass, 2 ignored by design. The page opened in the browser pane from the file: the four buttons read 48 by 40 each, the icons 10, 10, 10 and 12, and a screenshot shows the X plainly larger than the minimize and maximize marks beside it. Console: no messages. Side effect named: the X is drawn from a 10 unit viewBox, so its 1 px line scales with it to 1.2 px. The look on a real screen is Rotem's eye, after the release. | pass | low | `4f62e7f` | revert the commit |
| 2026-09-21 | The release from `194b1f7`, the bigger close X | `git archive 194b1f7` into `.tmp/release-194b1f7/`, `cargo build --release --features own-extensions` there into `.tmp/release-target/`. No Recon from the release path was running, so a plain copy over `host/target/release/recon-host.exe`. No source changed. | Build finished in 35 s, SHA256 `F2663EFA...E6C92710`; the copy reads back the same, where the release in place before it was `896CC9CE...C5A2381B`. The PowerShell route was refused twice by the session's permission check; the plain copy went through. | pass | low | `194b1f7` | copy an earlier build back |
| 2026-09-21 | Two stale tab checks fixed, the maximize failure traced | `editor/editor-checks.js`: the plus check reads 12 px in, from 8 (`2626b04`); the tab check reads 16 px text, from 14 (`03ece74`). Check-only code, not in the product, so no release build. Scratch: `.tmp/max-probe.ps1`, `.tmp/win-watch.ps1`. | `--editor-check`, three runs, logs `.tmp/editor-check-fixall-1..3.log`: both tab checks pass in all three; maximize, restore and the strip at 500 fail in all three, and run 3 alone failed the HUD's document count, 3 for the expected number, once, with Rotem's Recon opened meanwhile. The trace: a plain WinForms window, framed and frameless, maximizes to the whole 5120 on this machine; the window library asks for a plain `SW_MAXIMIZE` (tao 0.35.3, `window_state.rs`); watched at 5 ms, the test window shows maximized 5136x1408 with its normal place already rewritten to -7,0 1720x1399, and 20 ms later sits at that place, not maximized, which is Windows' left third on 5120. The host has no resize or placement call. No Windows update since 2026-09-16; no tiling tool found in the process list. The strip check's 0 buttons is the 1390 tall window the failed restore leaves. Not found: who rewrites the place. fmt, clippy, build, 76 host and 5 pixel tests pass, 2 ignored by design. | 2 fixed, 3 open | low | `ad801ad` | revert the commit |
| 2026-09-21 | The maximize failure was the screen-splitting tool | No file changed but this row. Rotem named the cause, a new tool of his that splits the 5120 screen into three, and paused it. | `--editor-check`, once, log `.tmp/editor-check-fixall-4.log`: every check that ran passed; maximize 1280x800 to 5120x1392 and Restore, restored to 1280x800, 3 sidebar buttons under the strip at 500. A first start went to the plain debug build, which has no checks, and ended at once beside Rotem's open Recon; rebuilt with `stage0-checks`. With the tool running, a maximized Recon lands on the left third: whether that is wanted is Rotem's. | pass | low | `aa1b7cc` | nothing to undo |
| 2026-09-21 | The fast-mode catch-up for 2026-09-20 | The three commits `03ece74`, `49a0348`, `2626b04`, one line of `editor/index.html` each: `.tab` font `500 16px/32px "Google Sans"`, `#tab-add { order: 1 }`, `#tabbar { left: 12px }`. Now recorded: `project-os/Design.md` Tab bar, the plus button's place, the tab text, a Placement row. Their two stale checks were fixed in `aa1b7cc`. Left as it is: the check named `Main is right after the plus` reads the page's order, which still holds, while on screen the plus is after the last tab. `host/references/s06/environment.txt`, rewritten by a check run on 2026-09-20, stays out of the commit: its references were not looked at again. `notes/2026-09-19-frames-tool-plan.md`, the plan of Rotem's screen-splitting tool, goes in as it was found. | Rotem's eye per round, fast mode. `--editor-check` on 2026-09-21, `.tmp/editor-check-fixall-4.log`: every check that ran passed, the tabs' among them. fmt, clippy, build, tests pass at this commit. | pass | low | `7edff06` | revert the three commits |
| 2026-09-22 | The mouse pointer in a capture, as an element on top | New `host/src/capture/pointer.rs`: the pointer Windows shows, read beside the freeze and before the overlay, drawn on black and on white to get its alpha, asked for at the display's own pointer size on a scaled display, with Windows' own copy as the fallback. `capture/coords.rs`: where a box on the desktop lands in a selection. `main.rs`: the read, gated by the switch, and the hand-over when any part of the pointer is inside the selection. `editor.rs`: `CapturedPointer`, a slot under the capture's number, `pointer` in the image info, `region://?pointer=<id>` handing the PNG over once, a probe with a pointer for the checks. `config.rs`, `settings.rs`: `capture_pointer` in `recon.json`, on by default, its own command. `editor/index.html`: a shape of kind `pointer` carrying its PNG in the notes, a box in the scene like the ruler's with the ruler's x, taken before the capture's automatic copy, the history starting with it; the Settings row and switch. `editor-checks.js` section 46 and three Settings checks; `Design.md`, `QA.md`, `Map.md`, `Decisions.md`. Undo: a move and a delete are steps of Ctrl+Z like any shape's; the arrival is part of the capture, so nothing takes it off; the switch is Settings', outside undo. | fmt, clippy, build, 81 host and 5 pixel tests pass, 3 ignored by design. `--editor-check` twice, `.tmp/editor-check-pointer-1.log` and `-2.log`: all 17 checks of section 46 and the 3 switch checks pass in both, once on the 5120 display at 100% and once on the 3840 display at 225%, which Rotem's machine changed to between them. Run 1: 4 failed, 3 not run, none the change's: maximize, restore and the strip at 500 with the screen-splitting tool running, and a window over the checks' own. Run 2, at 225%: 10 failed, the scale's and the tool's, not chased. The real read, `cargo test --all-features pointer -- --ignored`: 32 by 32 in 397 us, the arrow white with its dark outline on a clear box, looked at in `pointer-grab.png`; on the 225% display Windows handed over 32 by 32 too, hence the ask at the display's size. NOT CHECKED: that ask itself, since Windows showed no pointer from then on (flags 0), so whether the pointer arrives at the size seen on a scaled display is open; a real capture by hand; the text pointer, kept as solid black; a pointer enlarged in Accessibility. Review of this diff: 2 found and fixed, a step made in the moment before the pointer joined would have been wiped by the history's reset, and a picture address in saved notes is shown only when it is a PNG carried in the notes; reach is shared, `present` and the image info, whose other callers pass none and read null, the shapes' list, which an older page ignores for a kind it does not know, and the settings file, whose other keys the tests show kept; 0 pre-existing flagged. Console: Passed: known existing errors only, the save refusals the checks provoke. | pass, one path unverified | high | `fdd2358` | revert the commit |
| 2026-09-22 | The release from `f15823c`, the mouse pointer in a capture | `git archive f15823c` into `.tmp/release-f15823c/`, `cargo build --release --features own-extensions` there into `.tmp/release-target/`. Recon, process 33112, ran from the release path, so `.tmp/swap-release-f15823c.ps1` waits for its exit as a background task of the session, trying the copy ten times while the exiting Recon still holds its file. No source changed. | Build finished in 34 s, SHA256 `71EAE072...D07800EC`; the release in place is still `F2663EFA...E6C92710`. The copy is not seen yet. | Built, waiting for the quit | low | `f15823c` | copy an earlier build back |
| 2026-09-22 | The `f15823c` release copied in | No file of the repository changed but this row. `.tmp/swap-release-f15823c.ps1`, the session's background task, saw Recon exit and copied the build over `host/target/release/recon-host.exe`. | `.tmp/swap-release.log`: waiting from 00:50:29, exited 01:16:32, copied 01:16:32, the copy reads back `71EAE072...D07800EC`, identical to the build; `sha256sum` of the file in place says the same. | pass | low | `2638313` | copy an earlier build back |
| 2026-09-22 | The pointer embedded in the capture, the element taken out | `host/src/capture/pointer.rs`: `burn_into`, the pointer blended over the frame's pixels where it stood, the frame kept solid. `capture/coords.rs`: `desktop_box_in_frame` in place of the selection's. `main.rs`: the pointer drawn into the frame right after the freeze, before the overlay, gated by the switch as before; `present` takes the frame alone again. Taken out whole by reversing `f15823c` in three files: the `pointer` shape, its box and x, `takePointer`, the host's slot, the `pointer` field of the image info, the `region://?pointer` route, the probe with a pointer and the checks' section 46. Kept: the read from Windows, the Settings switch, `capture_pointer`. `Design.md`, `QA.md`, `Map.md`, a Decisions entry superseding the morning's. Side effect, named: the scrolling capture in another session's uncommitted `main.rs` called `present(tall, None)`; that call reads `present(tall)` in the working file now, and is not in this commit. Undo: a capture is not an act Ctrl+Z takes back, and the pointer is its pixels now, so nothing joins. | Another session's unfinished work fails clippy in the shared tree, so the four checks ran on a clean copy of exactly what was staged, `.tmp/verify-burn2/`: fmt, clippy, build, 82 host and 5 pixel tests pass, 4 ignored by design. `cargo test pointer -- --ignored` there: the real pointer, 32 by 32 at 4463,203 on the 5120 display, drawn into a real frozen screen, 144 pixels changed and none outside its box, every alpha 255; `pointer-in-frame.png` looked at, the white arrow with its dark outline over the page under it. `--editor-check` once from that copy, `.tmp/editor-check-burn-1.log`: the 3 switch checks pass, nothing of the element is asked for; 4 failed, 3 not run, none the change's: maximize, restore and the strip at 500 with the screen-splitting tool running, the HUD's document count, seen once before on 2026-09-21, and a window over the checks' own. NOT CHECKED: a capture by the real shortcut; the pointer's size on the 225% display, since the machine was back at 100%; the text pointer; the overlay and its magnifier showing the pointer, which follows from their painting the frame but was not looked at. Review of this diff: 0 found; reach is shared, the frame handed to the overlay, the magnifier and the crop, all of which only read it, and `present`, whose one other caller in HEAD is the checks' probe; the self test and the measurements freeze for themselves and draw no pointer, so their pixel comparisons are as they were. | pass, three looks Rotem's | high | `2638313` | revert the commit |
| 2026-09-22 | The release from `a5cde12`, the pointer embedded | `git archive a5cde12` into `.tmp/release-a5cde12/`, `cargo build --release --features own-extensions` there into `.tmp/release-target/`. No Recon was running, so a plain copy over `host/target/release/recon-host.exe`. No source changed. | Build finished in 25 s, SHA256 `2880E9D2...CE60D22B`; the copy reads back the same, where the release in place before it was `71EAE072...D07800EC`. | pass | low | `a5cde12` | copy an earlier build back |
| 2026-09-22 | S3.7, the scrolling capture | New `host/pixels/src/stitch.rs`, the stitcher, and `host/src/scrolling/` with `mod.rs` the live part, `detect.rs` the question whether an area scrolls, `draw.rs` the button's and the bar's drawing. `host/src/overlay.rs`: the round button on a lit area that scrolls, its press, `Outcome::Scroll`, the foreground given to the page, the magnifier and the guide lines out of the button's way, the button's place repainted when a glide arrives. `host/src/main.rs`: the new outcome runs the capture and presents its frame; `--stand-in-scroll`. `host/Cargo.toml`: the windows crate's Accessibility feature. `host/src/selftest.rs`: section I, its stand-in, `--only-scroll`, `--scroll-trial`. `project-os/Plan.md` S3.7 and §3.10, `Design.md`, `QA.md`, `Map.md`, two Decisions. Undo: nothing new joins it, the tall picture is a capture and leaves by the capture's own undo. Side effect named: on an area that scrolls, a drag cannot start inside the button's 56 px disc. | fmt, clippy, build, 82 host and 13 pixel tests pass, 4 ignored by design. `--selftest --only-scroll`, passed four times on the final code, log `.tmp/scroll/selftest-14.log`: the button's blue found on the screen, a click handing back the stand-in's client area exactly, the bar 24 px up and absent from a screen copy, Escape handing back nothing, 19 notches and Enter giving 1067x2400, the page pixel for pixel. The whole `--selftest`, twice: the first before the last fixes, A to H passed; the last, `.tmp/scroll/selftest-full.log`, I passed and H failed saying itself that the pointer was not where it was put, a hand on the mouse, not chased. `--scroll-trial` on a long page of my own in an Edge 153 window of its own profile, closed after: the button after 330 ms, 1069 wide with a 15 px scrollbar cut off, sixty notches of 100 px giving exactly 6861 rows, then the rest of the page to its end; the pictures looked at piece by piece, no seam, the sticky header once, the footer once. The looks read from `s02-scrolling-looks.png` and two copies of the button on the overlay. Found on the way and fixed: a stuck bar's rows counted against the distance; a window's round corners read as a scrollbar and left in the seams; the button never painted when its answer came during a glide; Edge never saying a page scrolls. Review of this diff: 0 left open; reach is shared in the overlay, every capture passes through it, and the other paths are touched only behind the button's own conditions, sections C, D, F and H passing with it in. Another session's `--editor-check` ran beside one of my runs and each broke the other's. Not checked: the tall picture in the running editor, a page of tens of thousands of rows, a display at 225%, which the machine showed for the first runs only, an application that ignores all three signs. | pass | medium | `dd74ecd` | revert the commit |
| 2026-09-22 | The release from `39c670b`, the scrolling capture, built, not copied in | `git archive 39c670b` into `.tmp/release-39c670b/`, `cargo build --release --features own-extensions` there into `.tmp/release-target/`. Recon, process 9812, ran from the release path; the background run of `.tmp/swap-release-39c670b.ps1` was refused by the session's permission check as a deploy, and no other route was tried. No source changed. | Build finished in 24 s, SHA256 `4061ED08...B3E70BDE`. Nothing was copied, so the release in place is the one before it. | Built, not in place | low | `39c670b` | Nothing to undo: the release path is untouched. |
| 2026-09-22 | The scrolling capture's area is the lit frame | `host/src/overlay.rs`: the press hands over `hover.rect`; the application's area, its cut to the frame and `SCROLL_AREA_MIN` are gone, the answer only shows the button. `host/src/selftest.rs`, section I: the click has to hand back the stand-in's visible bounds, the bar is measured from them, and the tall picture is read as the frame with the page inside it. `project-os/QA.md`, one Decision superseding a clause of the morning's. BugAtlas: read first, no row matched; a first time, so no row added. Nothing joins undo. | The cause was not reproduced on Rotem's window, which is not known: it is inferred from the one place the area could differ from the frame. `--selftest --only-scroll`, once, `.tmp/scroll/selftest-20.log`: the click handed back 1086x993, the frame, where the scrolling part is 1067x961; the bar 24 px up from the frame's bottom and absent from a screen copy; Escape nothing; Enter 1076x2432, the title bar once, the page pixel for pixel, 10 px of the stand-in's 18 px scrollbar and border cut off, the rest left in. fmt, clippy, build, 82 and 13 tests pass, 4 ignored by design, run on the tree with another session's uncommitted pointer work in it; the commit holds this change's hunks alone and is compiled by the release build. Review: 0 found; reach is local, the button's press only. | pass | medium | `6ae6388` | revert the commit |
| 2026-09-22 | The pointer at the selection | `host/src/overlay.rs`: the mouse's desktop spot at the button's release, kept beside the outcome when a selection is made and taken once by `take_selected_at`. `capture/pointer.rs`: `arrow_at`, the ordinary arrow with its tip on a spot, at that display's pointer size, sharing the sized read with `grab`, which only the tests call now. `main.rs`: the read at the shortcut and the drawing before the overlay are gone; after the overlay returns a selection the arrow is drawn into the frozen pixels at that spot, before the crop, gated by the switch. `selftest.rs`: the drag and the click each check the recorded spot. `Design.md`, which now carries Rotem's name for the moment, the selection; `QA.md`; a Decisions entry superseding where and when. Undo: nothing joins, the pointer is the capture's pixels. | Another session's uncommitted scrolling work sits in `overlay.rs` and `selftest.rs`, so the four checks ran on a clean copy of the last commit plus these edits alone, `.tmp/verify-sel/`: fmt, clippy, build, 82 host and 13 pixel tests pass, 4 ignored by design; only the four files differ from the commit. That session then committed, `e62e7d7`, and the four checks ran again on the shared tree, whose difference from it is this change alone: all pass. `--selftest`, once, from the shared tree, `.tmp/selftest-at-selection.log`: every check this machine can run passed, the drag's spot recorded as (360, 270), the corner released on, and the click's as (625, 465), where it clicked. `cargo test pointer -- --ignored`: the arrow drawn at 200,200 into a real frozen screen, 144 pixels changed, none outside its box. NOT CHECKED: a capture by the real shortcut and the look of the result; the arrow's size on the 225% display. Review of this diff: 0 found; reach is shared, the overlay's release handler, where the scrolling button returns before the new lines and a cancel clears the spot, and `main.rs`, where the scrolling outcome and a cancel draw nothing; the frame is still only read by the overlay, since the drawing now comes after it. | pass, the look is Rotem's | high | `e62e7d7` | revert the commit |
