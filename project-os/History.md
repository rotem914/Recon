# Recon — History

The change log. Every completed change lands here, in two layers: a scan table
you always read, and an appendix you read only when digging.

The scan table answers "what happened lately". The appendix answers "what exactly
did that change do, and how do I undo it".

## How you maintain this file

- **After every completed change**, add one scan row AND one appendix row. Both,
  in the same change that did the work.
- **One change = one row.** Not one row per file, not one row per session. A log
  that has to be reassembled from fragments is a log nobody reads.
- Write the scan row for a reader who was not there. Name the behavior that
  changed, not the files.
- Keep appendix rows short — a few lines, not an essay. The full story is in the
  commit diff; a real decision belongs in `project-os/Decisions.md`.
- Record the commit SHA from **before** the change. That is the rollback target.
- Never rewrite or delete a past row. Correct a wrong one by adding a new row —
  an edited log cannot be trusted about anything.
- A `medium` or `high` risk row names its review result under **What was
  checked**: findings found, findings fixed, pre-existing ones flagged. A
  medium-or-higher row without that is a task that is not finished.
- Never write "tested" or "QA passed". Those phrases record nothing. Name the
  input, the screen, and what happened.
- When this file gets long, move the oldest rows into an archive file beside it.
  `project-os/rotate.ps1` does exactly that at `Go commit`, and creates
  the archive the first time it is needed. Rows move **verbatim** — never
  rewritten, never summarized, never merged, because the detail you drop is the
  one the next reader needed. Never hand-move rows: the script dedups, so it is
  safe to run every time, and a hand-move breaks that guarantee.

## Risk scale

The scale's one home is `project-os/Workflow.md` step 2 — read it there, so the
two files can never disagree. State the level at task pickup; the owner's
override wins.

## Scan log

_Older scan rows archived -> see `History-scan-archive.md` (moved by project-os/rotate.ps1, not rewritten)._

Newest at the bottom.

| Date | Area | What changed |
|---|---|---|
| 2026-09-10 | plan | **The plan is corrected where it was guessing.** The elevated-window hotkey claim is withdrawn to something to verify, the latency harness gained the mark it was missing, the coordinate contract became four named spaces, the output test became three separate checks, and Stage 0 shrank to one capture path and two real destinations. Callout numbers keep their gaps in every output. |
| 2026-09-10 | plan | **The plan is approved for Stage 0, and the product baseline now lives in the repository.** The four corrections landed: the two capture-path fallback lists agree, the typing check compares the live editor against the copied image, task pickup names the product plan and says which document wins where they differ, and every finding carries a status instead of an open checkbox. |
| 2026-09-10 | plan | **Stage 0 has nothing left to wait for.** The two paste destinations are Claude and ChatGPT, which also settles the clipboard question: both read it as a web application does, so PNG is the format that decides acceptance. |
| 2026-09-10 | plan | **Recon is now an image viewer as well as a capture tool, and both plans say so.** Opening a file is a normal way in, one window carries a viewing mode behind an explicit Annotate action, format support is stated per format instead of as common images, and an external file is never modified or imported by being looked at. |
| 2026-09-10 | plan | **There is one plan file now, and only one.** `project-os/Plan.md` holds the product, the behavior, the invariant, the architecture, the decisions and the stages in thirteen parts; `Product_plan.md` is gone and its content is inside. Splitting a plan across two documents is now a rule against it, in CLAUDE.md rule 13. |
| 2026-09-10 | plan | **The viewing and persistence contracts are now defined, and one of them was a real defect.** Reopening an annotated file resumes its one document, Save As can no longer overwrite anything, the displayed animation frame is a proved requirement rather than a figure of speech, an SVG's raster size is fixed when annotation begins, orientation and color are tested through the whole cycle, and the navigation context has four activation rules. |
| 2026-09-10 | plan | **The corrections are consistent now, and the plan comes to you unasked from here on.** Six leftovers were found and fixed: the navigation keys still walked "captures" in three places, the JPG row still applied orientation at display time, S0.7 never recorded the display gamut its own revisit trigger depends on, and part 6c justified deferring TIFF with a reason the rest of the plan contradicts. |
| 2026-09-10 | plan | **The technology is now chosen on its merits, and choosing it exposed a defect in the plan.** A Rust host owns the pixels and a web view lays out the text; the two-provider decode split is gone, because a preserved image created inside the renderer cannot survive a premultiplied canvas byte for byte. The familiarity argument is struck, the three open format decisions closed, and Stage 0 gained a boundary measurement before anything is built on it. |
| 2026-09-10 | plan | **The stack is a candidate again, and three real defects are fixed.** A fit-to-window proxy could never have served 100% zoom, the stated text-direction override did not exist in the mechanism named for it, and the annotation export was sized to cut off anything in the added margin. The stack argument also overstated what the alternatives cannot do, so part 5 now names what is not established and carries a comparison, and part 8 can reject the design. |
| 2026-09-10 | host | **Recon exists as a process now: it sits in the tray and answers a global hotkey with no window open.** S0.1 is built and two of its three checks are proved in a log: the hotkey fired twice with no window ever created, and a second instance reported the clash instead of dying quietly. The third, quit from the tray menu, needs one click that no automation route here could drive. |
| 2026-09-10 | host | **S0.1 is closed: the tray quit works and leaves nothing behind.** Rotem started the host himself, fired the hotkey with a real keypress, and clicked Quit; the process logged the quit and exited, and no recon-host and nothing else from the project tree remained. |
| 2026-09-11 | host | **Recon can take a screenshot now: hotkey, frozen screen, dimmed overlay, drag a region, and a PNG comes out.** Escape cancels and captures nothing, a second hotkey during a selection is ignored, and an open menu lands in the freeze. The self test caught the important defect: the process was DPI-unaware, so a 3840x2160 display at 225% was reporting itself as 1707x960 and every coordinate was quietly wrong. |
| 2026-09-11 | plan | **S0.2 is closed, with the two-display check deferred instead of pretended.** Rotem has one display, so a negative desktop origin and a mixed-scale layout cannot be produced here. The arithmetic for both is unit-tested; the platform half becomes a watch item for the first time a second display is ever attached. |
| 2026-09-11 | host | **The boundary is measured, and it says the pinned design was the right one for a different reason than the plan gave.** A whole frame crosses into the web view in 290 ms, the proxy the design pins in 40 to 74 ms. The figure the design was argued against was two to four times pessimistic. A local socket is about twice as fast as the message channel for large payloads, and the annotation layer should cross encoded: 162 KB and 3.5 ms against 31.6 MB and 359 ms. |
| 2026-09-11 | editor | **Recon has an editor: an image on a canvas, and a callout you can place, type into, move, re-anchor and delete.** Twenty-six checks pass, including the two that matter: a wrapped line lays out at exactly the same height at every zoom, and a one-pixel checkerboard is still a checkerboard at actual size after a fit view and a pan. The pre-created hidden window's first frame is current, proved by looking at the screen rather than asking the page. Two open findings came out of it: the fit view costs a second, and the window is not DPI-scaled. |
| 2026-09-11 | editor | **Looking at the editor found two things twenty-six assertions did not.** An image smaller than the window sat in the corner rather than the middle, now fixed and centred with the callout layer following it; and a note is unreadable at fit zoom on a wide capture, which is a product question rather than a defect. |
| 2026-09-11 | editor | **A note's text size is now yours: 20 pixels by default, and `Ctrl +` or `Ctrl -` changes it.** The size belongs to each note, and the size you used last is what the next note gets; the bubble's padding and number badge scale with it. That closes the question from looking at the editor: there is no minimum on-screen size, because a floor would make the editor and the exported file disagree. |
| 2026-09-13 | review | **Everything built so far was reviewed as a whole, and the direction holds.** Twelve findings, none blocking: the four Stage 0 pieces have never run as one, every figure in the plan is a debug-build number, S0.5 cannot close as written because its cycle tests need the export and the store, and a two-line note probably loses its line break on commit. The document is `notes/2026-09-13-review.md`, with a recommended step order and the verdicts left to Rotem. |
| 2026-09-13 | project-os | **The rule files describe the real project now, the plan has a new step before S0.5, and every step names Fable or Opus.** CLAUDE.md says what Recon is and its three setup blocks are filled from the project's own practice; Map.md names the stack and the checks; the two calibration files carry the worst bug class and rows seeded from the four steps' real defects. Plan: S0.4b wires the islands, fixes the fit view and re-measures in release; S0.5's cycle legs are carried to S0.6 and S1.8; the export spike runs first. Conversations.md rule 19: the Next step names its model. |
| 2026-09-13 | host, editor | **Recon works end to end now: hotkey, drag, and the capture is in the editor 13 ms later.** The editor is created hidden at startup on the product path and shown with the capture; a fit view is 47 ms instead of 1.1 s, because the resample was being compiled unoptimised and now runs from a pyramid in an optimised crate; one worker serves only the newest region request; a two-line note keeps its line break; the check-only commands are out of the product build; every figure in the plan has a release column, which closed the PNG encoder finding. |
| 2026-09-13 | editor, host | **The export route works on the shipping web view: the notes leave the editor as a layer, the host lays it over the untouched capture, and every pixel the notes did not touch is exact.** The one unspecified behaviour the design leaned on, reading a canvas back after drawing the serialized text layer, is clean on WebView2 152.0.4191.66. The whole export of a 5120x1440 capture takes about 65 ms in release. |
| 2026-09-13 | host, editor | **Recon opens files now: nine formats through one door, and a file is only ever read.** PNG, JPEG, BMP, GIF, WebP, TIFF and SVG decode with their orientation applied once, their colour profile converted once, their transparency kept and their frames or pages reachable by index; the editor shows an opened file and steps through it. Every test file hashed the same after being opened. HEIC and AVIF wait on a real file each, and AVIF's decoder is Windows' own rather than the one the plan named, which is a question for Rotem. |
| 2026-09-13 | host | **S0.5 is closed on twenty-eight real files from public test sets, and two of them found what a generated file could not.** HEIC and AVIF decode here, a rotated AVIF comes out upright, a real animated WebP steps through twelve frames, real Figma and Illustrator exports render like the web view. Windows' decoder was ignoring embedded colour profiles, fixed by reading the profile and converting it like every other route; and it hands back one frame of an animated AVIF, which sharpens the AVIF question for Rotem. |
| 2026-09-13 | plan | **AVIF is decided: it decodes through the Windows imaging stack, like HEIC, with the named-codec message as its degraded state and a first frame for an animation.** Rotem delegated the call; the alternative was a C toolchain on every machine that builds Recon, for an animated format daily use has not met. Recorded in Decisions, part 6c, §3.7, F51 and F53. |
| 2026-09-13 | host, editor | **S0.6 is built and closed: the composer, the clipboard, and the three fidelity checks, with the live editor and the copied image pixel-identical mid-typing.** A source pixel a note does not cover is exact in every output, alpha included; six reference documents are committed with their environment; a note inside a margin exports at its offset; the clipboard carries PNG, CF_DIBV5 and CF_DIB, and both Claude and ChatGPT took the paste. Ctrl+Shift+C copies. |
| 2026-09-13 | host, editor | **S0.7 is measured and closed: from the hotkey the overlay is usable in at most 87 ms, and from the selection the editor is usable in at most 134 ms, over thirty clean runs.** Memory has a floor of about 170 MB, nearly all the hidden web view, and does not grow across thirty captures or a twenty-eight-file walk. Six marks per run on one clock, a CSV per run set, startup measured apart. |
| 2026-09-13 | host, plan | **S0.8 is closed and Stage 0 is done: no part 8 row fired, and the recommendation is to go to Stage 1 on this stack.** HDR is now detected and logged at startup and at every freeze; the main display turns out to be wide gamut, which fires the sRGB decision's own trigger (F60); focus returns after Escape and a vanished window does no harm; the elevated-hotkey case waits on one key press by Rotem (F62). |
| 2026-09-13 | plan | **F60 is decided: a capture stays sRGB on a wide-gamut display, because the captured numbers are what the application drew.** The revisit trigger moves to a colour-managed desktop, which the product now detects. Rotem delegated the call. |
| 2026-09-13 | plan | **F62 is measured: the capture hotkey fires with an elevated window in front.** Rotem pressed it with Task Manager in front; the release product logged the fire, the overlay came up, and a selection reached the editor. Stage 0 has no open question left but the tray floor. |
| 2026-09-13 | host, editor, docs | **The whole repository reviewed after Stage 0, twelve findings, the seven that could bite fixed.** The one that mattered: clicking from a note being typed straight into another note lost the first note's text; a switch now commits it, and a check guards it. A superseded region answer no longer logs an error, the copy key works under a Hebrew layout, an opened still is no longer held twice, and the product build is warning-free. Five nits stay open with their homes. |
| 2026-09-14 | host, editor | **S1.1 is built: a new capture keeps the previous one and its notes, the editor hides on close or Escape and hands the focus back to the application the capture began in, the tray opens the editor, and the empty state names the hotkey.** The web view got its security policy and the region scheme answers the page's origin only. Run on a 225% display for the first time: the canvas offset now lands on a physical pixel (F74), and the antialiasing figure is recorded (F75). |
| 2026-09-14 | host, editor | **S1.2 is built up to the hand: every way in reaches the one window.** A file on the command line opens at startup, a second instance hands its file to the running window and exits, a dropped file opens, and Ctrl+O opens Windows' own picker. Registration lists Recon in "Open with" and in Default apps under the user and takes no default; running it, and the Explorer checks, wait for Rotem. |
| 2026-09-14 | host, editor | **S1.3 is built: the wheel pans, Ctrl with the wheel zooms around the pointer, F11 enters and leaves fullscreen, and the file's name is in the window title.** The rest of the viewing surface was already there from Stage 0. |
| 2026-09-14 | host, editor | **S1.4 is built: previous and next walk the opened file's folder in Explorer's own order, with the position and the context on screen.** The listing is read once, the ends stop, and a file that vanished since is skipped. |
| 2026-09-14 | plan | **S1.2 is closed by Rotem's hand: he ran the registration, a double-click in Explorer opened the image in Recon, and a dropped image replaced it.** |
| 2026-09-14 | host, editor | **S1.5 is built: a file opens in viewing, where a click creates nothing and a drag pans; A switches to annotation, which creates one managed document for the file or resumes it, never two; leaving annotation keeps the work.** A reopened file shows the file as it is, with the route to its edit; resuming shows the document's own preserved pixels. |
| 2026-09-14 | editor | **The Annotate control is both: the A key and a button at the top right, at Rotem's call.** |
| 2026-09-14 | editor | **S1.6 is built: a new note lands on the first free place around its anchor, in a fixed order; a note that cannot fit takes a neutral margin that goes when it is no longer needed; numbers keep their gaps in the output; the pan reaches the margin at any zoom.** |
| 2026-09-14 | editor | **S1.7 is built: every stage-1 key of the contract does what §3.6 says, the viewing keys are inert while a note is typed, and undo and redo walk the notes' history one operation at a time.** Copy and Return copies, then hides unless the document changed meanwhile. |
| 2026-09-14 | host, editor | **S1.8 is built: every document is on disk from its first moment, one folder each with the image written once and the notes saved as they are typed, the latest reopens at startup, and a failed save says so.** The document number is its creation time, so it survives a restart. |
| 2026-09-14 | host, editor | **S1.9 is built: the same keys walk Recon history, oldest to newest, captures and annotated files alike, with the position named; a capture or a document shown from history activates it, opening a file activates the folder, and Annotate changes nothing.** |
| 2026-09-14 | host, editor | **S1.10 is built: Copy and Return commits, copies, saves and only then hides, and every failure is a line on screen with the work kept: a held clipboard, a refused save, a change mid-copy, a target closed or absent, a focus the system refused.** |
| 2026-09-14 | host, editor | **S1.11 is built: Ctrl+S opens Windows' Save As on the last export folder with a name derived from the source and marked as annotated, writes a new file only, and offers a free name when the chosen one exists.** The notice under the HUD is state now, so a redraw cannot wipe it. |
| 2026-09-14 | plan, backlog | **Rotem's hand closed the Stage 1 path: a capture, Annotate, a note, Copy and Return, Save As, a restart and the document back from the tray. F79, F81, F82 and F83 go to the Backlog at his word.** |
| 2026-09-14 | host, editor | **S2.1 and S2.2, pulled forward at Rotem's word: a timeline of thumbnails along the bottom, one per document, a click showing it; a click on the tray icon opens the editor.** Stage 2's first steps are in the plan; the trial waits on S2.3. |
| 2026-09-14 | host, editor | **S2.3 and S2.4: Copy and Save As are buttons beside the mode as well as keys, and the HUD says what the store holds.** Rotem's first-day window was the midnight release, before those keys existed; the release is rebuilt at its path. |
| 2026-09-14 | plan | **Stage 2's remaining steps are in the plan: deletion, which waits on Rotem's call, and export beyond PNG.** The run stops at the first decision that is his. |
| 2026-09-14 | host, editor | **S2.5 is built at Rotem's calls: a document is deleted by the × on its thumbnail or by Ctrl+Delete, into Recon's own trash, swept for good after thirty days; the neighbour shows, the last one leaves the editor empty.** |
| 2026-09-14 | host | **S2.6 is built: Save As offers JPEG beside PNG, encoded by the chosen name's extension at a fixed quality, through the same never-overwrite path.** |
| 2026-09-14 | host, editor | **S2.7 is built: the timeline ends with a Trash chip when something was deleted, it opens the trash in the strip, and Restore brings a document back with its notes.** |
| 2026-09-14 | plan | **Rotem's hand on the new release: the timeline takes every capture, Save As works, Copy works.** |
| 2026-09-14 | editor | **S3.1 is built: a tool in hand, callout by default; L or the Arrow button, then a drag draws an arrow that is selected, moved, deleted, undone, saved and exported like a note.** Stage 3's four steps are in the plan. |
| 2026-09-14 | editor | **S3.2 is built: R draws a rectangle and H a highlight by the same drag, selected, moved, undone, saved and exported like the arrow.** |
| 2026-09-14 | editor | **S3.3 is built: T, then a click, types plain words where it was clicked, no number, no bubble, no arrow, and the notes' numbering is untouched.** |
| 2026-09-14 | host, editor | **S3.4 is built: B, then a drag, blurs a region, on screen through a box over the picture and in the output by the composer on a copy of the source, never the document's own pixels.** Stage 3's four tools are in. |
| 2026-09-14 | plan, backlog | **Everything to do with Rogers, Stage 4 whole, goes to the Backlog at Rotem's word.** |
| 2026-09-14 | host | **Launching Recon opens its window like any application; the tray, the hide on close and the tray's Quit stay as they were.** |
| 2026-09-14 | host | **Closing the window hides it at once; the host no longer waits a second on the page, which could not answer while the host waited.** Quit exits when the page has saved, or after a second and a half. |
| 2026-09-14 | host | **During a capture the window under the pointer is lit and framed, and a click with no drag captures it; a drag still draws its own rectangle.** The windows are listed once when the overlay comes up, a window's visible frame is what gets lit, cut to the pointer's display, and the click is told from a drag by the system's own drag threshold. Found in passing, and pre-existing: a copy of the screen with the overlay up shows the frame but not the dim, with the committed overlay too. |
| 2026-09-14 | host | **The capture veil darkens the screen now.** It was drawn all along, but at 45% opacity in the window's own alpha channel, and the compositor showed the identical live desktop through it, cancelling it exactly; Rotem confirmed the screen looked normal. The one black pixel the veil is stretched from now carries a solid alpha. |
| 2026-09-14 | plan | **Rotem's hand on the 19:05 release: the screen darkens, the window under the mouse is lit, and it works nicely.** |
| 2026-09-14 | host | **The selection follows the part of the window under the pointer, the way Snagit does: on a browser's page it is the page area without the tabs and the address bar, on a plain window it is the window.** A window's parts are its child windows at every depth, listed the first time the pointer rests on it; the smallest one under the pointer wins, cut to the window's frame and the display. Firefox draws its page without a child window, so there the whole browser is what gets lit. |
| 2026-09-14 | plan | **Rotem's hand on the 20:13 release: the frame sits on Chrome's page alone, and he called it perfect.** |
| 2026-09-14 | plan | **Firefox needs no support, at Rotem's word: the whole browser lit there is final, and nothing further is planned for it.** |
| 2026-09-14 | host | **The frame around the lit area is dashed now and walks slowly: light dashes and dark gaps, six pixels each, one pixel every 120 ms.** Only the frame's four strips are repainted on a tick, so the walk costs nothing to speak of, and a dash turns a corner instead of restarting. |
| 2026-09-14 | host | **The frame is tuned to Rotem's numbers: 2 pixels thick, blue #2D41D7 dashes of 12 on dark gaps of 8, one pixel every 256 ms.** The band sits just inside the lit area, the top and bottom bands taking the corners. |
| 2026-09-14 | host | **The frame's dashes are #00B9F7 now, at Rotem's word.** |
| 2026-09-14 | host | **The dashes walk at 1.48 pixels a second now, one pixel every 676 ms, at Rotem's word.** |
| 2026-09-14 | host | **The dashes walk at 8 pixels a second now, one pixel every 125 ms, at Rotem's word: at 1.48 they did not seem to move.** |
| 2026-09-14 | host | **The dashes glide now instead of stepping, at 12 pixels a second: the walk is read off the clock about sixty times a second, and the pixel at every dash edge is blended by the fraction in between.** Rotem saw the pixel steps at 8 a second and asked for smooth. |
| 2026-09-14 | host | **The dashes glide at 24 pixels a second now, at Rotem's word.** |
| 2026-09-14 | host, editor | **The window's top bar is Recon's own now: Windows' frame is gone, and the page draws a dark bar with the file's name on the left, the tool buttons in it, and minimize, maximize and close on the right.** The bar drags the window, a double-click on it maximizes, close hides as it did, and fullscreen puts the bar away. Rotem's call, between recolouring Windows' bar and drawing one. |
| 2026-09-14 | editor | **The top bar has no colour of its own and no line under it now: it sits on the page's own background.** Rotem's word, by eye, on the first look. |
| 2026-09-14 | editor | **The timeline runs newest first now: the latest picture is at the left.** Rotem's word. |
| 2026-09-14 | editor | **The app's background is #111217, at Rotem's word.** The page ground behind the picture and the panels changed from #101214; nothing else moved. |
| 2026-09-14 | editor | **A picture opens with no tool in hand now: the ordinary pointer over the whole window, and a click on the picture creates nothing until a tool is chosen.** A chosen tool works as before, crosshair included, until the next picture is shown; viewing shows the ordinary pointer too. Rotem's call. |
| 2026-09-14 | editor | **The wheel zooms in and out around the pointer now, with Ctrl or without; only a sideways wheel pans.** Rotem's call. In annotation the arrow keys are the pan that is left. |
| 2026-09-14 | plan | **The timeline has a spec for scale and for growing: S2.8, unbuilt.** Today the strip fetches every thumbnail at startup and keeps every one, so memory grows with the library against §3.8; the spec answers with a windowed strip that holds a screenful, no View More button, and a drag on the strip's top edge that enlarges thumbnails to 320 wide and then wraps rows, with the thumbnail file made at 320 once and served at the size shown. |
| 2026-09-14 | editor | **With no tool in hand a drag on the picture pans it now, and holding Space gives any tool the ordinary pointer and the same pan; letting go gives the tool back.** Space typed into a note is still a space. Rotem's call. |
| 2026-09-14 | host, editor | **S2.8 is built: the timeline holds a screenful of cells whatever the library holds, and its top edge drags it taller, the thumbnails growing to 320 wide and then wrapping into rows.** Cells are placed by arithmetic and only the ones near the screen exist, their pictures dropped when they scroll away, at most eight fetched at once; 96 is the floor and the default, 216 one row of 320 by 200, then whole rows up to 60% of the window, a double-click resets, the height is remembered. The host makes the thumbnail file at 320 by 200 and remakes an old 160 one once. Thirty thousand documents scan in about two seconds at startup, so the whole-list read stays, with the number in the plan for Rotem's call. |
| 2026-09-15 | host | **Recon can start with Windows now: a "Start with Windows" tick in the tray menu, and started that way it waits in the tray with no window.** The tick writes one value under the user's own Run key and unticking removes exactly that; a start by hand still opens the window. Rotem's ask; the tray-only start at logon is the assistant's call, his to veto. |
| 2026-09-15 | host | **The release at `host/target/release/recon-host.exe` carries the Start with Windows tick now, built from the commit alone.** Rotem's pick of the two next steps. |

## Appendix — deep rows

_Older rows archived -> see `History-archive.md` (moved by project-os/rotate.ps1, not rewritten)._

Newest at the bottom, same as the scan log.

| Date | Task | What changed | What was checked | Result | Risk | Commit before | Rollback |
|---|---|---|---|---|---|---|---|
| 2026-09-14 | The window pick and the veil, by Rotem's hand | `project-os/History.md`: this record. The release was rebuilt at 19:05 after Rotem quit his running Recon, which had held the 18:00 file. | Rotem launched it, pressed Ctrl+Shift+4, moved over a window: the screen darkened around it, the window was lit, and he called it working nicely. Risk low, a record. | Pass | low | `b1d843e` | Revert the file to `b1d843e`. |
| 2026-09-14 | The part under the pointer | `host/src/overlay.rs`: `parts_of_window` and `collect_part` list a window's visible child windows at every depth with their rectangles, cached per window in `State.parts` on first hover; `smallest_at` picks the smallest part under the point; `update_hover` cuts it to the window's frame, then the display, and lights the whole window when no part is under the pointer; `Hover.part` and `class_of` name it in the log. `host/src/selftest.rs`: the stand-in carries one static child at `STAND_IN_PART`; section H clicks near the corner for the whole window, on the child for the part, read from `ClientToScreen` and the known offsets, then drags. `project-os/QA.md`: the overlay row names the part. Plan: §3.2 says so. Measured first: a probe listed the running Chrome's child windows, and the page area is one of them, "Chrome_RenderWidgetHostHWND", 1705x1270 on the left display, the exact rectangle Snagit's marquee showed; it is click-through by style, so parts are not filtered on that, unlike top-level windows. | fmt, clippy with -D warnings and --all-features, both builds with no warning, 48 unit tests with one new on the smallest part. `--selftest` A to H passing: the corner click returned the stand-in's 466x313 frame, the click on the child returned its 200x100 rectangle at the place computed from the window manager, and the drag returned 60x40. The release could not be rebuilt: Rotem's Recon holds the file. Risk medium, review of this diff: the part list is read per window on first hover, off the hotkey path; a part that reaches past the frame is cut to it; a window without parts behaves exactly as before, which the corner click proves; the drag and Escape paths do not read parts. Not covered: a browser that draws its page with no child window, Firefox by the plan's own caution, lights whole. | Pass | medium | `e439ef5` | Revert the two files and the three docs to `e439ef5`. |
| 2026-09-14 | The part pick, by Rotem's hand | `project-os/History.md`: this record. The release was rebuilt at 20:13 after Rotem quit his running Recon. | Rotem launched it, pressed Ctrl+Shift+4, moved over a Chrome page: the frame sat on the page alone, and he called it working perfectly. Risk low, a record. | Pass | low | `49f5837` | Revert the file to `49f5837`. |
| 2026-09-14 | Firefox: no support | `project-os/Decisions.md`: a dated line under the part-pick entry closes its open route. | Docs only, read back. Risk low. | Pass | low | `543ef7a` | Revert the two files to `543ef7a`. |
| 2026-09-14 | The marching frame | `host/src/overlay.rs`: `ant_runs` lays one-pixel runs around the frame, clockwise as one path, light or dark by `ANTS_DASH` and the phase; `ring_rects` is the frame's four strips; a `SetTimer` per surface at `ANTS_STEP_MS` and `WM_TIMER` advance the phase and invalidate only the strips when that surface has a selection; the paint draws the runs that meet the dirty region with the light brush or the new dark `ink`, in place of `FrameRect`. Dash colours and lengths, the step and the dark gap are mine, named in the reply for Rotem to tune. | fmt, clippy with -D warnings and --all-features, both builds with no warning, 50 unit tests with two new: the runs cover the frame exactly once and alternate every six pixels corners included, and one phase step moves every pixel's ink to its neighbour's. `--selftest` A to H: passed on the second run; on the first, F reported the foreground after Escape on a stand-in other than the one it opened, and passed unchanged a minute later, so it is written down as a flake and not as green. The section H picture, read pixel by pixel along the frame's top edge: 240 six times, 32 six times, repeating; the magnified crop shows the dashes turning the corners. Motion itself is proved by the unit test and the timer, not by a picture. Risk medium, review of this diff: the timer touches state only through `client_selection`, so a surface with nothing lit repaints nothing; the paint's dirty test skips runs outside the strips; the brushes are deleted in teardown with the rest; the drag and pick paths are untouched. | Pass | medium | `630acfc` | Revert the file and the docs to `630acfc`. |
| 2026-09-14 | The frame tuned by eye | `host/src/overlay.rs`: `ANTS_WIDTH` 2, `ANTS_DASH` 12, `ANTS_GAP` 8, `ANTS_STEP_MS` 256, the dash brush #2D41D7; `ants_width` caps the band for a tiny selection; `ring_rects` is now four bands of that width, top and bottom taking the corners; `ink_at` is the dash-then-gap pattern; `ant_runs` walks the bands with it. Rotem's values, two rounds: 4 pixels first, then 2 and the blue. | fmt, clippy with -D warnings and --all-features, both builds with no warning, 51 unit tests, three of them on the frame: the band covers the frame once at its width, the runs alternate and never exceed their ink's length along one path, and a phase step shortens the first run by one. `--selftest` twice: every pick and drag passed both times. The section H picture, second run, read along the frame's top-left: 45/65/215 then 32/32/32 in runs, two rows deep, picture beneath. On the first run the picture held no frame at all and on the second, G reported the foreground on an unnamed window after the return; another assistant session was running its own checks on this machine at the time, which take the focus and the pointer, so both are written down as collisions, not as green. Risk medium, values only, the paint path as reviewed in the previous row. | Pass | medium | `839984c` | Revert the file to `839984c`. |
| 2026-09-14 | The frame's colour | `host/src/overlay.rs`: the dash brush is #00B9F7. | fmt, clippy with -D warnings and --all-features, both builds with no warning, 51 unit tests. Not rerun: `--selftest`, a brush colour has no assertion there; Rotem's eye on the release is the check. The release could not be rebuilt yet, his Recon holds the file. Risk low, one constant. | Pass | low | `9cf7db7` | Revert the file to `9cf7db7`. |
| 2026-09-14 | The frame's pace | `host/src/overlay.rs`: `ANTS_STEP_MS` 676, which is 1000 over 1.48 rounded. | fmt, clippy with -D warnings and --all-features, both builds with no warning, 51 unit tests. Not rerun: `--selftest`, the pace has no assertion there; Rotem's eye on the release is the check. Risk low, one constant. | Pass | low | `e0e860a` | Revert the file to `e0e860a`. |
| 2026-09-14 | The frame's pace, again | `host/src/overlay.rs`: `ANTS_STEP_MS` 125, which is 1000 over 8. | fmt, clippy with -D warnings and --all-features, both builds with no warning, 51 unit tests. Not rerun: `--selftest`, the pace has no assertion there; Rotem's eye on the release is the check. Risk low, one constant. | Pass | low | `3ef0095` | Revert the file to `3ef0095`. |
| 2026-09-14 | The frame glides | `host/src/overlay.rs`: `ANTS_SPEED` 12 pixels a second and `ANTS_FRAME_MS` 16 replace the per-tick step; `State.started` and `phase()` give whole pixels and a fraction off the clock; `Ant` carries each run's edge pixel when the run ends at a dash edge, not at a band's end; the paint fills that pixel with `mix` of the two inks by the fraction, two brushes made and freed per frame; the inks are `ANTS_DASH_RGB` and `ANTS_GAP_RGB` through `colorref`; the timer only invalidates the strips. | fmt, clippy with -D warnings and --all-features, both builds with no warning, 53 unit tests with two new: the edge pixel is the last column of a run that meets a dash edge and absent where a run turns a corner, and the blend walks from one ink to the other with the COLORREF byte order checked. `--selftest` A to H passing, no other Recon running. The section H picture along the frame's top row: 32 gaps, then 6/158/209, eleven 0/185/247, 26/59/70, seven 32s, repeating every twenty, which is the pattern caught 81% of the way through a pixel. Risk medium, the paint path: reviewed that the two frame brushes are freed on every paint, that the edge pixel lies inside its run so nothing spills past the band, and that a run cut by a band's end blends nothing, the ink continuing round the corner. | Pass | medium | `be2eee7` | Revert the file and the docs to `be2eee7`. |
| 2026-09-14 | The frame's pace, third time | `host/src/overlay.rs`: `ANTS_SPEED` 24. | fmt, clippy with -D warnings and --all-features, both builds with no warning, 53 unit tests. Not rerun: `--selftest`, the pace has no assertion there; Rotem's eye on the release is the check. Risk low, one constant. | Pass | low | `c3f99b3` | Revert the file to `c3f99b3`. |
| 2026-09-14 | The top bar | `host/src/editor.rs` and `host/src/main.rs`: the editor window is built with `decorations(false)`, product and checks builds alike. `host/capabilities/default.json`: the page may start a drag, minimize, toggle maximize and close its own window, four permissions, the side effect the bar needs. `editor/index.html`: `#bar`, 32 px, the drag region by `data-tauri-drag-region`; `#title` from the image info, the same text the host puts in the window title; `#controls` moved into the bar, no longer absolute; `#winbtns`, three 46-wide buttons drawn as SVG glyphs, minimize, maximize (restore when maximized, read on every resize) and close through the runtime's window API, close reaching the host's close handler and so hiding; `body.fullscreen` hides the bar and the stage takes the top. `editor/editor-checks.js`: section 36, and sections 27 and 28 measure the stage and the controls against the bar. | fmt, clippy with -D warnings and --all-features, both builds with no warning, 51 host and 4 pixel unit tests. Editor checks, debug, checks build: 278 pass, 2 not run (the two live compares, another window over ours; a self test from a second session was on the screen), and one FAIL on the first run, mine: the check wanted 32-tall buttons where the bar's bottom line leaves 31, so the check now measures the buttons against the bar's own inner height, 31; re-run at 21:58 on a build carrying a second session's edits too: 283 pass, section 36 all green, and two failures in section 16 (the notes kept when a document is told twice) that the other session's own run at 22:03 passed; its reading, a pre-existing race between the probe's announced load and the check's own load, is reported and not fixed here. Section 36 read: no Windows frame, the bar 32 tall and 1280 wide with the stage from 32, the title "reference-scene.png - Recon" on both, the buttons at 1142-1188, 1188-1234, 1234-1280, maximize to 5120x1392 (the work area above the taskbar) and back to 1280x800, focus off the button after a click, fullscreen with the stage from 0 and the bar gone, then back. Not run by a machine: the drag by the bar, the double-click, minimize, and resizing by the edges of an undecorated window, which the runtime documents and this build has not shown; all four are Rotem's hand on the new release. Risk medium, review of this diff: close goes through the same `CloseRequested` handler the frame's X used, so nothing about hiding or saving moved; the title is drawn from the same info the host titles the window from; the ratio measurement reads the stage's width, which the bar does not change; the four permissions are the window's own and reach no file, shell or clipboard. Pre-existing, reported not fixed: `#controls` is `display: flex` by rule, so its `hidden` attribute has never hidden it (the empty state shows the buttons). | Pass, with the hand checks open | medium | `839984c` | Revert the six files to `839984c`. |
| 2026-09-14 | The bar's colour and line | `editor/index.html`: `#bar` loses its background and its bottom border, nothing else. | The release rebuilt in place, no Recon was running; Rotem's eye is the check, and the editor checks were not re-run for a colour, the one measurement they take of the bar being its own height, which the check reads from the bar. Risk low, two style values. | Pass | low | `839984c` | Put the two declarations back. |
| 2026-09-14 | The timeline's order | `editor/index.html`: `refreshStrip` walks the host's list reversed, so the newest document is the first thumbnail; the Trash chip stays at the end. `editor/editor-checks.js`: sections 27 and 29 read the strip from the other end, and section 27 now asserts which document is at which end. | Editor checks, 22:12: 287 pass, both order checks green (the newest at the left and current, a click on the rightmost showing the oldest, the delete taken from the right); one FAIL that is not this change's, section 27's thumbnail sizes, where an uncommitted S2.8 change of another session makes thumbnails 320 by 200 and its author has that check to update; this change only flipped that line's indices. Risk low, an order on screen. | Pass, with the S2.8 line open | low | `839984c` | Walk the list forward again. |
| 2026-09-14 | The app background | `editor/index.html`: the `--paper` token is #111217, was #101214. | `cargo build` in host with the page embedded, no warning; the token read back from the file. Not driven in the running app: a colour Rotem judges by eye. Risk low. | Pass | low | `be2eee7` | Set `--paper` back to #101214. |
| 2026-09-14 | No tool in hand by default | `editor/index.html`: `model.tool` starts null; `setTool` toggles `body.armed`; one CSS rule gives the stage the ordinary pointer unless a tool is in hand in annotation; `loadImage` calls `setTool(null)` beside the mode line, so every picture shown starts with none; the stage's press returns before creating anything when no tool is in hand; the HUD says `tool none` and then drops "click to add". `editor/editor-checks.js`: section 20 clicks with no tool, then with the callout tool; section 32 checks the empty tool, the pointer, a click that creates nothing, the crosshair after L and the reset on reopening; the four end-of-section restores set no tool. Plan: §3.3, the §3.6 Annotate row, S3.1. Decisions: one entry. | fmt, clippy with -D warnings and --all-features, both builds with no warning, 51 host and 4 pixel unit tests. Editor checks from a private copy of a checks build, confirmed by its check-only string, report at 22:03: 287 pass, 1 not run (Escape on Save As, another application in front); the cursor read `auto` with no tool and `crosshair` after L. All of it on a working tree that also held two other sessions' uncommitted work; this commit carries only these hunks. **A slip on the way:** the first run launched `host/target/debug/recon-host.exe` while it was a product build, so a product Recon ran from 21:55:43 to 21:57:17, read the real store and wrote nothing to it (no file under `%LOCALAPPDATA%\Recon` newer than 21:50), held the hotkey through two presses and a second launch, and was quit from the tray. A second session's run on a build taken mid-edit stopped at section 32; its later run failed section 16's two note checks, which pass here: a race already in the checks, the page loading an announced capture twice at once. Reported, not fixed. Risk medium, review of this diff: every way a picture is shown goes through `loadImage`, so the reset reaches captures, files, the timeline, the trash, navigation and Annotate, all driven by the run; existing notes and shapes are still selected, moved and typed into with no tool. Impact shared. | Pass | medium | `c3f99b3` | Revert this commit's hunks in the two editor files, and the docs. |
| 2026-09-14 | The wheel zooms | `editor/index.html`: the wheel zooms around the pointer on any up or down movement, by the step Ctrl with the wheel already had, and pans only on a sideways wheel; the HUD says "wheel zoom". `editor/editor-checks.js`: section 18 wheels out and back in around one point, then pans sideways at zoom 8 from the left end. Plan: the §3.6 zoom row, S1.3. Decisions: one entry. | The runs of the row above. Section 18: the wheel from 7.5 to 6.0 with the point under the pointer moving 0.00 image px, back in around it, and a sideways wheel panning 0.0 to 15.0 at zoom 8 with the zoom unchanged. Not run by a machine: a real mouse wheel and a touchpad, Rotem's hand; a touchpad's two-finger scroll now zooms too. Risk medium, review of this diff: a sideways wheel with Ctrl now pans by its sideways amount where it used to zoom out, an edge nothing sends in practice; nothing else reads the wheel. Impact local. | Pass | medium | `c3f99b3` | Revert this commit's wheel hunk and section 18, and the docs. |
| 2026-09-14 | The timeline at scale, S2.8 spec | `project-os/Plan.md`: one new step, S2.8, after S2.7 in the pulled-forward Stage 2 block: what the strip does today and where it gets heavy, the case against a View More button, the windowed strip, the drag on the top edge to 320-wide thumbnails and wrapped rows, the thumbnail file at 320 served at the size shown, and three calls left open for Rotem. Nothing built, no code touched. | Read back: the step sits between S2.7 and the Stage 3 heading, the plan's own numbering is unbroken (S2.6 and S2.7 exist, S2.8 was free), every section it cites exists (§3.8, S1.8, S1.9, S0.4, QA §6). The facts about today's strip were read from `editor/index.html` (`refreshStrip`, `thumbFor`) and `host/src/editor.rs` (`thumbnail`, the thread per request, `Preserved::OnDisk`). Risk low, docs only. | Pass | low | `be2eee7` | Remove the S2.8 step from the plan and these two rows. |
| 2026-09-14 | The drag and Space pan | `editor/index.html`: `startPan`, shared by viewing's drag, a drag from the empty picture with no tool in hand, and any drag with Space held; `spaceHeld` and `setSpaceHeld` toggle `body.space`, which gives the stage the ordinary pointer and takes the scene off the pointer; Space by physical key below the typing guard, let go on keyup and on the window's blur; with no tool a press on the empty picture commits a note being typed, clears a selection and starts the pan; the HUD hint reads "click to add  Space+drag pan" with a tool and "drag to pan" without. `editor/editor-checks.js`: section 32 drags with no tool at zoom 8, holds Space with the arrow in hand and drags, lets go, blurs the window, and presses Space in a note being typed. Plan: §3.3, a §3.6 row, S3.1. Decisions: one entry and a dated note under the wheel entry. | fmt, clippy with -D warnings and --all-features, both builds with no warning, 54 host and 4 pixel unit tests. Editor checks from a private copy of a checks build confirmed by its check-only string, report at 22:29: every check of this change passes, the drag with no tool panning 240.0,216.0 to 235.0,212.3 at zoom 8 and creating nothing, Space held reading cursor `auto` and scene pointer events `none` and its drag panning the same without drawing, the crosshair back on keyup, Space let go on blur, Space in a typed note left to the note; sections 18, 20 and 33 to 35 pass around it. The run as a whole: 285 pass, 9 fail, 9 not run; the 9 failures are all in another session's uncommitted timeline work (sections 27, 29 and 37), not in this commit, and the 9 not run are screen and foreground refusals with other windows in front. Risk medium, review of this diff: viewing's drag now goes through `startPan`, the same three lines it had, and section 20's drag still pans; Space is taken only below the typing guard, so the keyboard contract's typing checks hold; the new blur listener sits beside the save-on-blur one and changes nothing it does; a drag that starts on a note or a shape with no tool still moves it. Impact shared. | Pass | medium | `c1412c6` | Revert this commit's hunks in the two editor files, and the docs. |
| 2026-09-14 | S2.8, the timeline at scale | `editor/index.html`: the timeline block rewritten as a windowed strip (`cells`, `layoutStrip`, `cellRange`, `renderStrip`, `makeCell`, `loadThumb`, a fetch queue of eight, blob URLs revoked on drop), `--strip-h` on the page with `#strip-handle` and its drag, double-click and remembered height (`recon.strip-height` in the page's storage), the strip's scroll length as its `::after` block, `setFullscreen` and the resize listener (`stageResized`) reading the cells, five test hooks. `host/src/editor.rs`: `THUMB_W/H` 320 by 200, `outgrown` and `outgrown_by` with a unit test, `thumbnail` remaking an outgrown kept file. `host/src/store.rs`: an ignored test, `thirty_thousand_documents_scan_in`, run by hand. `editor/editor-checks.js`: section 27 reads the default height and the 320 sizes; section 37 new, sixteen checks. `project-os/Plan.md` S2.8 built paragraph, `project-os/Decisions.md` one entry. | The four project checks green in order (fmt reformatted my own Rust, clippy clean, build, 54 + 4 tests). `--editor-check` on the debug build: 294 pass, 0 fail, 9 not run, all screen copies and foreground refusals from other windows; section 37 read: 30 documents make 19 elements in a 1280-wide strip, scroll width 4088, the end shows the oldest and drops the newest, back shows the newest with its picture, 300 wanted snaps to 216 with 320 by 200 cells, 500 to 424 with two rows of three and scroll height 2088, 100000 to 424 (60% of 800), a pointer drag of 200 up lands 216 and remembers it, a double-click 96, the Trash chip is the last cell and exists once scrolled there, the trash view is two cells, Back returns with the current in view. Sections 27, 29 and 31 pass on the new strip (267x200, 150x200, 320x100 thumbnails kept on disk). The scan measurement: 30,000 documents written in 23 s, scanned in 1.87, 2.16 and 1.99 s, warm. Two defects found by the run and fixed before it went green: the picture was asked for before the cell was in the strip, so every fetch was refused; and an absolutely placed `::after` pixel did not extend the scroll length, an in-flow block does. Not driven by a machine: the handle under a real mouse and the look of a grown strip, Rotem's hand on the next release. Review (rule 17, medium): my own diff, uncommitted; blast radius shared, the strip's readers being four check sections and `refreshStrip`'s callers, all run; nothing further found; pre-existing and untouched: the window's top-bar work sits uncommitted in the same tree from an earlier session (`decorations(false)`, the capabilities file, the bar's CSS). | Pass | medium | `a718546` | Revert the timeline block, the strip CSS, the handle element and the test hooks in `editor/index.html`; put `THUMB_W/H` back to 160 by 100 and drop `outgrown`; drop section 37 and restore section 27's sizes; drop the store test; remove the S2.8 built paragraph and the Decisions entry. |
| 2026-09-15 | Start with Windows | `host/src/startup.rs`, new: `RUN_KEY`, `VALUE`, `FLAG`, `command_line`, `is_enabled` (the value exists), `enable` (writes `"<exe>" --startup` as the value `Recon`), `disable` (removes it), and two tests, one ignored. `host/src/registration.rs`: `wide`, `write` and `delete_value` are `pub(crate)`, nothing else. `host/src/main.rs`: `mod startup`; `started_by_windows` read from the arguments; a `CheckMenuItemBuilder` item "Start with Windows" between "Default apps settings..." and the separator, ticked from the registry at startup; the menu handler is `move` and answers `startup` by reading the flipped tick, writing or removing the value, and putting the tick back if the write failed; at startup `None if started_by_windows` logs and shows no window. `project-os/Map.md`: the tree line and a Data row. `project-os/Plan.md`: a §3.1 paragraph and a line under S2.2. `project-os/QA.md`: one row. `project-os/Decisions.md`: one entry. | fmt, clippy with -D warnings and --all-features, build, 55 + 4 tests, all green; the ignored round trip run by hand: a value `Recon (check)` written under the real Run key, found, removed, not found, with `reg query` showing no `Recon` value before or after. Read in the sources: muda flips a check item before the event reaches the handler, and a main-thread task sent from the main thread runs inline, so reading the tick inside the handler cannot block. Not run by a machine: the tick in the tray, a logon, and the hidden window at that start, Rotem's hand; the product build was not launched with `--startup` here because it reads the real store and holds the hotkey. Review (rule 17, medium), this diff: the three registration helpers only changed visibility; `file_argument` skips `--startup` as it skips every `--` argument, so nothing opens by mistake; no other code reads the menu. Two edges reported, not fixed: Task Manager's own disable leaves the value and the tick on; a second instance with `--startup` shows the running window. Impact local. | Pass, with the hand checks open | medium | `47419ee` | Delete `startup.rs`, drop its `mod` line, the menu item, the handler arm, the `started_by_windows` arm and the three `pub(crate)`; remove the doc lines. |
| 2026-09-15 | The release rebuilt for the tick | `host/target/release/recon-host.exe`, rebuilt at 01:12 from commit `122069d` checked out into a scratch folder under `.tmp/`, so the other session's uncommitted editor, store and page work in the live tree stayed out of it; the scratch checkout was removed after. No source changed. | `cargo build --release` from that checkout into the usual target folder: finished in 35 s, no warning; the file's time read back as 01:12. No Recon was running, so the file was free. Not run: the tick, a logon and the hidden window, Rotem's hand on this release. Risk low, a build. | Pass | low | `122069d` | Rebuild the release from the tree as it is. |
