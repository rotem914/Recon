# Recon — History

The change log. Every completed change lands here, in two layers: a scan table
you always read, and an appendix you read only when digging.

The scan table answers "what happened lately". The appendix answers "what exactly
did that change do, and how do I undo it".

## How you maintain this file

- **After every completed change**, add one scan row AND one appendix row. Both,
  in the same change that did the work.
- **One change = one row.** Not one row per file, not one row per session. A log
  that has to be reassembled from fragments is a log nobody reads.
- Write the scan row for a reader who was not there. Name the behavior that
  changed, not the files.
- Keep appendix rows short — a few lines, not an essay. The full story is in the
  commit diff; a real decision belongs in `project-os/Decisions.md`.
- Record the commit SHA from **before** the change. That is the rollback target.
- Never rewrite or delete a past row. Correct a wrong one by adding a new row —
  an edited log cannot be trusted about anything.
- A `medium` or `high` risk row names its review result under **What was
  checked**: findings found, findings fixed, pre-existing ones flagged. A
  medium-or-higher row without that is a task that is not finished.
- Never write "tested" or "QA passed". Those phrases record nothing. Name the
  input, the screen, and what happened.
- When this file gets long, move the oldest rows into an archive file beside it.
  `project-os/rotate.ps1` does exactly that at `Go commit`, and creates
  the archive the first time it is needed. Rows move **verbatim** — never
  rewritten, never summarized, never merged, because the detail you drop is the
  one the next reader needed. Never hand-move rows: the script dedups, so it is
  safe to run every time, and a hand-move breaks that guarantee.

## Risk scale

The scale's one home is `project-os/Workflow.md` step 2 — read it there, so the
two files can never disagree. State the level at task pickup; the owner's
override wins.

## Scan log

Newest at the bottom.

| Date | Area | What changed |
|---|---|---|
| 2026-09-10 | project-os | **This project now has its own written rules, and they are repeated to the assistant on every message.** ProjectOS was installed into an empty repository: the rule files, the living files, and two guards that refuse a write outside the project or a one-way command. Values only Rotem can supply are marked as waiting, never guessed. |
| 2026-09-10 | project-os | **The install is now in git, and one process correction is on record.** Rotem pointed out that the commit is mine to make and the push is his, so the waiting-on-you framing was wrong and is logged in Mistakes.md. |
| 2026-09-10 | plan | **The product plan now has a written review beside it, and a build plan for the first stage.** Sixteen findings, three of them blocking, and six product decisions left open for Rotem. The build plan carries the proposed component split, the Stage 0 steps with the evidence that closes each one, and a named fallback for every way the stack can fail. |
| 2026-09-10 | plan | **The plan is one file now, not two.** The review, the six open decisions and the technical stages all sit in `project-os/Plan.md`, which is what was asked for; the separate review document is gone and its content moved in whole. |
| 2026-09-10 | plan | **The plan is corrected where it was guessing.** The elevated-window hotkey claim is withdrawn to something to verify, the latency harness gained the mark it was missing, the coordinate contract became four named spaces, the output test became three separate checks, and Stage 0 shrank to one capture path and two real destinations. Callout numbers keep their gaps in every output. |
| 2026-09-10 | plan | **The plan is approved for Stage 0, and the product baseline now lives in the repository.** The four corrections landed: the two capture-path fallback lists agree, the typing check compares the live editor against the copied image, task pickup names the product plan and says which document wins where they differ, and every finding carries a status instead of an open checkbox. |
| 2026-09-10 | plan | **Stage 0 has nothing left to wait for.** The two paste destinations are Claude and ChatGPT, which also settles the clipboard question: both read it as a web application does, so PNG is the format that decides acceptance. |
| 2026-09-10 | plan | **Recon is now an image viewer as well as a capture tool, and both plans say so.** Opening a file is a normal way in, one window carries a viewing mode behind an explicit Annotate action, format support is stated per format instead of as common images, and an external file is never modified or imported by being looked at. |
| 2026-09-10 | plan | **There is one plan file now, and only one.** `project-os/Plan.md` holds the product, the behavior, the invariant, the architecture, the decisions and the stages in thirteen parts; `Product_plan.md` is gone and its content is inside. Splitting a plan across two documents is now a rule against it, in CLAUDE.md rule 13. |
| 2026-09-10 | plan | **The viewing and persistence contracts are now defined, and one of them was a real defect.** Reopening an annotated file resumes its one document, Save As can no longer overwrite anything, the displayed animation frame is a proved requirement rather than a figure of speech, an SVG's raster size is fixed when annotation begins, orientation and color are tested through the whole cycle, and the navigation context has four activation rules. |
| 2026-09-10 | plan | **The corrections are consistent now, and the plan comes to you unasked from here on.** Six leftovers were found and fixed: the navigation keys still walked "captures" in three places, the JPG row still applied orientation at display time, S0.7 never recorded the display gamut its own revisit trigger depends on, and part 6c justified deferring TIFF with a reason the rest of the plan contradicts. |
| 2026-09-10 | plan | **The technology is now chosen on its merits, and choosing it exposed a defect in the plan.** A Rust host owns the pixels and a web view lays out the text; the two-provider decode split is gone, because a preserved image created inside the renderer cannot survive a premultiplied canvas byte for byte. The familiarity argument is struck, the three open format decisions closed, and Stage 0 gained a boundary measurement before anything is built on it. |
| 2026-09-10 | plan | **The stack is a candidate again, and three real defects are fixed.** A fit-to-window proxy could never have served 100% zoom, the stated text-direction override did not exist in the mechanism named for it, and the annotation export was sized to cut off anything in the added margin. The stack argument also overstated what the alternatives cannot do, so part 5 now names what is not established and carries a comparison, and part 8 can reject the design. |
| 2026-09-10 | host | **Recon exists as a process now: it sits in the tray and answers a global hotkey with no window open.** S0.1 is built and two of its three checks are proved in a log: the hotkey fired twice with no window ever created, and a second instance reported the clash instead of dying quietly. The third, quit from the tray menu, needs one click that no automation route here could drive. |
| 2026-09-10 | host | **S0.1 is closed: the tray quit works and leaves nothing behind.** Rotem started the host himself, fired the hotkey with a real keypress, and clicked Quit; the process logged the quit and exited, and no recon-host and nothing else from the project tree remained. |
| 2026-09-11 | host | **Recon can take a screenshot now: hotkey, frozen screen, dimmed overlay, drag a region, and a PNG comes out.** Escape cancels and captures nothing, a second hotkey during a selection is ignored, and an open menu lands in the freeze. The self test caught the important defect: the process was DPI-unaware, so a 3840x2160 display at 225% was reporting itself as 1707x960 and every coordinate was quietly wrong. |
| 2026-09-11 | plan | **S0.2 is closed, with the two-display check deferred instead of pretended.** Rotem has one display, so a negative desktop origin and a mixed-scale layout cannot be produced here. The arithmetic for both is unit-tested; the platform half becomes a watch item for the first time a second display is ever attached. |
| 2026-09-11 | host | **The boundary is measured, and it says the pinned design was the right one for a different reason than the plan gave.** A whole frame crosses into the web view in 290 ms, the proxy the design pins in 40 to 74 ms. The figure the design was argued against was two to four times pessimistic. A local socket is about twice as fast as the message channel for large payloads, and the annotation layer should cross encoded: 162 KB and 3.5 ms against 31.6 MB and 359 ms. |

## Appendix — deep rows

Newest at the bottom, same as the scan log.

| Date | Task | What changed | What was checked | Result | Risk | Commit before | Rollback |
|---|---|---|---|---|---|---|---|
| 2026-09-10 | Install ProjectOS | Copied `CLAUDE.md`, `Installation.md` and `project-os/` in. Filled the project name, the owner, the role and the reply language; wrote the honest state for stack, local address and checks. Replaced the Map skeleton with the real tree and filled its Data and Ownership tables. Deleted the six example blocks. Adapted `hooks-settings.json` to this project and merged it into `.claude/settings.local.json`. | Both hook commands run in bash and in PowerShell: the text came back whole in each. Destructive guard, fake recursive-delete payload: exit 2 with a reason; a `git status` payload: exit 0. Path guard, a write aimed at the home folder: exit 2; the same write inside the project: exit 0. `rotate.ps1 -DryRun`: every target file resolved, nothing to move. `backup.ps1`: 42 files, all 42 verified back out of the ZIP. No placeholder left in `CLAUDE.md` or `project-os/` outside the two `mcp/` setup tables. Every referenced path resolves except three that are conditional by design (`README.md`, `project-os/Plan.md`, the analytics venv). | Pass | medium | none, the repository had no commits before this | Delete `CLAUDE.md`, `Installation.md`, `project-os/`, `.gitignore`, `.claude/settings.local.json` and `backups/`. Nothing else was touched. |
| 2026-09-10 | Commit the install, record the commit slip | One row added to the Open table in Mistakes.md, then the whole install staged and committed. Nothing else in the docs changed. | Ran the archive script: every target resolved, nothing to move. Checked the staged list by hand: no snapshot, no machine-local settings, no secrets. Read the commit back with git log and git show --stat. | Pass | low | none, this is the first commit in the repository | Nothing to revert to. Delete the files the install row lists, or reset the branch to no commits. |
| 2026-09-10 | Review the product plan, write the Stage 0 build plan | Added `notes/2026-09-10-plan-review.md` (16 findings with severities and verdict boxes, plus six owner decisions) and `project-os/Plan.md` (component split, Stage 0 steps S0.1 to S0.8 each with a suggested model and its evidence, go or no-go fallbacks, Stage 1 order). Updated the Map tree and Ownership table for the new folder and file. | Read both documents back: every path they name resolves, the finding counts match the summary line, and each of the six decisions matches its finding. The rule 17 review of my own output found three defects, all fixed: long dashes in three of my headings, a missing decision pointer in F2, and steps naming no model. Nothing was measured or benchmarked, since the project has no code. | Pass | medium | `4757906` | Delete the two new documents and revert the Map tree and Ownership rows. |
| 2026-09-10 | Merge the review into the one plan | `project-os/Plan.md` rewritten as eight parts: the review with its sixteen findings and verdict boxes, the six decisions, the architecture, Stage 0, the go or no-go fallbacks, Stage 1, the later stages, and what is measured against what is assumed. `notes/2026-09-10-plan-review.md` deleted, its content moved into part 1. Map tree and ownership row updated. | Read the merged file back: sixteen findings present, the counts match the summary line, each decision matches its finding, each step names the findings it carries, and no path it names is missing. No long dash in my own text. Nothing measured, the project has no code. | Pass | medium | `caa3340` | Revert the merge commit; the deleted review document is intact in `caa3340`. |
| 2026-09-10 | Revise the plan on Rotem correction pass | `project-os/Plan.md` rewritten as revision 2 in ten parts. F2 rerated from blocking to important and its documentation claim withdrawn. F3 given six timestamp marks and two named intervals, with the drag excluded. F11 resolved against renumbering. Part 4 given four coordinate spaces, with the annotation margin stored as an edge offset so a growing margin rewrites no coordinate. S0.4 split into three fidelity checks, one of them copying while the caret is active. Stage 0 reduced to six steps, one capture path, two destinations, no packaging. Stage 1 stripped of Rogers preparation and given the undo acceptance sequence. Part 9 split into measured, documented, to verify, assumed. New part 3 for the three answers needed before Stage 0, new part 10 for the sources. Two entries added to `project-os/Decisions.md`. | Read the revision back: sixteen findings, counts now two blocking, eight important and six small, and the summary line agrees. Every decision in part 2 points at a step that exists. Every carried-into target exists. Every path named resolves. No long dash in my own text. Self-review found three defects, all fixed: F4 pointed at the wrong part 3 decision, the header said nine parts against ten, and the parts list omitted the sources. Nothing measured, the project has no code. | Pass | medium | `6fb217a` | Revert the revision commit; revision 1 is intact in `6fb217a`. |
| 2026-09-10 | Apply the four corrections, file the product baseline | `project-os/Product_plan.md` added, the product plan as given, so the pickup instruction can name a real path. `project-os/Plan.md` revised to revision 3: part 6 now earns a second capture path on a diagnosed performance failure too, so it agrees with part 3; the S0.4 typing check compares the live editor against the copied image instead of two outputs of one renderer, decorations excluded; the header requires the product plan at pickup and states that an approved revision here wins; all sixteen findings carry a status line, with the severity marker labelled as the rating at the time it was raised. Part 3 rewritten to Rotem directions: initial capture path settled behind a small capture interface that leaves a video source possible, reference environment settled, only the two paste destinations outstanding and blocking S0.4 acceptance alone. `project-os/QA.md` gained §12, a check must be able to fail, promoted from a repeated slip, plus its checklist line. Map tree and ownership row updated. | Read the revision back: sixteen findings and sixteen status lines, the summary line agrees, ten parts, every path named resolves, every part 3 reference points at the right item, no long dash in my own text. Self-review found one defect, fixed: the parts list still described part 3 by its old title. Nothing measured, the project has no code. | Pass | medium | `5bcce34` | Revert the commit; revision 2 is intact in `5bcce34`. |
| 2026-09-10 | Close the last Stage 0 input | `project-os/Plan.md` revision 4: part 3 item B settled with Claude and ChatGPT, the consequence for clipboard formats written into part 3 and into S0.4, the requirement to record the surface and version beside each paste result, the F4 status line closed, and the header status changed to nothing outstanding. | Read the changed sections back: part 3 now says all three settled, sixteen findings and sixteen status lines unchanged, every path resolves, no long dash in my own text. Nothing measured, the project has no code. | Pass | low | `f4c7243` | Revert the commit; revision 3 is intact in `f4c7243`. |
| 2026-09-10 | Fold image viewing into the product and build plans | `project-os/Product_plan.md` revision 2: three core capabilities, principle 8 that viewing never changes the file, file-opening entry points with type registration and the default-app path, one window with two entry behaviors, a new §3.4 for everyday viewing and folder navigation, §4.5 for annotating an animation, a page or a vector, a stage column plus the viewing keys in §5, a new §5b stating support per format, a new §6.0 keeping external files out of the managed library, and Stage 0, Stage 1 and the acceptance checks adjusted. Its §0 revision note lists all of it. `project-os/Plan.md` revision 5: findings F17 to F21 added, part 3 gained the three open format decisions, Stage 0 gained S0.4 and renumbered the steps after it, Stage 1 grew to twelve items, part 4 gained the image source interface, part 6 gained the will-not-decode row, parts 9 and 10 updated. CLAUDE.md rule 11 carries its first real invariant, replacing the example block. One entry added to `project-os/Decisions.md`. | Read both back: twenty-one findings, severities three, ten and eight, and the summary line agrees. Part 3 reads A to F in order. Stage 0 is S0.1 to S0.7 in order, Stage 1 is S1.1 to S1.12, and every reference to a renumbered step points at the right one. Every path named resolves. No long dash in my own text. Self-review found two defects, both fixed: part 3 had C after F, and the Stage 0 exclusion line missed file management. Nothing measured, the project has no code. | Pass | medium | `fa1ad16` | Revert the commit; revision 4 and product plan revision 1 are intact in `fa1ad16`. |
| 2026-09-10 | Consolidate both plans into one file | `project-os/Plan.md` rewritten as one document in thirteen parts: what Recon is, the principles, how it behaves in ten sections, the invariant, the architecture with its two interfaces and four coordinate spaces, the decisions settled and the three still open, Stage 0 in seven steps, what Stage 0 may conclude, Stage 1 in twelve items with stage-wide acceptance, the later stages, the evidence status, the twenty-one findings as one table, and the sources. `project-os/Product_plan.md` deleted, its whole content inside part 1 to part 3. The revision blocks were dropped, since History.md is where revisions belong. CLAUDE.md rule 13 gained the one-plan rule, promoted from a slip that happened twice, and Mistakes.md records the promotion. Map tree and ownership updated, and one stale part reference fixed in Decisions.md. | Read the merged file back: every section the old product plan had is present, thirteen parts in order, S0.1 to S0.7 and S1.1 to S1.12 with every cross-reference resolving, twenty-one findings in the trail table, no path named that does not exist, no long dash anywhere in it. A four-lens verification pass ran against both pre-merge documents, each finding then adversarially checked by a second agent: 21 issues raised, 16 refuted, 5 confirmed and all 5 fixed here. The five: the naming and standalone-product section was dropped, the starting stack was never named, the open-source README requirement was dropped, the reading guide stopped at part 12 of 13, and one pointer promised a filename comparison that the target section deferred. Nothing measured, the project has no code. | Pass | medium | `dc6396b` | Revert the commit; both documents are intact in `dc6396b`. |
| 2026-09-10 | Apply the seven viewing and persistence corrections | `project-os/Plan.md`: §3.3 gained the resume contract with its worked a.png and b.png example, §3.4 gained the four context-activation rules, §3.5 gained the frame-access requirement and the fixed SVG raster size, §3.6 lost the Save As overwrite exception, §3.7 gained the decoded-image contract with orientation once, sRGB and addressable frames plus the wide-gamut caveat for captures, the history sections now speak of documents rather than captures, part 5 rules out the image-element decode path, part 6a drops the bitmap-only clipboard claim, a new part 6c names the required format set, S0.4 gained three end-to-end tests, part 8 bounds the shorter-list rule, S1.5, S1.9 and S1.11 follow, and the stage acceptance gained six checks. Findings F22 to F29 added to the trail, and two sources. Five entries added to `project-os/Decisions.md`. | Read it back: 13 parts, 29 findings with severities five, fifteen and nine matching the trail, seven Stage 0 steps, twelve Stage 1 items, no stale capture-history wording outside the Snagit sentence, no overwrite exception left, no long dash, every path resolves. An eight-lens verification pass then checked the corrections against the seven items as given, each finding adversarially re-checked by a second agent: 28 issues raised, 22 refuted, 6 confirmed, and the six are fixed in the same commit. Nothing measured, the project has no code. | Pass | medium | `bad690d` | Revert the commit; the pre-correction plan is intact in `bad690d`. |
| 2026-09-10 | Fix the six verified leftovers, and make the plan self-delivering | `project-os/Plan.md`: the keyboard row, S1.4 and the stage acceptance now name Recon history rather than captures; the JPG row states orientation applied once at decode, matching the decoded-image contract; S0.7 records each display's color gamut, which is what the sRGB revisit trigger points at; part 6c replaces the false claim that TIFF is the only host-provider format with the real distinguisher, no degraded pass. Same rationale corrected in `project-os/Decisions.md`. `project-os/Conversations.md` gained rule 18: the plan is attached to the reply that changes it, unasked, on Rotem's instruction after asking for it three times. | The eight-lens pass that found these ran before the fixes; after them, read back: no "captures" left in a navigation sense, the JPG row and the contract agree, S0.7 names the gamut, part 6c and part 6b agree about HEIC, 13 parts, 29 findings, severities five, fifteen and nine, no long dash, every path resolves. Nothing measured, the project has no code. | Pass | low | `bad690d` | Revert this commit; the plan before the seven corrections is in `bad690d`. |
| 2026-09-10 | Decide the stack on merits, and flip the decode route | Five candidate stacks evaluated against the plan's twelve requirements, each attacked by a second reviewer, then ranked. `project-os/Plan.md`: part 5's stack section rewritten with the real argument, the decode surface, the bidirectional editing model and this project's own browser-driven QA gate, and the familiarity reason struck (F32); the layers table now has one native decode route; the boundary rule pinned, the original never enters the web view at full resolution and never round-trips a canvas (F30); a new section for the DOM text layer and its `foreignObject` export with fonts inlined; part 6a gained four decided rows, part 6b closed, part 6c's void justification replaced; S0.3 added for the boundary measurement, which renumbered the steps after it; S0.4 gained the hidden-window first-frame check; S0.5 gained SVG content fidelity and per-format animated frame access; S0.6 check 1 gained a semi-transparent case (F31) and check 3 gained the trigger with its three remedies; S0.7 reads memory as a slope; S0.8 records the runtime version; part 8 gained two switch rows; part 11 records one assumption as answered and three watch items. Four entries added to `project-os/Decisions.md`, one of them superseding a rationale rather than a decision. | Read it back: 13 parts, eight Stage 0 steps S0.1 to S0.8 in order, twelve Stage 1 items, 32 findings with severities seven, sixteen and nine, the step count in part 7 matching the steps, no long dash, every path resolves, no stale reference to two decode providers or to the old step numbers. The stack material came from eleven agents, five evaluating, five attacking and one ranking; their claims about premultiplied alpha, about SVG support in the rejected renderers and about the export path were checked against the reasoning rather than accepted. Nothing measured on this machine: the project still has no code. | Pass | medium | `539d8a3` | Revert this commit; the plan before the stack decision is in `539d8a3`. |
| 2026-09-10 | Apply the architecture review: reopen the stack, fix three contract defects | `project-os/Plan.md`: part 5's stack section rewritten as a recommended candidate with a section naming what is not established (Qt has an editing model, native UI has automation peers, `resvg` has a C interface, Qt SVG has extensions since 6.7, and the QA doctrine is a tooling cost not a product requirement) plus a comparison table of concrete implementations and what replacing Tauri would really cost; the boundary rule split into a pinned correctness rule and a measured transport policy, with a display-detail requirement so zoom shows source pixels (F35); direction became three explicit modes because `unicode-bidi: plaintext` ignores `direction` and resolves per paragraph (F36); the export now covers source plus stored margins (F37); the occlusion flag left the gate (F34); S0.5 tests the chosen native route and names the libavif and dav1d chain; S0.4 gained the display-detail check; S0.6 gained the margin case; S0.7 defines usable as content plus input on one timing basis and states the percentile method (F39); part 8 gained a row that reopens the editor architecture or the stack on combined cost; part 11 downgrades the one-versus-two-route cost to a judgment; leftovers cleared (F38). `project-os/QA.md` gained §13, a claim carries its evidence class, promoted from a slip that happened twice. One superseding entry in `project-os/Decisions.md`, with the old index line italicized. | Read it back: 13 parts, eight Stage 0 steps, twelve Stage 1 items, 39 findings with severities ten, twenty and nine, no stale two-provider or web-view-decoder text, F18 no longer marked open, no long dash, every path resolves. Not verified by me: the vendor documentation the review cites, which I read as the review presented it rather than fetching each page. Nothing measured, the project has no code. | Pass | medium | `48d872b` | Revert this commit; the previous plan is in `48d872b`. |
| 2026-09-10 | Build S0.1: tray, configurable hotkey, clean quit | First code in the project. `host/` is a Rust crate using Tauri v2 with zero windows: `src/main.rs` builds the tray with a disabled hotkey line and Quit, registers the hotkey through the global-shortcut plugin, logs every moment with a millisecond stamp, and refuses an exit that carries no code so only the tray can end the process. `src/config.rs` reads the hotkey from `%APPDATA%\Recon\recon.json` with a built-in default of Ctrl+Shift+4, and reports in words where the value came from; `--hotkey=` overrides it so the clash path can be exercised. `editor/index.html` is a placeholder for S0.4. Tray icon generated as a 64px selection marquee, PNG and ICO, a placeholder for a real design. CLAUDE.md now names the standing checks, Map.md carries the host and editor trees plus their data and ownership rows. | Toolchain first: Rust 1.98.1, MSVC 14.51, Windows SDK 10.0.26100, WebView2 runtime 152, and a link probe that built and ran. Checks on the final tree: `cargo fmt --check` clean, `cargo clippy --all-targets` zero warnings, `cargo build` exit 0. Evidence for the step, in .tmp/s01-a.log and s01-b.log: instance A logged tray created, hotkey registered, then two HOTKEY FIRED lines from synthesized Ctrl+Shift+4, each naming that no window had been created; instance B, started with the same hotkey, logged HOTKEY UNAVAILABLE with the underlying error and kept running. NOT proved: quit from the tray menu. Three automation routes failed the same way, the icon lives in the hidden-icons flyout whose element loses its rectangle as soon as a scan moves focus. After a forced stop no process remained, which is not the same test. Two gaps recorded honestly: stderr was not captured in the run script, so a startup panic would have left an empty log, and whether a Windows logoff arrives as an exit with no code, which the run loop would currently refuse, is noted in the code as unverified. | Pass, with one check open | medium | `9560348` | Delete `host/` and `editor/`, and revert the CLAUDE.md and Map.md rows. Nothing else was touched, and nothing outside the project was written. |
| 2026-09-10 | Close S0.1 with the owner's own run | `project-os/Plan.md`: S0.1 ticked, with a closing note naming what proved the third check and the one number worth carrying, a 20 MB tray-only floor with no window. No code changed. | Rotem ran `cargo run` in `host/` and his terminal shows the whole sequence: tray created, hotkey registered, then HOTKEY FIRED from his own keypress rather than a synthesized one, then "quit chosen in the tray menu" and the shell prompt returning. I then checked the machine: no recon-host process, and no process at all running from the project tree. That closes the check three automation routes could not drive. The two gaps from the build stand and are not closed by this: stderr is still uncaptured in the run script, and the logoff-with-no-exit-code question is still unverified and belongs to S0.8. | Pass | low | `8cb5a80` | Untick S0.1 and remove its closing note. |
| 2026-09-11 | Build S0.2: the freeze, the overlay, the region | `host/src/capture/` is the interface part 6a asked for plus one implementation: `mod.rs` the trait and the frame, `screen.rs` the whole-virtual-screen copy with CAPTUREBLT, `display.rs` the live layout and the DPI declaration, `coords.rs` the one desktop-to-image conversion. `overlay.rs` is hand-written Win32, one borderless topmost window per display on its own thread with its own message pump, painting the frozen slice with the unselected area dimmed by a stretched 1x1 black bitmap, repainting only the union of the old and new rectangle. `main.rs` wires the hotkey to freeze, select, convert once, crop, and write a diagnostic PNG beside the executable. `selftest.rs` is the evidence, run with --selftest. | Checks: fmt clean, clippy with -D warnings clean, build green, 14 unit tests pass including seven that put the coordinate arithmetic through negative origins, because this machine cannot produce one. Self test, exit 0: all four corners of the 3840x2160 display crop identical to a fresh screen copy, byte for byte; the live middle of the screen is correctly reported as a moving region rather than a failure; a synthesized drag returns exactly the rectangle dragged; the captured pixels equal the screen with the overlay gone, which is the overlay-never-in-the-output check; escape cancels; the guard refuses a second selection and releases afterwards. End to end through the real hotkey: three captures, one committed with a 900x675 PNG on disk, one cancelled, one ignored as a repeat. A Notepad context menu, shadow and all, is in the capture. Two defects found by my own review and fixed: the selection guard was a plain flag that would have stayed set if the overlay thread panicked, and a drag past the edge of its display produced a rectangle the conversion then refused, losing the whole capture, where §3.2 says it should clamp. NOT COVERED: two displays, mixed scaling, and a display left of the primary so desktop coordinates go negative. One display is attached, so that is unit-tested arithmetic only, and the clamp is likewise unreachable here. | Pass, with the two-display case open | medium | `3e36b99` | Delete `host/src/capture/`, `host/src/overlay.rs` and `host/src/selftest.rs`, and revert main.rs, Cargo.toml, Map.md and the Plan's part 11. |
| 2026-09-11 | Close S0.2 and defer its two-display condition | `project-os/Plan.md`: S0.2 ticked with a closing note that separates what held from what is deferred and says why the deferral is safe, namely that the untested part is Windows reporting a negative origin rather than code written here, and that seven unit tests cover the code's own handling. Part 11 gained a "deferred until the hardware exists" paragraph naming the two conditions and the one command that closes them. No code changed. | Nothing new was run: this records the state the S0.2 commit already produced and verified. The claim that a scale error is ruled out rests on both the near and the far corner comparing identical, which is in that commit's self-test output. | Pass | low | `5a131a8` | Untick S0.2 and remove the two added blocks. |
| 2026-09-11 | Build and run S0.3: the boundary in isolation | `host/src/bench.rs` and `editor/bench.html`, reached only with --bench and not product code: five payloads, three routes in (the message channel, a custom scheme, and a hand-rolled loopback HTTP server with no dependency added), two routes out, five repetitions each, timed by the web view's own clock with the empty round trip measured separately at about 2 ms. The page checksums a sample of every payload it receives, so a route that truncates is caught rather than timed. `withGlobalTauri` turned on so a page with no build step can call in. Plan updated: S0.3 ticked with what it settled, part 5 now says the layer crosses encoded, part 8's boundary row carries the numbers and records that its trigger did not fire, part 11 has the full table, and F40 to F42 are in the trail. | The run itself is the evidence, and the numbers are in part 11. Checks: fmt clean, clippy with -D warnings clean, build green, 14 unit tests still pass. Two things worth naming: the host's own PNG encoder took 633 ms for a 1920x1080 image at default settings against the web view's 78 ms for a larger one, which is F41 and sits on the Save As path; and on exit the web view runtime prints a harmless "Failed to unregister class Chrome_WidgetWin_0" line, which is teardown noise rather than a defect. NOT measured: anything about the editor, which is S0.4, and the first-window cold start, which belongs to S0.7's instrumentation. | Pass | medium | `22f0f32` | Delete `host/src/bench.rs` and `editor/bench.html`, drop the --bench branch and withGlobalTauri, and revert the plan blocks. |
